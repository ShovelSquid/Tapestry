{
  "name": "@tapestry/plugin-mathspace",
  "version": "0.1.0",
  "private": true,
  "description": "Mathspace: the deterministic fixed-point engine over the kernel, as WebAssembly",
  "main": "index.js",
  "scripts": {
    "engine:wasm": "bash scripts/build-wasm.sh",
    "test": "vitest run --config vitest.config.mjs"
  },
  "devDependencies": {
    "vitest": "2.1.9"
  },
  "license": "MIT"
}
