{
  "comment": "The plan's checkpoint fixture: the velocity golden as kernel nodes. n2 sits at (0,0) with velocity (1,2) in the implicit space; after 60 ticks of the bootstrap integrate rule the engine's diff must be exactly these two set ops.",
  "ticks": 60,
  "nodes": [
    {
      "id": "n2",
      "type": "tapestry.notes/note@1",
      "props": {
        "title": { "type": "text", "value": "mover" },
        "position.x": { "type": "real", "value": 0 },
        "position.y": { "type": "real", "value": 0 },
        "velocity.x": { "type": "real", "value": 1 },
        "velocity.y": { "type": "real", "value": 2 },
        "pinned": { "type": "bool", "value": false }
      }
    },
    {
      "id": "n3",
      "type": "tapestry.notes/note@1",
      "props": {
        "title": { "type": "text", "value": "no position, not in the image" }
      }
    }
  ],
  "expected": [
    { "op": "setProperty", "target": "n2", "key": "position.x", "type": "real", "value": 60 },
    { "op": "setProperty", "target": "n2", "key": "position.y", "type": "real", "value": 120 }
  ]
}
