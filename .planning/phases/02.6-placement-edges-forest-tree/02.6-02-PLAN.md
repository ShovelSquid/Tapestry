---
phase: 02.6-placement-edges-forest-tree
plan: 02
type: execute
wave: 2
depends_on: ["02.6-01"]
files_modified:
  - app/src/main/space/shapes.ts
  - app/src/main/space/forest-store.ts
  - app/src/main/space/home-tree.ts
  - app/src/main/space/migrate.ts
  - app/src/main/space/space-service.ts
  - app/src/main/space/space-service.test.ts
  - app/src/main/settings.ts
  - app/src/main/settings.test.ts
autonomous: false
requirements: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-13 / D-14 / SC-7: no code writes a Tapestry-tree record, a forest record or a v2 settings file until Kaelen has answered the Task 1 checkpoint, and the answer is recorded in shapes.ts and the SUMMARY"
    - "D-02 / SC-1: starting the space opens one Tapestry tree, and settings.json holds a pointer to it; the Tapestry tree's only job in 2.6 is to reference the forest (Plan 03 calls this at app launch)"
    - "D-06: the forest is one normal visible `.tree` file at an injected path (the app passes the approved folder, default `~/Documents/Tapestry/Forest.tree`, in Plan 03), opened by the space without Kaelen picking it"
    - "D-10 / SC-1: on the first launch after upgrade, every open tree and its frame position in settings.json is copied into a new forest in one system commit; the `trees` list stays exactly as it was; `version` becomes 2 and the pointer is written last"
    - "D-01 / D-04 / SC-3: each frame is a `placement` edge from the forest's space node to a member stand-in carrying origin.x and origin.y as real; frames leave size unset; no kernel verb or value type is added"
    - "D-11 / SC-2: one `moveFrames` batch (a drop with every frame it pushed aside) is exactly one forest commit signed by the person"
    - "SC-2: closing and relaunching reproduces the arrangement from the forest alone, even when settings.json `trees` holds different frame numbers"
    - "D-03: members are stored with path.hint (and digest once known); a registry `path:` id is never written into the forest file"
    - "D-05 / SC-6: note positions and D-27's thread frame are untouched — nothing under app/src/main/space reads or writes a note-position property"
    - "D-07: exactly one forest is resolved; no forest switching exists"
    - "D-09 groundwork: the forest bridge is never rewound, so every later drag, fit or frame undo can commit"
    - "TREE-01 / PROV-03 (exercised, not owned — the phase owns no requirement IDs): the forest is readable text and every commit in it names its actor"
  prohibitions:
    - "No forest or Tapestry-tree record, and no settings version 2, is written before Task 1 is answered"
    - "No registry `path:` id string is written to the forest file"
    - "The forest and Tapestry bridges are never adopted by TreeRegistry and are unreachable through primaryBridgeProxy, resolveRef, list or summary"
    - "No module, class or type string reuses the names 2.2 Plan 15 reserves"
    - "The kernel (tapestry/kernel/**) and the addon (app/native/**) are not modified"
    - "app/src/main/commands/notes.ts and app/src/renderer/components/NoteCard.tsx are not modified"
    - "No test uses app.getPath, ~/Documents or the live userData; every forest and Tapestry path is injected"
    - "No dependency is installed or changed"
  artifacts:
    - path: "app/src/main/space/shapes.ts"
      provides: "The approved #17 names — the only place they are spelled"
    - path: "app/src/main/space/forest-store.ts"
      provides: "ForestStore: one forest journal outside the registry; members, placements, one-commit creation and origin writes"
      exports: ["ForestStore"]
    - path: "app/src/main/space/home-tree.ts"
      provides: "TapestryHome: the Tapestry tree's root node and its forest reference; rootNodeId() for 2.4"
      exports: ["TapestryHome"]
    - path: "app/src/main/space/migrate.ts"
      provides: "Launch resolution: classifySpace, the case A import, the case D reopen, SpaceProblem and its approved wording"
      exports: ["classifySpace", "importFromSettings", "reopenFromPointer", "problemMessage"]
    - path: "app/src/main/space/space-service.ts"
      provides: "SpaceService: start, list join by member identity, moveFrames, close"
      exports: ["SpaceService"]
    - path: "app/src/main/space/space-service.test.ts"
      provides: "Two-launch integration tests against the real addon"
  key_links:
    - from: "app/src/main/space/space-service.ts start()"
      to: "app/src/main/space/migrate.ts classifySpace / importFromSettings / reopenFromPointer"
      via: "start resolves the pointer, imports (case A) or reopens (case D), then restores members through registry.tryOpen"
      pattern: "classifySpace\\("
    - from: "app/src/main/space/space-service.ts moveFrames"
      to: "app/src/main/kernel-bridge.ts submitAs"
      via: "ForestStore.setOrigins submits one commit with the actor main resolved"
      pattern: "submitAs\\("
    - from: "app/src/main/space/migrate.ts importFromSettings"
      to: "app/src/main/settings.ts setTapestryPointer"
      via: "the pointer and version 2 are written only after both trees exist"
      pattern: "setTapestryPointer\\("
---

<objective>
**As** Kaelen, **I want to** arrange my trees and have that arrangement kept in a readable forest tree referenced from one always-open Tapestry tree, **so that** arranging trees gets an actor, undo, history and branches like any other edit.

*(User story derived from the ROADMAP Phase 2.6 Goal line; its "so that" clause is quoted verbatim. The Goal line is not in user-story form, which was flagged to the orchestrator.)*

This plan opens with the phase's one-way doors and then walks through them on the thinnest real path:

1. **Task 1 — blocking checkpoint (D-13, D-14, D-10 specifics).** Kaelen approves the on-disk record shape (#17), the settings migration (#18), how far Ctrl+Z reaches after a frame drag (RESEARCH Open Question 2), and every new sentence the phase shows (Open Question 3). It also records the answer to Open Question 6. Nothing after it may run until he answers.
2. **Task 2 — tracer.** Start the space → import today's `settings.json` arrangement into a new forest and Tapestry tree → restore members from the forest → list frames from placement edges → one drop batch writes one signed commit → a second start puts every frame back from the forest alone. The far end reachable by an automated check is the `SpaceService` API, because `index.ts` imports Electron and has no tests. The integration test drives exactly the calls `index.ts` will make.

Plan 03 attaches the app ends (startup, shutdown, `trees:list`, `trees:moveFrames`, preload, types, the Canvas drop). It is a separate plan so this one stays inside the context budget, and so the launch cases that write nothing (E, G, H) are proved there, beside the startup code that relies on them.

**Name guard.** 2.2 Plan 15 (pending on main) creates `app/src/main/commands/forest.ts`, the class `ForestCommands` and the type `tapestry.forest/remote@1`. This phase lives in `app/src/main/space/` and must not use those module or class names. 2.5 (`ws/agent-trees`) uses "placement" for note positions stored on the node; the `placement` **edge** here is a different record, and moving notes onto placement edges is phase 2.7, not 2.6.

**Interim state until Plans 04-05.** Nothing in the app calls the space until Plan 03. After Plan 03, `trees:open`, `trees:create`, `trees:close`, `vault:add` and `trees:setFrame` still write the old settings list until Plan 04 (membership) and Plan 05 (fit) convert them. Do not run the app against real user data before Plan 06's human check.

**Base caveat and shared-machine rules:** as in Plan 01. Plans target main `8c4e219`; the merge was confirmed in Plan 01 Task 1. Never `npm install`; stage by name; `git show --stat HEAD` after each commit; tests inject every path.

Output: the approved shapes, the space module, the settings pointer, and a drop batch that is recorded in the forest.

Requirements: the phase owns none (ROADMAP). This plan exercises TREE-01 (readable file) and PROV-03 (actor on every edit) through integration only, so `requirements` is empty.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-CONTEXT.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-01-SUMMARY.md

<!-- Context budget: RESEARCH.md (84 KB), PATTERNS.md, FORMAT.md and VALIDATION.md are NOT loaded whole.
     Read only the sections each task's read_first names. -->

<interfaces>
<!-- From Plan 01 (on disk once it lands). -->
```typescript
// app/src/main/settings.ts
class SettingsStore {
  read(): AppSettings                                  // version is the file's own number
  update(mutator): AppSettings                         // keeps unknown keys and raw trees
  readLegacyTrees(): { trees: TreeSetting[]; skipped: number }
  migrateLastOpened(file: string): boolean             // still present; Plan 03 stops index.ts calling it
}
// app/src/main/trees/registry.ts
export function classifyOpenFailure(detail: string): 'locked' | 'damaged'
export function closeQuietly(bridge: KernelBridge): void
export class TreeIdentityClash extends Error { treeId: string; openPath: string; openName: string }
interface OpenTreeOptions { kind?; vaultRoot?; name?; expect?: { id: string; reason: string } }
TreeRegistry.setReserved(r: { paths: string[]; ids: string[]; refusal: (name: string) => string } | null): void
TreeRegistry.tryOpen(path, opts): OpenTree | UnavailableTree
TreeRegistry.entry(treeId): TreeEntry | null
TreeRegistry.summary(): TreeSummary[]      // { id, name, kind, path, vaultRoot?, status, reason? }
```

<!-- KernelBridge at 8c4e219 (app/src/main/kernel-bridge.ts). -->
```typescript
create(path: string, worldName: string): void   // kernel opens O_CREAT|O_EXCL; does not create parent dirs; worldName is one token
open(path: string): void                        // does not throw on a damaged journal: check status().kind === 'Ok'
close(): void                                   // releases flock; a second bridge in the same process gets "journal lock"
submitAs(actor: Actor, message: string, ops: OpObject[]): CommitResult  // throws while rewound
getNodes(): NodeData[]   // { id, type, props: Record<string, { type, value }> }
getEdges(): EdgeData[]   // { id, from, to, label, props }
getHeaderDigest(): string  // 'sha256:<hex>'
getNextIds(): { node: string; edge: string }    // throws while rewound
getHistoryIndex(): HistoryIndex
status(): { kind: 'Ok' | 'TornTail' | 'Corrupt'; lastGoodSeq: number; reason: string; ... }
get isRewound(): boolean
// CommitResult { seq, digest, nodeIds: string[], edgeIds: string[] }
```

<!-- Op objects the addon accepts (app/native/addon.cpp jsToOp). -->
```typescript
{ op: 'createNode', type, props: { key: { type: 'text' | 'real' | ..., value } } }
{ op: 'createEdge', from: 'n1', to: 'n2', label, props }
{ op: 'setProperty', target: 'e1', key, type: 'real', value }   // type is required
{ op: 'deleteNode', id: 'n2' }                                   // cascades to its edges
```

<!-- Actors (app/src/main/commands/actor.ts). -->
```typescript
export function humanActor(userName: string): Actor   // { kind: 'human', id: 'user.<name>' }
export const SYSTEM_ACTOR: Actor                       // { kind: 'system', id: 'tapestry' }
```

<!-- Commit record shape in a .tree file (tapestry/docs/tree/example.tree). -->
```text
@commit 2 303
parent sha256:...
branch main
recorded 2026-09-08T21:17:07Z
tick 0
actor human kaelen
message "who Sam is"
create-node n2 example.people/person@2
```
</interfaces>
</context>


<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Decision: lock the Tapestry-tree and forest record shape (#17, D-13), the settings.json migration (#18, D-10, D-14), Ctrl+Z reach after a frame drag, and the new wording (visible change)</name>
  <read_first>
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q7 (record shape proposal), § Q8 (migration proposal, cases A-J), Open Questions 2, 3 and 6
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-CONTEXT.md` — D-06, D-10, D-13, D-14
    - `~/Tapestry Tales/Decision Register.md` — Pending #17 and #18 (read with `cat`; outside the repo)
    - Pre-check before presenting: no code in this worktree can yet write a forest, a Tapestry tree or a v2 settings file, so `git grep -n "SETTINGS_POINTER_KEY" -- app/src` prints nothing
  </read_first>
  <decision>Approve the exact names written into every Tapestry tree and forest, the exact first-launch migration of settings.json, how far Ctrl+Z walks back through frame moves, and every new sentence this phase shows</decision>
  <context>
    **Why this is a one-way door.** A `.tree` history is append-only and replay keeps old names forever. Once one forest is written, its node types, property keys and edge label must be read by every later version of Tapestry, with no migration (D-13). The v2 `settings.json` shape and the imported forest are what every later launch reads (D-10, D-14). Nothing after this checkpoint runs until Kaelen answers. Every name below is already legal `.tree` v1: types and labels are tokens, keys match `[A-Za-z_][A-Za-z0-9_.:-]*`, and no kernel verb or value type is added (D-04). Source: RESEARCH § Q7 and § Q8.

    **Visible change.** Part 4 adds new sentences to the app. UI-SPEC generation for 2.6 was skipped, so this checkpoint is where that wording is approved.

    ---

    **PART 1 — Record shape (#17, D-13)**

    *Option A (Recommended).* A generic space type, a forest-specific member type, and a separate `tapestry.home` namespace for the Tapestry tree. 2.2 Plan 15 already claims `tapestry.forest/` for `tapestry.forest/remote@1`; `member@1` would sit beside it.

    `~/Documents/Tapestry/Tapestry.tree` (world `Tapestry`):

        create-node n1 tapestry.home/tapestry@1
        set n1 title text "Tapestry"
        create-node n2 tapestry.home/forest@1
        set n2 digest text "sha256:(64 hex digits of Forest.tree's header)"
        set n2 path.hint text "/Users/kaelencook/Documents/Tapestry/Forest.tree"
        set n2 title text "Forest"
        create-edge e1 n1 n2 forest

    `~/Documents/Tapestry/Forest.tree` (world `Forest`), imported from today's settings.json:

        create-node n1 tapestry.spaces/space@1
        set n1 kind text "canvas"
        set n1 title text "Forest"
        create-node n2 tapestry.forest/member@1
        set n2 kind text "native"
        set n2 path.hint text "/Users/kaelencook/Documents/bega.tree"
        create-node n3 tapestry.forest/member@1
        set n3 kind text "vault"
        set n3 path.hint text "/Users/kaelencook/Vault/Vault.tree"
        set n3 vault.root.hint text "/Users/kaelencook/Vault"
        create-edge e1 n1 n2 placement
        set e1 origin.x real -245.01090741236075
        set e1 origin.y real -65.50898866060388
        create-edge e2 n1 n3 placement
        set e2 origin.x real 1378.6410165714733
        set e2 origin.y real -123.50898866060388

    Later, when bega.tree first opens, a system commit writes `set n2 digest text "sha256:..."`. A drag writes `set e1 origin.x real 40` and `set e1 origin.y real 12.5` under `actor human user.kaelen`.

    *Option B.* Everything under `tapestry.forest/`: `tapestry.forest/forest@1` (space), `tapestry.forest/member@1` with keys `member.tree` (digest) and `member.path`, and `tapestry.forest/tapestry@1` for the root. Gain: one vocabulary with 2.2's `remote.tree`. Cost: the space type is forest-specific, so 2.7 (notes onto placements) needs a second space type. It also departs from the Register #17 wording (`digest`, `path.hint`) you have already seen.

    *Option C.* `tapestry.spaces/space@1` for the space, and `tapestry.spaces/tree@1` for members. Gain: one namespace for the whole space model. Cost: `tree@1` may collide with a future "tree placed on a board" in 2.7.

    Sub-questions (each is part of the door):
    - 1.1 Names: **A (Recommended)** / B / C.
    - 1.2 Member identity key: **`digest` (Recommended)** / `tree.digest` / `member.tree`.
    - 1.3 Path hints: **absolute, as the code already stores them (Recommended)** / home-relative (`~/Documents/bega.tree`). Home-relative keeps your account name out of a file you might share or sync and survives a different username on another machine, at the cost of one expansion step when reading. (Threat T-2.6-06.)
    - 1.4 Defaults: **write only `origin.x` and `origin.y` for a frame; leave `origin.z`, `direction.*`, `roll` and `size.*` unwritten, with their defaults stated in the reader's guide (Recommended)** / write every value explicitly.
    - 1.5 **Store `vault.root.hint` (Recommended)** / derive it from the path hint's folder.
    - 1.6 Member trees record "I belong to forest ...": **not in 2.6 (Recommended)**, because it would write into every member, including vault trees and other people's trees / yes.
    - 1.7 The forest names its Tapestry tree back: **no (Recommended)** / yes.
    - 1.8 Actors: drag, add, close and frame undo are signed **`human user.(your name)`**; import, first-open digest, automatic fit and duplicate clean-up are signed **`system tapestry`** (Recommended; follows D-11, D-12 and 2.2 D-21).
    - 1.9 World names: **`Tapestry` and `Forest` (Recommended)**.
    - 1.10 Where the Tapestry tree lives: **`~/Documents/Tapestry/Tapestry.tree`, beside the forest, readable without the app (Recommended)** / `~/Library/Application Support/@tapestry/app/Tapestry.tree`, hidden with the settings.
    - 1.11 Forest file: **`~/Documents/Tapestry/Forest.tree` (Recommended, D-06's example)**.
    - 1.12 Commit messages (free text, for completeness): `import (n) trees and their frames from settings.json` (plus `; skipped (k) unreadable entries`), `create Tapestry tree referencing Forest.tree`, `add tree "(name)"`, `remove tree "(name)" from the forest`, `move frame "(name)"` or `move frame "(name)" and push (k) aside`, `undo move frame "(name)"`, `redo move frame "(name)"`, `fit frame "(name)" beside its neighbours`, `record identity of "(name)"`, `forget "(name)" at (file): it is the same world as "(other)"`.

    ---

    **PART 2 — First-launch migration (#18, D-10, D-14)**

    Today's settings.json on this machine: `version 1`, `userName "kaelen"`, `agentsEnabled true`, and `trees` with `bega.tree` (native) and `Vault/Vault.tree` (vault). `last-opened.json` is also present.

    Proposed v2:

        {
          "version": 2,
          "userName": "kaelen",
          "agentsEnabled": true,
          "trees": [ ...exactly as before, never written again... ],
          "tapestry": { "path": "/Users/kaelencook/Documents/Tapestry/Tapestry.tree" }
        }

    - 2.1 Pointer key: **`tapestry` (Recommended)** / `home` / `tapestryTree`. Contents: **path only (Recommended)**. The Tapestry tree's own header is its identity, and a stored digest would add a "pointer disagrees with file" case / path plus digest.
    - 2.2 **`version` becomes 2 when the pointer is written. `trees` stays exactly as it is and is never written again. The three settings tree writers are deleted at the end of the phase (Recommended)** / kept, marked deprecated.
    - 2.3 Import commit: **`actor system tapestry`, message `import (n) trees and their frames from settings.json` (Recommended)**. Nobody authored it at that moment, and on a fresh install no name exists yet / signed `human user.kaelen`.
    - 2.4 Order: create the folder, then `Forest.tree` in one system commit (space node plus every valid entry, vault entries included, positions exactly as stored), then `Tapestry.tree` in one system commit, then **write the pointer last**. A crash before the pointer leaves files without a pointer, which is case B or C.
    - 2.5 **When `trees` is empty and last-opened.json names a file that exists, that file becomes the first member at (0, 0). `trees` is not written (Recommended).**
    - 2.6 Rollback: an older build reading a v2 file still sees `trees` and shows the arrangement as it was before the upgrade. Moves made after the upgrade are not in `trees`. Several builds of the app share one settings folder (RESEARCH Pitfall 6).
    - 2.7 Behaviour change: adding, creating and closing a tree now writes a signed commit, so it needs your name first ("Set your name before making changes"), like any other edit. The first-run name prompt already blocks this.

    Cases (risky ones marked):
    - **A. No pointer, no Tapestry.tree** (first launch after upgrade, or a fresh install): import as in 2.4.
    - **B. No pointer, Tapestry.tree exists (HIGH RISK).** Cause: a crash before the pointer write, or an older build rewrote settings.json. **(i) Recommended:** open it; if its root has the Tapestry root type, re-add the pointer and **do not import `trees` again**. If it is not a Tapestry file, refuse and show a notice; never overwrite it. **(ii)** Also re-import any trees listed in `trees` that the forest lacks, as a system commit. With (i), changes made in an older build after the upgrade do not appear.
    - **C. No pointer, no Tapestry.tree, Forest.tree exists (MEDIUM RISK).** Cause: a crash between the two creates, or your own file with that name. **(i) Recommended:** if its space node has the forest's space type, reuse it without importing, then create Tapestry.tree referencing it. Otherwise refuse with a notice, and never write into it. **(ii)** Always refuse with a notice.
    - **D. Pointer present, file present and healthy:** open Tapestry.tree, read the forest reference, open the forest at its path hint, and check its digest equals the reference.
    - **E. Pointer present, Tapestry.tree missing (HIGH RISK).** **(i) Recommended:** show a notice, open the space empty, write nothing, do not create a new one (that would start a second identity). **(ii)** Create a new Tapestry tree and forest.
    - **F. Pointer present, Tapestry.tree damaged (HIGH RISK):** as E(i), with the kernel's reason. It is never repaired.
    - **G. Tapestry.tree or Forest.tree locked (another Tapestry is running; MEDIUM RISK):** **Recommended:** show a notice, open no members (they would all be locked too), write nothing.
    - **H. The forest's digest differs from the reference** (a different file now sits there): treat the forest as missing, close it, write nothing.
    - **I. settings.json `version` above 2** (a newer build ran): read what is understood and keep everything else on write.
    - **J. A malformed `trees` entry:** skip it in the import, leave it in the file, and count it in the commit message.

    ---

    **PART 3 — How far Ctrl+Z reaches (RESEARCH Open Question 2; D-08, D-09)**

    After two frame drags in a row, a second Ctrl+Z:
    - **(a) run (Recommended):** also undoes the earlier drag. Ctrl+Shift+Z redoes frame moves. The run ends when you edit, select or start editing a note or frame, or undo a tree. After that, Ctrl+Z undoes the selected tree, as today.
    - **(b) single:** falls through to the selected tree's undo.

    Either way a frame undo is a new commit that writes the old position back, not a rewind (D-09). The choice lives in one constant, `FRAME_UNDO_REACH` in `app/src/renderer/state/undo-target.ts`.

    ---

    **PART 4 — New wording (visible change; tone follows the 2.2 UI-SPEC Copywriting Contract)**

    - 4.1 Where space problems appear: **the existing app-error banner, which stays until dismissed (Recommended)** / the 6-second transient notice.
    - 4.2 Tapestry tree missing (E): "Tapestry can't find its Tapestry tree at [path]. Your trees are untouched, but the space stays empty until the file is back. Nothing was written."
    - 4.3 Forest missing: "Tapestry can't find your forest at [path]. Your trees are untouched, but the space stays empty until the file is back. Nothing was written."
    - 4.4 Damaged (F, or a damaged forest): "[file name] is damaged, so Tapestry won't open or write to it. Your trees are untouched, and the space stays empty until the file is restored. ([kernel reason])"
    - 4.5 Locked (G): "Tapestry is already open in another window, so this window left your space closed. Quit the other window, then relaunch."
    - 4.6 Wrong forest (H): "[file name] at [path] isn't the forest your Tapestry tree names, so Tapestry left it alone. The space stays empty until the right file is back."
    - 4.7 Not a Tapestry file (B or C refused): "[file name] at [path] isn't a Tapestry file, so Tapestry left it alone and didn't set up your space."
    - 4.8 Setup failed (for example, the folder can't be created): "Tapestry couldn't set up your space in [folder]: [reason]. Nothing in settings.json was changed."
    - 4.9 Refusal while there is no space: "Your space isn't open, so trees can't be added, closed or moved right now."
    - 4.10 Adding Tapestry's own file: "[file name] is Tapestry's own arrangement file and can't be added as a tree."
    - 4.11 Frame reason when a different world sits at a member's path: "A different world is now at [path], so this tree's file can't be found."
    - 4.12 A drop that could not be recorded: "Could not move the frame: [reason]"
  </context>
  <options>
    <option id="recommended">
      <name>Option A names with every answer marked Recommended in Parts 1-4</name>
      <pros>Matches the Register #17 wording you have seen (`digest`, `path.hint`). Keeps a general space type that 2.7 can reuse for boards. Leaves `tapestry.forest/` holding only forest-specific things beside 2.2's `remote@1`. Import is signed by the system, so no author is guessed. Recovery never imports twice and never overwrites a file it did not create.</pros>
      <cons>Uses three namespaces (`tapestry.home`, `tapestry.spaces`, `tapestry.forest`). Absolute path hints include your account name. Under case B(i), changes made in an older build after the upgrade are not carried over.</cons>
    </option>
    <option id="option-b">
      <name>Option B names (everything under tapestry.forest/, keys member.tree and member.path), other answers as recommended</name>
      <pros>One vocabulary with 2.2's cross-tree stubs (`remote.tree` beside `member.tree`).</pros>
      <cons>The forest-specific space type does not carry over to 2.7 boards. Departs from the Register #17 key names.</cons>
    </option>
    <option id="option-c">
      <name>Option C names (tapestry.spaces/space@1 and tapestry.spaces/tree@1), other answers as recommended</name>
      <pros>A single namespace for the whole space model.</pros>
      <cons>`tapestry.spaces/tree@1` may collide with a later "tree placed on a board" record.</cons>
    </option>
    <option id="custom">
      <name>Any mix — name the numbered items to change</name>
      <pros>Exact control over each part of the door.</pros>
      <cons>Each change is recorded verbatim in shapes.ts and the SUMMARY, and later plans follow it.</cons>
    </option>
  </options>
  <resume-signal>Reply `recommended`, `option-b` or `option-c`, or list overrides by number, for example "1.3 home-relative, 2.B ii, 3 single, 4.5: (your wording)". Execution does not continue until you reply.</resume-signal>
  <acceptance_criteria>
    - Kaelen's reply is present in this session and names `recommended`, `option-b`, `option-c`, or a list of numbered overrides
    - Every numbered item (1.1-1.12, 2.1-2.7, cases A-J, Part 3, 4.1-4.12) has one resolved value, recorded by Task 2 in `02.6-02-SUMMARY.md` § Checkpoint decisions
    - No file under `app/src/main/space/` exists before the reply
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2 (tracer): Launch imports today's arrangement into the forest, a drop is one signed commit, and a second launch puts every frame back from the forest alone</name>
  <precondition>Kaelen has answered Task 1 in this session (a reply of `recommended`, `option-b`, `option-c` or a list of overrides); without that reply this task does not start</precondition>
  <reversibility rating="one-way">Every forest and Tapestry tree written from here on replays these type strings, keys and edge labels forever, and every later launch reads the v2 settings shape (D-10, D-13, D-14); D-06's default folder is costly to move later.</reversibility>
  <files>app/src/main/space/shapes.ts, app/src/main/space/forest-store.ts, app/src/main/space/home-tree.ts, app/src/main/space/migrate.ts, app/src/main/space/space-service.ts, app/src/main/space/space-service.test.ts, app/src/main/settings.ts, app/src/main/settings.test.ts</files>
  <read_first>
    - Kaelen's Task 1 reply (this session)
    - `app/src/main/obsidian/shapes.ts` — header comment and constant style (analog for `space/shapes.ts`)
    - `app/src/main/obsidian/vault-service.ts` — file header (why a host service lives in main), constructor with hooks, `requireOpen`, the `fail` helper, listener isolation with `console.error`
    - `app/src/main/commands/notes.ts` — `createFrom` only, the predicted-id single commit (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/main/commands/notes.ts`, lines 289-322). Read only; this file is never edited (D-05)
    - `app/src/main/kernel-bridge.ts` — `create`, `open`, `close`, `submit`, `submitAs`, `getHeaderDigest`, `getNextIds`, `isRewound`, `status`
    - `app/src/main/trees/registry.ts` — Plan 01's exports, `tryOpen`, `entry`, `summary`, `primaryBridgeProxy`, `resolveRef`
    - `app/src/main/settings.ts` and `app/src/main/settings.test.ts` — Plan 01's passthrough, `readLegacyTrees`, and `migrateLastOpened` (its validation is reused for last-opened.json)
    - `app/src/main/commands/actor.ts` — `humanActor`, `SYSTEM_ACTOR`, `Actor`
    - `app/src/main/trees/registry.test.ts` and `app/src/main/kernel-history.test.ts` — the `afterEach` flock cleanup, the locked-case pattern, `getHistoryIndex` assertions
    - `app/test/helpers/temp-tree.ts`, `app/test/helpers/real-data-guard.ts`
    - `tapestry/docs/tree/FORMAT.md` — "Keys and tokens", "Value types", "Identifiers"
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q1, § Q3, § Q6, § Q7, § Q8, Patterns 1-2, Pitfalls 2, 3, 6, 8, 9
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-PATTERNS.md` — § shapes, § space-service, § forest-store and home-tree, § migrate, § tests
  </read_first>
  <action>
    Wherever this action names a value in brackets, it is the **Recommended** value from Task 1. Use the value Kaelen approved instead wherever his reply differs.

    **0. Record the answers.** Create `.planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md` now with a `## Checkpoint decisions` section. It holds Kaelen's reply verbatim, then the resolved value of every numbered item (1.1-1.12, 2.1-2.7, cases A-J, Part 3, 4.1-4.12). Plans 03-06 read this section. Complete the rest of the SUMMARY at the end as usual.

    **1. `app/src/main/space/shapes.ts`** — the only place the #17 names are spelled. Follow the `obsidian/shapes.ts` style. Its doc comment says the names were approved at the 2.6 Plan 02 checkpoint (with the date), that they are legal in `.tree` format version 1 (FORMAT.md "Keys and tokens"), that frames write only the origin keys and leave size, depth, direction and roll unwritten per answer 1.4 (D-04), that 2.2 already uses `tapestry.forest/remote@1`, and that 2.5's note "placement" is a node property and not this edge (notes onto placement edges is phase 2.7). Constants (the names stay the same whichever option was chosen; only the values change): `HOME_ROOT_TYPE` [`tapestry.home/tapestry@1`], `HOME_FOREST_TYPE` [`tapestry.home/forest@1`], `HOME_FOREST_LABEL` [`forest`], `SPACE_TYPE` [`tapestry.spaces/space@1`], `MEMBER_TYPE` [`tapestry.forest/member@1`], `PLACEMENT_LABEL` [`placement`], `KEY_TITLE` [`title`], `KEY_KIND` [`kind`], `KEY_DIGEST` [`digest`], `KEY_PATH_HINT` [`path.hint`], `KEY_VAULT_ROOT_HINT` [`vault.root.hint`], `KEY_ORIGIN_X` [`origin.x`], `KEY_ORIGIN_Y` [`origin.y`], `SPACE_KIND_CANVAS` [`canvas`], `MEMBER_KIND_NATIVE` [`native`], `MEMBER_KIND_VAULT` [`vault`], `FOREST_TITLE` [`Forest`], `HOME_TITLE` [`Tapestry`], `FOREST_WORLD` [`Forest`], `HOME_WORLD` [`Tapestry`], `FOREST_FILE` [`Forest.tree`], `HOME_FILE` [`Tapestry.tree`], `HOME_LOCATION` [`'space-dir'`; the other value is `'user-data'`, per answer 1.10], `SETTINGS_POINTER_KEY` [`tapestry`], `SETTINGS_VERSION` [`2`]. If answer 1.3 chose home-relative hints, also export `toHint(absPath, home)` and `fromHint(hint, home)` here and use them everywhere a hint is written or read.

    **2. `app/src/main/settings.ts` — the pointer (D-02, answer 2.1).** Import `SETTINGS_POINTER_KEY` and `SETTINGS_VERSION` from `./space/shapes`. Add `getTapestryPointer(): string | null`. It reads the raw object's pointer key and returns its `path` only when that is a safe absolute path ending in `.tree` (the same checks `validateTree` uses); otherwise it returns null. Add `setTapestryPointer(path: string): void`. It refuses an unsafe path with `Invalid Tapestry tree path`, then writes through the Plan 01 merge: the raw object with the pointer object set, `version` set to the larger of the file's valid version and `SETTINGS_VERSION`, and `trees` left raw. The pointer is not a field of `AppSettings`, so `update()` keeps it as an unknown key. Tests in `settings.test.ts`: a relative path, a `..` segment, a non-`.tree` path and a non-object pointer each read as null; after `setTapestryPointer`, the file has `version` 2, the pointer, and a `trees` value deep-equal to before; a later `setUserName` keeps the pointer; an invalid path throws and leaves the file bytes unchanged.

    **3. `app/src/main/space/forest-store.ts` — `ForestStore`.** It wraps one `KernelBridge` that is never handed to `TreeRegistry`. The header says why (RESEARCH Pitfall 2: plugins and agents reach trees only through the registry).
    - `static createWithMembers(path, seeds: MemberSeed[], actor, message): ForestStore`. `MemberSeed` is `{ kind, pathHint, vaultRootHint?, digest?, origin: { x, y } }`. It runs `mkdirSync(dirname(path), { recursive: true })`, then `bridge.create(path, FOREST_WORLD)`, then submits one commit: the space node (title, kind), then one member node per seed (kind, path hint, vault root hint for vault seeds, digest only when known), then one placement edge per seed from the space node to that member, with `origin.x` and `origin.y` as `{ type: 'real', value }` (RESEARCH Pitfall 3; never an untyped number). Predict ids from `getNextIds()` with one helper, `nodeIdAt(first, offset)`. It accepts only the `n(digits)` form and throws on anything else, because Phase 3's branch-tagged ids will break the arithmetic. Afterwards, check every entry of `result.nodeIds` against the prediction (the `createFrom` pattern) and throw with both lists if they differ. If the commit itself throws, close the bridge, delete the file this call just created (the kernel created it with exclusive-create moments earlier, so it holds only a header and is ours), and rethrow.
    - `static open(path): ForestStore | OpenProblem`. `OpenProblem` is `{ status: 'missing' | 'locked' | 'damaged' | 'foreign'; reason: string }`. Follow `registry.tryOpen`: check existence first, then open in try/catch using `classifyOpenFailure`, then `status().kind !== 'Ok'` means damaged. Release the bridge with `closeQuietly` on every failure. A journal with no node of `SPACE_TYPE` is `foreign` and is closed without writing anything.
    - Read helpers: `path`, `digest()`, `spaceNodeId()` (the lowest-id node of `SPACE_TYPE`), `members(): ForestMember[]` sorted by numeric node id. `ForestMember` is `{ nodeId, kind, pathHint, vaultRootHint?, digest?, placement: { edgeId, x, y } | null }`, and the placement is the edge from the space node with `PLACEMENT_LABEL`. Also `headSeq()` returning `status().lastGoodSeq`.
    - `setOrigins(changes: Array<{ edgeId, x, y }>, actor, message): CommitResult` writes `setProperty` real ops for both origin keys.
    - Every write first calls `assertWritable()`, which throws if the bridge is rewound (RESEARCH Pitfall 8). This module never calls the bridge's rewind or redo methods; D-09's frame undo is a compensating commit (Plan 05).
    - `close()`.

    **4. `app/src/main/space/home-tree.ts` — `TapestryHome`.**
    - `static createReferencing(path, forest: { digest, pathHint }, actor, message)`: mkdir the parent, `bridge.create(path, HOME_WORLD)`, then one commit containing the root node (title), the forest node (digest, path hint, title) and the edge from root to forest labelled `HOME_FOREST_LABEL`. It uses the same predicted-id check and the same delete-on-failed-first-commit rule as the forest.
    - `static open(path): TapestryHome | OpenProblem`. It is `foreign` when there is no node of `HOME_ROOT_TYPE`.
    - `rootNodeId()`, with a doc comment that 2.4's lock ceiling hangs its properties on this node (Spec - Locks and Rank §3). `forestRef(): { digest, pathHint } | null`, read from the lowest-id forest node linked from the root. `digest()`, `headSeq()`, `close()`.

    **5. `app/src/main/space/migrate.ts` — launch resolution (D-10, D-14).**
    - Export `SpacePaths` as `{ forest, home, lastOpenedFile }`, all absolute and injected.
    - Export `SpaceProblem` as `{ kind: 'home-missing' | 'home-damaged' | 'home-locked' | 'home-foreign' | 'forest-missing' | 'forest-damaged' | 'forest-locked' | 'forest-foreign' | 'forest-mismatch' | 'not-set-up' | 'setup-failed'; path: string; reason?: string }`, and `problemMessage(problem): string` returning the approved Part 4 wording (4.2-4.8) with the file name, path and reason filled in. Both locked kinds use 4.5, and `not-set-up` uses 4.7 until Plan 06 adds case B and C recovery.
    - Pure `classifySpace({ pointer, homeExists, forestExists }): 'import' | 'reopen' | 'recover-home' | 'recover-forest'`. A pointer means `reopen` (cases D-H). No pointer with a home file means `recover-home` (B). No pointer with only a forest file means `recover-forest` (C). Otherwise `import` (A). When a pointer is present, the home path used is the pointer's path, not the default.
    - `importFromSettings(settings, paths): { home: TapestryHome; forest: ForestStore }` for case A. Read `settings.readLegacyTrees()`. If it holds no trees, apply answer 2.5: read `paths.lastOpenedFile` once with the same checks `migrateLastOpened` uses (safe absolute path, `.tree` suffix, file exists) and seed it as one native member at (0, 0). Build seeds in file order, vault entries included, each vault entry's `vaultRoot` becoming its root hint, and origins exactly as stored. Create the forest with `SYSTEM_ACTOR` and the approved import message, adding `; skipped (k) unreadable entries` when `skipped > 0` (case J). Then create the home tree referencing the forest's digest and path. Only then call `settings.setTapestryPointer(paths.home)`. Nothing else in settings.json changes, and `trees` is not written (D-10).
    - `reopenFromPointer(homePath): { home, forest } | SpaceProblem` for cases D-H. Open the home; a problem maps to the `home-*` kinds. A home with no forest reference is `home-foreign`. Before opening the forest path hint, check it has the safe-absolute `.tree` shape. Open the forest; a problem maps to the `forest-*` kinds. If the forest digest differs from the reference digest, close the forest and return `forest-mismatch` (case H). Any problem closes whatever this call opened and writes nothing.
    - Cases `recover-home` and `recover-forest` return `not-set-up` in this plan and write nothing. Plan 06 implements the approved recovery. Any exception during the import, such as a folder that cannot be created, returns `setup-failed` with the reason. Settings stay byte-identical, because the pointer is written last.

    **6. `app/src/main/space/space-service.ts` — `SpaceService`.** Copy the `VaultService` header reasoning: the arrangement is host state, so plugins and agents must not reach it.
    - The constructor takes `{ registry, settings, paths: SpacePaths, hooks?: { approvePath?(path): void; restoreVault?(req: { treePath, vaultRoot, name }): Promise<void> } }`.
    - `start(): Promise<{ problem: SpaceProblem | null; restored: number }>`. Classify, then import or reopen, then keep `home` and `forest`. Restore members in stand-in order. Skip a path hint that fails the shape check, logging `[SpaceService]`. Call `hooks.approvePath` for each restored member. Open native members with `registry.tryOpen(hint, { kind: 'native' })` inside a try/catch. For vault members, await `hooks.restoreVault` when it is provided (name = the vault folder's basename, vault root = the root hint or else the hint's folder). When it is not provided, skip the vault member: it stays in the forest, exactly as main at `8c4e219` skips vault entries at launch (RESEARCH Pitfall 9). A problem leaves `home` and `forest` unset and opens no member (case G).
    - `get ready(): boolean`.
    - `list()` returns `registry.summary()` entries, each with a `frame`. For an entry with a `sha256:` id, find the member whose digest equals the id, or else the member whose path hint equals the entry's path and has no digest. For an entry with a `path:` id, find the member whose path hint equals the entry's path. The frame is that member's placement origin, or `{ x: 0, y: 0 }` when no member matches (a tree opened through a handler that Plan 04 has not yet converted).
    - `moveFrames(moves: unknown, actor: Actor): { committed: boolean }`. Validate in main (T-2.6-05): `moves` is an array of length 1 to the member count, each entry is an object with a string `treeId` and finite `x` and `y`, and tree ids are unique. Each id must resolve through `registry.entry` (so unavailable members may move; CONTEXT discretion) to a member with a placement. Anything else throws `Unknown tree (id)` or a shape error, and nothing is written. Keep only entries whose origin differs from the stored one. If none remain, return `{ committed: false }`, because a move that changes nothing makes no history entry. Otherwise call `forest.setOrigins` with the approved move message, using the first entry's tree name and the number of other entries, and return `{ committed: true }`.
    - `close()` closes the forest and home bridges quietly. It never rewinds a bridge.
    - Join and resolve with the registry id only. No registry id is ever written: the stand-in's identity on disk is the path hint plus digest (D-03).

    **7. `app/src/main/space/space-service.test.ts`** — real addon, `makeTempDir`, a `SettingsStore` on a temp folder, and injected `paths` inside a not-yet-existing `space/` subfolder. Every service and registry is closed in `afterEach`. Temp worlds get distinct names. Tests:
    - Case A import: a `version: 1` settings fixture with two native temp worlds, one vault-kind entry and one malformed entry. After `start()`, both files exist. The forest text contains the placement label, `origin.x real`, `actor system tapestry` and the import message with `skipped 1`. The settings JSON has `version` 2, the pointer, and a `trees` value deep-equal to the fixture. `list()` frames equal the fixture frames for the two native trees.
    - Setup failure: when the space folder's parent is a regular file, `start()` returns `setup-failed`, and the settings bytes are unchanged.
    - One drop is one commit: `moveFrames` with two entries adds exactly one `@commit` record, whose `actor` line is `human user.kaelen`, and both origins change.
    - An unchanged batch adds no `@commit` record.
    - Relaunch: close the service and registry, rewrite the settings `trees` frames to different numbers in the raw JSON (keeping the pointer), then start a new service. `list()` returns the moved values (SC-2).
    - `origin.x` stored as real at (0, 0): the last-opened fold-in member's edge prop type is `real`.
    - Forest text contains no `size.` key.
    - Isolation: after `start()`, `registry.list()`, `registry.summary()` and `registry.primaryBridgeProxy().getNodes()` contain no node of the space or member type, and `registry.resolveRef('Forest')` throws.
    - A malformed batch (not an array, NaN, a duplicate id, an unknown id, more entries than members) throws and adds no commit.
    - A member whose file does not exist is imported, listed as unavailable, and movable by its `path:` id, and the forest text never contains the substring `path:`.

    Launch cases E, G and H (nothing written, no member opened) are implemented here in `reopenFromPointer` and `start()`, but their tests belong to Plan 03 Task 2, beside the startup wiring that relies on them. Plan 06 tests the remaining cases.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/space/space-service.test.ts src/main/settings.test.ts</automated>
    <fails_when>non-zero exit, or `No test files found` in the output, or `Test Files` does not report 2 passed</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>! grep -rn "\.undo(" app/src/main/space</automated>
    <fails_when>any line printed — the space module rewinds a bridge, which D-09 forbids</fails_when>
    <automated>! grep -rnE "position[.](x|y)" app/src/main/space</automated>
    <fails_when>any line printed — the space module touches note positions (D-05)</fails_when>
    <automated>! grep -rn "ForestCommands" app/src/main/space</automated>
    <fails_when>any line printed — a name reserved by 2.2 Plan 15 is reused</fails_when>
    <automated>grep -n "Checkpoint decisions" .planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md</automated>
    <fails_when>non-zero exit because no line matched — the checkpoint answer was not recorded</fails_when>
  </verify>
  <acceptance_criteria>
    - `02.6-02-SUMMARY.md` has a `## Checkpoint decisions` section containing Kaelen's reply and a resolved value for every numbered item
    - `app/src/main/space/shapes.ts` exports every constant listed in step 1, with the approved values
    - `ForestStore`, `TapestryHome` and `SpaceService` are exported, and no file under `app/src/main/space/` calls `registry.adopt`, `registry.open(` or `registry.create(` on the forest or home path
    - The case A test proves: one system import commit, placement edges with real origins, `trees` deep-equal to the fixture, `version` 2, the pointer present, and frames listed from the forest
    - The setup-failure test proves settings bytes are unchanged when the import fails
    - The drop test proves exactly one new `@commit` signed `human user.kaelen`; the no-change test proves zero
    - The relaunch test proves frames come from the forest even when settings `trees` hold different numbers
    - The isolation test proves the forest is absent from `list`, `summary`, `primaryBridgeProxy().getNodes()` and `resolveRef`
    - The forest text never contains `path:` or `size.`
    - All three negated greps print nothing, and `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>The arrangement Kaelen has today becomes a forest on first launch, one drop is one signed commit, and a relaunch restores every frame from the forest alone, proved against the real addon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `settings.json` pointer → main | A user-editable absolute path that decides which file is opened as the Tapestry tree |
| `Tapestry.tree` / `Forest.tree` → main | Readable files a person may hand-edit; their path hints decide which member files are opened |
| caller → `SpaceService.moveFrames` | An untrusted batch of ids and numbers (Plan 03 exposes it to the renderer over IPC) |
| plugins / agents → trees | They reach trees only through `registry.primaryBridgeProxy()` and `resolveRef()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2.6-02 | Tampering | forest written by a plugin or agent | high | mitigate | ForestStore and TapestryHome hold their own bridges and are never adopted by TreeRegistry; the isolation test proves the forest is absent from list, summary, primaryBridgeProxy and resolveRef |
| T-2.6-03 | Tampering | hand-edited settings pointer | high | mitigate | `getTapestryPointer` returns only a safe absolute `.tree` path; the file must have the approved root type or it is `home-foreign`; nothing is ever written into a file that fails these checks |
| T-2.6-04 | Tampering | hand-edited forest `path.hint` | medium | mitigate | Each hint passes the safe-absolute `.tree` shape check before `registry.tryOpen`; opening never writes to a non-Ok journal; hints join `approvedPaths` exactly as settings paths did |
| T-2.6-05 | Denial of Service | `moveFrames` flood | medium | mitigate | Validated in `SpaceService` (so every caller, including Plan 03's IPC handler, gets it): batch length capped at the member count; ids unique and resolvable to members; numbers finite; an unchanged batch writes nothing |
| T-2.6-06 | Information disclosure | absolute path hints name the account | low | accept | Decided at Task 1 item 1.3; if home-relative hints are chosen, `toHint`/`fromHint` apply everywhere a hint is written or read |
| T-2.6-11 | Repudiation / Spoofing | actor on frame commits | high | mitigate | Every write takes an `Actor` resolved by main (`humanActor` / `SYSTEM_ACTOR`); the batch carries no actor; the drop test reads the `actor` line from the file |
| T-2.6-12 | Tampering | migration overwriting a file | high | mitigate | The kernel creates with exclusive-create, so an existing file is never overwritten; only a header-only file this call just created is removed on a failed first commit; the pointer is written last and `trees` is never written |
| T-2.6-15 | Information disclosure | registry `path:` id persisted | medium | mitigate | Only path hints and digests are written; a test asserts the forest text never contains `path:` (D-03) |
| T-2.6-DATA | Tampering | tests touching real data | high | mitigate | Every forest, home, settings and last-opened path is injected under `makeTempDir`; `~/Documents` is a real-data root and is never used in tests |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Location |
|---|---|---|
| Node types (approved values) | `HOME_ROOT_TYPE` [`tapestry.home/tapestry@1`], `HOME_FOREST_TYPE` [`tapestry.home/forest@1`], `SPACE_TYPE` [`tapestry.spaces/space@1`], `MEMBER_TYPE` [`tapestry.forest/member@1`] | `app/src/main/space/shapes.ts`; written into `Tapestry.tree` and `Forest.tree` |
| Edge labels | `PLACEMENT_LABEL` [`placement`], `HOME_FOREST_LABEL` [`forest`] | same |
| Property keys | `title`, `kind`, `digest`, `path.hint`, `vault.root.hint`, `origin.x`, `origin.y` (approved names) | same |
| World names / files | `Tapestry` / `Tapestry.tree`, `Forest` / `Forest.tree` | default `~/Documents/Tapestry/` (D-06) |
| Settings shape | `version: 2`, pointer key [`tapestry`] `{ path }`; `trees` untouched | `userData/settings.json` |
| Methods | `SettingsStore.getTapestryPointer`, `setTapestryPointer` | `app/src/main/settings.ts` |
| Classes | `ForestStore`, `TapestryHome`, `SpaceService` | `app/src/main/space/` |
| Functions / types | `classifySpace`, `importFromSettings`, `reopenFromPointer`, `problemMessage`, `SpaceProblem`, `SpacePaths`, `MemberSeed`, `ForestMember`, `OpenProblem` | `app/src/main/space/migrate.ts`, `forest-store.ts` |
| Commit messages | import, create Tapestry tree, move frame (approved wording) | forest and Tapestry trees |
| Tests | two-launch integration suite; pointer tests | `app/src/main/space/space-service.test.ts`, `app/src/main/settings.test.ts` |

<verification>
- `npm --prefix app run test -- src/main/space src/main/settings.test.ts` passes
- `npm --prefix app run test` and `npm --prefix app run typecheck` pass (`build:js` is unaffected: nothing imports the space module until Plan 03)
- `ctest --test-dir tapestry/build-kernel` passes with the Plan 01 baseline count (no kernel change)
- `git diff --stat "$(git merge-base HEAD phase-2-implementation-v1)" HEAD -- tapestry/kernel app/native app/src/main/commands/notes.ts app/src/renderer/components/NoteCard.tsx` prints nothing
</verification>

<success_criteria>
Kaelen has approved the record shape, the migration, the Ctrl+Z reach and the new wording. Starting the space for the first time turns today's settings arrangement into a readable forest referenced from a Tapestry tree, without touching the old list. One drop batch is one signed forest commit, and a second start restores the arrangement from the forest alone. Plan 03 puts this under the app.
</success_criteria>

<output>
Complete `.planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md` (its `## Checkpoint decisions` section was written at the start of Task 2).
</output>
