---
phase: 02.6-placement-edges-forest-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/settings.ts
  - app/src/main/settings.test.ts
  - app/src/renderer/layout/frame-moves.ts
  - app/src/renderer/layout/frame-moves.test.ts
  - app/src/renderer/state/undo-target.ts
  - app/src/renderer/state/undo-target.test.ts
  - app/src/main/trees/registry.ts
  - app/src/main/trees/registry.test.ts
autonomous: true
requirements: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-10: a settings.json carrying keys this build does not know (a pointer a later plan adds, 2.2's agentHttp, anything a newer build wrote) keeps every one of them, value for value, after setUserName and the agents switch rewrite the file"
    - "D-10: a malformed entry in settings.json `trees` is still in the file after any name or agents write — the old list is an untouched backup, never cleaned"
    - "D-10 / D-14: settings `version` is read from the file and written back as found; nothing in this plan raises it"
    - "D-11: a frame drop produces one batch — the dragged frame first, then every frame pushApart displaced, each origin moved by the same delta as its rect, in a deterministic order"
    - "D-08: the Ctrl+Z routing decision returns frames only while frame moves are the last thing the person did; a note commit, a selection, starting to edit, or a tree undo ends that run"
    - "D-08 / D-09: the reach of a frame-undo run is one named constant in one pure module, so Kaelen's checkpoint answer changes one line"
    - "D-03 / D-04 / D-13: the registry can refuse a world whose header digest is not the one expected, and can refuse Tapestry's own files, without holding any user-visible copy itself — the caller supplies the words after Plan 02's checkpoint"
    - "D-13 / D-14: nothing in this plan writes a forest record, a Tapestry-tree record or a v2 settings file — the blocking checkpoint is Plan 02 Task 1"
  prohibitions:
    - "No `.tree` file is created or written outside a test temp directory"
    - "settings.json `version` is never raised by this plan"
    - "No dependency is installed, removed or upgraded; `npm install` is never run"
    - "app/src/main/commands/notes.ts and app/src/renderer/components/NoteCard.tsx are not modified (D-05)"
    - "frame-moves.ts and undo-target.ts import no React module and touch no DOM or browser global"
    - "The registry holds no new user-visible sentence; refusal and mismatch wording is always passed in by its caller"
  artifacts:
    - path: "app/src/main/settings.ts"
      provides: "Passthrough of unknown top-level keys and the raw `trees` value on every write; `version` read from the file; `readLegacyTrees()`"
      exports: ["SettingsStore", "DEFAULT_SETTINGS", "suggestUserName"]
    - path: "app/src/renderer/layout/frame-moves.ts"
      provides: "Pure batch builder for one frame drop (D-11)"
      exports: ["buildFrameMoveBatch", "FrameMove"]
    - path: "app/src/renderer/state/undo-target.ts"
      provides: "Pure Ctrl+Z routing and the frame-undo run reducer (D-08)"
      exports: ["chooseUndoTarget", "nextFrameRun", "FRAME_UNDO_REACH", "EMPTY_FRAME_RUN"]
    - path: "app/src/main/trees/registry.ts"
      provides: "Exported open-failure helpers, TreeIdentityClash, the `expect` open option and reserved paths/ids"
      exports: ["TreeRegistry", "TreeIdentityClash", "classifyOpenFailure", "closeQuietly"]
  key_links:
    - from: "app/src/main/settings.ts update()"
      to: "app/src/main/settings.ts write()"
      via: "write merges the known fields over the raw object read from disk, keeping unknown keys and the raw trees value"
      pattern: "readRaw\\("
    - from: "app/src/renderer/layout/frame-moves.ts"
      to: "app/src/renderer/layout/frames.ts"
      via: "buildFrameMoveBatch calls pushApart and applies each rect delta to the frame origin"
      pattern: "pushApart\\("
    - from: "app/src/main/trees/registry.ts tryOpen"
      to: "app/src/main/trees/registry.ts recordUnavailable"
      via: "an `expect.id` that differs from the opened header digest releases the bridge and records the tree as missing with the caller's reason"
      pattern: "expect\\?\\.id|expect\\.id"
---

<objective>
**As** Kaelen, **I want to** arrange my trees and have that arrangement kept in a readable forest tree referenced from one always-open Tapestry tree, **so that** arranging trees gets an actor, undo, history and branches like any other edit.

*(User story derived from the ROADMAP Phase 2.6 Goal line, whose "so that" clause is quoted verbatim. The Goal line itself is not written in user-story form; this was flagged to the orchestrator.)*

This plan is the groundwork that must exist **before** anything writes a forest record or a v2 settings file, and it writes neither. Three slices:

1. **Tracer:** a `settings.json` this build does not fully understand survives every write this build makes. Today `SettingsStore.update()` rewrites the file from a whitelisted `read()`, so a later pointer key would vanish on the next name change and malformed `trees` entries would be silently cleaned out of what D-10 promises is an untouched backup (RESEARCH Pitfall 1). This must land before any code writes `version: 2`.
2. The two pure renderer decisions the later wiring needs: which frames one drop moves (D-11), and whether Ctrl+Z means "the frame I just dragged" or "the selected tree" (D-08).
3. Registry seams that hold no copy: exported open-failure helpers, a typed identity-clash error, an "expect this digest" open option, and reserved paths/ids.

Purpose: Plan 02's blocking checkpoint (D-13, D-14) can be answered while this runs, and the tracer after it lands on a settings store that cannot lose Kaelen's data.

Output: `SettingsStore` passthrough, `frame-moves.ts`, `undo-target.ts`, registry seams, and their tests. Also the Wave 0 builds (native addon, kernel tests) and a re-baselined test count after the Pending #15 merge.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). Must-haves derive from ROADMAP success criteria SC-1..SC-7 and CONTEXT decisions D-01..D-14.

**Base caveat.** This worktree's base (`fe33d69`) is stale for every file this phase touches. Plans are written against main at `8c4e219`. Executors run only after main is merged into `ws/spatial-canvas` (Decision Register Pending #15). Task 1 checks that first and stops if the merge is missing. If 2.2 Plans 08-16 landed on main before the merge, the files will differ from `8c4e219`; re-read the current file rather than trusting a line number quoted here.

**Shared-machine rules (bind every task):** never run `npm install`; stage files by name only and run `git show --stat HEAD` after each commit; never edit `.claude/CLAUDE.md`, `.planning/config.json` or `.gitignore`; tests use `app/test/helpers/temp-tree.ts` (`makeTempDir`, `createTempTree`) and never `app.getPath(...)`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-CONTEXT.md

<!-- Context budget: RESEARCH.md (84 KB), PATTERNS.md, FORMAT.md and VALIDATION.md are NOT loaded whole.
     Read only the sections each task's read_first names. -->

<interfaces>
<!-- Current shapes at main 8c4e219 (re-read after the merge). -->
```typescript
// app/src/main/settings.ts
export interface TreeFrameSetting { x: number; y: number }
export interface TreeSetting { path: string; kind: 'native' | 'vault'; vaultRoot?: string; frame: TreeFrameSetting }
export interface AppSettings { version: 1; userName: string | null; agentsEnabled: boolean; trees: TreeSetting[] }
export const DEFAULT_SETTINGS: AppSettings
class SettingsStore {
  read(): AppSettings                 // whitelist: always version 1, drops malformed trees
  update(mutator: (s: AppSettings) => AppSettings): AppSettings   // write(mutator(read()))
  private write(settings: AppSettings): void                      // tmp + renameSync, keep as is
}

// app/src/renderer/layout/frames.ts (pure, no DOM)
export const FRAME_GAP = 64; export const FRAME_MIN_WIDTH = 480; export const FRAME_MIN_HEIGHT = 320
export interface FramePosition { x: number; y: number }
export interface PositionedRect { id: string; x: number; y: number; width: number; height: number }
export function pushApart(rects: PositionedRect[], movedId: string, gap?: number): Map<string, { x: number; y: number }>

// app/src/main/trees/registry.ts
export type UnavailableStatus = 'damaged' | 'locked' | 'missing'
export interface OpenTreeOptions { kind?: TreeKind; vaultRoot?: string; name?: string }
function classifyOpenFailure(detail: string): 'locked' | 'damaged'   // module-private today
function closeQuietly(bridge: KernelBridge): void                    // module-private today
// adopt() throws new Error(`${clash.name} is already open from ${clash.path}`)
```

<!-- Canvas.tsx settleFrames at 8c4e219: the arithmetic frame-moves.ts extracts. -->
```typescript
const displaced = pushApart(rects, movedTreeId)
for (const [id, next] of displaced) {
  const before = rects.find((rect) => rect.id === id)
  const tree = trees.find((t) => t.id === id)
  if (!before || !tree) continue
  const x = tree.frame.x + (next.x - before.x)
  const y = tree.frame.y + (next.y - before.y)
}
```

<!-- App.tsx undo precedence at 8c4e219: undo-target.ts keeps it when no frame run is active. -->
```typescript
const undoTargetTreeId = editingRef?.treeId ?? selectedRef?.treeId ?? selectedTreeId ?? lastChangedTreeId ?? trees[0]?.id ?? null
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): A settings file this build does not fully understand survives every write this build makes</name>
  <precondition>Main has been merged into ws/spatial-canvas (Decision Register Pending #15): `grep -n "UnavailableTree" app/src/main/trees/registry.ts` prints at least one line</precondition>
  <files>app/src/main/settings.ts, app/src/main/settings.test.ts</files>
  <read_first>
    - `app/src/main/settings.ts` — the whole file. It must be the post-merge file. The reference copy at main is `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/main/settings.ts`; if the local file lacks `vaultRoot` handling in `validateTree`, the merge has not landed and this task stops
    - `app/src/main/settings.test.ts` — `withTempDir`, the byte-compare "writes nothing" test, and the hand-written v1 fixture with a vault entry (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/main/settings.test.ts`)
    - `app/src/main/index.ts` — the `agents:setEnabled` handler (search `agentsEnabled: enabled`), which is the second writer through `update()`
    - `app/test/helpers/temp-tree.ts` — `makeTempDir`
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q4, § Pitfall 1, § Pitfall 6, § Q8 cases I and J
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-PATTERNS.md` — § `app/src/main/settings.ts`
  </read_first>
  <action>
    **Step 0 — confirm the base, then build (Wave 0).** Run `grep -n "UnavailableTree" app/src/main/trees/registry.ts`. If it prints nothing, stop and report "Pending #15 merge has not landed on ws/spatial-canvas" without editing anything. Otherwise record in the SUMMARY the output of `git log -1 --format='%H %s'` and `git merge-base HEAD phase-2-implementation-v1`. Build the native addon in this worktree with `npm --prefix app run build:native` (it writes only `app/native/build/`; it is not an install). Configure and build the kernel tests with `cmake -S tapestry -B tapestry/build-kernel -DTAPESTRY_BUILD_APP=OFF -DTAPESTRY_BUILD_RENDER=OFF -DCMAKE_BUILD_TYPE=RelWithDebInfo` then `cmake --build tapestry/build-kernel -j`. Run `ctest --test-dir tapestry/build-kernel` and `npm --prefix app run test` once, before any edit, and record both pass counts in the SUMMARY as the post-merge baseline. The kernel is untouched in this phase, so its count must be the same at the end of Plan 06.

    **Step 1 — make `SettingsStore` a passthrough (D-10, RESEARCH Pitfall 1, cases I and J).**
    - Change `AppSettings.version` from the literal `1` to `number`. `DEFAULT_SETTINGS.version` stays `1`.
    - Add a private `readRaw(): Record<string, unknown> | null` that parses the file once and returns null for a missing, unreadable, unparseable or non-object file. Make `read()` use it. `read()` now reports `version` as the file's own value when that is a positive safe integer, and `1` otherwise. Every other field keeps today's validation exactly.
    - Change `update(mutator)` to read once, run the mutator, and pass `write` a flag saying whether the mutator replaced the `trees` array (reference inequality between the returned `trees` and the array `read()` produced in that same call).
    - Change `write(settings, opts)` into a merge. Start from `readRaw() ?? {}` so unknown top-level keys keep their values and their order. Overlay every own key of `settings` except `trees`, so any known field (including 2.2's `agentHttp` if Plan 14 has landed) is written from the validated value. For `trees`: when the flag says the mutator replaced the array, write the normalized array (the legacy tree writers, whose callers Plans 04-05 remove); otherwise write the raw `trees` value from disk unchanged, or the validated array when the file had no `trees` key. Never write a `version` lower than a valid integer version already in the file. Keep the atomic `.tmp` + `renameSync` write exactly as it is.
    - Add `readLegacyTrees(): { trees: TreeSetting[]; skipped: number }`, where `skipped` is the raw `trees` array length minus the number of entries that validate (0 when `trees` is absent or not an array). Plan 02's import uses it for the "skipped k unreadable entries" commit message (case J).
    - Update the file's header comment: the arrangement moves to the forest tree in phase 2.6 (D-01 supersedes 2.2 D-18); this store therefore keeps keys it does not understand and the raw `trees` list, so the old list stays a readable backup (D-10) and a file written by a newer or older build is not damaged by this one (RESEARCH Pitfall 6).

    **Step 2 — tests.** Add a `describe('SettingsStore passthrough (2.6 D-10)')` block to `settings.test.ts`, using `withTempDir` and hand-written JSON fixtures:
    - A file with `version: 2`, a `tapestry` object key holding a path, an `agentHttp` object, a `futureKey` array, and `trees` containing one valid native entry and one malformed entry (a relative path). After `setUserName('kaelen')`, `JSON.parse` of the file has `version` 2, deep-equal `tapestry`, `agentHttp` and `futureKey`, and a `trees` value deep-equal to the fixture's, malformed entry included.
    - The same fixture through `settings.update((s) => ({ ...s, agentsEnabled: false }))` keeps all of the above and writes `agentsEnabled: false`.
    - A file with `version: 7` still says `7` after `setUserName`.
    - A plain `version: 1` file still says `1` after `setUserName` (this plan never bumps).
    - `readLegacyTrees()` on the malformed fixture returns one tree and `skipped: 1`.
    - Every existing test in the file still passes unchanged, including the legacy `addTree` / `setTreeFrame` / `removeTree` round-trip.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/settings.test.ts</automated>
    <fails_when>non-zero exit, or `No test files found` in the output, or the `Tests` summary line does not report any passed tests</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>test -f app/native/build/Release/tapestry_addon.node &amp;&amp; test -d tapestry/build-kernel &amp;&amp; ctest --test-dir tapestry/build-kernel -N</automated>
    <fails_when>non-zero exit, or `Total Tests: 0` in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "UnavailableTree" app/src/main/trees/registry.ts` prints at least one line (the merge landed), and the SUMMARY records the HEAD commit, the merge base, the app test pass count and the ctest pass count
    - `app/src/main/settings.ts` contains `readRaw(` and `readLegacyTrees(`, and `grep -n "passthrough (2.6 D-10)" app/src/main/settings.test.ts` prints a line
    - `grep -n "version: 1, userName" app/src/main/settings.ts` prints nothing (read no longer hard-codes the version)
    - The new passthrough tests pass: unknown keys, `agentHttp`, the pointer-shaped key and the malformed `trees` entry all survive `setUserName` and the agents-switch update, and `version` 7 stays 7
    - A v1 file is still `version: 1` after `setUserName`
    - `npm --prefix app run typecheck` exits 0
    - `app/native/build/Release/tapestry_addon.node` exists and `ctest -N` lists a non-zero total
  </acceptance_criteria>
  <done>Every write SettingsStore makes keeps what it does not understand and leaves the old `trees` list exactly as it found it, the builds exist in this worktree, and the post-merge baseline is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two pure renderer decisions — one batch per drop (D-11) and where Ctrl+Z goes (D-08)</name>
  <files>app/src/renderer/layout/frame-moves.ts, app/src/renderer/layout/frame-moves.test.ts, app/src/renderer/state/undo-target.ts, app/src/renderer/state/undo-target.test.ts</files>
  <read_first>
    - `app/src/renderer/layout/frames.ts` — header comment, `FramePosition`, `PositionedRect`, `pushApart` (its return contract: only frames that moved, as top-left rect positions)
    - `app/src/renderer/layout/frames.test.ts` — the `frame()` fixture builder and header style
    - `app/src/renderer/components/Canvas.tsx` — `settleFrames` and `handlePointerUp` (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/renderer/components/Canvas.tsx`, lines 379-393 and 514-548)
    - `app/src/renderer/App.tsx` — the `undoTargetTreeId` expression and its comment, `handleUndo`, `handleRedo`, and the keydown handler (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/renderer/App.tsx`, lines 570-645)
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q5 items 1 and 4, Open Question 2
  </read_first>
  <behavior>
    - buildFrameMoveBatch with no overlap returns exactly one entry: the dragged tree at its new origin
    - A displaced frame whose rect sits 48 units right of its origin moves its origin by the rect delta, not to the rect position
    - The dragged entry is always first; displaced entries follow sorted by tree id; two calls with equal inputs return deep-equal arrays
    - A displaced id with no matching tree or rect is skipped, and the dragged id never appears twice
    - chooseUndoTarget with an empty run follows editing note, then selected note, then selected frame, then last changed tree, then first tree, and returns null when all are null
    - After one frames-moved event an undo returns the frames target; a redo with nothing redoable falls through to the tree chain
    - With reach `run`: two moves then one undo still returns frames, a second undo returns frames, a third returns the tree chain
    - With reach `single`: two moves then one undo returns the tree chain on the next undo, while a redo returns frames once
    - Each of tree-edited, selection-changed, editing-started, tree-undo and tree-redo resets the run to empty; frames-moved after an undo clears what was redoable; counts never go negative
  </behavior>
  <action>
    **`app/src/renderer/layout/frame-moves.ts`.** Copy the `frames.ts` header style ("pure functions, no React, no DOM", world-space note) and say why this exists: one drop is one commit (D-11), so the renderer hands main the dragged frame and everything it displaced as one list. Import `pushApart`, `FramePosition` and `PositionedRect` from `./frames`. Export `interface FrameMove { treeId: string; x: number; y: number }` and `buildFrameMoveBatch(trees: ReadonlyArray<{ id: string; frame: FramePosition }>, rects: PositionedRect[], movedId: string, movedOrigin: FramePosition): FrameMove[]`. The first entry is `{ treeId: movedId, ...movedOrigin }`. Then call `pushApart(rects, movedId)` and, for each displaced id other than `movedId`, apply the same delta arithmetic `settleFrames` uses today (origin plus rect delta). Skip ids with no tree or rect. Sort the displaced entries by tree id so the batch never depends on Map iteration order. No side effects.

    **`app/src/renderer/state/undo-target.ts`.** Header: history is per journal, so Ctrl+Z must pick one target; right after a frame drag that target is the forest's compensating undo (D-08, D-09), otherwise it is the tree precedence App uses today (keep that comment's reasoning). Export:
    - `type FrameUndoReach = 'run' | 'single'` and `const FRAME_UNDO_REACH: FrameUndoReach = 'run'`, with a comment that Plan 02's checkpoint (RESEARCH Open Question 2) decides this value and that this constant is the only place the choice lives.
    - `interface FrameRun { undoable: number; redoable: number }` and `const EMPTY_FRAME_RUN: FrameRun`.
    - `type FrameRunEvent = 'frames-moved' | 'frames-undone' | 'frames-redone' | 'tree-edited' | 'selection-changed' | 'editing-started' | 'tree-undo' | 'tree-redo'`.
    - `nextFrameRun(run, event, reach = FRAME_UNDO_REACH): FrameRun`. frames-moved: `run` reach adds one undoable, `single` reach sets undoable to 1; both clear redoable. frames-undone: `run` moves one from undoable to redoable; `single` sets undoable 0 and redoable 1. frames-redone: moves one from redoable to undoable (never below 0). Every other event returns the empty run.
    - `type UndoTarget = { kind: 'frames' } | { kind: 'tree'; treeId: string } | null` and `chooseUndoTarget(input: { direction: 'undo' | 'redo'; frameRun: FrameRun; editingTreeId: string | null; selectedNoteTreeId: string | null; selectedTreeId: string | null; lastChangedTreeId: string | null; firstTreeId: string | null }): UndoTarget`. Undo with `undoable > 0`, or redo with `redoable > 0`, returns frames. Otherwise it returns the first non-null id in the existing precedence, or null.

    Neither module imports React or any browser API, so vitest's node environment runs them. Write the tests first (the behaviors above), watch them fail, then implement. Reuse the `frame()` builder shape from `frames.test.ts` in `frame-moves.test.ts`.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/layout/frame-moves.test.ts src/renderer/state/undo-target.test.ts</automated>
    <fails_when>non-zero exit, or `No test files found` in the output, or fewer than 2 test files reported as passed</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>! grep -nE "from 'react'|document\.|window\." app/src/renderer/layout/frame-moves.ts app/src/renderer/state/undo-target.ts</automated>
    <fails_when>any line printed (exit status of the negated grep is non-zero) — a pure module reaches React or a browser global</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/src/renderer/layout/frame-moves.ts` exports `buildFrameMoveBatch` and `FrameMove`, and calls `pushApart(`
    - `app/src/renderer/state/undo-target.ts` exports `chooseUndoTarget`, `nextFrameRun`, `FRAME_UNDO_REACH`, `EMPTY_FRAME_RUN`, and `FRAME_UNDO_REACH` is declared exactly once
    - Every behavior listed above has a passing test, including the origin-delta case and both reach values
    - Neither module imports React or reads a browser global (the negated grep prints nothing)
    - `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>The drop batch and the Ctrl+Z decision exist as pure, tested functions that Plans 03 and 05 wire in without further design.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registry seams that hold no copy — exported open helpers, TreeIdentityClash, an expected digest, reserved files</name>
  <files>app/src/main/trees/registry.ts, app/src/main/trees/registry.test.ts</files>
  <read_first>
    - `app/src/main/trees/registry.ts` — the whole file, post-merge (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/main/trees/registry.ts`): `classifyOpenFailure`, `closeQuietly`, `refusalCause`, `open`, `create`, `tryOpen`, `reopen`, `recordUnavailable`, `adopt`, `summary` and its doc comment
    - `app/src/main/trees/registry.test.ts` — the `afterEach` flock cleanup, the distinct-world-name warning in the header, and the locked-case test using a second `KernelBridge` (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/main/trees/registry.test.ts`, lines 1-49 and 324-340)
    - `app/test/helpers/temp-tree.ts` — `makeTempDir`, `createTempTree`
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q2, Pattern 2, Pitfalls 4, 5 and 10
  </read_first>
  <behavior>
    - Opening a second path whose world is already open throws a `TreeIdentityClash` carrying the open tree's id and path, and its message is unchanged
    - `tryOpen(path, { expect: { id, reason } })` on a file whose digest differs returns an unavailable entry with status `missing` and exactly `reason`, releases the journal lock (a fresh KernelBridge can open the file afterwards), and leaves the file's bytes unchanged
    - `reopen` of that entry passes the same `expect` again, so a retry of the same mismatch stays missing
    - `tryOpen` with a matching `expect.id` opens normally
    - With `setReserved({ paths: [p], ids: [], refusal })`, `open(p)`, `create(p, …)` and `tryOpen(p)` throw `refusal(basename)` and record nothing; a symlink pointing at `p` is refused too
    - With a reserved id, opening a copy whose digest equals that id throws the refusal and holds no lock; `setReserved(null)` removes both refusals
  </behavior>
  <action>
    Keep every existing behavior and message. Changes:
    - Export `classifyOpenFailure` and `closeQuietly` so `app/src/main/space/` reuses them instead of copying them (PATTERNS § forest-store).
    - Export `class TreeIdentityClash extends Error` with readonly `treeId`, `openPath` and `openName`. `adopt` throws it (after releasing the bridge, as today) with the identical message text, so every current `toThrow(string)` assertion still passes. Plan 04 catches it to fold a duplicate stand-in (RESEARCH Pitfall 10).
    - Add `expect?: { id: string; reason: string }` to `OpenTreeOptions`, and an optional readonly `expect` to `UnavailableTree` so `reopen` can pass it back. In `tryOpen`, after the status check succeeds and before `adopt`, compare `bridge.getHeaderDigest()` with `opts.expect.id`. On a difference, call `closeQuietly(bridge)` and record the tree as unavailable with status `missing` and `opts.expect.reason` as the reason. This is RESEARCH Pitfall 5: a different world now sits at a member's path, so it is not adopted as that member (D-03). `reopen` passes `expect` through with kind, name and vault root. `summary()` does not expose `expect`.
    - Add `setReserved(reserved: { paths: string[]; ids: string[]; refusal: (name: string) => string } | null)`. Store each path resolved, plus its `realpathSync.native` form when the file exists (wrapped in try/catch). `open`, `create` and `tryOpen` check the target first — its resolved form, and its real path when it exists — and throw `new Error(refusal(basename(target)))` without recording anything. `adopt` checks the digest against the reserved ids after reading it and, on a match, closes the bridge and throws the same refusal. This is RESEARCH Pitfall 4: without it, "Open tree…" on the forest file hits Tapestry's own lock and reports the wrong reason. The registry spells no refusal sentence of its own; the approved words arrive from Plan 04 after the checkpoint.
    - Rewrite the `summary()` doc comment: frames no longer live in settings; from phase 2.6 they are placement edges in the forest tree (D-01, D-04), joined to these entries by member identity outside the registry.

    Tests go in `registry.test.ts` following its `afterEach` cleanup. Give every temp world a distinct name (same name in the same second means the same digest). Build the "copy" cases by copying a created world's file with `copyFileSync` after closing its bridge.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/trees/registry.test.ts</automated>
    <fails_when>non-zero exit, or `No test files found` in the output, or the `Tests` summary line does not report any passed tests</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/src/main/trees/registry.ts` contains `export class TreeIdentityClash`, `export function classifyOpenFailure`, `export function closeQuietly` and `setReserved(`
    - `grep -n "Frames live in settings" app/src/main/trees/registry.ts` prints nothing
    - All pre-existing registry tests pass with unchanged assertions, and `grep -nE "TreeIdentityClash|setReserved" app/src/main/trees/registry.test.ts` prints at least one line
    - The mismatch test proves status `missing`, the caller's reason verbatim, a released lock, and unchanged file bytes
    - The reserved tests cover `open`, `create`, `tryOpen`, a symlink, and a reserved id, and `setReserved(null)` clears them
    - `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>The registry can refuse Tapestry's own files and a world that is not the one expected, and names an identity clash by type, without owning any new user-visible words.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `userData/settings.json` → SettingsStore | A user-editable file, possibly written by an older or newer build sharing the same userData |
| renderer drop → batch builder | Frame positions computed in the renderer become the batch main will commit (validated again in main by Plan 02) |
| filesystem path → TreeRegistry | A path chosen in a dialog, restored from settings, or later from a forest `path.hint`; may be a symlink or a copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2.6-07 | Tampering | `SettingsStore.write` passthrough | low | mitigate | Unknown keys are copied back verbatim and never interpreted; every known field keeps its existing validation on read, so a hand-edited value cannot reach an actor line or a path check |
| T-2.6-08 | Tampering | `settings.json` `version` | low | mitigate | Read as the file's value only when it is a positive safe integer, otherwise 1; never used as an index; a write never lowers a valid higher version (case I) |
| T-2.6-09 | Tampering | `TreeRegistry.setReserved` | medium | mitigate | Reserved paths compared in resolved and real-path form, so a symlink to the forest or Tapestry file is refused before any open; tested with a real symlink |
| T-2.6-10 | Spoofing | a copied forest or Tapestry file | medium | mitigate | Reserved ids are checked in `adopt` after the digest is read; a copy with the same digest is refused and its lock released |
| T-2.6-13 | Spoofing | a different world at a member's path | high | mitigate | `expect.id` compared with the opened header digest; on mismatch the bridge is closed and the tree recorded `missing` with the caller's reason, so the old frame is never given to a new identity (D-03, Pitfall 5) |
| T-2.6-DATA | Information disclosure / Tampering | tests touching real data | high | mitigate | All tests use `makeTempDir` / `createTempTree`, which call `assertNotRealData`; no test uses `app.getPath` or `~/Documents` |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Location |
|---|---|---|
| Method (private) | `SettingsStore.readRaw` | `app/src/main/settings.ts` |
| Method | `SettingsStore.readLegacyTrees` → `{ trees, skipped }` | `app/src/main/settings.ts` |
| Type change | `AppSettings.version: number` (was literal `1`) | `app/src/main/settings.ts` |
| Behaviour | Writes keep unknown top-level keys and the raw `trees` value; `version` never lowered | `app/src/main/settings.ts` |
| Function / type | `buildFrameMoveBatch`, `FrameMove` | `app/src/renderer/layout/frame-moves.ts` |
| Functions / types / const | `chooseUndoTarget`, `nextFrameRun`, `FRAME_UNDO_REACH`, `FrameUndoReach`, `FrameRun`, `EMPTY_FRAME_RUN`, `FrameRunEvent`, `UndoTarget` | `app/src/renderer/state/undo-target.ts` |
| Class | `TreeIdentityClash` | `app/src/main/trees/registry.ts` |
| Exported functions | `classifyOpenFailure`, `closeQuietly` | `app/src/main/trees/registry.ts` |
| Option / method | `OpenTreeOptions.expect`, `UnavailableTree.expect`, `TreeRegistry.setReserved` | `app/src/main/trees/registry.ts` |
| Tests | passthrough, frame batch, undo routing, registry seams | `settings.test.ts`, `frame-moves.test.ts`, `undo-target.test.ts`, `registry.test.ts` |
| Build outputs (untracked) | native addon, kernel test build | `app/native/build/`, `tapestry/build-kernel/` |

No `.tree` record, node type, property key or edge label is introduced by this plan.

<verification>
- `npm --prefix app run test -- src/main/settings.test.ts src/main/trees/registry.test.ts src/renderer/layout/frame-moves.test.ts src/renderer/state/undo-target.test.ts` passes
- `npm --prefix app run test` passes with at least the baseline count recorded in Task 1
- `npm --prefix app run typecheck` and `npm --prefix app run build:js` exit 0
- `ctest --test-dir tapestry/build-kernel` passes with the baseline count
- `git diff --stat "$(git merge-base HEAD phase-2-implementation-v1)" HEAD -- app/src/main/commands/notes.ts app/src/renderer/components/NoteCard.tsx` prints nothing (D-05)
</verification>

<success_criteria>
Settings written by any build keep what this build does not understand, including the old `trees` list byte for value. The drop batch and the Ctrl+Z decision are pure and tested. The registry can refuse Tapestry's own files and a world that is not the one expected. No forest record, Tapestry-tree record or v2 settings file has been written.
</success_criteria>

<output>
Create `.planning/phases/02.6-placement-edges-forest-tree/02.6-01-SUMMARY.md` when done. Include the merge base, the baseline app and kernel test counts, and the note that no record or v2 settings file was written.
</output>
