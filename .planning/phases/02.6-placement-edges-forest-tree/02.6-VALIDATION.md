---
phase: "2.6"
slug: "placement-edges-forest-tree"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-16"
---

# Phase 2.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Seeded from `02.6-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (`environment: 'node'`, `pool: 'forks'`), and ctest for the kernel |
| **Config file** | `app/vitest.config.ts` |
| **Quick run command** | `npm --prefix app run test -- src/main/space src/renderer/layout/frame-moves.test.ts src/renderer/state/undo-target.test.ts src/main/settings.test.ts` |
| **Full suite command** | `npm --prefix app run test && npm --prefix app run typecheck && npm --prefix app run build:js && ctest --test-dir tapestry/build-kernel` |
| **Estimated runtime** | ~60 seconds (app suite) plus ~2 seconds (kernel) |

Only pure renderer modules can be unit-tested, because there is no DOM. Main-process handler logic lives in `app/src/main/space/`, and its integration tests use the real native addon and temp worlds (`app/test/helpers/temp-tree.ts`). Forest and home paths are always injected; tests never use `app.getPath`.

---

## Sampling Rate

- **After every task commit:** the quick run command, limited to the touched files, plus `npm --prefix app run typecheck`.
- **After every plan wave:** the full suite command.
- **Before `/gsd-verify-work`:** the full suite is green, and the kernel stays at 59/59 (the kernel is untouched in 2.6).
- **Max feedback latency:** 90 seconds.

---

## Per-Task Verification Map

Task IDs are filled in by the planner. Each row maps a success criterion or decision to its proving test.

| Req / Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------------|----------|-----------|-------------------|-------------|--------|
| SC-1, D-10, D-14 | Migrating v1 settings creates the forest and home tree and writes the pointer and `version: 2`. `trees` stays deep-equal to before, and the pointer is written last | integration | `npm --prefix app run test -- src/main/space/migrate.test.ts` | ❌ W0 | ⬜ pending |
| SC-1 | `setUserName` after v2 keeps the pointer, unknown keys and malformed raw `trees` entries | unit | `npm --prefix app run test -- src/main/settings.test.ts` | ✅ extend | ⬜ pending |
| D-10 edge cases | Pointer missing but files exist → no re-import. Missing, damaged or locked files → nothing written, and the reason is reported | integration | `npm --prefix app run test -- src/main/space/migrate.test.ts` | ❌ W0 | ⬜ pending |
| SC-2, D-11 | `moveFrames` makes exactly one commit under the user actor with every origin updated, and reopening gives the same origins | integration | `npm --prefix app run test -- src/main/space/space-service.test.ts` | ❌ W0 | ⬜ pending |
| D-12 | `fitFrame` with no change → no commit. A change → one `system` commit | integration | same | ❌ W0 | ⬜ pending |
| D-08, D-09 | `undoFrames` makes a new commit (not a rewind), the forest is not rewound, and a following move succeeds | integration | same | ❌ W0 | ⬜ pending |
| D-08 routing | `chooseUndoTarget` returns `'frames'` only right after a frame drag | unit | `npm --prefix app run test -- src/renderer/state/undo-target.test.ts` | ❌ W0 | ⬜ pending |
| D-11 renderer | `buildFrameMoveBatch` builds one deterministic batch: the dragged frame plus the frames it displaced | unit | `npm --prefix app run test -- src/renderer/layout/frame-moves.test.ts` | ❌ W0 | ⬜ pending |
| SC-3, D-04 | The forest has `placement` edges with `origin.* real` and no `size.*`. No new kernel verb (ctest 59/59) | integration + ctest | `npm --prefix app run test -- src/main/space/space-service.test.ts && ctest --test-dir tapestry/build-kernel` | ❌ / ✅ | ⬜ pending |
| SC-4, D-03 | A moved path reuses the stand-in and updates `path.hint`. A copied world is refused. The forest file contains no `path:` string | integration | `npm --prefix app run test -- src/main/space/space-service.test.ts` | ❌ W0 | ⬜ pending |
| SC-5 | Unavailable members are listed with status and reason, and their frames can be moved | integration | same | ❌ W0 | ⬜ pending |
| SC-6, D-05 | Note-position code is untouched | grep gate | `grep -rn "position\.x" app/src/main/space` (expect no output) | — | ⬜ pending |
| Isolation | The forest is not in `registry.summary()` / `resolveRef` / `primaryBridgeProxy`, and the forest's own path is refused as a member | integration | `npm --prefix app run test -- src/main/space/space-service.test.ts` | ❌ W0 | ⬜ pending |
| Types | Preload, `global.d.ts` and renderer agree | typecheck | `npm --prefix app run typecheck` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Build the native addon in this worktree: `npm --prefix app run build:native`
- [ ] Build the kernel tests (cmake command in `02.6-RESEARCH.md` § Environment Availability)
- [ ] `app/src/main/space/*.test.ts`, using `makeTempDir` and injected paths
- [ ] `app/src/renderer/layout/frame-moves.test.ts` and `app/src/renderer/state/undo-target.test.ts`
- [ ] A helper to read a `.tree` file's commit `actor` / `message` lines
- [ ] Re-baseline test counts after the Pending #15 merge

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| First launch migrates Kaelen's real layout | SC-1, D-10 | Real `userData` and Documents access; macOS privacy prompt | Run against a **copy** of `userData` and a copied `~/Documents/Tapestry` target, with Kaelen present. Frames reopen where they were, and `settings.json`'s `trees` is unchanged |
| Ctrl+Z after a frame drag puts the frame back | D-08 | Keyboard routing in a live window | Drag a frame, press Ctrl+Z (the frame returns), then edit a note and press Ctrl+Z (the note edit is undone) |
| Forest file is readable without the app | D-06 | Readability constraint | Open `Forest.tree` in a text editor. Members, hints and placements are identifiable |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
