---
phase: 02.6-placement-edges-forest-tree
plan: 06
type: execute
wave: 6
depends_on: ["02.6-05"]
files_modified:
  - app/src/main/space/migrate.ts
  - app/src/main/space/migrate.test.ts
  - app/src/main/space/space-service.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/App.tsx
  - tapestry/docs/tree/forest.md
  - app/src/main/settings.ts
  - app/src/main/settings.test.ts
autonomous: true
requirements: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-14 / D-10: a launch with no pointer but an existing Tapestry tree (case B) or only an existing forest (case C) finishes the upgrade exactly as Kaelen approved, never imports the settings list twice, never overwrites a file, and writes the pointer last"
    - "D-14: a missing, damaged, locked, foreign or mismatched Tapestry tree or forest (cases E, F, G, H and the refused B/C) leaves the space empty, writes nothing, and says why in the window with the approved wording"
    - "D-14: settings.json with a version above 2 (case I) and with malformed `trees` entries (case J) behave as approved"
    - "D-10: after this plan nothing in the app can write the settings `trees` list — the legacy writers are gone (or deprecated, per the approved answer) and the list is carried through every write unchanged"
    - "D-06 / TREE-01: a reader's guide explains every name the Tapestry tree and forest use, their defaults, who signs what, and what happened to settings.json, so the files can be read without Tapestry"
    - "D-07: the guide states there is one forest and switching between forests is a later phase"
    - "D-05: the guide states note positions stay node properties and that moving notes onto placement edges is phase 2.7, and keeps the forest's `placement` edge distinct from 2.5's note placement"
    - "D-01 / D-02 / D-03 / D-04 / D-08 / D-09 / D-11 / D-12 / D-13: the whole phase passes its automated gates and Kaelen's end-of-phase check on copied data"
  prohibitions:
    - "No code path writes the settings `trees` list"
    - "A file that is not a Tapestry tree or forest is never written to, and no existing file is overwritten"
    - "The forest is never repaired automatically (Phase 1 PD-04)"
    - "The human check never runs against the live userData or ~/Documents/Tapestry without Kaelen present; it uses the dev path variables"
    - "The kernel, the addon, notes.ts and NoteCard.tsx are unchanged across the whole phase"
    - "No dependency is installed or changed"
  artifacts:
    - path: "app/src/main/space/migrate.ts"
      provides: "recoverHome (case B) and recoverForest (case C) as approved; every problem kind with its approved wording"
      exports: ["recoverHome", "recoverForest", "classifySpace", "problemMessage"]
    - path: "app/src/main/space/migrate.test.ts"
      provides: "Cases A-J against the real addon"
    - path: "app/src/renderer/App.tsx"
      provides: "Space problem banner and verbatim refusals for add tree / add vault"
    - path: "tapestry/docs/tree/forest.md"
      provides: "Reader's guide to Tapestry.tree and Forest.tree"
      min_lines: 80
    - path: "app/src/main/settings.ts"
      provides: "No tree writers; trees carried through unchanged"
  key_links:
    - from: "app/src/renderer/App.tsx"
      to: "app/src/main/index.ts trees:spaceProblem"
      via: "on load and on every trees-changed, the renderer asks for the space problem and shows it once per distinct message"
      pattern: "spaceProblem\\("
    - from: "app/src/main/space/space-service.ts start"
      to: "app/src/main/space/migrate.ts recoverHome / recoverForest"
      via: "classifySpace routes cases B and C"
      pattern: "recover(Home|Forest)\\("
    - from: "tapestry/docs/tree/forest.md"
      to: "app/src/main/space/shapes.ts"
      via: "every type, label and key constant value appears in the guide"
      pattern: "placement"
---

<objective>
**As** Kaelen, **I want to** arrange my trees and have that arrangement kept in a readable forest tree referenced from one always-open Tapestry tree, **so that** arranging trees gets an actor, undo, history and branches like any other edit.

*(User story derived from the ROADMAP Phase 2.6 Goal line; its "so that" clause is quoted verbatim.)*

The last slice makes the space trustworthy when things go wrong, and readable without the app:

1. **Tracer: a space that can't open says why, and a half-finished upgrade finishes without importing twice.** This covers cases B and C exactly as approved at the Plan 02 checkpoint, every problem shown in the window with the approved wording, and add-tree and add-vault refusals shown word for word.
2. **The reader's guide, the removal of the old settings writers, and the phase gate,** including Kaelen's end-of-phase check on copied data.

The migration behaviour here is D-14's one-way door. Kaelen confirmed it at Plan 02 Task 1, and this plan implements that answer without asking again. Read `02.6-02-SUMMARY.md` § Checkpoint decisions for the resolved cases B and C (i or ii), 2.2 (delete or deprecate), 4.1 (banner or transient) and every 4.x sentence. The values in brackets are the Recommended ones.

**Visible changes in this plan (all approved at Plan 02 Task 1):** the space-problem message (4.2-4.8), and add-tree and add-vault refusals now shown verbatim (4.9, 4.10) instead of the generic file-open sentence.

**Shared-machine rules:** never `npm install`; stage by name; `git show --stat HEAD` after each commit; tests inject every path and use `makeTempDir`; the human check uses copies.

Requirements: the phase owns none (ROADMAP). This plan exercises TREE-01 (the files read without Tapestry) through integration only, so `requirements` is empty.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-CONTEXT.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-01-SUMMARY.md
@.planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md

<!-- Context budget: RESEARCH.md (84 KB), PATTERNS.md, FORMAT.md and VALIDATION.md are NOT loaded whole.
     Read only the sections each task's read_first names. -->

<interfaces>
<!-- From Plan 02 (app/src/main/space/migrate.ts), extended in Plans 04-05. -->
```typescript
export interface SpacePaths { forest: string; home: string; lastOpenedFile: string }
export type SpaceProblem = { kind: 'home-missing' | 'home-damaged' | 'home-locked' | 'home-foreign'
  | 'forest-missing' | 'forest-damaged' | 'forest-locked' | 'forest-foreign' | 'forest-mismatch'
  | 'not-set-up' | 'setup-failed'; path: string; reason?: string }
export function classifySpace(i: { pointer: string | null; homeExists: boolean; forestExists: boolean }):
  'import' | 'reopen' | 'recover-home' | 'recover-forest'
export function importFromSettings(settings, paths): { home: TapestryHome; forest: ForestStore }
export function reopenFromPointer(homePath): { home; forest } | SpaceProblem
export function problemMessage(problem: SpaceProblem): string
// SpaceService.start() currently maps recover-home / recover-forest to 'not-set-up' and writes nothing.
// SpaceService.requireReady() throws the 4.9 sentence. Reserved refusals are built by SpaceService (4.10).
```

<!-- App.tsx at 8c4e219: how add-tree failures are shown today. -->
```typescript
const addTreeToSpace = useCallback(async (filePath, result: { ok; treeId?; error? }) => {
  if (!result.ok || !result.treeId) {
    setNotice(`Could not open ${fileNameOf(filePath)} -- The file may be damaged or in an ` +
      'unrecognized format. Create a new world or choose another file.')
    return
  }
  ...
})
// handleAddVault: setNotice(`Could not add ${...} as a tree -- ${added.error ?? 'unknown error'} Nothing in the vault was changed.`)
// notice → <TransientNotice> (6 s); showAppError(message) → persistent banner until dismissed
```
</interfaces>
</context>

<!-- planner-discipline-allow: addTree -->
<!-- planner-discipline-allow: setTreeFrame -->
<!-- planner-discipline-allow: removeTree -->
<!-- planner-discipline-allow: migrateLastOpened -->

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): A space that can't open says why in the window, and a half-finished upgrade finishes without importing twice</name>
  <precondition>`02.6-02-SUMMARY.md` § Checkpoint decisions records answers for cases B and C, item 2.2 and item 4.1</precondition>
  <files>app/src/main/space/migrate.ts, app/src/main/space/migrate.test.ts, app/src/main/space/space-service.ts, app/src/main/index.ts, app/src/preload/index.ts, app/src/renderer/global.d.ts, app/src/renderer/App.tsx</files>
  <read_first>
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md` — § Checkpoint decisions (cases A-J, 2.4, 4.1-4.10)
    - `app/src/main/space/migrate.ts`, `app/src/main/space/space-service.ts`, `app/src/main/space/forest-store.ts`, `app/src/main/space/home-tree.ts`, `app/src/main/space/space-service.test.ts` (Plans 02-05)
    - `app/src/main/trees/registry.test.ts` — the damaged-journal pattern (`truncateSync`) and the locked pattern
    - `app/src/main/index.ts` — every `trees:*` handler and `vault:add` as they stand after Plan 05
    - `app/src/preload/index.ts` and `app/src/renderer/global.d.ts` — the `trees` block and the `vault.add` result type
    - `app/src/renderer/App.tsx` — `notice`, `TransientNotice`, `showAppError`, `addTreeToSpace`, `handleAddVault`, and the effect that subscribes to `onRedoDiscarded` (reference for subscribe and cleanup shape: `git -C /Users/kaelencook/Tapestry show 8c4e219:app/src/renderer/App.tsx`, lines 150-300)
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-RESEARCH.md` — § Q8 (cases A-J), Pitfalls 1 and 6, Open Question 3
  </read_first>
  <action>
    **`app/src/main/space/migrate.ts` — cases B and C as approved (D-14).**
    - `recoverHome(settings, paths): { home, forest } | SpaceProblem` (case B, a home at the default path and no pointer). Open the home with `TapestryHome.open(paths.home)`, whose problems map to the `home-*` kinds (foreign means `home-foreign`, 4.7). Resolve its forest exactly as `reopenFromPointer` does, including the digest check. On success, write the pointer with `settings.setTapestryPointer(paths.home)` **last**. With answer B(i) [Recommended], nothing is imported. With answer B(ii), first add every valid `readLegacyTrees()` entry whose path matches no stand-in's hint, in one `SYSTEM_ACTOR` commit that uses the approved import message and count, then write the pointer.
    - `recoverForest(settings, paths): { home, forest } | SpaceProblem` (case C, only a forest at the default path). Open the forest; foreign means `forest-foreign` (4.7), and it is never written to. With answer C(i) [Recommended], create the home referencing that forest (system, the approved create message) and then write the pointer last, importing nothing. With answer C(ii), return `not-set-up` (4.7) and write nothing.
    - Make `problemMessage` cover every kind with its approved sentence. Remove the kind `not-set-up` only when no approved answer produces it.

    **`SpaceService`.**
    - Route `recover-home` to `recoverHome` and `recover-forest` to `recoverForest` in `start()`, then restore members and set reserved files exactly as for the other paths.
    - Keep the last problem and add `problemNotice(): string | null`.
    - Add `noticeFor(err: unknown): string | null`. It returns the error's message when the error is the 4.9 refusal (have `requireReady` throw a small exported `SpaceRefusal` error subclass) or a 4.10 reserved refusal (record every string the reserved `refusal` callback returns in a set, and match against it). Otherwise it returns null.

    **`app/src/main/index.ts`.**
    - Add `trees:spaceProblem`, returning `{ message: space?.problemNotice() ?? null }`.
    - In `trees:open`, `trees:create`, `trees:close` and `vault:add`, include `notice: space?.noticeFor(err) ?? undefined` in the failure result.
    - Change the startup log to use `problemNotice()`.

    **Preload and types.** Add `spaceProblem(): Promise<{ message: string | null }>` to `trees`. Add an optional `notice?: string` to the result types of `trees.open`, `trees.create`, `trees.close` and `vault.add`, in both files.

    **`app/src/renderer/App.tsx`.**
    - Add an effect that calls `window.tapestry.trees.spaceProblem()` once on mount and again on every `window.tapestry.onTreesChanged` event, unsubscribing on cleanup. When it returns a message different from the last one shown (tracked in a ref), show it with the approved surface: `showAppError(message)` for 4.1 [banner], or `setNotice(message)` for the transient choice.
    - In `addTreeToSpace` and `handleAddVault`, when the result carries `notice`, show it verbatim with `setNotice` in place of the generic sentence. Otherwise keep today's copy unchanged.

    **`app/src/main/space/migrate.test.ts`** (real addon, injected paths, distinct world names, flock cleanup):
    - `classifySpace` is table-tested for all four outcomes.
    - Case A is summarized by one test here: import, `version` 2, pointer present, and `trees` deep-equal to before.
    - **Case B, an older build dropped the pointer:** after a case A import, rewrite settings.json as a `version: 1` file without the pointer, keeping `trees`. `start()` re-adds the pointer and leaves the forest's `@commit` count unchanged under B(i), or adds exactly one import commit covering only the missing trees under B(ii). `trees` stays deep-equal.
    - **Case B, a foreign file:** a plain temp world at the default home path gives `home-foreign`. Its bytes and settings are unchanged, and no forest is created.
    - **Case C:** delete the home after an import and remove the pointer. `start()` under C(i) creates a new home referencing the same forest digest and writes the pointer; the forest's `@commit` count is unchanged. A foreign forest gives `forest-foreign`, and its bytes are unchanged.
    - **Case F:** truncate the home's last bytes to damage it. The result is `home-damaged` with the kernel's reason, and the file bytes are unchanged (never repaired).
    - Cases E, G and H are already tested in `space-service.test.ts` (Plan 03 Task 2); do not duplicate them here.
    - **Case I:** a settings file at `version` 7 with a valid pointer reopens normally and still says 7 after `setUserName`.
    - **Case J:** the import message says `skipped 1`, and the malformed entry is still in the file.
    - `problemMessage` returns text containing the file name for every kind.
    - `noticeFor` returns the 4.9 sentence for a refused add while no space is open, and the 4.10 sentence for adding the forest path.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/space/migrate.test.ts src/main/space/space-service.test.ts</automated>
    <fails_when>non-zero exit, or `No test files found` in the output, or `Test Files` does not report 2 passed</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or `error` during any bundle step</fails_when>
    <automated>grep -n "trees:spaceProblem" app/src/main/index.ts app/src/preload/index.ts</automated>
    <fails_when>non-zero exit, or output missing either file</fails_when>
    <automated>grep -n "spaceProblem(" app/src/renderer/App.tsx app/src/renderer/global.d.ts</automated>
    <fails_when>non-zero exit, or output missing either file</fails_when>
    <automated>grep -nE "recover(Home|Forest)\(" app/src/main/space/space-service.ts</automated>
    <fails_when>non-zero exit, or output lacks either recovery call</fails_when>
  </verify>
  <acceptance_criteria>
    - `migrate.ts` exports `recoverHome` and `recoverForest`, and `start()` routes cases B and C to them
    - Every case test above passes. The B and C tests prove the forest's commit count and the `trees` value, the foreign tests prove unchanged bytes, and the damaged test proves no repair
    - `problemMessage` covers every `SpaceProblem` kind with approved wording, and `noticeFor` returns the 4.9 and 4.10 sentences
    - The window shows the space problem through the approved surface once per distinct message
    - Add-tree and add-vault refusals show the notice verbatim
    - Typecheck and build pass
  </acceptance_criteria>
  <done>Every way the space can fail to open is handled without writing, says why in the window, and a half-finished upgrade completes as Kaelen approved without importing twice.</done>
</task>

<task type="auto">
  <name>Task 2: The reader's guide, the end of the old settings writers, and the phase gate with Kaelen's check on copied data</name>
  <files>tapestry/docs/tree/forest.md, app/src/main/settings.ts, app/src/main/settings.test.ts</files>
  <read_first>
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md` — § Checkpoint decisions (1.1-1.12, 2.1-2.7)
    - `tapestry/docs/tree/obsidian-vault.md` — structure and tone of a reader's guide (reference: `git -C /Users/kaelencook/Tapestry show 8c4e219:tapestry/docs/tree/obsidian-vault.md`)
    - `tapestry/docs/tree/FORMAT.md` — "Keys and tokens", "Value types", "Identifiers" (link to them; do not restate them)
    - `app/src/main/space/shapes.ts` — every approved name
    - `app/src/main/space/space-service.test.ts` — a test that writes a forest you can print once for the example
    - `app/src/main/settings.ts` and `app/src/main/settings.test.ts` — the legacy tree writers, `migrateLastOpened`, the Plan 01 `treesChanged` write branch
    - `app/src/main/index.ts`, `app/src/main/space/migrate.ts` — confirm nothing calls the legacy writers or `migrateLastOpened`
    - `.planning/phases/02.6-placement-edges-forest-tree/02.6-VALIDATION.md` — Manual-Only Verifications
  </read_first>
  <action>
    **`tapestry/docs/tree/forest.md`** — a reader's guide in the shape of `obsidian-vault.md`. It opens with "A reader's guide to what Tapestry writes when it arranges your trees" and says it describes **records**, not a format: FORMAT.md is unchanged and every line is ordinary `.tree` format version 1. Sections:
    1. **Where the files live.** A tree diagram of the approved folder (`Tapestry.tree`, `Forest.tree`), the pointer in `settings.json`, and the world names (tokens).
    2. **The Tapestry tree.** The root node (and that a later phase hangs lock settings on it), the forest node, `digest`, `path.hint`, and the root-to-forest edge. Why the reference is text and not a `ref` (a `ref` must name something in the same file).
    3. **The forest.** The space node (`kind`, `title`), one stand-in per tree (`kind`, `path.hint`, `vault.root.hint`, `digest`), and one `placement` edge per frame carrying `origin.x` and `origin.y` as `real`. List the unwritten keys and their defaults per answer 1.4 (`origin.z` 0, `direction` 0 0 -1, `roll` 0), and say that a frame's size is computed from its notes, so `size.*` is not written for frames (D-04).
    4. **A tree is its digest.** The digest is the identity and the path is a hint. A moved file keeps its frame when you open it from its new place. A copy of an open world is refused. The app's temporary name for a tree it has never read is never written (D-03).
    5. **Who signs what.** A table of each commit kind, its actor and its message (answers 1.8 and 1.12).
    6. **Undo.** Undoing a frame move is a new commit that writes the old position back; nothing is rewound (D-09).
    7. **What happened to settings.json.** What the upgrade copied, that `trees` was left exactly as it was and is never written again, what an older build shows, and what each launch case does, in plain words (answers 2.1-2.7, cases A-J).
    8. **What this is not.** Note positions stay node properties, and moving notes onto placement edges is phase 2.7 (D-05). 2.5's "placement" of a note is a node property, not this edge. There is one forest, and switching between several is a later phase (D-07). Members do not record their forest in 2.6 (answer 1.6).

    Include one fenced example of a forest copied from a real file: print a forest written by `space-service.test.ts` once with a temporary `console.log`, then remove that line. Replace the temp path with `/Users/you/...` and shorten digests with `...`. If answer 1.3 chose home-relative hints, show them that way.

    **`app/src/main/settings.ts` (D-10, answer 2.2).** With the approved "delete" [Recommended], remove `addTree`, `setTreeFrame`, `removeTree` and `migrateLastOpened`, remove the `treesChanged` write branch, and make `write` always carry the raw `trees` value through unchanged (a file with no `trees` key keeps none). Keep `TreeSetting`, `validateTree`, `read().trees` and `readLegacyTrees()` for reading. Remove `TreeFrameSetting` only if nothing imports it. With the "deprecate" answer, keep the four methods, mark each `@deprecated` with the reason, and still make `write` carry `trees` through unchanged. Update the header comment to match.

    **`app/src/main/settings.test.ts`.** Remove the tests for the deleted methods, or keep them for the deprecated ones. Add a test that `update()` with a mutator returning a different `trees` array still leaves the file's `trees` deep-equal to before. Every Plan 01 and Plan 02 settings test still passes.

    **Phase gate.** Run the full suite, the kernel tests, typecheck, build, and the grep gates below. In the SUMMARY, record the final app and kernel counts against the Plan 01 baseline, and state that the kernel count is unchanged.
  </action>
  <verify>
    <automated>npm --prefix app run test</automated>
    <fails_when>non-zero exit, or fewer passed tests than the baseline recorded in 02.6-01-SUMMARY.md</fails_when>
    <automated>npm --prefix app run typecheck &amp;&amp; npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or any line matching `error TS`, or `error` during a bundle step</fails_when>
    <automated>cmake --build tapestry/build-kernel &amp;&amp; ctest --test-dir tapestry/build-kernel --output-on-failure</automated>
    <fails_when>non-zero exit, or a pass count different from the Plan 01 baseline</fails_when>
    <automated>! grep -nE "(addTree|setTreeFrame|removeTree|migrateLastOpened)\(" app/src/main/index.ts app/src/main/space/migrate.ts app/src/main/space/space-service.ts</automated>
    <fails_when>any line printed — something still calls a settings tree writer (D-10)</fails_when>
    <automated>test -z "$(git diff --stat "$(git merge-base HEAD phase-2-implementation-v1)" HEAD -- tapestry/kernel app/native app/src/main/commands/notes.ts app/src/renderer/components/NoteCard.tsx)"</automated>
    <fails_when>non-zero exit — the kernel, the addon or note-position code changed during the phase (D-05, SC-3, SC-6)</fails_when>
    <automated>! grep -rnE "ForestCommands|commands/forest" app/src/main/space</automated>
    <fails_when>any line printed — a name reserved by 2.2 Plan 15 is used by the space module</fails_when>
    <automated>for v in $(grep -oE "^export const ([A-Z_]+_TYPE|[A-Z_]+_LABEL|KEY_[A-Z_]+) = '[^']+'" app/src/main/space/shapes.ts | sed -E "s/.*= '([^']+)'/\1/"); do grep -qF -- "$v" tapestry/docs/tree/forest.md || { echo "missing from forest.md: $v"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or `missing from forest.md:` in the output — the guide does not name every record type, label or key</fails_when>
    <human-check>End-of-phase check with Kaelen (it also covers the human checks from Plans 03 and 05). Close every running Tapestry window first. Copy `~/Library/Application Support/@tapestry/app` to a scratch folder, make an empty scratch space folder, and launch with `TAPESTRY_USER_DATA_DIR=(copy) TAPESTRY_SPACE_DIR=(space folder) npm --prefix app run dev`.
    1. Your trees appear where they were. `Forest.tree` and `Tapestry.tree` exist in the space folder. In the copy's `settings.json`, `trees` is unchanged, `version` is 2, and the pointer is set.
    2. Open `Forest.tree` in a text editor. You can tell which tree each stand-in is, where each frame sits, and who wrote each commit. Compare it with `tapestry/docs/tree/forest.md`.
    3. Drag a frame into another: one move, one commit. Press Ctrl+Z: both frames return. Press Ctrl+Shift+Z: they move again. Edit a note and press Ctrl+Z: the note edit is undone.
    4. Add a tree, close it, and relaunch: the closed tree is gone, and its removal is a signed commit.
    5. Quit. Rename one member's file, relaunch, and see it listed as can't-be-found. Add it again from its new name: it returns to its old frame.
    6. Choose Open tree on `Forest.tree`: you see the approved refusal.
    7. Quit. Rename `Tapestry.tree` in the space folder and relaunch: the approved message appears, the space is empty, and nothing new is written. Rename it back and relaunch: everything returns.
    8. Quit. Launch a second window with the same variables while one is running: the approved locked message appears in the second window.
    Say `approved`, or describe what looked wrong.</human-check>
  </verify>
  <acceptance_criteria>
    - `tapestry/docs/tree/forest.md` exists with the eight sections, a fenced forest example, and every type, label and key value from `shapes.ts`
    - The guide states that note positions move in phase 2.7, that 2.5's note placement is different, and that there is one forest in 2.6
    - `settings.ts` matches the approved 2.2 answer, and a mutator returning a new `trees` array leaves the file's `trees` unchanged
    - No call to a settings tree writer remains in `index.ts` or the space module
    - The full app suite passes at or above the baseline, the kernel count equals the baseline, typecheck and build pass, and the kernel, addon, `notes.ts` and `NoteCard.tsx` are unchanged across the phase
  </acceptance_criteria>
  <done>The files can be read without Tapestry, nothing can write the old settings list, every automated gate passes, and Kaelen has seen the whole phase work on copied data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| files at the default space folder → launch | A Tapestry tree or forest left by a crash, an older build, or a person, possibly not Tapestry's at all |
| main → renderer notices | Main-composed sentences that include file names and paths |
| settings.json → every write | The old list must survive every write untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2.6-18 | Tampering | foreign file at the default path (cases B and C) | high | mitigate | The root or space type must match the approved names, or the file is refused with 4.7 and never written; tests prove unchanged bytes |
| T-2.6-19 | Repudiation / Tampering | importing the settings list twice after an older build | medium | mitigate | Case B follows the approved answer: no re-import under (i); under (ii) only trees absent from the forest, in one system commit whose message says so |
| T-2.6-20 | Information disclosure | notices containing absolute paths | low | accept | Shown only in the local window, in wording Kaelen approved; nothing is sent anywhere |
| T-2.6-21 | Tampering | writes while no space is open | medium | mitigate | Every write goes through `requireReady`, which throws `SpaceRefusal` (4.9); tested through `noticeFor` |
| T-2.6-26 | Tampering | damaged journal | medium | mitigate | Case F closes the bridge and reports; nothing repairs it (PD-04); the test compares bytes |
| T-2.6-27 | Tampering | legacy settings writers | medium | mitigate | Deleted (or deprecated) per 2.2; `write` carries `trees` through unchanged even if a mutator returns a new array; grep gate on callers |
| T-2.6-DATA | Information disclosure / Tampering | human check against live data | high | mitigate | The check uses copies through the dev-only path variables, with Kaelen present, after all windows are closed |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Location |
|---|---|---|
| Functions | `recoverHome`, `recoverForest`; `problemMessage` covering every kind | `app/src/main/space/migrate.ts` |
| Class / methods | `SpaceRefusal`; `SpaceService.problemNotice`, `noticeFor` | `app/src/main/space/space-service.ts` |
| IPC channel | `trees:spaceProblem`; `notice` field on open, create, close and vault-add failures | `app/src/main/index.ts` |
| Preload API / types | `trees.spaceProblem`; optional `notice` on four result types | `app/src/preload/index.ts`, `app/src/renderer/global.d.ts` |
| Visible copy | space-problem message (4.2-4.8); verbatim add refusals (4.9, 4.10) | `app/src/renderer/App.tsx` |
| Removed or deprecated | `SettingsStore.addTree`, `setTreeFrame`, `removeTree`, `migrateLastOpened` (per answer 2.2) | `app/src/main/settings.ts` |
| Document | Reader's guide to `Tapestry.tree` and `Forest.tree` | `tapestry/docs/tree/forest.md` |
| Tests | launch cases A-J | `app/src/main/space/migrate.test.ts` |

<verification>
- `npm --prefix app run test` passes at or above the Plan 01 baseline
- `npm --prefix app run typecheck` and `npm --prefix app run build:js` pass
- `ctest --test-dir tapestry/build-kernel` equals the Plan 01 baseline
- Kernel, addon, `notes.ts` and `NoteCard.tsx` unchanged since the merge base
- Kaelen's end-of-phase check approved
</verification>

<success_criteria>
All seven ROADMAP success criteria hold. A Tapestry tree is always open and pointed to from settings.json, and the migration left the old list untouched. The arrangement lives in the forest, with an actor, undo and replay. Frames are placement edges with no new kernel verb. Members are digests with path hints. Unavailable members still render with their reason. Note positions are untouched. Every record shape and migration behaviour is the one Kaelen approved.
</success_criteria>

<output>
Create `.planning/phases/02.6-placement-edges-forest-tree/02.6-06-SUMMARY.md` when done, with final test counts against the baseline and Kaelen's human-check result.
</output>
