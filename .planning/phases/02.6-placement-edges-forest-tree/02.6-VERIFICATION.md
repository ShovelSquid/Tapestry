---
phase: 02.6-placement-edges-forest-tree
verified: 2026-09-25T00:42:37Z
status: human_needed
score: 6/7 roadmap success criteria verified (SC-7 awaits Kaelen's approval by definition)
covered_files:
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-01-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-01-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-02-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-02-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-03-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-03-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-04-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-04-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-05-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-05-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-06-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-06-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-07-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-07-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-08-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-08-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-09-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-09-SUMMARY.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-10-PLAN.md
  - .planning/phases/02.6-placement-edges-forest-tree/02.6-10-SUMMARY.md
  - app/src/main/index.ts
  - app/src/main/settings.test.ts
  - app/src/main/settings.ts
  - app/src/main/space/forest-store.ts
  - app/src/main/space/frame-fit.test.ts
  - app/src/main/space/frame-undo.test.ts
  - app/src/main/space/home-tree.ts
  - app/src/main/space/member-identity.test.ts
  - app/src/main/space/membership.test.ts
  - app/src/main/space/migrate.test.ts
  - app/src/main/space/migrate.ts
  - app/src/main/space/open-into-space.test.ts
  - app/src/main/space/open-into-space.ts
  - app/src/main/space/shapes.ts
  - app/src/main/space/space-service.test.ts
  - app/src/main/space/space-service.ts
  - app/src/main/trees/registry.test.ts
  - app/src/main/trees/registry.ts
  - app/src/preload/index.ts
  - app/src/renderer/App.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/FrameHeader.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/global.d.ts
  - app/src/renderer/layout/frame-moves.test.ts
  - app/src/renderer/layout/frame-moves.ts
  - app/src/renderer/state/undo-target.test.ts
  - app/src/renderer/state/undo-target.ts
  - tapestry/docs/tree/forest.md
covered_digest: "v1:sha256:c5978ffb5fb872c1d4ca04c0899467fb04d9f5d685855394c73c992a77186583"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/7
  gaps_closed:
    - "SC-1 (gap 1, CR-01): an unreadable settings.json is no longer written over at launch; the migration stays non-destructive"
    - "SC-4 (gap 2, CR-02): a `path:` id no longer outlives the first successful open at its path; one stand-in per frame"
    - "SC-5 (gap 3, WR-01): after Open tree on a path now holding a different world, the old member stays listed as unavailable at its frame across relaunches"
  gaps_remaining: []
  regressions: []
advisory:
  - finding: "Review WR-01 (new): openWithRollback closes every new non-member registry entry, including one another overlapping vault add has opened but not yet recorded"
    category: other
    reason: "Reachable only with two overlapping vault adds (async catch-up). The review reproduced it. Worst result: a vault stand-in the forest holds but the registry does not list this session, which looks the same as the known IN-06 limitation. No roadmap SC is broken. Fix: serialize adds in index.ts openIntoSpace, or scope the rollback to the ids this call adopted"
    evidence_status: "reproduced by the reviewer (02.6-REVIEW.md); code path confirmed by the verifier at open-into-space.ts:56-78"
  - finding: "Review WR-02 (new): agents:setEnabled does not catch SettingsUnreadableError, so the switch silently does nothing while settings.json is unreadable"
    category: other
    reason: "A side effect of the CR-01 fix. Nothing is written, so the fix itself is correct; only the error goes unreported. Fix: wrap settings.update in try/catch at index.ts:442 and show result.error in AgentsPanel"
    evidence_status: "code-confirmed (index.ts:442, no try)"
  - finding: "Review WR-03 (new): case B (a relaunch with an existing Tapestry.tree) with an unreadable settings.json now returns setup-failed, so the space is empty until the file is fixed"
    category: other
    reason: "Plan 07 chose this deliberately (truth 3) and it is fail-safe: nothing is written, and the notice names the file and the parse error. The review suggests opening read-only via reopenFromPointer and skipping only the pointer write. This is a product decision for Kaelen, not a broken SC"
    evidence_status: "tested behaviour (migrate.test.ts 'case B: a relaunch returns setup-failed before opening anything')"
  - finding: "Review WR-04 (partly open): main still signs a growth push as the person's `move frame \"<pushed>\"` and pushes a frame-undo step"
    category: other
    reason: "The renderer no longer arms frame undo for it (plan 09). The commit still names an actor, so SC-2 holds, but the provenance text is misleading. The wording fix needs Kaelen's answer (REVIEW item 6, question 1)"
    evidence_status: "code-confirmed (space-service.ts moveFrames; Canvas.tsx recordFrameGrowth)"
human_verification:
  - test: "REVIEW item 1, the SC-7 gate: approve or override the #17/#18 record shape and settings migration (Option A with every Recommended answer). Compare 02.6-02-PLAN.md Task 1 PART 1-4 with app/src/main/space/shapes.ts and 02.6-02-SUMMARY § Checkpoint decisions"
    expected: "Kaelen writes `item 1: approved`, or overrides by number, into autonomy/RESPONSE"
    why_human: "SC-7 is defined as Kaelen's approval at a blocking checkpoint. The autonomy driver resolved it as `recommended` (commit 98e6eed), and autonomy/RESPONSE does not exist yet"
  - test: "REVIEW item 5, which also covers items 2-4 and item 6's hands-on checks: the eight-step end-of-phase check on COPIED data with TAPESTRY_USER_DATA_DIR and TAPESTRY_SPACE_DIR, plus item 6's WR-04/WR-05/WR-03 checks and the CR-01 trailing-comma check (shasum before and after)"
    expected: "Every step behaves as autonomy/REVIEW.md items 5 and 6 list. The copied settings.json keeps `trees` unchanged, with version 2 and the pointer set. A settings.json with a trailing comma gives setup-failed ending 'Nothing in settings.json was changed.' and is byte-identical"
    why_human: "Needs a real Electron window, drag and keyboard input, visible notices, and a real upgraded userData. The unattended runs never launched the app"
  - test: "REVIEW item 6, wording: (1) the commit message for a growth push (a/b/c); (2) the new banners `Close tree failed: <error>` and `Close tree failed: unknown error`"
    expected: "Kaelen picks a wording or approves, in autonomy/RESPONSE"
    why_human: "New user-visible and on-disk wording needs Kaelen's approval (shapes.ts answer 1.12)"
---

# Phase 2.6: Placement Edges & Forest Tree Verification Report

**Phase Goal:** The arrangement of your trees stops being an unrecorded preference: it lives in a forest tree, referenced from one always-open Tapestry tree, where every frame is a placement edge and every member is named by its header digest. So arranging trees gets an actor, undo, history and branches like any other edit.
**Verified:** 2026-09-25T00:42:37Z (HEAD `07f3efd`)
**Status:** human_needed
**Re-verification:** Yes, after gap-closure plans 02.6-07, 08, 09 and 10.

## Goal Achievement

All three gaps from the previous run are closed:

- **Gap 1 (CR-01, SC-1)**
- **Gap 2 (CR-02, SC-4)**
- **Gap 3 (WR-01, SC-5)**

I did not rely on the executors' tests alone. I wrote a separate throwaway vitest probe that used temp directories and the real addon, and deleted it afterwards. It drove the handler-equivalent flows through `openWithRollback`, `removeMember` and `registry.close`, and replayed each failing scenario from the previous report exactly. All four probe cases pass.

SC-2, SC-3 and SC-6 show no regressions: the kernel and addon are untouched, and no `position.*` or `size.*` writes appear. The full app suite passes: 31 files, 661 tests.

The one criterion left is SC-7. It is Kaelen's approval of the record shape and migration, and only he can give it.

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Launch opens one always-open Tapestry tree; settings.json holds a pointer; the migration is non-destructive (`trees` readable, `version` bumped) | VERIFIED | `readRaw()` returns missing, unreadable or ok (`settings.ts:240-261`). Every writer (`setTapestryPointer`, `write` and therefore `update`/`setUserName`) goes through `writableBase()`, which throws `SettingsUnreadableError` before `writeRaw`. The only settings writers in main are `index.ts:442,468` and `migrate.ts:224,353,417`, and all of them are guarded. `importFromSettings`, `recoverHome` and `recoverForest` call `assertReadable()` first. **Probe:** I replayed the previous report's exact CR-01 file (userName, one tree, trailing comma). Launch returned setup-failed with the notice ending "Nothing in settings.json was changed." The file stayed byte-identical, and no Forest.tree or Tapestry.tree was created. `setUserName`, `update` and `setTapestryPointer` all threw without writing. After I fixed the file, the next launch imported the tree at its frame (5, 6), with version 2, userName kept and `trees` intact. Named tests: `migrate.test.ts` "an unreadable settings.json (gap 1, CR-01)" (cases A, B, C) and `settings.test.ts` "SettingsStore with an unreadable file". See advisory WR-03 for the case-B hard stop |
| 2 | The arrangement lives in the forest; a frame move writes a commit with an actor; undo, close-and-reopen and replay reproduce it without reading frame positions from settings.json | VERIFIED (regression check) | Unchanged from the previous run: `moveFrames` → `forest.setOrigins(..., actor)`, and frame-undo.test.ts (10) passes. Gap 2 made this stronger: a drop naming every listed frame now commits. Before, the stale shared stand-in threw "1 to 0 moves". I probed this after Create at a missing path, and after the different-world relaunch. Advisory WR-04: a growth push is still signed as the person's move |
| 3 | Each frame is a `placement` edge carrying `origin.*` (and `size.*` where set); no kernel verb or value type added; nothing refuses a kind of thing from being placed | VERIFIED (regression check) | `git diff 8af7bb8~1 HEAD -- tapestry/kernel app/native` is empty. `git diff 280d4e9 HEAD -- tapestry app/native app/src/main/space/shapes.ts` is also empty, so gap closure did not touch the shapes or the kernel. No `size.*` writer exists; `shapes.ts:24` documents that it is left unwritten |
| 4 | Members named by header digest plus `path.hint`: a moved or renamed file keeps its frame; two paths with one digest are one member; the in-memory `path:` id lasts until the first successful open and is never written | VERIFIED | `adopt()` deletes both `unavailableId(target)` and the adopted id (`registry.ts:491-492`), and `open`, `create` and `tryOpen` all pass through it. `memberFor` is one-to-one (`space-service.ts:810-835`). `removeMember` writes nothing when another live entry shares the stand-in (`:659-662`). **Probe:** I added a digest-less member at a missing path, then created a world there through the `trees:create` flow. `list()` showed exactly one entry, with the new sha256 id at the original frame. A drop of every listed frame committed, and the forest had no `path:` text. After a relaunch the new world was at its moved frame. Closing it emptied the space, and the space stayed empty after a further relaunch. A second probe, the reopen path: a `path:` member whose file later appears. `reopenMember` gave a sha256 id and kept the frame. No `path:` id remained in the registry, and the digest was written to the forest. Named tests: `membership.test.ts` "a world created at a missing member path (gap 2, CR-02)" (two tests), plus the earlier moved-file, copy-fold and never-writes-`path:` tests |
| 5 | A member that will not open still renders as its frame with its status and the kernel's reason (T-02.2-32) | VERIFIED | Unavailable records use the expected digest as their id (`registry.ts:433`). `tryOpen` with `expect` records the member even when a different world is already open at the path (`:343-353`). **Probe:** I replayed the previous report's three-launch scenario, using the `trees:open` flow for Open tree. Launch 2 lists D as `missing` at (1, 2) and E as `ok`. Launch 3 lists both again. A drop of both commits. Closing D removes only D, and launch 4 lists only E. Named tests: `member-identity.test.ts` "a different world at a member path, then Open tree (gap 3, WR-01)" (two tests, including Create New World at a deleted member's path) |
| 6 | Note positions untouched: `position.x/y` stay node properties; D-27's thread frame not moved | VERIFIED (regression check) | No `position.x/y` reference under `app/src/main/space/`. Gap closure touched no note, spatial-command or kernel file (see the 20-file diff list, `280d4e9..HEAD`) |
| 7 | No code writes the Tapestry-tree or forest record shape, or migrates settings.json, until Kaelen has approved the exact strings and the migration at a blocking checkpoint | ? UNCERTAIN (human) | Unchanged. The autonomy driver resolved the gate as `recommended` (commit `98e6eed`), and it is queued as autonomy/REVIEW.md item 1. `autonomy/RESPONSE` does not exist. Nothing has run against real data; every test and probe uses `makeTempDir`, which asserts it is not real data. Only Kaelen's answer can satisfy this criterion |

**Score:** 6/7 truths verified (0 present but behavior-unverified; 1 awaiting human approval)

### Previous Gaps: Closure Check

| Gap | Previous finding | Closed? | Evidence |
|-----|------------------|---------|----------|
| 1 (SC-1) | Unparseable settings.json overwritten at launch; forest imported empty | **Closed** | Code at `settings.ts` / `migrate.ts` as above. Independent probe on the exact prior file. Tests for cases A, B and C |
| 2 (SC-4) | Create at a missing member path left a stale `path:` record; two frames shared one stand-in; closing one emptied the forest | **Closed** | `adopt()` clears the record, and `removeMember` has a shared-stand-in guard. Independent probe through the handler flows, with a relaunch. Tests |
| 3 (SC-5) | After Open tree on a replaced path, the old member was hidden and unremovable | **Closed** | Records are keyed by digest, and `tryOpen` honours `expect` on an already-open path. Independent three-launch probe. Tests |

### Plan-level must-haves (gap-closure plans)

- **07:** All truths verified. There are three read states, and every writer refuses an unreadable file. Cases A, B and C stop before creating anything. An empty or whitespace-only file counts as missing. The notice carries the path and the parse error, and ends with the approved sentence. I saw it in the probe output.
- **08:** All truths verified. `adopt()` clears both records, the join is one-to-one, `removeMember` has its guard, and every member is listed across relaunches. The prohibition holds: no stand-in digest is rewritten, and `addMember` adds a new stand-in beside the old one.
- **09:** Wired. `frameRunEventForDrop` is used in `App.tsx:715`. `originAfterFit` gates the fit in `Canvas.tsx:530`. `onCloseTree` runs from FrameHeader, both TreeFrame paths and `App.handleCloseTree`, and no `void …close()` calls remain. The on-screen behaviour is a human check (item 6).
- **10:** Verified for single adds. `openWithRollback` wraps both `open` and `record` (`index.ts:531-540`), and `record` checks `space.ready`. The overlapping-add case is advisory WR-01.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/main/settings.ts` | Three-state read, `SettingsUnreadableError`, `assertReadable`, guarded writers | VERIFIED | Wired from migrate.ts and index.ts |
| `app/src/main/space/migrate.ts` | A, B and C stop on an unreadable file before touching any store | VERIFIED | `assertReadable()` is the first statement at `:172, :344, :387` |
| `app/src/main/trees/registry.ts` | `adopt()` clears records; digest-keyed unavailable records; `expect` on an already-open path | VERIFIED | `:343-353, :433, :491-492` |
| `app/src/main/space/space-service.ts` | One-to-one `memberFor`, shared-stand-in guard, `isMember` | VERIFIED | `:652-681, :810-835` |
| `app/src/main/space/open-into-space.ts` | `openWithRollback` | VERIFIED (advisory WR-01) | Used by `index.ts openIntoSpace` for `trees:open`, `trees:create` and `vault:add` |
| `app/src/renderer/state/undo-target.ts`, `layout/frame-moves.ts` | `frameRunEventForDrop`, `originAfterFit` | VERIFIED | Pure, and wired into App and Canvas |
| Earlier artifacts (shapes, forest-store, home-tree, forest.md) | Unchanged since the previous run | VERIFIED | No diff since `280d4e9` for shapes.ts or tapestry/ |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `migrate.ts` import/recover paths | `SettingsStore.assertReadable` | first statement | WIRED |
| `setTapestryPointer` / `write` | `writableBase` → `SettingsUnreadableError` | before `writeRaw` | WIRED |
| `registry.adopt()` | `unavailable` map | `delete(unavailableId(target))`, `delete(id)` | WIRED |
| `recordUnavailable` | `opts.expect.id` | record id | WIRED |
| `removeMember` | `registry.summary()` + `memberFor` | shared-stand-in guard | WIRED |
| `index.ts openIntoSpace` | `openWithRollback` | open / record / isMember | WIRED |
| `Canvas.recordFrameMoves` | `App.handleFramesMoved` | `onFramesMoved(result, frameMoved)` | WIRED |
| Canvas fit effect | `originAfterFit` | fitFrame result | WIRED |
| FrameHeader / TreeFrame Close | `App.handleCloseTree` | `onCloseTree` | WIRED |
| `SpaceServiceHooks.restoreVault` | 2.2 Plan 08 vault launch | not supplied | NOT WIRED (known, out of scope; 2.2 Plan 08 has not landed; IN-06) |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|----------|------|--------|-----------|--------|
| `trees:list` frames | `frame.x/y` | `forest.members()` placement `origin.x/y` via one-to-one `memberFor` | Yes | FLOWING |
| Space-problem banner | `problemNotice()` | `SpaceProblem` from `start()`; for CR-01 it carries the settings path and parse error | Yes (seen in probe output) | FLOWING |
| Undo before-values | `step.entries[].before` | forest read in main before the write | Yes | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full app suite (run once) | `npm --prefix app run test` | 31 files, 661 passed | PASS |
| Gap 1 (exact prior repro plus writer refusals, then fix and import) | throwaway vitest `zz-verifier-probe.test.ts` (temp dirs, deleted afterwards) | setup-failed; bytes identical; no space files; after the fix, imports at (5, 6), version 2, userName kept | PASS |
| Gap 2 (path: member, Create via the trees:create flow, move all, relaunch, close, relaunch) | same | 1 listed entry; move committed; no `path:` in forest; empty after close and relaunch | PASS |
| SC-4 reopen (path: member, file appears, reopenMember) | same | sha256 id, same frame, digest written, no `path:` left | PASS |
| Gap 3 (D replaced by E, Open tree via the trees:open flow, relaunch, move all, close D, relaunch) | same | launch 2 and launch 3 list D (missing) and E (ok); close D leaves only E | PASS |
| Kernel/addon/shapes untouched by gap closure | `git diff 280d4e9 HEAD -- tapestry app/native app/src/main/space/shapes.ts` | empty | PASS |
| Working tree clean after probe | `git status --short` | empty | PASS |

The kernel `ctest` (59/59) was established earlier this session and not re-run, because no kernel file changed.

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist and no plan declares one. Skipped.

### Requirements Coverage

All ten plans declare `requirements: []`. REQUIREMENTS.md maps no ID to Phase 2.6, and the roadmap entry says "none owned — exercises TREE and PROV requirements through integration". There are no orphaned IDs.

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| (none owned) | 02.6-01 … 02.6-10 | n/a | `requirements: []` × 10 |

### Code Review (post gap closure) Findings: Classification

| Finding | Breaks a roadmap SC? | Classification |
|---------|----------------------|----------------|
| WR-01 rollback closes an overlapping in-flight vault add | No. It is limited to overlapping vault adds, and its effect matches IN-06 | Advisory, fix in a follow-up |
| WR-02 `agents:setEnabled` does not catch the unreadable-settings throw | No. Nothing is written; only the error is not shown | Advisory, a small fix |
| WR-03 case B hard stop on an unreadable settings.json | No. Plan 07 chose this fail-safe deliberately, and the notice says why | Advisory, Kaelen's product call |
| WR-04 growth push still signed as the person's move in main | No. The actor is recorded; only the wording is misleading | Advisory, waits on REVIEW item 6 |
| IN-01 … IN-06 | No | Info |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| gap-closure diff (`280d4e9..HEAD`, 20 files) | — | TBD/FIXME/XXX/TODO/HACK added | none | — |
| `app/src/renderer/state/use-forest.ts` | 304 | Stale comment naming the removed `trees.setFrame` | Info | Doc only (carried over) |
| `app/src/main/space/space-service.ts` | 634-639, 713-738 | `recordIdentity`/`relocateMember` are used only by tests (review IN-02) | Info | Dead until `vault:locate` lands |

### Human Verification Required

1. **SC-7 gate (REVIEW item 1).** Kaelen approves or overrides the record shape and migration answers. Compare `02.6-02-PLAN.md` Task 1 PART 1-4 with `app/src/main/space/shapes.ts`.
   - **Expected:** `item 1: approved`, or overrides by number, in `autonomy/RESPONSE`.
   - **Why human:** SC-7 is defined as Kaelen's approval.
2. **End-of-phase check on copied data (REVIEW item 5, which also covers items 2-4 and item 6's hands-on list).** The gap closure has now landed, which REVIEW item 5 said to wait for.
   - **Steps:** Run the eight steps with `TAPESTRY_USER_DATA_DIR` and `TAPESTRY_SPACE_DIR` pointed at copies. Then run the WR-04, WR-05 and WR-03 checks, and the CR-01 trailing-comma check with `shasum` before and after.
   - **Expected:** Everything behaves as those items list.
   - **Why human:** It needs a real window, drag and keyboard input, the visible notices, and a real upgraded userData.
3. **Wording (REVIEW item 6).** Two choices:
   - the growth-push commit message: (a), (b) or (c);
   - the new `Close tree failed: …` banners.
   - **Why human:** New wording needs Kaelen's approval.

It would also help if Kaelen decided on advisory WR-03 at the same time: should an ordinary relaunch with a broken settings.json still open the space read-only, as the review proposes?

### Gaps Summary

There are no gaps. The two root causes named last time have both been fixed:

- **(A)** settings.json could not tell "missing" from "unreadable";
- **(B)** unavailable records were keyed only by path, and the join was not one-to-one.

Each fix is in code, has named regression tests, and passed an independent reproduction of the exact scenarios that failed before.

The phase is `human_needed`, not `passed`, for two reasons. SC-7 is an approval only Kaelen can give, and the phase's own end-of-phase check on copied data has not been run. The four new review warnings are listed as advisory. None of them breaks a success criterion. WR-01 (serialize adds) and WR-02 (catch in `agents:setEnabled`) are small, self-contained fixes worth doing in a follow-up.

---

_Verified: 2026-09-25T00:42:37Z_
_Verifier: Claude (gsd-verifier)_
