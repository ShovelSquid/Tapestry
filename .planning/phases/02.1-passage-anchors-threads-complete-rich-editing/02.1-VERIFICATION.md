---
phase: 02.1-passage-anchors-threads-complete-rich-editing
verified: 2026-09-10T17:50:00Z
status: human_needed
score: 5/5 must-haves verified
---

# Phase 2.1 Verification

## Success Criteria

### SC-1: Passage-to-note linking with thread highlighting
**Status: VERIFIED (code level)**

- `schema.ts` defines `passageMark` with `anchorId` attribute and `excludes: ""` for overlapping (11 passage references)
- `passage-plugin.ts` (251 lines) tracks cursor position, identifies passage marks, renders highlight decorations
- Canvas.tsx renders ConnectionLine components for thread-arm edges
- Thread topology: source note → thread-arm edge → center node → thread-arm edge → destination note

**Human verification needed:** Visually confirm passage highlighting appears on hover in the running app.

### SC-2: Thread center nodes as editable/connectable nodes
**Status: VERIFIED (code level)**

- `ThreadCenterNode.tsx` (164 lines) created with shared `useProseMirror` hook
- Auto-positioned at midpoint between endpoint notes via Canvas.tsx computation
- Pinnable by drag (`isPinned` prop, `onPositionChange` callback)
- Empty centers use `ghost` CSS class (invisible until hovered, D-18)

**Human verification needed:** Confirm thread center appears between notes and is editable.

### SC-3: Gradient-of-focus hover hierarchy
**Status: VERIFIED (code level)**

- `passage-plugin.ts` sorts overlapping passages by span length
- Renders CSS decoration classes: `.passage-focus-1` (25% accent), `.passage-focus-2` (15%), `.passage-focus-3` (10%), default (8%)
- `PassageChooser.tsx` provides click disambiguation for overlapping regions (D-15)

**Human verification needed:** Confirm gradient opacity visually distinguishes nested/overlapping passages.

### SC-4: Complete floating formatting toolbar
**Status: VERIFIED (code level)**

- `FloatingToolbar.tsx` (245 lines): bold, italic, headings (H1/H2/H3), bullet/ordered lists, text color (8 colors), alignment (left/center/right), font family (system/serif/mono)
- `toolbar-commands.ts` (115 lines): ProseMirror command dispatch and active-state queries
- Hover-expand submenus with 300ms collapse delay via timer (D-25)
- All commands use `onMouseDown` + `preventDefault()` to preserve text selection (D-24)

**Human verification needed:** Confirm toolbar appears near selection and submenus stay open on hover.

### SC-5: Universal editing across notes and thread centers
**Status: VERIFIED (code level)**

- Both `NoteCard.tsx` and `ThreadCenterNode.tsx` import and use `useProseMirror` from `editor/use-prosemirror.ts`
- Same `tapestrySchema` consumed by both
- `FloatingToolbar` is view-agnostic — works with any `EditorView`

**Human verification needed:** Confirm formatting toolbar works identically in both note and thread center editors.

## Automated Checks

- [x] TypeScript compiles clean (`npx tsc --noEmit`)
- [x] Electron-vite build passes (52 modules, 734KB JS, 11.9KB CSS)
- [x] All key artifacts exist on disk
- [x] Shared editor hook used by both NoteCard and ThreadCenterNode

## Human Verification Required

1. Open the app and create two notes with text
2. Select text in one note and connect to another — verify passage mark highlight appears
3. Verify thread center node appears between the two notes
4. Click thread center to edit — verify same editor behavior as notes
5. Create overlapping passages — verify gradient-of-focus (strongest on smallest passage)
6. Select text — verify floating toolbar appears with all formatting options
7. Hover toolbar submenus — verify they stay open while pointer is inside
8. Test formatting in a thread center node — verify identical behavior to notes
