---
phase: 02.1-passage-anchors-threads-complete-rich-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/src/renderer/editor/schema.ts
  - app/src/renderer/editor/use-prosemirror.ts
  - app/src/renderer/components/NoteCard.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/NoteControls.tsx
  - app/src/renderer/components/ThreadCenterNode.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-5

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Selecting text in a note and clicking another note's connection handle creates a passage mark on the source text and a thread edge in the kernel graph"
    - "A thread center node appears at the midpoint between connected notes and is editable with the same ProseMirror editor as ordinary notes (D-26)"
    - "The shared ProseMirror schema and editor hook are consumed identically by NoteCard and ThreadCenterNode"
    - "Passage-marked text displays an idle highlight (8% accent background) that survives save and reload"
    - "Dragging a thread center node pins it at the user's chosen position; un-dragged centers follow their endpoints"
  artifacts:
    - app/src/renderer/editor/schema.ts
    - app/src/renderer/editor/use-prosemirror.ts
    - app/src/renderer/components/ThreadCenterNode.tsx
  key_links:
    - "passage mark anchorId in ProseMirror JSON body <-> thread center node thread.sourceAnchorId property"
    - "shared useProseMirror hook <-> NoteCard + ThreadCenterNode consumers"
    - "thread-arm edges <-> thread center node <-> endpoint notes"
---

<objective>
Shared ProseMirror editor infrastructure, passage-to-note linking with kernel-backed thread topology, and editable thread center nodes -- the end-to-end tracer proving the passage anchor architecture (SC-1, SC-2, SC-5, D-09, D-10, D-11, D-16, D-17, D-18, D-19, D-20, D-26).

Purpose: Prove that passage marks in ProseMirror serialize through the kernel, that thread center nodes can be created and edited with the same shared editor, and that the graph topology (thread-arm edges through a center node) is structurally sound before building gradient-of-focus or the formatting toolbar.

Output: A working passage-to-note link creating a highlighted passage mark, a thread center node at the midpoint, and thread-arm connection lines -- with universal editing in both notes and thread centers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-passage-anchors-threads-complete-rich-editing/02.1-RESEARCH.md
@.planning/phases/02.1-passage-anchors-threads-complete-rich-editing/02.1-UI-SPEC.md
@.planning/phases/02-plugin-host-sdk-feasibility-gate/02-CONTEXT.md
@app/src/renderer/components/NoteCard.tsx
@app/src/renderer/components/Canvas.tsx
@app/src/renderer/components/NoteControls.tsx
@app/src/renderer/components/ConnectionLine.tsx
@app/src/renderer/App.tsx
@app/src/renderer/App.css
@app/src/renderer/global.d.ts
@app/src/main/kernel-bridge.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "select text, connect as passage to another note, see idle highlight and thread line"</name>
  <files>
    app/package.json,
    app/src/renderer/editor/schema.ts,
    app/src/renderer/editor/use-prosemirror.ts,
    app/src/renderer/components/NoteCard.tsx,
    app/src/renderer/components/NoteControls.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <action>
    Install prosemirror-schema-list (version ^1.5.0) via npm in the app directory. The package is verified OK in the RESEARCH Package Legitimacy Audit.

    Create app/src/renderer/editor/schema.ts exporting a single tapestrySchema. Import basicSchema from prosemirror-schema-basic. Call addListNodes from prosemirror-schema-list to add orderedList, bulletList, listItem to the basic nodes with content expr "paragraph block*" and group "block". Define custom MarkSpec objects: passageMark with attrs anchorId (default empty string), excludes set to empty string (allows overlapping), inclusive set to false (D-11 boundary affinity), parseDOM matching span[data-passage-id] reading the anchorId, toDOM emitting span with data-passage-id and class tapestry-passage. Define textColorMark with attrs color (default #2C2C2C), parseDOM matching span[data-text-color], toDOM emitting span with inline style and data-text-color. Define fontFamilyMark with attrs family (default empty string), parseDOM matching span[data-font-family], toDOM emitting span with inline font-family style. Extend the paragraph NodeSpec from basicSchema with an align attr (default null) for block-level text alignment (D-23). Compose the Schema: use the list-extended nodes with the aligned paragraph override, and compose marks by adding passage before link, textColor and fontFamily at the end. Export tapestrySchema and the individual mark/node specs for direct reference.

    Create app/src/renderer/editor/use-prosemirror.ts exporting a useProseMirror React hook. Extract ProseMirror lifecycle from the current NoteCard: the hook accepts nodeId, initialBody, isEditing, onSave callback, onMarkDirty callback, onMarkClean callback, and a plugins array. It returns refs for the editor container div and the EditorView instance. Internally: import tapestrySchema from schema.ts. Implement deserializeBody (try JSON.parse for ProseMirror doc JSON, fall back to plainTextToDoc using tapestrySchema) and serializeBody (doc.toJSON stringified). The hook creates an EditorState with tapestrySchema, the provided plugins array plus history(), the formatting keybindings (Mod-b toggleMark strong, Mod-i toggleMark em, Mod-z undo, Mod-Shift-z redo, Mod-1/2/3 toggleHeading), and baseKeymap. It creates an EditorView on the editorRef div. dispatchTransaction applies the new state, checks docChanged (excluding externalSync meta), and debounces saves at 300ms: marks dirty on change, marks clean when debounce fires, serializes body and calls onSave. It syncs external body changes (undo/redo, plugin writes) using the same lastEmittedBodyRef pattern from current NoteCard: skip echoes of own saves, skip while debounce active, replace doc content with externalSync and addToHistory=false meta. Update editability when isEditing changes. On unmount, flush pending debounce. The hook must also export a getSelection function that returns the current ProseMirror from/to positions and whether text is selected, for use by the passage connection flow.

    Refactor NoteCard.tsx: remove the inline noteSchema, deserializeBody, serializeBody, plainTextToDoc, and the ProseMirror initialization effect. Import useProseMirror from editor/use-prosemirror.ts. Call the hook passing the relevant props. Replace editorRef and viewRef with the hook's returned refs. Keep all existing drag, resize, hover, title editing, and control rendering unchanged. The external behavior of NoteCard must be identical after refactoring.

    Modify NoteControls.tsx: accept a new prop hasTextSelection (boolean). When hasTextSelection is true, change the connection handle button title from "Connect to another note" to "Connect selected text" (per UI-SPEC copywriting). No other changes.

    Modify Canvas.tsx: add state for tracking which note has an active ProseMirror text selection. NoteCard should report its selection state upward via a new onSelectionChange callback (nodeId, hasSelection, from, to, anchorId). In the Canvas, when a connection handle is clicked on note B while note A has an active text selection: instead of starting drag-connect, call a new onPassageConnect(sourceNodeId, sourceFrom, sourceTo, destNodeId) prop. If no text is selected in another note, proceed with the existing drag-connect behavior. Pass the hasTextSelection prop down to NoteControls through NoteCard.

    Modify App.tsx: add a handlePassageConnect callback. This callback implements the passage-to-note connection flow (D-09, D-10 Flow A). The sequence: (1) generate a UUID string for the anchorId, (2) apply the passage mark to the source note's ProseMirror selection by dispatching a transaction with addMark using tapestrySchema.marks.passage.create with the anchorId attr, (3) force-save the source note's body immediately (not debounced) to persist the mark, (4) submit a kernel commit creating the thread center node (type tapestry.notes/thread-center@1) with properties: position.x and position.y at the midpoint between source and dest note centers, body as empty text, thread.sourceAnchorId as the anchorId, thread.sourceNode as the source node id, thread.destNode as the dest node id, pinned as false, (5) using the returned center node id, submit a second kernel commit creating two edges: from source to center with label thread-arm, and from center to dest with label thread-arm, (6) refresh nodes and edges. Pass handlePassageConnect to Canvas as onPassageConnect. Note: the passage mark must be applied to the EditorView before the transaction is dispatched -- the hook should expose an applyPassageMark(anchorId, from, to) method or accept a callback that receives the view for direct transaction dispatch.

    Add CSS to App.css: a tapestry-passage class with background rgba(74, 124, 255, 0.08) for the idle passage highlight (UI-SPEC passage idle state). This class is applied by the passage mark's toDOM spec.
  </action>
  <verify>
    <automated>cd app && npm install && npx tsc --noEmit 2>&1 | tail -5 && echo TYPECHECK_OK && npx electron-vite build 2>&1 | tail -5 && echo APP_BUILD_OK</automated>
  </verify>
  <done>prosemirror-schema-list installed. Shared tapestrySchema exported from editor/schema.ts with passage mark (excludes empty, inclusive false), textColor mark, fontFamily mark, aligned paragraph, and list nodes. Shared useProseMirror hook exported from editor/use-prosemirror.ts encapsulating ProseMirror lifecycle with debounce save, external sync, and formatting keybindings. NoteCard refactored to consume the shared hook with identical external behavior. NoteControls tooltip reads "Connect selected text" when text is selected. Selecting text in note A and clicking note B's connection handle creates a passage mark with anchorId on the source text, a thread center node at the midpoint, and thread-arm edges from source through center to dest. Passage-marked text shows 8% accent background tint. Typecheck and build pass.</done>
</task>

<task type="auto">
  <name>Task 2: ThreadCenterNode editing, auto-positioning, visual states, and deletion</name>
  <files>
    app/src/renderer/components/ThreadCenterNode.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <action>
    Create app/src/renderer/components/ThreadCenterNode.tsx. This component renders a thread center node using the shared useProseMirror hook from editor/use-prosemirror.ts (D-26 universal editing). Accept props: node (NodeInfo), isEditing, isHovered, isVisible (whether the thread is hovered/selected), zoom, onStartEditing, onSave, onMarkDirty, onMarkClean, onPositionChange, onDeleteNode, onHover, onRegisterDims, onDragMove, onDragEnd. Read position.x, position.y, body, and pinned from node.props.

    Render two visual states per UI-SPEC: (A) When body is empty and isVisible is true, render a 44px diameter circle with dashed 1px #B0ADA6 border, white background, pointer-events auto only on the circle target (D-18 visibility rule; when isVisible is false, render nothing). Clicking the empty circle enters editing mode with the ProseMirror placeholder "Add a thought..." (UI-SPEC copywriting). (B) When body is non-empty, render a rounded rectangle (border-radius 16px per UI-SPEC) that auto-sizes to content (min-width 120px, min-height 44px), white background, 1px solid #E0DDD7 border. Hovered state: 2px solid accent border. Editing state: 2px solid accent border with ProseMirror editable.

    Implement drag-to-reposition following the same pattern as NoteCard: pointerdown on the node starts drag, pointermove updates local position, pointerup commits position via onPositionChange. After drag, set the pinned property to true via a kernel submit (the onPositionChange handler in App.tsx should detect thread-center nodes and also set pinned to true).

    Auto-positioning (D-17): in Canvas.tsx, when rendering ThreadCenterNode nodes where pinned is false, compute position.x and position.y as the midpoint between the two endpoint nodes. Read endpoint node IDs from the thread center node's thread.sourceNode and thread.destNode properties. When either endpoint is dragged (during onDragMove), recompute and update the unpinned center's rendered position in real time. This is a display-only computation -- the kernel's stored position is updated when endpoints settle.

    Deletion (D-19, D-20): the ThreadCenterNode component includes a small delete bubble (same visual as NoteCard's delete bubble from NoteControls) visible on hover. Clicking it deletes the thread center node via onDeleteNote. The kernel's DeleteNode cascading automatically removes the thread-arm edges touching the center. Content deletion (clearing body text via editing) preserves the node and thread per D-20.

    In Canvas.tsx: add a type check in the nodes rendering loop. For nodes with type tapestry.notes/thread-center@1, render ThreadCenterNode instead of NoteCard or FallbackNodeView. Pass isVisible based on whether any edge touching this center is currently hovered or the center itself is hovered. Track hovered edges: when the pointer is near a connection line in the SVG, set hoveredEdgeId, and compute isVisible for centers connected to that edge.

    In App.tsx: extend handlePositionChange to detect thread center nodes and additionally set the pinned property to true when the node type is tapestry.notes/thread-center@1.

    Add CSS to App.css for ThreadCenterNode: tapestry-thread-center class with position absolute, transitions on border-color and box-shadow. tapestry-thread-center--empty for the 44px circle. tapestry-thread-center--has-text for the rounded rectangle. tapestry-thread-center--hovered with accent border. tapestry-thread-center--editing with accent border. The ProseMirror editor inside thread center uses the same tapestry-note-editor styles (shared via the common class or duplicated class targeting). Thread center placeholder text "Add a thought..." styled in #B0ADA6 italic per the existing placeholder pattern.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tail -5 && echo TYPECHECK_OK && npx electron-vite build 2>&1 | tail -5 && echo APP_BUILD_OK</automated>
  </verify>
  <done>ThreadCenterNode.tsx renders at the midpoint between connected notes, displays as a 44px dashed circle when empty and visible, expands to a rounded rectangle when it has text. Editing uses the shared useProseMirror hook with identical formatting to NoteCard. Dragging pins the center at the user's position. Deleting the center node structure removes it and its thread-arm edges. Clearing body text preserves the node. Auto-positioned centers follow endpoint movement in real time. Typecheck and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProseMirror editor -> kernel IPC | Passage anchorId strings and body JSON cross from renderer to main process |
| User text input -> ProseMirror DOM | Untrusted text enters through keyboard/IME |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2.1-01 | Tampering | passage mark anchorId | low | mitigate | AnchorIds are opaque UUID strings used only as data-passage-id attributes; never used in CSS selectors, DOM IDs, or script execution. Validated as non-empty strings only. |
| T-2.1-02 | Tampering | ProseMirror body JSON | medium | mitigate | Existing deserializeBody uses JSON.parse + plainTextToDoc fallback, never DOMParser.parseFromString. The shared schema enforces valid node/mark types. No raw HTML parsing path. |
| T-2.1-03 | Tampering | thread center node properties | low | mitigate | Node properties are typed values (text, real, bool) validated by kernel prepare(). No executable content in property values. |
| T-2.1-SC | Tampering | npm install prosemirror-schema-list | low | mitigate | Package verified OK in RESEARCH.md legitimacy audit: 8+ years old, 16M weekly downloads, official ProseMirror organization repo. |
</threat_model>

<verification>
All verification is automated build checks. Manual verification of visual behavior deferred to /gsd-verify-work.

1. TypeScript compilation passes with no errors (npx tsc --noEmit)
2. Electron-Vite production build succeeds (npx electron-vite build)
3. prosemirror-schema-list installed and importable
4. NoteCard behavior unchanged after refactoring to shared hook
5. Passage mark serializes in document JSON and survives save/reload
</verification>

<success_criteria>
- Shared schema and editor hook consumed by both NoteCard and ThreadCenterNode
- Selecting text and connecting to another note creates a passage mark, thread center node, and thread-arm edges
- Passage-marked text displays idle 8% accent background
- Thread center nodes are editable, auto-positioned, and pinnable
- All existing note editing, drag, resize, connect, and delete behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/02.1-passage-anchors-threads-complete-rich-editing/02.1-01-SUMMARY.md` when done
</output>
