---
phase: 02.7-file-windows-workspace-sandbox
verified: 2026-09-24T23:22:26Z
status: human_needed
score: 7/7 active roadmap success criteria verified (SC8 parked with 02.7-07); 63/63 automatable plan truths (01-06) verified
covered_files:
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-01-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-01-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-02-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-02-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-03-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-03-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-04-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-04-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-05-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-05-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-06-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-06-SUMMARY.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-07-PLAN.md
  - .planning/phases/02.7-file-windows-workspace-sandbox/02.7-CONTEXT.md
  - app/src/main/agents/connect-command.ts
  - app/src/main/agents/registry.ts
  - app/src/main/agents/socket-server.ts
  - app/src/main/chat/chat-service.ts
  - app/src/main/chat/claude-cli-engine.ts
  - app/src/main/chat/claude-cli.ts
  - app/src/main/chat/engine.ts
  - app/src/main/commands/actor.ts
  - app/src/main/commands/agent-tools.ts
  - app/src/main/commands/file-tools.ts
  - app/src/main/commands/locks.ts
  - app/src/main/commands/notes.ts
  - app/src/main/commands/spatial.ts
  - app/src/main/index.ts
  - app/src/main/kernel-bridge.ts
  - app/src/main/mcp/schemas.ts
  - app/src/main/mcp/shim.ts
  - app/src/main/mirror/atomic-write.ts
  - app/src/main/mirror/fs.ts
  - app/src/main/mirror/plan.ts
  - app/src/main/mirror/quiet-queue.ts
  - app/src/main/mirror/watcher.ts
  - app/src/main/obsidian/vault-fs.ts
  - app/src/main/settings.ts
  - app/src/main/trees/registry.ts
  - app/src/main/workspace/guards.ts
  - app/src/main/workspace/sandbox.ts
  - app/src/main/workspace/shapes.ts
  - app/src/main/workspace/workspace-service.ts
  - app/src/preload/index.ts
  - app/src/renderer/App.css
  - app/src/renderer/App.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/ChatPanel.tsx
  - app/src/renderer/components/ContextMenu.tsx
  - app/src/renderer/components/FolderFrame.tsx
  - app/src/renderer/components/ForestBar.tsx
  - app/src/renderer/components/FrameHeader.tsx
  - app/src/renderer/components/NoteCard.tsx
  - app/src/renderer/components/ProvenanceBadge.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/components/VaultNoteCard.tsx
  - app/src/renderer/components/WorkspaceFileCard.tsx
  - app/src/renderer/global.d.ts
  - app/src/renderer/layout/frames.ts
  - app/src/renderer/layout/subspaces.ts
  - app/src/renderer/state/chat.ts
  - app/src/renderer/state/use-forest.ts
  - app/src/renderer/state/workspace-status.ts
  - plugins/workspace-files/index.js
  - plugins/workspace-files/tapestry.plugin.json
covered_digest: "v1:sha256:34f94f1252d522c2ba098b807041bf8f8bfb47cd819e247db6fa3063a742ddde"
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "SC8: the panel talks to a ChatEngine interface, so a later API-key engine (Anthropic SDK over the same sandboxed tools) plugs in without changing the panel"
    addressed_in: "02.7-07 (PARKED, awaiting the blocking-human @anthropic-ai/sdk legitimacy gate, autonomy/REVIEW.md item 5)"
    evidence: "02.7-07 truth 'D-16/SC8: the chat panel has an engine picker ... the same ChatService, events and UI serve both engines'. Not implemented by design; not a gap and needs no gap-closure plan. The seam half of SC8 (engine.ts ChatEngine interface, engine-agnostic panel, createEngine factory) is already present from 02.7-02."
human_verification:
  - test: "REVIEW.md item 1 (02.7-02 + 02.7-03 combined checkpoint): connect-on-start shows agent.claude 'Connected now' before any tool call and falls back to 'Last connected'; folder subspaces nest, drag, collapse, persist and leave git status clean; the chat panel streams, edits as agent.claude-chat, refuses shell and /etc/hosts, Stop + resume, relaunch continuity; Ask Claude… from a file card, a native note and empty canvas"
    expected: "Every step A1-D3 in autonomy/REVIEW.md item 1 behaves as described"
    why_human: "Needs the running Electron app and the real signed-in claude CLI; automated tests use a fake claude and never render the panel or frames"
  - test: "REVIEW.md item 2 (02.7-04): Allow shell switch, confirmation, red banner, shell edit recorded as workspace.watcher, reset after relaunch"
    expected: "Steps 1-4 of autonomy/REVIEW.md item 2 behave as described"
    why_human: "Needs the real claude binary with Bash enabled and the rendered panel; SHELL_TOOLS names were read from the binary, not from a live session"
  - test: "REVIEW.md item 3 (02.7-05): open_file pans to and opens the window without an 'Expand folder' commit; list_files/write_file/refusal from a live Claude Code; Cmd+Z undo refusal notice; double-click in a workspace frame creates no note"
    expected: "Steps 1-6 of autonomy/REVIEW.md item 3 behave as described"
    why_human: "Canvas pan, window opening and on-screen notices are renderer behaviour no test renders"
  - test: "REVIEW.md item 4 (02.7-06): live watching within ~2 s of an outside edit, grouped checkout commit, Retry write after chmod, missing-folder header and heal, pan/zoom/typing feel on the ~740-file repo"
    expected: "Steps 1-6 of autonomy/REVIEW.md item 4 behave as described"
    why_human: "Real FSEvents timing on the target Mac, visual header states and perceived performance cannot be checked by grep or by the injected-watcher tests"
  - test: "02.7-01 backstop: window states (collapsed preview, opened window, writing soon, up to date, file-wins notice, write error, non-text file, mixed line endings) are legible on Kaelen's machine with ~740 file notes, and typing never deletes a note or rewinds the tree"
    expected: "All listed states render legibly; no note loss or rewind while typing"
    why_human: "Visual legibility at real scale. The D-11 dogfood itself is recorded as approved by Kaelen in 02.7-01-SUMMARY; the per-state legibility pass overlaps REVIEW.md items 3 and 4 (step 5 'Feel')"
---

# Phase 2.7: File Windows & Workspace Sandbox Verification Report

**Phase Goal:** A person opens a workspace folder in Tapestry and edits its files in windows on the canvas; agents connected over the MCP bridge can list, read, open and write files, but only inside that folder. Tapestry is then used to do its own development: the workspace is pointed at this repository and this branch's work is done from inside it.
**Verified:** 2026-09-24T23:22:26Z
**Status:** human_needed
**Re-verification:** No (initial verification)

Plan 02.7-07 (API-key chat engine, D-16 / SC8) is **parked, not implemented, awaiting the human gate** in autonomy/REVIEW.md item 5. It is listed under Deferred Items, not under gaps.

## Automated Checks (run by the verifier)

| Check | Command | Result |
| ----- | ------- | ------ |
| Build (incl. `out/main/mcp.js` for the shim tests) | `npm --prefix app run build:js` | exit 0, built in 436 ms |
| Typecheck (node + web) | `npm --prefix app run typecheck` | exit 0, no errors |
| Test suite (run once) | `npm --prefix app run test` | **38 files passed, 681 tests passed**, 0 failed (17.6 s) |
| Worktree clean after the suite | `git status --porcelain` | empty (tests touched only temp dirs) |

## Goal Achievement

### Observable Truths (ROADMAP success criteria)

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | SC1: choose a workspace folder; remembered across restarts; files open as editable windows that save back to disk | ✓ VERIFIED | `dialog:showOpenWorkspaceFolder` + `workspace:add` (index.ts:775-812) call `WorkspaceService.addWorkspace`, then `settings.addTree({kind:'workspace', workspaceRoot})`; settings.ts validates `workspaceRoot`; launch loop (index.ts:895-907) calls `openWorkspace` + `startWatchingSafely`. `WorkspaceFileCard` debounces 1000 ms then `window.tapestry.workspace.saveFile` -> `workspace:saveFile` -> `WorkspaceService.saveFile` -> `writeFileAtomicSync`. Tests: settings.test, workspace.test ("catches up on a change made while the tree was closed", "mirrors git's view byte-exactly and survives close and reopen"), save-file.test ("writes the file and records the edit as the person", file-wins cases) |
| 2 | SC2: agents get list/read/open-in-window/write file tools over MCP, attributed `actor plugin agent.<name>` | ✓ VERIFIED | schemas.ts registers list_files, read_file, edit_file, write_file, open_file; agent-tools.ts routes each to `commands.files.*` with the socket's actor. `open_file` -> `onReveal` -> `webContents.send('reveal-note')` -> Canvas `panToNote`. shim-files.test over the real built shim: "edits a file on disk and records it as agent.claude", "creates a file and its folder with write_file, recorded in one commit as agent.claude", "opens a file in its window with open_file" |
| 3 | SC3: every agent path resolves inside the root; `..`, absolute-outside and symlinks refused with a clear message; a refusal writes nothing | ✓ VERIFIED | sandbox.ts `resolveWorkspaceTarget` refuses NUL/backslash, `..`, absolute outside (incl. prefix-sibling via `path.relative`), `.git` any case, Tapestry temp names, any symlink along an lstat walk from `realRoot`, git-ignored paths, the root itself. file-tools.ts runs sandbox, lock and text validation before `observePath`/`commitWrite`. sandbox-matrix.test: every escape x every tool refused with byte fingerprint (sha256 of every file incl. a sibling folder + journal size, node count, head seq) unchanged |
| 4 | SC4: with no workspace open, file tools refuse, never falling back to a default directory | ✓ VERIFIED | `NO_WORKSPACE_MESSAGE` returned first in `resolveWorkspaceTarget` when `openWorkspaces()` is empty, and by agent-tools.ts when `commands.files` is absent. Tests: sandbox.test "refuses everything when no workspace is open", sandbox-matrix.test "with no workspace open, every tool says so and nothing changes", workspace.test "refuses everything with no workspace open" |
| 5 | SC5: dogfood — Claude Code over MCP opens and edits a real source file in a Tapestry window; the edit shows in `git diff` | ✓ VERIFIED | Automated: shim-files.test drives the real stdio shim, `edit_file` changes the file, `git diff --name-only` equals exactly `src/hello.ts`, and the last commit reads `actor plugin agent.claude` / `edit src/hello.ts (1 replacement)`. Human: 02.7-01-SUMMARY records the D-11 dogfood on ~/Tapestrees/windows as approved by Kaelen 2026-09-24 (a SUMMARY claim; the open_file half is re-checked in REVIEW item 3) |
| 6 | SC6: in-app chat panel runs Claude via the user's Claude Code CLI login in the workspace folder; by default only sandboxed tools; edits appear as `agent.<name>` | ✓ VERIFIED (service level; UI in REVIEW item 1) | FrameHeader "Chat with Claude" -> `openChat` -> ChatPanel -> `useChat` -> `chat:send` -> ChatService -> ClaudeCliEngine. `buildClaudeArgs` emits `--tools ""`, `--allowedTools mcp__tapestry`, `--strict-mcp-config`, `--permission-mode dontAsk`; `spawn(..., {cwd, shell:false, detached:true})`. Tests (fake claude + real shim): chat-engine.test "edits a file through the real shim as agent.claude-chat", cwd equals workspace realRoot, token only in a 0600 config file deleted on close, process group incl. grandchildren killed on Stop/dispose/quit; claude-cli.test "names no built-in tool with the shell off" |
| 7 | SC7: per-chat "Allow shell" switch, off by default, labelled not sandboxed; enables shell + built-in tools; those edits recorded as observed changes | ✓ VERIFIED (service level; UI in REVIEW item 2) | `SHELL_TOOLS = Bash,Read,Edit,Write,Glob,Grep`, only reached when `allowShell === true`. ChatPanel `ShellSwitch` -> `chat:setAllowShell` -> `ChatService.setAllowShell` -> engine. Tests: "starts off...", "is off again after a relaunch, and the chat says it was reset", "records a file the shell changed as observed by workspace.watcher, after the turn", "never catches up for a turn that ran with the shell off" |
| 8 | SC8: the panel talks to a ChatEngine interface so a later API-key engine plugs in without changing the panel | PARKED (not implemented, awaiting human gate) | 02.7-07 is parked at its blocking-human package-legitimacy task (REVIEW item 5). The seam exists (engine.ts `ChatEngine`; ChatPanel/renderer never import main-side engines; `createEngine` factory in ChatService). The API engine that would prove SC8 is intentionally not built. See Deferred Items and the advisory note below |

**Score:** 7/7 active success criteria verified; SC8 parked. 0 truths present-but-behavior-unverified.

### Plan must_haves (01-06)

All non-backstop plan truths (01: 16, 02: 13, 03: 10, 04: 6, 05: 9, 06: 9 = 63) map to code and to named tests in the passing suite (workspace.test, sandbox.test, sandbox-matrix.test, save-file.test, file-tools.test, file-locks.test, guards.test, subspaces.test (main and renderer), watch.test, scale.test, quiet-queue.test, chat-engine.test, claude-cli.test, shim.test, shim-files.test, socket-server.test, chat.test, workspace-status.test, workspace-file-card.test, provenance-badge.test, real-data-guard.test, workspace-files plugin test). The 6 `verification: backstop` truths (one per plan) are human-only and are routed below to REVIEW items 1-4.

Behavior-dependent truths with direct test evidence (not presence-only):
- Process-group cleanup: "Stop ends the turn and kills the whole process group, grandchildren included", "disposeAll leaves no process behind", "killAllNow kills every chat process group synchronously, for app quit"
- Observe-first ordering and seq guard: "records a change the tree had not seen as observed before the write", "never commits a plan read before an in-process write landed (seq guard)", "rebuilds its model when a write lands between reading the folder and planning"
- Watch lifecycle: "records nothing after stopWatching", "says it is not watching after an error, then restarts with a full catch-up", "stops restarting once the frame is closed"
- Lock ordering: "lock.text refuses edit_file and write_file before any observe commit"
- D-20: "shows the agent connected before any tool call or MCP initialize", "refuses a hello with an unknown token and marks nobody connected"

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | SC8 / D-16 API-key chat engine | 02.7-07, PARKED at REVIEW item 5 (blocking-human @anthropic-ai/sdk legitimacy and install-location decision) | Intentionally not implemented; nothing installed, no code written. Not a gap. |

### Required Artifacts

| Artifact | Status | Details |
| -------- | ------ | ------- |
| 01: mirror/fs.ts, mirror/plan.ts, mirror/atomic-write.ts, workspace/sandbox.ts, workspace/workspace-service.ts, commands/file-tools.ts, plugins/workspace-files/index.js | ✓ VERIFIED | gsd verify.artifacts 7/8 |
| 01: renderer/components/WorkspaceFileCard.tsx export `WorkspaceFolderLabel` | ℹ️ SUPERSEDED | Removed when 02.7-03 (D-21) replaced flat folder labels with `FolderFrame`. TreeFrame.tsx:59-61 documents it; workspace folders are drawn from the hierarchy, never via this node view. The plugin still registers the dangling name, which falls through to FallbackNodeView (see Anti-Patterns) |
| 02: chat/engine.ts, chat/claude-cli.ts, chat/claude-cli-engine.ts, chat/chat-service.ts, ChatPanel.tsx, fake-claude.mjs, mcp/shim.ts (`type: 'hello'`), ContextMenu.tsx | ✓ VERIFIED | 8/8 |
| 03: layout/subspaces.ts, FolderFrame.tsx, mirror/plan.ts (`layoutSubspaces`, `planSubspaceMigration`) | ✓ VERIFIED | 3/3 |
| 04: claude-cli.ts (`SHELL_TOOLS`), chat-service.ts | ✓ VERIFIED | 2/2 |
| 05: file-tools.ts (`validateWorkspaceText`), workspace/guards.ts, commands/locks.ts (`WORKSPACE_LOCK_POLICY`, `lockPolicyForTree`), sandbox-matrix.test.ts, file-locks.test.ts | ✓ VERIFIED | 5/5 |
| 06: mirror/watcher.ts, mirror/quiet-queue.ts, workspace-service.ts (`WorkspaceStatus`, `ReconcileStats`), state/workspace-status.ts | ✓ VERIFIED | 4/4 |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| agent-tools.ts | file-tools.ts | `commands.files.*` with socket actor | ✓ WIRED |
| file-tools.ts | sandbox.ts | `resolveWorkspaceTarget(` before any disk access | ✓ WIRED |
| workspace-service.ts | atomic-write.ts | `writeFileAtomicSync(` in commitWrite and saveFile | ✓ WIRED |
| WorkspaceFileCard.tsx | workspace-service.ts | `workspace.saveFile` -> preload -> `workspace:saveFile` -> `saveFile(getHumanActor(), ...)` | ✓ WIRED |
| index.ts | workspace-service.ts | restore loop `openWorkspace(` then `startWatchingSafely` | ✓ WIRED |
| mcp/shim.ts | socket-server.ts | `'hello'` -> verify -> noteSeen/markConnected | ✓ WIRED |
| ContextMenu / Canvas / NoteCard / VaultNoteCard / WorkspaceFileCard | state/chat.ts | `useContextMenu` + `AskClaudeButton` -> `ChatContext.openChat(` (the gsd pattern check missed it because `openChat` is called by the cards and Canvas, not inside ContextMenu.tsx) | ✓ WIRED (manual trace) |
| ChatPanel.tsx | chat-service.ts | `useChat` -> state/chat.ts `window.tapestry.chat.send` -> preload `chat:send` -> index.ts handler -> `chat.send` (the gsd pattern check missed the preload hop) | ✓ WIRED (manual trace) |
| ChatPanel.tsx | chat-service.ts | `ShellSwitch` -> state/chat.ts `chat.setAllowShell` -> preload `chat:setAllowShell` -> `ChatService.setAllowShell` -> engine | ✓ WIRED (manual trace) |
| claude-cli-engine.ts | claude-cli.ts | `buildClaudeArgs(` into `spawn(..., {shell:false, detached:true})` | ✓ WIRED |
| chat-service.ts | connect-command.ts | `chatShimLaunch(` | ✓ WIRED |
| chat-service.ts | workspace-service.ts | `catchUp(` after a shell-on turn | ✓ WIRED |
| FolderFrame.tsx | frames.ts / FrameHeader.tsx | `settleSubspace(`, `<FrameHeader variant='folder'>` | ✓ WIRED |
| workspace-service.ts | plan.ts | `planSubspaceMigration(` -> `submitAs(SYSTEM_ACTOR, ...)` | ✓ WIRED |
| spatial.ts | subspaces.ts / locks.ts | `absolutePositions(`, `lockPolicyForTree(tree.kind)` | ✓ WIRED |
| file-tools.ts | locks.ts | `checkLock(..., 'text', lockPolicyForTree('workspace'))` before observePath | ✓ WIRED |
| index.ts | Canvas.tsx | `reveal-note` | ✓ WIRED |
| kernel-bridge.ts | guards.ts | `beforeSubmit` / `beforeReplay` for workspace trees (index.ts:253-254) | ✓ WIRED |
| watcher.ts -> workspace-service.ts -> workspace-status.ts | | `reconcileNow(`, `startWatching(` / `stopWatching(` / `stopAll(`, `workspace-status` IPC | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
| -------- | ---- | ------ | --------- | ------ |
| WorkspaceFileCard | `file.text` | tree node props written by mirror/plan from `readBoundedBytes` + `decodeText` of the real file | Yes (byte-exact round trip tested) | ✓ FLOWING |
| FrameHeader watch status | `WorkspaceStatus` | `WorkspaceService.setStatus` -> `workspace-status` IPC -> `useWorkspaceStatus` | Yes | ✓ FLOWING |
| ChatPanel transcript | `ChatEvent` stream | ClaudeCliEngine stdout stream-json -> ChatService.emit -> `chat-event` IPC | Yes (fake claude in tests; real claude in REVIEW item 1) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full suite, run once | `npm --prefix app run test` | 38 files / 681 tests passed | ✓ PASS |
| Real MCP shim edit shows in git diff | covered by the run above (shim-files.test "edits a file on disk and records it as agent.claude") | passed | ✓ PASS |
| Refusal matrix fingerprints | covered by the run above (sandbox-matrix.test) | passed | ✓ PASS |
| Electron app / real claude | not run (task forbids launching the app or touching real user data) | n/a | ? SKIP -> REVIEW items 1-4 |

### Probe Execution

No probes are declared by the phase and no `scripts/*/tests/probe-*.sh` apply. Skipped.

### Prohibitions

| Plan | Prohibition | Tier | Result |
| ---- | ----------- | ---- | ------ |
| 01 | D-06: never record a change Tapestry did not make under any actor but `plugin workspace.watcher` | judgment | Held: `recordObserved` and `runReconcile` only use `WORKSPACE_WATCHER_ACTOR`; tested |
| 01 | D-03: no diverging `file.text` after a write completes, fails or loses | judgment | Held: saveFile records the observed disk state after file-wins and after a failed write; tested |
| 01 | D-05: never merge/auto-retry/re-send a losing edit | judgment | Held: file-wins path returns `written:false`; Retry write is person-initiated and resends against the original base (tested "lets a file that changed before the retry win") |
| 01 | Discretion: no file left in the workspace but the one written | judgment | Held: trees under `<userData>/workspaces` (index.ts:307, shapes.ts `workspaceTreeFiles`); workspace.test "keeps the tree in app data and leaves the workspace untouched" |
| 02 | D-14: no Bash/built-in tools unless the shell switch is on | judgment | Held: `buildClaudeArgs` is the only place naming them, gated on `allowShell === true`; tested both argv forms |
| 02 | D-13: no message text/token/API key in argv | judgment | Held: messages via stdin `userMessageLine`; token only in the 0600 config file; tested |
| 02 | D-13: CLI only in the workspace root, never through a shell | judgment | Held: `cwdFor` has no fallback; `shell:false`; tested cwd equals realRoot |
| 03 | D-21: folder layout writes no file on disk and no `file.*` key | judgment | Held: migration touches only position/subspace/collapsed (tested) |
| 03 | D-21: no forked FrameHeader/computeFrameBounds/pushApart | judgment | Held: FolderFrame reuses FrameHeader `variant='folder'` and `settleSubspace` over `pushApart` |
| 04 | D-15: shell only after a confirmed switch in this app session | judgment | Held: reset to off on relaunch (tested) |
| 04 | D-06: shell edits never attributed to agent.claude-chat | judgment | Held: end-of-turn `catchUp` records as workspace.watcher (tested) |
| 05 | D-09: production code never writes a `lock.*` property | judgment | Held: no production code writes a `lock.` key (grep over app/src/main and renderer, tests excluded) |
| 05 | D-03: no path but the file tools and window save diverges `file.*`, adds free notes or rewinds | judgment | Held: guards.ts `workspaceSubmitRefusal` / `WORKSPACE_REPLAY_REFUSAL` wired into kernel-bridge; note tools refuse workspace trees (tested) |
| 06 | D-06: watcher never infers an author | judgment | Held: only `WORKSPACE_WATCHER_ACTOR` in reconcile |
| 06 | Discretion: never follow a directory symlink while watching/reconciling | judgment | Held by design: watcher emits hints only (FSEvents does not traverse dir links); reconcile reads via git listing / `walkFolder` / lstat-based `readPathState` |

No prohibition is test-tier; none is flagged unverified. The judgment calls above are the verifier's reading and are not authoritative. Kaelen's REVIEW items remain the human confirmation.

### Requirements Coverage

ROADMAP lists `Requirements: TBD` for Phase 2.7 and every plan declares `requirements: []`. REQUIREMENTS.md maps no IDs to this phase, so there are no orphaned requirements. Coverage is tracked through the decision IDs D-01..D-21 in the plan truths above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (all 51 phase-changed source files) | - | TBD / FIXME / XXX / TODO / HACK | none found | - |
| plugins/workspace-files/index.js | 30 | Registers node view component `WorkspaceFolderLabel`, which no longer exists | ℹ️ Info | Harmless: falls through to FallbackNodeView and is never reached for workspace trees (folders are FolderFrames). A small cleanup could map it to a real name or drop the entry |
| app/src/main/chat/chat-service.ts | 34, 74, 337-344 | ChatService imports `ClaudeCliEngine` as the default factory, types `createEngine` with `ClaudeCliEngineOptions`, and resolves the `claude` binary before creating any engine | ⚠️ Warning (advisory for 02.7-07) | 02.7-02's truth "ChatService and the panel depend only on the ChatEngine interface" holds for the panel, only partly for ChatService. 02.7-07 already lists chat-service.ts in files_modified for its engine picker, so this is expected work there. It is not a gap for 01-06 |
| app/src/main/commands/file-tools.ts | 401-425 | write_file creates missing folders after the observe-first commit; a symlink found at that step (only possible via a race after the sandbox walk) returns a refusal after the observe commit | ℹ️ Info | TOCTOU-only; the sandbox walk already refuses links. No test path reaches it |

### Human Verification Required

These are already queued in autonomy/REVIEW.md. Nothing new needs adding.

1. **REVIEW item 1: 02.7-02 + 02.7-03 combined checkpoint.** Covers connect-on-start (D-20), folder subspaces (D-21), the chat panel end to end with the real `claude` (D-12..D-14), and Ask Claude… entry points (D-19). Expected: steps A1-E behave as written.
2. **REVIEW item 2: 02.7-04 Allow shell.** Covers the switch label, confirmation, banner, a shell edit landing as `workspace.watcher`, and reset on relaunch.
3. **REVIEW item 3: 02.7-05 open_file and guards.** Covers the pan and open with no "Expand folder" commit, live list/write/refuse, the Cmd+Z refusal notice, and double-click creating no note.
4. **REVIEW item 4: 02.7-06 live watching.** Covers the ~2 s live update, the grouped checkout, Retry write, missing-folder heal, and the performance feel on the ~740-file repo.
5. **02.7-01 backstop: window-state legibility at scale.** This overlaps REVIEW items 3 and 4 (item 4 step 5). The D-11 dogfood itself is recorded as approved.

Why human: all of these need the running Electron app, the real signed-in Claude Code CLI, real FSEvents timing, or visual judgement. None can be launched by the verifier under this task's constraints.

### Gaps Summary

No gaps for plans 01-06. All seven active roadmap success criteria are backed by wired code and by passing tests that exercise the behaviour: the real MCP shim edit showing in `git diff`, the full refusal matrix with byte fingerprints, the no-workspace refusal, restore with catch-up, the chat CLI argv, cwd and process-group lifecycle with a fake `claude`, the shell switch reset, shell edits recorded as watcher changes, live watching, and the seq guard. Status is `human_needed` only because each plan's on-screen backstop truth waits on Kaelen's hands-on checks in REVIEW items 1-4. SC8 / 02.7-07 is parked behind REVIEW item 5 by design and needs no gap-closure plan. The ChatService coupling to the CLI engine is flagged for 02.7-07 to handle.

---

_Verified: 2026-09-24T23:22:26Z_
_Verifier: Claude (gsd-verifier)_
