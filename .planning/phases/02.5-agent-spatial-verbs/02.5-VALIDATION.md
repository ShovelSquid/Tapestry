---
phase: "02.5"
slug: "agent-spatial-verbs"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-16"
---

# Phase 02.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `02.5-RESEARCH.md` § Validation Architecture.
>
> **Location note (orchestrator decision, 2026-09-16):** the shared placement geometry lives at `app/src/renderer/layout/placement.ts`, not at research's `app/src/shared/placement/geometry.ts`. That keeps this phase off the tsconfig edit, which counts as a config change outside the orchestrator's grant. `tsconfig.node.json` already has `rootDir: ".."`, so main can import it; `tsconfig.web.json` already includes `src/renderer/**`. The module must stay DOM-free and Electron-free.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (node environment, `pool: 'forks'`) |
| **Config file** | `app/vitest.config.ts` (includes `src/**/*.test.ts`) |
| **Quick run command** | `npm --prefix app run test -- src/renderer/layout/placement.test.ts src/main/commands/spatial.test.ts` |
| **Full suite command** | `npm --prefix app run test && npm --prefix app run typecheck && npm --prefix app run build:js` |
| **Estimated runtime** | ~60 seconds (full), ~10 seconds (quick) |

---

## Sampling Rate

- **After every task commit:** the quick run command, plus `npm --prefix app run typecheck`
- **After every plan wave:** the full suite command
- **Before `/gsd-verify-work`:** full suite green, plus the grep gates below
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

Task IDs are filled in by the planner. The rows map each roadmap criterion and decision to its check.

| Ref | Wave | Behavior | Test Type | Automated Command | File Exists | Status |
|-----|------|----------|-----------|-------------------|-------------|--------|
| SC1 | 1 | `look` returns same-tree neighbours nearest-first, each with exactly `note, space, relation, order, guess`. No coordinate keys at any depth. `limit` has no ceiling. `toward` filters. | integration + unit | `npm --prefix app run test -- src/main/commands/spatial.test.ts src/renderer/layout/placement.test.ts` | ❌ W1 | ⬜ pending |
| SC1 | 1 | `look` is advertised with `readOnlyHint: true`; the tool list is 9 names | e2e (shim) | `npm --prefix app run build:js && npm --prefix app run test -- src/main/mcp/shim.test.ts` | ✅ update | ⬜ pending |
| SC3 | 1 | At draw time a follower tracks its parent. A note with no `pinned`, or with `pinned true`, never moves. A fresh place gives derived == stored. | unit | `npm --prefix app run test -- src/renderer/layout/placement.test.ts` | ❌ W1 | ⬜ pending |
| SC3 | 1 | A drag of a follower routes to the takeover; an ordinary drag does not | unit + grep | `npm --prefix app run test -- src/renderer/layout/placement.test.ts` | ❌ W1 | ⬜ pending |
| SC5 | 1–2 | No kernel or addon change; the only ops are `setProperty`/`unsetProperty` | grep gate | `test "$(git diff --stat fe33d69 -- tapestry app/native \| wc -l)" -eq 0` | ✅ | ⬜ pending |
| SC6 | 1–2 | No `facing`, `direction` or `on` in the new schemas or commands | grep gate | `! grep -nE "facing\|direction\|\bon:" app/src/main/mcp/schemas.ts app/src/main/commands/spatial.ts app/src/renderer/layout/placement.ts` | ✅ | ⬜ pending |
| D-02 | 1 | `isFollowing` is false for a missing key, `true`, text `"false"`, and `false` without `grew-from` | unit | placement.test.ts | ❌ W1 | ⬜ pending |
| D-05 | 1 | Moving a parent writes no commit for its follower | unit + integration | placement.test.ts, spatial.test.ts | ❌ W1 | ⬜ pending |
| D-07 | 1 | Only stored or default sizes are used; the defaults are exported once; the tool description carries the overlap caveat | unit + grep | `test "$(grep -rn "DEFAULT_NOTE_WIDTH = " app/src \| wc -l)" -eq 1` | ❌ W1 | ⬜ pending |
| D-09/D-10 | 1 | Collision step, step bound, `beyond` geometry, degenerate cases refused | unit | placement.test.ts | ❌ W1 | ⬜ pending |
| D-12 | 1 | An anchor live only in another tree → `not a live note in <tree>`, nothing written | integration | spatial.test.ts | ❌ W1 | ⬜ pending |
| D-14 | 1 | A deep scan of `look` output finds no coordinates; errors carry no coordinates | integration | spatial.test.ts | ❌ W1 | ⬜ pending |
| SC2/D-11 | 2 | `place` moves the note with `{near}`/`{beyond,from}`; `{on}`, `{facing}`, extra keys and `actor` are refused by the schema; `create_note` without `where` still gives x = 390, y = 20 | unit + integration | `npm --prefix app run test -- src/main/commands/spatial.test.ts src/main/commands/notes.test.ts` | ❌ W2 | ⬜ pending |
| SC3/D-01/D-06 | 2 | `near <parent>` writes `pinned false` plus the resolved position; other placements write no `pinned`; props survive reopen | integration | spatial.test.ts | ❌ W2 | ⬜ pending |
| SC4 | 2 | Every refusal leaves file size, counts, the note's pos/pinned and the commit count unchanged | integration | spatial.test.ts | ❌ W2 | ⬜ pending |
| D-04 | 2 | The commit reads `actor plugin agent.<name>` and `Place note … near n…` | integration | spatial.test.ts | ❌ W2 | ⬜ pending |
| D-08 | 2 | Every op key is in `{position.x, position.y, pinned}` | unit | spatial.test.ts | ❌ W2 | ⬜ pending |
| D-13 | 2 | A vault-shaped node keeps `md.*` unchanged | integration | spatial.test.ts | ❌ W2 | ⬜ pending |
| D-15 | 2 (after 2.4) | Refused under `lock.layout` (message names the owner); allowed via `.allow`; `assertOwnNote` is never called | integration + grep | `! grep -n "assertOwnNote\|writeToOwnNote" app/src/main/commands/spatial.ts` | ❌ W2 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `npm --prefix app run build:native` — a local cmake build into `app/native/build/` (not a package install). Without it, 11 of 12 test files fail to load the addon.
- [ ] `npm --prefix app run build:js` — needed by `shim.test.ts`
- [ ] Record the baseline: full suite green before any edit, with the file and test counts
- [ ] `app/src/renderer/layout/placement.test.ts` — new
- [ ] `app/src/main/commands/spatial.test.ts` — new
- [ ] `app/src/main/mcp/shim.test.ts` — tool-list update (9, then 10)

---

## Manual-Only Verifications

| Behavior | Ref | Why Manual | Test Instructions |
|----------|-----|------------|-------------------|
| A follower stays beside its parent while the parent is dragged | D-05 | Needs a running window | Place a note near its parent through MCP, drag the parent, and watch the follower |
| One drag of a follower pins it, and it survives relaunch | D-03 | Needs a running window | Drag the follower, relaunch, and confirm it stays put and `.tree` shows `pinned bool true` |
| A card taller than the default can overlap while `look` says clear | D-07 | Rendered height | Type a long note, then `look` from a neighbour; the overlap is documented, not a bug |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
