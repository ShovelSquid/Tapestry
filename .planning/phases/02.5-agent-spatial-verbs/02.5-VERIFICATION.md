---
phase: 02.5-agent-spatial-verbs
verified: 2026-09-24T08:10:00Z
status: human_needed
score: 20/23 must-haves verified
covered_files:
  - .planning/phases/02.5-agent-spatial-verbs/02.5-01-PLAN.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-01-SUMMARY.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-02-PLAN.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-02-SUMMARY.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-03-PLAN.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-03-SUMMARY.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-04-PLAN.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-04-SUMMARY.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-05-PLAN.md
  - .planning/phases/02.5-agent-spatial-verbs/02.5-05-SUMMARY.md
  - app/src/main/commands/agent-tools.ts
  - app/src/main/commands/notes.test.ts
  - app/src/main/commands/notes.ts
  - app/src/main/commands/spatial.test.ts
  - app/src/main/commands/spatial.ts
  - app/src/main/index.ts
  - app/src/main/mcp/schemas.ts
  - app/src/main/mcp/shim.test.ts
  - app/src/renderer/App.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/FallbackNodeView.tsx
  - app/src/renderer/components/NoteCard.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/layout/placement.test.ts
  - app/src/renderer/layout/placement.ts
covered_digest: "v1:sha256:5537873c85391566aeded043635624fafa4124dba14d89fe14db8d58b63c2524"
behavior_unverified: 3
overrides_applied: 0
behavior_unverified_items:
  - truth: "SC3 / D-05 (renderer): a note with pinned bool false is drawn beside its grew-from parent and moves live while the parent is dragged, with no commit for the follower"
    test: "npm --prefix app run dev; have an agent create_note from your note A with where { near: A } (or place B near A); drag A slowly around the frame"
    expected: "B stays CHILD_GAP (80px) right of A for the whole drag, the connection line never detaches, the frame grows to contain B, and the .tree file gains no commit for B. A note with no pinned key stays put."
    why_human: "The renderer has no component test harness; displayPositions is unit-tested but the Canvas -> TreeFrame -> NoteCard drawing path is only proven by presence and wiring"
  - truth: "D-03 / D-16 (renderer): a person's drop of a following note, or a left/top resize, writes position plus pinned bool true in one 'Move note' commit signed by the human actor; a right/bottom resize does not pin"
    test: "Drop follower B; drag A; relaunch; resize follower C from its left edge; resize follower D from its right edge then drag A; drag an ordinary note E. Read the .tree file after each step."
    expected: "(1) one 'Move note' commit with actor human user.<name>, both position lines and 'set B pinned bool true'; (2) B stays put; (3) B still in place after relaunch; (4) C's commit carries pinned bool true and C stops following; (5) D keeps following, no pinned line; (6) E's commit has position lines only"
    why_human: "State transition driven by pointer events in NoteCard/TreeFrame; no test exercises writePosition -> onTakeOverPosition -> handleTakeOverPosition"
  - truth: "D-05 / 02.2 D-15 (renderer): card, connection lines, knot midpoints and frame bounds all use the same displayPositions spot, and a follower's card does not jump back when its own drag ends"
    test: "With a follower B beside A, drag B and release; watch the card, its line and the frame border during and after the drop"
    expected: "No flicker back to the old spot on release; the line stays attached to the card; B is never drawn outside its frame"
    why_human: "Visual/timing behaviour of React state (localPos vs followSpot) that grep cannot observe"
human_verification:
  - test: "Plan 03 Task 1: follower drawn beside its parent while the parent is dragged (see behavior_unverified_items[0])"
    expected: "B tracks A at 80px right, line attached, frame contains B, no commit for B"
    why_human: "No renderer component harness"
  - test: "Plan 03 Task 2: takeover by drop and by left/top resize; right/bottom resize does not pin (see behavior_unverified_items[1])"
    expected: "One 'Move note' commit with pinned bool true under the human actor; follower stops following"
    why_human: "Pointer-driven state transition, untested"
  - test: "Follower card does not jump back after its own drag; frame and lines agree (see behavior_unverified_items[2])"
    expected: "No snap-back; line and frame follow the drawn spot"
    why_human: "Visual timing"
  - test: "Plan 05 end-to-end UAT: with an agent connected to `npm --prefix app run dev`, ask it to create_note from one of your notes with where { near: <that note> }, then place another of its notes beyond it; drag your note; drag the agent's follower; quit and relaunch; type a long text into a note so the card grows, then ask the agent to look from a neighbour; finally ask the agent to place one of your own notes"
    expected: "Created note appears beside your note and follows it; the beyond-placed note stays put; after your drag the follower stays where dropped and the .tree shows pinned bool true under your human actor; relaunch reproduces every position; look may report the tall card clear of a neighbour it visibly overlaps (documented D-07 caveat); placing your note is refused with a message naming you (e.g. 'n1 layout is locked by user.kaelen')"
    why_human: "Real MCP agent + live window + relaunch; only the in-process shim path is automated"
  - test: "DECISION (not a test): SC1 says look returns coordinates 'only for placements it may write, which before Phase 2.4 is none'. Phase 2.4 is now merged, but look still returns no coordinates for any note (Plan 04 explicitly kept it relations-only; CONTEXT D-14 says 'when 2.4 lands, coordinates switch on through the lock check'). No later ROADMAP phase owns switching them on."
    expected: "Kaelen either (a) accepts relations-only as satisfying SC1's ceiling and files 'look coordinates for layout-writable notes' to a later phase/backlog, or (b) asks for a gap-closure plan in 2.5 that adds coordinates through checkLock(..., 'layout')"
    why_human: "Scope interpretation of a roadmap criterion; the literal SC (an upper bound) is met, the D-14 follow-through has no owner"
---

# Phase 2.5: Agent Spatial Verbs Verification Report

**Phase Goal:** Agents can perceive where notes are and say where notes go, in task space: `look` returns relations rather than coordinates, `place` takes a relation rather than a position, and the host resolves one into the other, so an agent can move its own body through a tree, and a lock on layout finally has a verb to refuse.
**Verified:** 2026-09-24T08:10:00Z
**Worktree:** `/Users/kaelencook/Tapestrees/agent-trees`, branch `ws/agent-trees`, HEAD `2370adb`
**Status:** human_needed
**Re-verification:** No, this is the initial verification.

**Source note:** The design specs under `~/Tapestry Tales/` (Spec - Agent Spatial Verbs, Spec - Locks and Rank, Decision Register) no longer exist on disk, so I could not check against them. I verified against ROADMAP SC1-SC6, the phase CONTEXT decisions D-01..D-19, every plan's `must_haves`, and 2.4's actual `locks.ts`.

## Gates I ran myself

| Gate | Command | Result |
|---|---|---|
| Full suite | `cd app && npx vitest run` | **18 files / 411 tests passed**, exit 0 (spatial.test.ts 64, notes.test.ts 32, locks.test.ts 62, notes-policy 22, …) |
| Typecheck | `cd app && npm run typecheck` | exit 0 (node and web tsconfigs) |
| SC6 vocabulary | `grep -nE 'facing\|direction\|\bon:'` over schemas.ts, spatial.ts, placement.ts, agent-tools.ts | no matches |
| D-15 superseded gate | `grep -nE 'assertOwnNote\|writeToOwnNote\|isAgentActor' spatial.ts` | no matches |
| Single declarations | `DEFAULT_NOTE_WIDTH = `, `CHILD_GAP = `, `GREW_FROM_LABEL = ` in app/src (non-test) | each exactly once, in placement.ts |
| SC5 kernel/addon | `git show --stat` on every `(02.5-0x)` commit filtered to `tapestry/` and `app/native/` | none touched (the only `tapestry/` diff in range is a docs file from the 2.2/2.4 merge b39ecd5) |
| D-18 no tsconfig change | `git log 4c27e51^..HEAD -- app/tsconfig*.json` | no commits |
| Debt markers | TBD/FIXME/XXX/TODO/HACK over all 10 phase source files | none |

I did not re-run `build:js`, because it writes build output. Plan 05 reports it exits 0, and the typecheck covers compilation.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | **SC1:** `look(tree, from, [toward], [limit])` returns same-tree neighbours nearest first, each exactly `note, space, relation, order, guess`; no coordinates; no `limit` ceiling | ✓ VERIFIED | `spatial.ts` `look` → `orderNeighbours` → 5-key map. `LookArgs.limit` is `z.number().int().min(1)` with no max. Tests: `returns exactly note, space…`, `honours a limit above 100`, `accepts a limit of 1000`, `walks the reply and finds numbers only under order`, `keeps only the notes that lie toward…`. Over the real shim: `looks around the grown note and sees relations, never coordinates`. See the decision item on coordinates after 2.4. |
| 2 | **SC2:** `place(tree, note, where)` with `{near}` or `{beyond, from}` (ids only, `.strict()`) moves the note; `create_note` accepts the same optional `where`, and without it behaves exactly as before; `{on}` is deferred | ✓ VERIFIED | `schemas.ts` has strict `NearArgs`/`BeyondArgs`, `WhereArgs = z.union`, `PlaceArgs` strict, and `CreateNoteArgs.where: WhereArgs.optional()`. `agent-tools.ts` has `case 'place'` → `commands.spatial.place(actor, …)`, and `create_note` passes `where`. Tests: `PlaceArgs (D-11)` refuses the space and orientation forms, a lone beyond, mixed forms, extra keys, and an actor key at either level. `createFrom without where (D-02: unchanged)` checks four set lines, no pinned, and the same keys. The original x 390 test is unedited. |
| 3 | **SC3 (stored side):** near the grew-from parent writes the resolved spot plus `pinned false`; every other placement is fixed; no `pinned` key means it never moves; reopen reproduces the stored spots with no model | ✓ VERIFIED | `placeOps` and `resolveWhere`, with `follows` only for near the note's own parent. `isFollowing` is true only for bool false. Tests: `near its own grew-from parent follows…`, `keeps a grown note with no pinned, or pinned true, at its stored spot`, `keeps the stored position and pinned across close and reopen, and draws the follower there (D-06)`, plus the placement.test `isFollowing (D-02)` table. |
| 4 | **SC3 (renderer):** drawn beside the parent wherever it moves, until a person drags it (`pinned true`) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The code is wired: `Canvas.tsx:312` calls `displayPositions(tree.nodes, tree.edges, overrides)`; `TreeFrame.tsx:489,527` passes `followSpot` to NoteCard/FallbackNodeView; `writePosition` routes a follower to `onTakeOverPosition`; `App.tsx:410` writes position plus `pinned bool true` as 'Move note'. No component test harness exists, so this needs human UAT. |
| 5 | **SC4:** every refusal is `{ok:false, error}` naming what was wrong and commits nothing (unknown or unplaced anchor, self, `lock.layout` naming its owner); a user-made note is locked by default | ✓ VERIFIED | `place refusals write nothing (SC4)` checks file size, node, edge and @commit counts, the note's position and pinned, and the onCommitted count, for every case. `refuses to move a note user.kaelen made, with no lock property, naming user.kaelen`. Over the shim: `refuses to move the person's note, naming user.kaelen, and writes nothing`. `createFrom where refusals write nothing`. |
| 6 | **SC5:** no new kernel verb or value type; placements stay `position.x/y`; no placement-edge migration | ✓ VERIFIED | `placeOps` emits only `setProperty` of position.x, position.y or pinned, or `unsetProperty` of pinned (test `only ever emits…`). No `tapestry/` or `app/native/` change in any phase commit. No `placement.near`, `placement.rule` or `placed-near` anywhere in app/src. |
| 7 | **SC6:** no `facing`; D-27 `direction` is not written by agents | ✓ VERIFIED | The vocabulary grep is clean. The schemas refuse extra keys. |
| 8 | D-18: the geometry is pure, in `renderer/layout/placement.ts`, imports only a type from `./frames`, and main imports it with no tsconfig change | ✓ VERIFIED | The only import is `import type { ContentBox } from './frames'`. spatial.ts and notes.ts import `'../../renderer/layout/placement'`. No tsconfig commits in range. |
| 9 | D-07: stored width/height or the defaults 280/120, never measured; the look and place descriptions state the taller-card caveat | ✓ VERIFIED | `noteSize` in placement.ts. Descriptions at schemas.ts:225 and 233 contain "taller". |
| 10 | `look` is read-only: no reconcile, no commit, file unchanged | ✓ VERIFIED | `look` never calls `prepareWriteFor` or `submitAs`. Tests: `succeeds on a rewound tree without reconciling it…` and `leaves the file unchanged after a successful look`. |
| 11 | D-12: anchors resolve only in the named tree | ✓ VERIFIED | `resolveWhere` looks up `world.nodes` only. Tests `refuses an id that is live only in another open tree…` exist for look, place and create_note. |
| 12 | PLUG-03: look and place go through TOOL_DEFINITIONS → runAgentTool → command layer; the shim advertises 10 tools | ✓ VERIFIED | `index.ts:282` has `spatial: new SpatialCommands(registry, commandHooks)`. Test: `advertises exactly the ten tools, with no actor argument anywhere`. |
| 13 | D-09 / D-10: `resolveNear` steps down from CHILD_GAP right; `resolveBeyond` extends the centre line by CHILD_GAP + width; both are bounded by MAX_PLACE_STEPS=64 and deterministic | ✓ VERIFIED | placement.ts `stepDown`, `resolveNear`, `resolveBeyond`, with no trig, clock or random calls. The placement.test groups `resolveNear (D-09)` and `resolveBeyond (D-10)` pass (67 cases in the file). |
| 14 | D-06 / D-08: the stored spot equals the drawn spot; replay never re-derives it | ✓ VERIFIED | `resolveWhere` simulates the follow case through `displayPositions`. notes.test places a note at 390,164 and `displayPositions` draws it there. The spatial reopen test passes. |
| 15 | D-17 / D-03: an agent never removes or overwrites `pinned true`; near its own parent on a pinned-true note is refused | ✓ VERIFIED | `heldByPerson` and `placeOps`, plus the step-5b guard in `place`. Tests: `fixing a note with pinned true writes only the two position lines…` and `refuses near its own parent once pinned by a person, and writes nothing`. |
| 16 | D-04 / PROV-01: one commit, intent message (`Place note n2 near n1`), actor `plugin agent.<name>` from the socket | ✓ VERIFIED | `placeMessage`. The actor comes from `runAgentTool`'s parameter, not from args. Test: `passes the socket actor, never one from the arguments`. |
| 17 | D-15: gated by 2.4's `checkLock(…,'layout')`; the owner is named; allow, `open` and the owner itself may place | ✓ VERIFIED | spatial.ts step (5) uses `createdBy` from the history index. The `place and the layout lock (D-15)` group has 4 passing tests. |
| 18 | D-13: placing a vault note changes only its Tapestry-side position; md.* is untouched | ✓ VERIFIED | Adapted per Kaelen's decision: bridge-created vault notes are layout-locked by default. Tests: `refuses a vault note the obsidian.bridge created, by default`, `moves only the Tapestry-side position once its layout is open, leaving md.path byte-equal`, and `places a vault-typed note an agent created`. |
| 19 | A refusal decided from the arguments alone does not discard a person's redo on a rewound tree | ✓ VERIFIED | The id-shape, self and same-note checks run before `prepareWriteFor`. Tests are in `place on a rewound tree`. |
| 20 | Plan 05: `create_note` without `where` behaves exactly as before; with `where` it produces one commit holding the node and its grew-from edge, signed by the agent, and the message says where | ✓ VERIFIED | The notes.ts no-where branch is unchanged, with where applied as an overlay on `ops[0].props`. Tests are in `createFrom with where`. Over the shim: `creates a note with where near the note it grew from, so it follows, stepping past n2`. |
| 21 | D-16: a left or top resize of a follower pins it; a right or bottom resize does not | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | NoteCard.tsx:480 calls `onPositionChange` only when `localPos` is set, which happens only when x or y changes (a left or top resize). That call reaches TreeFrame's `writePosition`, which sends a follower to the takeover. No test covers this. |
| 22 | The card, lines, knot midpoints and frame bounds agree on a follower's spot, and the card does not jump back after its own drag | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `getNodeCenter` reads the spot first, and Canvas frame boxes read the spot first. The card receives `followSpot`, not its own override. This is visual behaviour and is untested. |
| 23 | Phase gate: suite and typecheck green; no kernel or addon change; SC6 clean; no superseded gate; one declaration of each constant | ✓ VERIFIED | I ran all of these myself (see Gates). |

**Score:** 20/23 truths verified (3 present, behavior-unverified: all renderer truths, blocked only by the missing component harness).

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/src/renderer/layout/placement.ts` | Pure geometry, resolvers, displayPositions, resolveWhere | ✓ VERIFIED | 680 lines. Every export listed in the Plan 01 and 02 frontmatter is present. Imported by spatial.ts, notes.ts, agent-tools.ts and Canvas.tsx. |
| `app/src/main/commands/spatial.ts` | `look`, `place`, `placeOps`, `placeMessage`, `heldByPerson` | ✓ VERIFIED | 380 lines. Constructed in index.ts and dispatched from agent-tools.ts. |
| `app/src/main/mcp/schemas.ts` | `LookArgs`, `WhereArgs`, `PlaceArgs`, `CreateNoteArgs.where`, the look and place definitions | ✓ VERIFIED | Imports only zod. Every schema is strict. |
| `app/src/main/commands/notes.ts` | `createFrom` with optional where | ✓ VERIFIED | Calls `resolveWhere(` and `whereRefusalText`. |
| `app/src/renderer/components/{Canvas,TreeFrame,NoteCard,FallbackNodeView}.tsx`, `App.tsx` | displayPositions feed, followSpot, takeover handler | ✓ WIRED (behaviour needs UAT) | See truths 4, 21 and 22. |
| Tests: `placement.test.ts`, `spatial.test.ts`, `notes.test.ts`, `shim.test.ts` | Unit, integration and shim coverage | ✓ VERIFIED | All pass in the 411-test run. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| agent-tools.ts | spatial.ts | `commands.spatial.look(`, `commands.spatial.place(actor` | ✓ WIRED |
| index.ts | spatial.ts | `new SpatialCommands(registry, commandHooks)` | ✓ WIRED |
| spatial.ts / notes.ts | placement.ts | `from '../../renderer/layout/placement'` | ✓ WIRED |
| spatial.ts | locks.ts (2.4) | `checkLock(noteId, note.props, entry.createdBy, actor, 'layout')` after `prepareWriteFor`, before `resolveWhere` | ✓ WIRED |
| spatial.ts `look` | placement.ts | `displayPositions(nodes, edges, new Map())` | ✓ WIRED |
| agent-tools.ts `create_note` | notes.ts | `where?: WherePlacement` passed through | ✓ WIRED |
| Canvas.tsx | placement.ts | `displayPositions(tree.nodes, tree.edges, overrides)` | ✓ WIRED |
| TreeFrame.tsx | NoteCard / FallbackNodeView | `displayPosition={…followSpot…}` | ✓ WIRED |
| TreeFrame → Canvas → App | takeover | `handlers.onTakeOverPosition` → Canvas wrapper plus `settleFrames` → `handleTakeOverPosition` | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|---|---|---|---|---|
| `look` result | neighbours | `tree.bridge.getNodes()/getEdges()` (native kernel) → `displayPositions` → `orderNeighbours` | Yes, against the real addon in spatial.test and shim.test | ✓ FLOWING |
| `place` commit | ops | `resolveWhere` over live bridge state → `placeOps` → `bridge.submitAs` | Yes; the tests read the `.tree` file back | ✓ FLOWING |
| Follower card position | `followSpot` | `tree.nodes/edges` from renderer state → `displayPositions` | Wired; the visual result needs UAT | ⚠️ needs human |

### Requirements Coverage

The phase owns no requirements (ROADMAP: "none owned — exercises PROV-01/03, TREE-01/02 and PLUG-03 through integration").

| Requirement | Plans | Evidence | Status |
|---|---|---|---|
| PROV-01 | 03, 04 | Agent placements are signed `plugin agent.<name>` by the bridge. The takeover is signed by the human actor through `kernel:submit`, which needs UAT to observe in the app. | ✓ SATISFIED (renderer side needs UAT) |
| PROV-03 | 04, 05 | One commit per placement or creation, with an intent message | ✓ SATISFIED |
| TREE-01 | 03, 04, 05 | Only readable `set … position.x real`, `pinned bool` lines; no new keys | ✓ SATISFIED |
| TREE-02 | 02, 04 | The reopen test reproduces the stored spots | ✓ SATISFIED |
| PLUG-03 | 01, 04, 05 | Same TOOL_DEFINITIONS → runAgentTool path; 10 tools over the real shim | ✓ SATISFIED |

No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (all phase files) | — | TBD/FIXME/XXX/TODO/HACK | none found | — |
| `TreeFrame.tsx` | 464-480 | `VaultNoteCard` gets no `displayPosition`, and its drop goes straight to `onPositionChange` rather than `writePosition` | ℹ️ Info | If a vault-typed note ever followed a parent, its card would draw at its stored spot while its lines and frame used the follow spot, and a drag would not pin it. This is unreachable today: the bridge writes no grew-from edges, `connect_notes` always writes the `link` label, and `create_note` makes native notes. Worth closing when vault notes can grow. |
| `spatial.ts` | step (3) → (5) | A lock refusal on a rewound tree comes after `prepareWriteFor`, so it discards the person's redo | ℹ️ Info | This is by design and matches `notes.ts` `assertMayWrite`. Plan 04 limits redo preservation to argument-only refusals. No history commit is written. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| All look, place and create_note-where behaviour, including over the real MCP shim | `cd app && npx vitest run` (the single allowed full run) | 411/411 passed | ✓ PASS |
| Types across the main and renderer boundary (main importing the renderer layout module) | `npm run typecheck` | exit 0 | ✓ PASS |
| Renderer drawing and takeover | — | No harness, and the app must not be launched | ? SKIP → human |

### Probe Execution

Step 7c: SKIPPED. The phase declares no `probe-*.sh` scripts, and none exist for this phase.

### Human Verification Required

#### 1. Follower drawn beside its parent (Plan 03 Task 1, SC3 renderer)
**Test:** Run `npm --prefix app run dev`. Grow note B from note A with `where: { near: A }`, then drag A slowly.
**Expected:** B stays 80px right of A, the line stays attached, the frame contains B, and no commit is written for B. A note with no `pinned` key stays put.
**Why human:** There is no renderer component harness.

#### 2. Takeover by drop and by left/top resize (Plan 03 Task 2, D-03, D-16)
**Test:** Drop follower B, drag A, relaunch, left-resize follower C, right-resize follower D and then drag A, and drag an ordinary note E. Read the `.tree` file after each step.
**Expected:** B's drop produces a single `Move note` commit under `actor human user.<name>`, with `set B pinned bool true`. C is pinned. D keeps following. E's commit writes position only.
**Why human:** This is a pointer-driven state transition that no test covers.

#### 3. No snap-back, and lines and frame agree (truth 22)
**Test:** Drag a follower and release it.
**Expected:** The card does not flicker back, and the line and frame track the drawn spot.
**Why human:** Visual timing.

#### 4. Plan 05 end-to-end with a real agent
**Test:** Connect an agent. `create_note` with `where: { near: <your note> }`, then `place` another agent note beyond it. Drag your note, drag the follower, relaunch, grow a tall card and `look` from a neighbour, then ask the agent to `place` your note.
**Expected:** The created note follows your note. The beyond note stays put. The follower is pinned after your drag, with `pinned bool true` under your human actor. Relaunch reproduces every position. `look` may say the tall card is clear although it overlaps on screen (the D-07 caveat). Placing your note is refused with `… layout is locked by user.<you>`.
**Why human:** Needs the live app, a real MCP client and a relaunch.

#### 5. Decision: `look` coordinates now that 2.4 is merged
SC1 grants coordinates "only for placements it may write, which before Phase 2.4 is none", and CONTEXT D-14 says "when 2.4 lands, coordinates switch on through the lock check". Phase 2.4 is merged, but Plan 04 deliberately kept `look` relations-only ("turning on coordinates is not part of this phase"), and no later ROADMAP phase owns the switch. The literal SC is met, because it is an upper bound, so this is not a gap. The follow-through does need a home. Kaelen's options:
- **(a)** Accept the phase as is, and file "look coordinates for layout-writable notes via `checkLock(…,'layout')`" to a later phase or the backlog.
- **(b)** Request a small gap-closure plan in 2.5.

### Gaps Summary

**No blocking gaps.** Code and tests verify every roadmap success criterion and every plan must-have that main-process tests can reach. I ran the full suite (411 pass) and the typecheck myself. The phase-hygiene gates also pass: SC5 kernel/addon, SC6 vocabulary, the D-15 superseded gate, D-18 no tsconfig change, and single constant declarations.

What remains:
- **(b) Human UAT in the running app.** Three renderer truths are wired but have no component tests: following while the parent is dragged, the drop and left/top-resize takeover, and snap-back and frame agreement. The Plan 05 end-to-end agent check also needs a live window, a real MCP client and a relaunch.
- **(c) One scoping decision.** Switching on `look` coordinates for layout-writable notes now that 2.4 is merged. SC1 is literally satisfied, but D-14's follow-through is unowned.
- **Informational.** VaultNoteCard does not take part in following or takeover. This is unreachable today and worth noting for later.

---

_Verified: 2026-09-24T08:10:00Z_
_Verifier: Claude (gsd-verifier)_
