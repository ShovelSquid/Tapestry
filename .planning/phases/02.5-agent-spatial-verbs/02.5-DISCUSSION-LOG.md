# Phase 2.5: Agent Spatial Verbs - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-16
**Phase:** 02.5-agent-spatial-verbs
**Areas discussed:** What a placement writes, Following and sensing, The rule and its version, Boundaries

**How answers were given:** Kaelen was away. Questions went to the Tapestry Permission Queue: area selection as #2, then Areas 1–4 as #3, #4, #5 and #6 (#7 was a duplicate of #3 and was closed). The orchestrator `tapestry-e2` answered each by message under Kaelen's standing grant (2026-09-16, 11:0x), always choosing the recommended option. One-way-door options were marked as such and not chosen. Areas were batched, one call per area, and the "more questions?" checks were skipped, with the orchestrator's approval.

---

## Area selection (#2)

| Option | Selected |
|--------|----------|
| What a placement writes | ✓ |
| Following and sensing | ✓ |
| The rule and its version | ✓ |
| Boundaries | ✓ |

---

## What a placement writes (#3)

**How is the note being followed recorded?**

| Option | Description | Selected |
|--------|-------------|----------|
| Follow the grew-from parent (Recommended) | No new key; only the parent is followed | ✓ |
| New key `placement.near` (ONE-WAY DOOR) | Follow any note; adds a key to the format | |
| New edge label (ONE-WAY DOOR) | Adds a label; overlaps spatial-canvas's `placement` design | |

**What does a missing `pinned` key mean on a note?**

| Option | Selected |
|--------|----------|
| Missing means fixed (Recommended) | ✓ |
| Missing means following, as knots do | |

**What does a person's drag write?**

| Option | Selected |
|--------|----------|
| position + pinned true (Recommended) | ✓ |
| position + unset pinned | |

**What commit message does `place` write?**

| Option | Selected |
|--------|----------|
| State the intent (Recommended) | ✓ |
| Same as a drag: 'Move note' | |

**Notes:** The orchestrator asked that the two one-way doors be recorded in CONTEXT as a possible later widening that needs Kaelen, and that the edge-label option's overlap with the `placement` design (waiting on #21) be noted.

---

## Following and sensing (#4)

| Question | Options | Selected |
|----------|---------|----------|
| Where is following computed? | Renderer at draw time (Rec.) / Main rewrites positions on anchor move | Renderer |
| What does the stored position hold? | Spot resolved at place time (Rec.) / Leave position unwritten | Resolved spot |
| What sizes does main use? | Stored size, else fixed defaults (Rec.) / Renderer reports measured sizes | Stored, else defaults; document the overlap limit in the tool description |

---

## The rule and its version (#5)

| Question | Options | Selected |
|----------|---------|----------|
| How does replay stay faithful? | Snapshot is the record (Rec.) / `placement.rule` stamp (ONE-WAY) / Policy property on the tree (ONE-WAY) | Snapshot |
| What if `near X` is occupied? | Step downward until clear (Rec.) / Allow overlap | Step down, with a bounded count and a refusal naming the anchor |
| What happens to `{ on }`? | Defer to placement-edges phase (Rec.) / Keep: first free spot in own frame | Defer; the roadmap criterion 2 edit is approved |
| How is `beyond X from Y` resolved? | Extend the line Y→X (Rec.) / You decide | Extend the line |

---

## Boundaries (#6)

| Question | Options | Selected |
|----------|---------|----------|
| Anchor in a different tree | Refuse, commit nothing (Rec.) / Copy the note into that tree | Refuse |
| What may `place` do to a vault note? | Tapestry position only (Rec.) / Allow group changes | Position only |
| What gates `place` before 2.4? | `place` waits for 2.4 (Rec.) / Interim authorship gate | Waits for 2.4 |
| `look` coordinates before 2.4 | None, relations only (Rec.) / Notes the agent created | None |

**Notes:** The orchestrator narrowed Pending #16: only `place` waits for the 2.4 merge.

---

## Flags raised during discussion
- **Pending #22:** `02.3-CONTEXT.md:149` records Kaelen giving "the anchor cursor … together with agents moving as clusters of notes" its own phase after 2.3. Phase 2.5 splits the plain verbs out of it. The orchestrator treats this as needing Kaelen's word; discussion and planning continue because they are reversible docs.

## Claude's Discretion
- Tool names; the default card height; the `near`/`beyond` distance threshold and the overlap test; the collision step bound; where the resolver module lives.

## Deferred Ideas
- One-way doors: `placement.near`, an anchoring edge label, the `placement.rule` stamp, and a tree policy property.
- `{ on }`, which goes to the placement-edges phase. `facing`, bodies, clusters and waypoints, which go to the anchor-cursor phase.
