---
phase: 02.5-agent-spatial-verbs
plan: 04
type: execute
wave: 4
# GATED ON PHASE 2.4 (D-15, D-19): Task 1 is a blocking human checkpoint that stops
# unless the Phase 2.4 lock model has been merged into ws/agent-trees by Kaelen.
depends_on: ["02.5-02", "02.5-03"]
gated_on: "Phase 2.4 lock model (lock.layout) merged into ws/agent-trees — Kaelen's call; never merged by the executor"
files_modified:
  - app/src/main/mcp/schemas.ts
  - app/src/main/commands/agent-tools.ts
  - app/src/main/commands/spatial.ts
  - app/src/main/commands/spatial.test.ts
  - app/src/main/mcp/shim.test.ts
autonomous: false
requirements: [PROV-01, PROV-03, TREE-01, TREE-02, PLUG-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-19: no code in this plan is written until a person has confirmed, at Task 1, what Phase 2.4 actually shipped (the lock check's name, signature and refusal text); if 2.4 is not merged the plan stops there"
    - "SC2 / D-11: an agent calling place(tree, note, where) with { near } or { beyond, from } — ids only, strict — moves the note; the space form, the orientation form, a lone beyond, mixed forms, extra keys and an actor key are refused by the schema"
    - "D-01 / SC3: place near the note's own grew-from parent writes the resolved position and pinned bool false; every other placement is fixed"
    - "D-17 / D-03: an agent's place never removes or overwrites an existing pinned true — a fixed placement unsets pinned only when its current value is the bool false and otherwise leaves pinned exactly as it was, and place near the note's own grew-from parent on a note with pinned true is refused (`n2 was pinned by a person; it will not follow n1 again`) and commits nothing"
    - "D-04 / PROV-01: a placement is one commit whose message states the intent (`Place note n2 near n1`, `Place note n2 beyond n1 from n3`) and whose actor line is `plugin agent.<name>`, stamped by the bridge"
    - "D-06 / TREE-02: after closing and reopening the tree, the stored position and pinned are unchanged, and a following note is drawn exactly at its stored spot"
    - "D-08 / SC5: every op place writes is setProperty of position.x, position.y or pinned, or unsetProperty of pinned — no rule key, no new key, type string or edge label, no kernel or addon change"
    - "SC4: every refusal (unknown or unplaced anchor, self, same note, no clear spot, no line, locked, a person's takeover) returns { ok: false, error } naming what was wrong and commits nothing — file size, node and edge counts, commit count, the note's position and pinned, and the onCommitted count are all unchanged"
    - "D-15 / SC4: a placement held by lock.layout is refused with a message naming the lock's owner; an actor in lock.layout.allow, or a lock whose value is open, may place; a user-made note cannot be moved by an agent by default; the superseded authorship gate is never called"
    - "D-12: anchors resolve only in the named tree; D-13: placing a vault note changes only its Tapestry-side position and leaves every md.* key untouched"
    - "D-14: neither place's success value nor any refusal carries a coordinate; look stays relations-only"
    - "A refusal decided from the arguments alone (bad id shape, self, same note) does not discard a person's redo on a rewound tree"
  artifacts:
    - path: "app/src/main/commands/spatial.ts"
      provides: "SpatialCommands.place, placeOps, placeMessage, heldByPerson"
      exports: ["SpatialCommands", "PlaceResult", "placeOps", "placeMessage", "heldByPerson"]
    - path: "app/src/main/mcp/schemas.ts"
      provides: "WhereArgs, PlaceArgs and the place tool"
      contains: "name: 'place'"
    - path: "app/src/main/commands/spatial.test.ts"
      provides: "place success, refusal, lock, vault, reopen and rewound-tree tests"
  key_links:
    - from: "app/src/main/commands/agent-tools.ts"
      to: "app/src/main/commands/spatial.ts"
      via: "case 'place' passes the socket actor to commands.spatial.place"
      pattern: "commands\\.spatial\\.place\\(actor"
    - from: "app/src/main/commands/spatial.ts"
      to: "Phase 2.4 lock check (name recorded at Task 1)"
      via: "called after prepareWriteFor, before resolveWhere"
      pattern: "lock"
    - from: "app/src/main/commands/spatial.ts"
      to: "app/src/renderer/layout/placement.ts"
      via: "resolveWhere and whereRefusalText"
      pattern: "resolveWhere\\("
---

<!-- planner-discipline-allow: facing -->
<!-- planner-discipline-allow: direction -->
<!-- planner-discipline-allow: on: -->
<!-- planner-discipline-allow: assertOwnNote -->
<!-- planner-discipline-allow: writeToOwnNote -->
<!-- planner-discipline-allow: isAgentActor -->
<!-- planner-discipline-allow: op: ' -->

<objective>
**GATED ON PHASE 2.4.** This plan runs only after Kaelen has merged Phase 2.4's lock model (`lock.layout`, built on `ws/notifications-rank`) into `ws/agent-trees`. Task 1 is a blocking human checkpoint; if 2.4 is not merged, stop there and write no code (D-15, D-19).

**As an** agent, **I want to** say "put this note near n7" or "beyond n7 as seen from n4" and have the host work out the spot, **so that** I can arrange my own notes in task space — and a person's locked layout finally has a verb to refuse.

Task 1 reads what Phase 2.4 actually shipped and records the adaptation. Task 2 adds the `place` tool (ten tools in total) and its success path. Task 3 proves every refusal writes nothing, including the lock refusals, the vault case and the rewound-tree case.

Purpose: SC2, SC3's write half, SC4 and D-15 — the write verb, gated only by the lock model, never by the superseded authorship rule.

Output: `SpatialCommands.place`, `WhereArgs`/`PlaceArgs`, the `place` tool, and its tests.
</objective>

## Working rules for this plan

- Work only in `/Users/kaelencook/Tapestry-trees/agent-trees` on branch `ws/agent-trees`; run every command from there; do not create a worktree or switch branches.
- **Never merge a branch.** Merging Phase 2.4 is Kaelen's decision. If it has not happened, stop at Task 1.
- Stage files by name only (never `-A`, `.` or a directory), never stage `app/native/build/` or `app/out/`, and check `git show --stat HEAD` after each commit.
- Do not install packages, edit tsconfig files, `.gitignore`, `.claude/CLAUDE.md` or `.planning/config.json`.
- Do not build or change the lock model itself. If 2.4 lacks something SC4 needs, stop and report; do not re-implement it here.
- `schemas.ts` keeps importing only zod; `schemas.ts`, `spatial.ts` and `placement.ts` stay free of the SC6 words named in Plan 01's gate.
- If any step seems to need a new `.tree` key, type string or edge label, stop and report it to Kaelen as a one-way door; this phase writes only `position.x`, `position.y` and `pinned`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-agent-spatial-verbs/02.5-CONTEXT.md
@.planning/phases/02.5-agent-spatial-verbs/02.5-02-SUMMARY.md
@.planning/phases/02.5-agent-spatial-verbs/02.5-VALIDATION.md

Read RESEARCH.md by section only: Q1 (op shapes and the rewound-tree pitfall), Q2 (test fixtures), Q7 (vault notes and D-13), Pattern 1 and Pattern 2, Pitfalls 3, 4 and 8, and the Assumptions Log row A6. Read `~/Tapestry Tales/Connections/Spec - Locks and Rank.md` §2 and §4 (the working assumption for the lock check).

<interfaces>
Working assumption until Task 1 replaces it (Locks spec §2, RESEARCH A6):

```text
lock.layout        text   absent -> derived default: locked to the author when the note was created by a human actor, open otherwise
                          "open" -> explicitly unlocked
                          "<actor id>" -> locked; that actor is the owner
lock.layout.allow  text   whitespace-separated actor ids who may write despite the lock
assumed check      layoutLock(tree, noteId, actor) -> { owner: string } | null   (synchronous, main process)
assumed refusal    the layout of <note> in <tree> is locked by <owner>
```

From Plan 02, `app/src/renderer/layout/placement.ts`:

```typescript
export type WherePlacement = { near: string } | { beyond: string; from: string }
export function resolveWhere(world: { nodes; edges }, subject: PlacementNode, where: WherePlacement): WhereOutcome
export function whereRefusalText(refusal: WhereRefusal, subjectId: string, treeName: string): string
export function isKnot(node): boolean
export function displayPositions(nodes, edges, overrides): Map<string, DisplaySpot>
```

From `app/src/main/commands/notes.ts`: `prepareWriteFor(tree, actor, hooks)` (:165), `CommandHooks`, `CommandResult`. Kernel rule verified in `tapestry/kernel/World.cpp:197-208`: `unsetProperty` of an absent key is rejected; `setProperty` of an unchanged value is accepted. `.tree` lines look like `set n2 pinned bool false`, `set n2 position.x real 360` and `unset n2 pinned` (`tapestry/docs/tree/FORMAT.md`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decision — confirm what Phase 2.4 shipped before any `place` code is written (D-19)</name>
  <precondition>Phase 2.4's lock model is in this branch: `git log --oneline ws/agent-trees` shows its merge or commits, and `grep -rn 'lock\.layout' app/src/main` prints at least one line outside a `.test.ts` file</precondition>
  <read_first>
    - `~/Tapestry Tales/Connections/Spec - Locks and Rank.md` §2 and §4
    - `.planning/phases/02.5-agent-spatial-verbs/02.5-CONTEXT.md` — D-15 and D-19
    - every non-test file the grep in the precondition prints, in full
    - `app/src/main/commands/notes.ts`, `agent-tools.ts` and `app/src/main/mcp/schemas.ts` — as they stand after the merge
  </read_first>
  <decision>How `place` calls Phase 2.4's layout lock, or whether this plan must stop</decision>
  <context>
    D-15 plans `place` against `lock.layout` as the Locks spec §2 describes it, and D-19 makes this plan provisional until a person has seen what 2.4 really shipped. This checkpoint is that look. It is a blocking human gate on purpose: the default option must not be taken unattended.

    Before presenting the options, gather these facts with read-only commands and put them in the checkpoint message (they also go in the SUMMARY):
    1. Is 2.4 merged? `git log --oneline --merges -10` and `git log --oneline fe33d69..HEAD -- app/src tapestry | head -40`. If nothing from 2.4 is there, the answer is `stop`.
    2. The lock check: its exported name, file, parameters, return shape, and whether it is synchronous.
    3. Its semantics: an explicit `lock.layout` owner value; the literal `open`; `lock.layout.allow`; the derived default that locks a human-made note to its author; the scope cascade; and the exact refusal sentence, including whether it names the owner.
    4. What else 2.4 changed that this plan or Plan 05 touches: `createFrom` in `notes.ts` (and whether it now carries a lock check of its own), `agent-tools.ts`, `schemas.ts` (the current tool count), and anything under `tapestry/` or `app/native/`.
    5. The new baseline: rebuild with `npm --prefix app run build:native` if 2.4 changed `tapestry/` or `app/native/`, always run `npm --prefix app run build:js`, then run `npm --prefix app run test` and `npm --prefix app run typecheck` and record both. Plans 01-03's tests must still pass.
    6. `PLACE_BASE`: the output of `git rev-parse HEAD` at this moment.

    `look` stays relations-only in this plan whatever 2.4 provides; turning on coordinates is not part of this phase.
  </context>
  <options>
    <option id="as-assumed">
      <name>2.4 matches the Locks spec §2 shape — proceed</name>
      <pros>A synchronous check that honours an explicit owner, `open`, `.allow` and the human-author default, and whose refusal names the owner. Task 2 calls it directly by its real name; the refusal sentence is 2.4's own.</pros>
      <cons>None beyond recording the real name and signature for Tasks 2 and 3.</cons>
    </option>
    <option id="adapt">
      <name>2.4 has the semantics SC4 needs but a different shape — proceed with a thin adapter</name>
      <pros>Task 2 adds one private method on `SpatialCommands` that calls 2.4's check and returns the refusal sentence or null, producing `the layout of <note> in <tree> is locked by <owner>` when 2.4's own text does not name the owner. Only the shape is adapted; the lock rules stay 2.4's.</pros>
      <cons>One more indirection to keep in step with 2.4. Record the exact differences so the executor follows them.</cons>
    </option>
    <option id="stop">
      <name>2.4 is not merged, has no layout lock, or lacks a rule SC4 depends on — stop</name>
      <pros>Nothing is written against a guessed API. Plans 04 and 05 stay pending, and the inventory goes to Kaelen (Decision Register Pending #16).</pros>
      <cons>`place` and `create_note`'s `where` wait. Plans 01-03 remain shipped and useful.</cons>
    </option>
  </options>
  <resume-signal>Reply `as-assumed`, `adapt` (with the recorded name, signature, return shape and refusal text), or `stop`. Include `PLACE_BASE` and the current tool count.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: An agent places a note near or beyond another note, in one intent-named commit</name>
  <precondition>Task 1 was answered `as-assumed` or `adapt`, and its reply recorded the lock check's name and `PLACE_BASE`</precondition>
  <reversibility rating="costly">This is the first code that writes `pinned bool false` on notes (D-02); once saved worlds contain it, the meaning of a missing `pinned` cannot change without changing how those worlds render.</reversibility>
  <files>app/src/main/mcp/schemas.ts, app/src/main/commands/agent-tools.ts, app/src/main/commands/spatial.ts, app/src/main/commands/spatial.test.ts, app/src/main/mcp/shim.test.ts</files>
  <read_first>
    - `app/src/main/commands/spatial.ts` and `spatial.test.ts` — as left by Plan 02
    - `app/src/main/commands/notes.ts` lines 154-170 and 244-336 — `prepareWriteFor` and `createFrom` (the refusal order and commit shape to copy)
    - `app/src/main/mcp/schemas.ts`, `app/src/main/commands/agent-tools.ts`, `app/src/main/mcp/shim.test.ts` — as they stand after the merge
    - the lock-check file recorded at Task 1
    - `app/src/renderer/layout/placement.ts` — `resolveWhere`, `whereRefusalText`, `WherePlacement`, `isKnot`, `displayPositions`
  </read_first>
  <behavior>
    - `place` near the note's own grew-from parent returns `{ ok: true, value: { tree, note, seq, follows: true } }`; the last commit block contains `actor plugin agent.claude`, `Place note n2 near n1`, `set n2 position.x real …`, `set n2 position.y real …` and `set n2 pinned bool false`
    - `place` near another note returns `follows: false` and writes no `pinned` line
    - `place` beyond a note from another note is fixed, and with equal cards on a horizontal line lands 80 right of the beyond-note
    - moving a following note (`pinned` bool false) to a fixed spot writes `unset n2 pinned` (D-17); fixing a note with no `pinned` writes no unset line
    - D-17 / D-03: fixing a note that has `pinned` true (a person's takeover) writes only the two position lines — no `unset` and no `pinned` line — and after the commit `pinned` is still bool true
    - `placeOps` only ever emits `setProperty` for `position.x`, `position.y` or `pinned`, and `unsetProperty` for `pinned` (D-08); for a note with `pinned` true it emits neither a `pinned` set nor an unset, whatever `follows` says
    - `heldByPerson` is true for `pinned` bool true and for any other `pinned` value except the bool false, and false when `pinned` is absent or the bool false
    - after `registry.closeAll()` and reopening, the note's `position.x`, `position.y` and `pinned` are equal to what was written, and `displayPositions` over the reopened world draws a following note exactly at its stored spot (D-06)
    - the success value has exactly the keys tree, note, seq and follows, and its only number is `seq`
    - `PlaceArgs` accepts `{ near }` and `{ beyond, from }` and refuses the space form, the orientation form, a lone `beyond`, `near` together with `beyond`, an extra key, and an `actor` key (D-11)
    - the shim advertises exactly one more tool than Task 1 recorded (ten on this branch as planned), including `place`, and an MCP `place` call near n2's parent writes `set n2 pinned bool false`
  </behavior>
  <action>
    Write the `<behavior>` tests first. Seed the notes an agent places as agent-made (created with `agentActor('claude')`), so they are open under 2.4's default.

    In `app/src/main/mcp/schemas.ts`, add a doc-commented "Placement arguments" block **above** `CreateNoteArgs` (Plan 05 reuses it there): a strict `{ near }` object and a strict `{ beyond, from }` object, each id a string of 1-1024, combined as the exported `WhereArgs` with `z.union`. Its comment says ids only, and that the space and orientation forms belong to later phases (D-11, Decision Register #12) and fail both members. Add the exported strict `PlaceArgs` with `tree` (1-200), `note` (1-1024) and `where: WhereArgs`. Add a `place` entry to `TOOL_DEFINITIONS` with title `Move a note next to another note`, `annotations: {}`, and this description: `Moves a note near another note, or beyond one note as seen from another; the host works out the exact spot. Placing a note near the note it grew from makes it follow that note until a person moves it; every other placement stays where it is put. A note whose layout is locked is refused, the refusal names who holds the lock, and nothing is written. Placement uses saved note sizes, or a default when none is saved, so a card whose text made it taller can overlap on screen.` No new import.

    In `app/src/main/commands/agent-tools.ts`, type-import `WherePlacement` from `'../../renderer/layout/placement'` and add `case 'place'` calling `commands.spatial.place(actor, parsed.data as { tree: string; note: string; where: WherePlacement })`. The actor is the socket's, never read from the arguments.

    In `app/src/main/commands/spatial.ts`: update the header (place is gated only by Phase 2.4's layout lock, per D-15; the superseded 02.2 D-05 authorship helpers in `notes.ts` are deliberately not used). Do not write those helpers' names anywhere in `spatial.ts`, not even in a comment — the D-15 gate greps the file for them; refer to them as "the superseded D-05 authorship gate". Type-import `Actor` and `OpObject`, import `prepareWriteFor` from `./notes`, the placement functions and types, and the lock check recorded at Task 1. Export `PlaceResult` (`tree`, `note`, `seq`, `follows`).

    Export `heldByPerson(note)` (D-03, D-17): true when the note has a `pinned` prop whose value is anything other than exactly the bool false — in practice the `pinned` true a person's takeover writes. Absent, or the bool false, gives false. Its comment says a note an agent created and a person then dragged has no default layout lock, so this value is the only record of the takeover and `place` must never remove or overwrite it.

    Export `placeOps(note, spot)` where `spot` has `x`, `y` and `follows`: always a `setProperty` of `position.x` (`real`) and of `position.y` (`real`). Then, when `heldByPerson(note)` is true, add nothing more: `pinned` is left exactly as it is, whatever `follows` says (`place` has already refused the follow case). Otherwise, add a `setProperty` of `pinned` with type `bool` and value `false` when `follows`; or, for a fixed placement, an `unsetProperty` of `pinned` only when the current value is exactly the bool false (D-17), and nothing when `pinned` is absent. Write each op verb as a single-quoted string literal. Document the invariant: after `place`, `pinned` is the bool false (following), absent (fixed), or the person's untouched `pinned` true (fixed) — an agent never removes or overwrites a takeover (D-01, D-03, D-17). Export `placeMessage(noteId, where)` returning `Place note <note> near <id>` or `Place note <note> beyond <id> from <id>` (D-04).

    Add `place(actor, args: { tree: string; note: string; where: WherePlacement }): CommandResult<PlaceResult>`, checking in this order and returning `{ ok: false, error }` at the first failure: (1) `registry.resolveRef(args.tree)` in try/catch; (2) checks that need no world, **before** reconciling (RESEARCH Pitfall 3): the note id must match the node-id pattern, else `<note> is not a live note in <tree>`; each anchor id must match, else `<role> <id> is not a live note in <tree>`; no anchor may equal the note, else `whereRefusalText` for `self`; beyond and from must differ, else `whereRefusalText` for `same-note`; then inside try: (3) `prepareWriteFor(tree, actor, this.hooks)`; (4) read nodes and edges; a missing note gives the not-live sentence; a knot gives `<note> is not a note place can move in <tree>`; (5) the layout lock check exactly as Task 1 recorded, refusing with the owner-naming sentence; (5b) the takeover guard (D-17): when `where` is a near form, its anchor is `grewFromParent(note, edges)`, and `heldByPerson(note)` is true, refuse with `<note> was pinned by a person; it will not follow <parent> again` — a person's takeover is never turned back into following; (6) `resolveWhere({ nodes, edges }, note, args.where)`, refusing with `whereRefusalText(refusal, args.note, tree.name)`; (7) refuse if either coordinate is not finite; (8) `bridge.submitAs(actor, placeMessage(...), placeOps(...))`, then `this.hooks.onCommitted?.(tree.id, actor, result)`, then return `{ ok: true, value: { tree: tree.id, note: args.note, seq: result.seq, follows } }`. A thrown error becomes `{ ok: false, error: errorText(err) }`. Nothing in `place` reads or writes an `md.` key or calls any vault service (D-13). No sentence and no success value contains a coordinate (D-14).

    In `app/src/main/mcp/shim.test.ts`, rename the tool-count test to the new count (ten on this branch as planned; if Task 1 recorded a different starting count, use that count plus one and say so in the SUMMARY), insert `'place'` into the sorted list after `'look'`, and add two tests after the `look` test: request id 7 places `n2` near `n1` and expects no error, `set n2 pinned bool false` and `Place note n2 near n1` in the file; request id 8 places `n1` (the person's note) near `n2` and expects a refusal whose text names `user.kaelen` with the file size unchanged — include this second test only if Task 1 confirmed 2.4's human-author default.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line</fails_when>
    <automated>npm --prefix app run build:js</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>npm --prefix app run test -- src/main/commands/spatial.test.ts src/main/mcp/shim.test.ts src/renderer/layout/placement.test.ts</automated>
    <fails_when>non-zero exit, a `failed` count on the `Test Files` line, or `No test files found`</fails_when>
    <automated>grep -q "op: 'setProperty'" app/src/main/commands/spatial.ts && ! grep -n "op: '" app/src/main/commands/spatial.ts | grep -v -e "op: 'setProperty'" -e "op: 'unsetProperty'"</automated>
    <fails_when>non-zero exit, or any printed line — place emits an op other than setProperty/unsetProperty (SC5, D-08)</fails_when>
    <automated>! grep -nE 'assertOwnNote|writeToOwnNote|isAgentActor' app/src/main/commands/spatial.ts</automated>
    <fails_when>any line printed (exit status inverted to non-zero) — the superseded authorship gate was reused (D-15)</fails_when>
    <automated>! grep -nE 'facing|direction|(^|[{,])[[:space:]]*on[[:space:]]*:' app/src/main/mcp/schemas.ts app/src/main/commands/spatial.ts app/src/renderer/layout/placement.ts</automated>
    <fails_when>any line printed (exit status inverted to non-zero) — SC6 vocabulary reached a gated file</fails_when>
  </verify>
  <acceptance_criteria>
    - `schemas.ts` contains `export const WhereArgs`, `export const PlaceArgs` and `name: 'place'`, declares `WhereArgs` above `CreateNoteArgs`, and `test "$(grep -E '^import' app/src/main/mcp/schemas.ts)" = "import * as z from 'zod'"` still succeeds
    - `agent-tools.ts` contains `case 'place':` and `commands.spatial.place(actor,`
    - `spatial.ts` contains `export function placeOps(`, `export function placeMessage(`, `export function heldByPerson(`, `prepareWriteFor(tree, actor, this.hooks)`, `resolveWhere(`, `Place note` and `was pinned by a person`
    - `spatial.test.ts` has a test whose name mentions `pinned true` and asserts that `pinned` is still bool true after a fixed placement
    - `spatial.ts` does not contain `assertOwnNote`, `writeToOwnNote` or `isAgentActor`
    - `shim.test.ts` lists `'place'` and its tool-count test name matches the new count
    - the three test files, typecheck and `build:js` pass
  </acceptance_criteria>
  <done>An agent can place its note near or beyond another note; a placement near its parent makes it follow, every other placement is fixed, and the commit says what was intended and who asked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Every refusal writes nothing — locks, anchors, vault notes and rewound trees</name>
  <precondition>Task 2 is committed and Task 1's recorded lock semantics include the human-author default, `open` and `.allow` (otherwise Task 1 should have been answered `stop`)</precondition>
  <files>app/src/main/commands/spatial.test.ts, app/src/main/commands/spatial.ts</files>
  <read_first>
    - `app/src/main/commands/spatial.ts` and `spatial.test.ts` — as left by Task 2
    - `app/src/main/commands/notes-policy.test.ts` lines 1-110 and 420-489 — refusal fingerprints and the rewound-tree / `onRedoDiscarded` tests
    - `.planning/phases/02.5-agent-spatial-verbs/02.5-RESEARCH.md` § Q7 — the vault node shape (`obsidian.vault/note@1`, `md.path`)
    - the lock-check file recorded at Task 1 — how a test sets `lock.layout` and `lock.layout.allow`
  </read_first>
  <behavior>
    - a widened fingerprint — file size, node count, edge count, number of `@commit` records, the target note's `position.x`, `position.y` and `pinned`, and the `onCommitted` call count — is identical before and after every refusal below
    - refused with exact sentences: unknown tree; malformed note id; note not live; a knot as the note; near an id that is not live; from an id that is not live; near an unplaced note; near a knot; near the note itself; from the note itself; beyond and from the same note; a column fully blocked (one tall note covering every step); two distinct anchors with the same centre
    - D-17 / D-03: an agent-made note n2, grown from n1 and then dragged by `user.kaelen` (a raw `Move note` commit writing position and `pinned` bool true), is refused for `place` near n1 with exactly `n2 was pinned by a person; it will not follow n1 again`, and the widened fingerprint (including `pinned` still bool true) is unchanged
    - D-17 / D-03: the same note placed near another note, or beyond, succeeds as a fixed placement; its commit block has the two position lines and no `pinned` or `unset` line, and after the commit, and again after close and reopen, `pinned` is still bool true
    - D-12: with a second tree open, an anchor id live only there is refused as `near <id> is not a live note in spatial`
    - D-15: a note whose `lock.layout` names `agent.chatgpt` is refused for `agent.claude` and the message names `agent.chatgpt`; with `agent.claude` listed in `lock.layout.allow` it succeeds; with `lock.layout` set to `open` it succeeds; the owner itself succeeds
    - SC4 / Decision Register #8: a note created by `user.kaelen`, with no lock property, is refused for `agent.claude` and the message names `user.kaelen`
    - D-13: a node of type `obsidian.vault/note@1` carrying `md.path` text `Characters/Rune.md` (written by a non-human actor, so open by default) is placed successfully; its `md.path` is byte-equal afterwards, the node count is unchanged, and the commit block contains no `md.` line
    - rewound tree: after `undo()`, a self-placement is refused, `isRewound` stays true and `onRedoDiscarded` is not called; a valid placement on a rewound tree reconciles, announces the discarded redo once, and commits
    - D-05: after a follower placement, a person's `setProperty` on the parent adds exactly one commit and leaves the follower's stored props unchanged
    - D-14: no refusal sentence contains a digit once node ids are removed
  </behavior>
  <action>
    Write the `<behavior>` tests in `spatial.test.ts`. Extend the fingerprint helper as listed (count `@commit` records with a multiline match on the file text) and give `SpatialCommands` a hooks spy for `onCommitted` and `onRedoDiscarded`. Set lock properties the way Task 1 recorded (2.4's own command if it provides one, otherwise a raw `setProperty` of `lock.layout` / `lock.layout.allow` as `text`, signed by the owner). For the blocked column, seed one note right of the anchor whose stored height covers all `MAX_PLACE_STEPS` steps.

    If a test exposes a real defect in `place`, fix it in `spatial.ts` without changing the check order Task 2 set, and note the fix in the SUMMARY. Do not change the lock model; a lock-semantics failure means Task 1's answer was wrong — stop and report it.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/spatial.test.ts</automated>
    <fails_when>non-zero exit, a `failed` count on the `Tests` line, or `No test files found`</fails_when>
    <automated>npm --prefix app run test</automated>
    <fails_when>non-zero exit, or a `failed` count on the `Test Files` line</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line</fails_when>
  </verify>
  <acceptance_criteria>
    - `spatial.test.ts` has tests whose names mention `locked`, `allow`, `open`, `user.kaelen`, `md.path`, `rewound`, `pinned by a person` and `writes nothing`
    - the takeover-refusal test asserts the exact sentence and the unchanged widened fingerprint, and the fixed-placement test asserts `pinned` is still bool true after reopening
    - every refusal test asserts the widened fingerprint and the `onCommitted` count
    - the full suite and typecheck pass
  </acceptance_criteria>
  <done>Every way `place` can be refused is proven to leave the world, the file and the person's redo untouched, and the lock refusals name who holds the lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent client -> shim -> socket -> main | `place` arguments are untrusted; the actor comes from the socket token |
| main command layer -> kernel | The only place a placement becomes a commit; the lock check sits here |
| Phase 2.4 lock module -> `place` | An API this plan did not write, confirmed at Task 1 |
| vault tree -> `.md` files | A Tapestry-side position must never become a file move |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-04-01 | Tampering | `SpatialCommands.place` on a person's note | high | mitigate | Phase 2.4's layout lock is checked after reconciling and before resolving; tests cover an explicit owner, `.allow`, `open` and the human-author default (SC4, #8) |
| T-02.5-04-02 | Elevation of privilege | Reuse of the superseded D-05 authorship gate | high | mitigate | The D-15 grep gate forbids the three helper names in `spatial.ts` |
| T-02.5-04-03 | Spoofing | `PlaceArgs` | high | mitigate | Strict schema with no actor field, re-validated in main; tests refuse an `actor` key; the shim test's actor-key regex covers the new tool |
| T-02.5-04-04 | Information disclosure | `place` refusals and success value | medium | mitigate | Sentences name ids, the tree and the lock owner only; the success value has tree, note, seq and follows; tests strip ids and assert no digit remains |
| T-02.5-04-05 | Repudiation | A refused call discarding a person's redo | medium | mitigate | Checks that need no world run before `prepareWriteFor`; the rewound-tree test asserts `isRewound` and the `onRedoDiscarded` spy |
| T-02.5-04-06 | Tampering | `placeOps` and `place` against a person's takeover (`pinned` true), and invalid ops reaching the journal | high | mitigate | `placeOps` never writes or unsets `pinned` on a note `heldByPerson` reports, and unsets it only when its value is exactly the bool false (which also keeps the kernel from refusing an absent key); `place` refuses near-own-parent on a held note before resolving (D-03, D-17); Task 3 proves `pinned` true survives a fixed placement and reopen, and that the refusal writes nothing; a finite check runs before submit; the op gate allows only `setProperty`/`unsetProperty` |
| T-02.5-04-07 | Tampering | Vault note placement moving a `.md` file (02.2 D-26) | high | mitigate | `placeOps` is a closed list of three keys; `place` never touches `md.*` or the vault service; the D-13 test asserts `md.path` is byte-equal and no `md.` line was written |
| T-02.5-04-08 | Tampering | Acting on a guessed 2.4 API | high | mitigate | Task 1 is a `blocking-human` checkpoint with a precondition; the executor never merges and stops when 2.4 is absent |
| T-02.5-04-09 | Denial of service | Crowded trees during `place` | low | accept | Resolution is bounded by `MAX_PLACE_STEPS`; a full column is a refusal, not a loop |

No package-manager install occurs in this plan, so no package-legitimacy gate applies.
</threat_model>

## Artifacts this phase produces

Created or changed by this plan:

| Kind | Name | Location |
|---|---|---|
| Zod schema | `WhereArgs` (union of a strict `{ near }` and a strict `{ beyond, from }`) | `app/src/main/mcp/schemas.ts` |
| Zod schema | `PlaceArgs` | `schemas.ts` |
| MCP tool | `place` (tool count becomes ten as planned) | `TOOL_DEFINITIONS` in `schemas.ts` |
| Dispatch case | `case 'place'` | `app/src/main/commands/agent-tools.ts` |
| Method | `SpatialCommands.place(actor, args)` | `app/src/main/commands/spatial.ts` |
| Type | `PlaceResult` | `spatial.ts` |
| Function | `placeOps(note, spot)` | `spatial.ts` |
| Function | `placeMessage(noteId, where)` | `spatial.ts` |
| Function | `heldByPerson(note)` — true when `pinned` exists and is not the bool false | `spatial.ts` |
| Refusal sentence | `<note> was pinned by a person; it will not follow <parent> again` | `SpatialCommands.place` |
| Private method (only if Task 1 = `adapt`) | layout-lock adapter returning a refusal sentence or null | `spatial.ts` |
| Commit shape | `Place note <n> near <n>` / `Place note <n> beyond <n> from <n>`, actor `plugin agent.<name>` | written by `place` |
| Written keys | `position.x`, `position.y`, `pinned` (bool false, never over a person's `pinned` true), and `unset … pinned` only when the value was the bool false | existing keys; no new key, type or label |

<verification>
- `npm --prefix app run test`, `npm --prefix app run typecheck` and `npm --prefix app run build:js` all exit 0
- The op gate, the D-15 gate and the SC6 gate in Task 2 print nothing
- `S="$(git status --porcelain -- tapestry app/native)" && L="$(git log --format= --name-only --grep='(02\.5-0')" && test -z "$S" && ! printf '%s\n' "$L" | grep -E '^(tapestry|app/native)/'` exits 0 (SC5 — this plan's commits touch no kernel or addon file, whatever the 2.4 merge brought)
- `test "$(grep -rn 'DEFAULT_NOTE_WIDTH = ' app/src | wc -l)" -eq 1` still holds
</verification>

<success_criteria>
SC2 (for `place`), SC3's write half, SC4 and SC5 hold after Phase 2.4 is merged: `place` takes ids and a relation, resolves the spot in the host, writes only position and `pinned`, never removes or overwrites a person's `pinned true` (D-17), refuses locked placements naming the owner, cannot move a user-made note by default, and every refusal is proven to write nothing. If Phase 2.4 is not merged, the plan stops at Task 1 with nothing written.
</success_criteria>

<output>
Create `.planning/phases/02.5-agent-spatial-verbs/02.5-04-SUMMARY.md` when done (or when stopped at Task 1), including the Task 1 inventory, the decision, `PLACE_BASE` and the post-merge baseline.
</output>
