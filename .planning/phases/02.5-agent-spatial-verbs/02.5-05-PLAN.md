---
phase: 02.5-agent-spatial-verbs
plan: 05
type: execute
wave: 5
# GATED ON PHASE 2.4 through Plan 04 (D-15, D-19): runs only after Plan 04 passed its
# blocking checkpoint and shipped `place`. Task 1's precondition stops the plan otherwise.
depends_on: ["02.5-04"]
gated_on: "Plan 02.5-04 complete (Phase 2.4 merged and `place` shipped)"
files_modified:
  - app/src/main/mcp/schemas.ts
  - app/src/main/commands/agent-tools.ts
  - app/src/main/commands/notes.ts
  - app/src/main/commands/notes.test.ts
  - app/src/main/mcp/shim.test.ts
autonomous: true
requirements: [PROV-03, TREE-01, PLUG-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC2 / D-02: create_note without where behaves exactly as today — the note lands CHILD_GAP right of its parent at the parent's top (x 390, y 20 in the existing test), with no pinned key, no collision step, the same ops and the same commit message"
    - "SC2: create_note accepts the same optional where as place — { near } or { beyond, from }, ids only, strict — and resolves it with the same resolveWhere"
    - "D-01: create_note with where near its own grewFrom writes pinned bool false and the spot it will be drawn at; any other where writes a fixed position and no pinned key"
    - "D-06: a note created near its parent is drawn exactly at its stored spot, stepping down past notes already there"
    - "SC4 / D-12: a where refusal (unknown or unplaced anchor, same note, no clear spot, no line, an anchor live only in another tree, or naming the id the new note would get) returns { ok: false, error } and commits nothing"
    - "PROV-03 / TREE-01: a note created with where is still one commit holding the node and its grew-from edge, signed by the agent, and the message says where it was placed"
    - "PLUG-03: an agent reaches the same behaviour through the real shim, and the tool count does not change"
    - "Phase gate: the full suite, typecheck and build pass; no kernel or addon file changed; no SC6 vocabulary, no superseded authorship gate and one declaration of each spacing constant"
  artifacts:
    - path: "app/src/main/commands/notes.ts"
      provides: "createFrom with an optional where"
      contains: "resolveWhere("
    - path: "app/src/main/mcp/schemas.ts"
      provides: "CreateNoteArgs.where"
      contains: "where: WhereArgs.optional()"
    - path: "app/src/main/commands/notes.test.ts"
      provides: "No-where regression and where cases"
  key_links:
    - from: "app/src/main/commands/notes.ts"
      to: "app/src/renderer/layout/placement.ts"
      via: "resolveWhere and whereRefusalText for the where branch"
      pattern: "resolveWhere\\("
    - from: "app/src/main/commands/agent-tools.ts"
      to: "app/src/main/commands/notes.ts"
      via: "case 'create_note' passes where through"
      pattern: "where\\?: WherePlacement"
---

<!-- planner-discipline-allow: facing -->
<!-- planner-discipline-allow: direction -->
<!-- planner-discipline-allow: on: -->
<!-- planner-discipline-allow: assertOwnNote -->
<!-- planner-discipline-allow: writeToOwnNote -->
<!-- planner-discipline-allow: isAgentActor -->

<objective>
**GATED ON PHASE 2.4, through Plan 04.** Run this plan only after Plan 04 passed its blocking checkpoint and shipped `place`. Task 1's precondition stops the plan otherwise.

**As an** agent, **I want to** say where a new note should go as I create it, **so that** I do not need a second call to move it — while every `create_note` call that does not say so keeps working exactly as it does today.

Task 1 adds the optional `where` beside the existing path in `createFrom` (the orchestrator's resolved assumption: add alongside, never replace). Task 2 proves the path through the real shim and runs the phase gate.

Output: `CreateNoteArgs.where`, `createFrom` with a `where` branch, its tests, and an end-to-end MCP test.
</objective>

## Working rules for this plan

- Work only in `/Users/kaelencook/Tapestry-trees/agent-trees` on branch `ws/agent-trees`; run every command from there; do not create a worktree or switch branches; never merge a branch.
- Stage files by name only (never `-A`, `.` or a directory), never stage `app/native/build/` or `app/out/`, and check `git show --stat HEAD` after each commit.
- Do not install packages, edit tsconfig files, `.gitignore`, `.claude/CLAUDE.md` or `.planning/config.json`.
- `notes.ts` is shared with Phase 2.4: keep the edit to `createFrom` and its imports. Do not touch the update, rename or delete paths or their helpers.
- Add no lock check of your own. If Plan 04's Task 1 recorded that 2.4 gated `createFrom`, the `where` branch passes through that same gate; otherwise the note being positioned is the one this commit creates, which is open by default.
- If any step seems to need a new `.tree` key, type string or edge label, stop and report it to Kaelen as a one-way door; this phase writes only `position.x`, `position.y` and `pinned`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-agent-spatial-verbs/02.5-CONTEXT.md
@.planning/phases/02.5-agent-spatial-verbs/02.5-04-SUMMARY.md

Read RESEARCH.md by section only: Q1 (`createFrom`'s exact shape and check order), Open Question 5, and the Anti-Patterns list (the no-`where` path must not change).

<interfaces>
`createFrom` today (`app/src/main/commands/notes.ts:244-336`; re-read it — Phase 2.4 may have changed it):

```text
createFrom(actor, { tree, grewFrom, title, text }) -> { tree, note, edge, seq }
  resolveRef(tree) -> validateTitle -> validateText -> grewFrom id shape
  try: prepareWrite -> parent live -> x = parentX + parentWidth + CHILD_GAP, y = parentY
       next = bridge.getNextIds()                 // { node, edge }
       ops = [createNode(NATIVE_NOTE_TYPE, { position.x, position.y, title, body }),
              createEdge(next.node -> grewFrom, GREW_FROM_LABEL)]
       submitAs(actor, `grow note "<title>" from <grewFrom>`, ops) -> id check -> onCommitted
```

From Plans 02 and 04:

```typescript
// app/src/renderer/layout/placement.ts
export type WherePlacement = { near: string } | { beyond: string; from: string }
export function resolveWhere(world: { nodes; edges }, subject: PlacementNode, where: WherePlacement): WhereOutcome
export function whereRefusalText(refusal: WhereRefusal, subjectId: string, treeName: string): string
// app/src/main/mcp/schemas.ts
export const WhereArgs   // union of a strict { near } and a strict { beyond, from }, declared above CreateNoteArgs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `create_note` takes an optional `where`, and without it nothing changes</name>
  <precondition>Plan 04 is complete: `grep -n "name: 'place'" app/src/main/mcp/schemas.ts` prints a line and `02.5-04-SUMMARY.md` records Task 1's decision as `as-assumed` or `adapt`</precondition>
  <files>app/src/main/mcp/schemas.ts, app/src/main/commands/agent-tools.ts, app/src/main/commands/notes.ts, app/src/main/commands/notes.test.ts</files>
  <read_first>
    - `app/src/main/commands/notes.ts` — lines 1-80 and the whole of `createFrom`, as they stand after the 2.4 merge
    - `app/src/main/commands/notes.test.ts` — the whole file (the x 390 / y 20 test must stay green, unedited)
    - `app/src/main/mcp/schemas.ts` — `WhereArgs` and `CreateNoteArgs`
    - `app/src/main/commands/agent-tools.ts` — the `create_note` case
    - `app/src/renderer/layout/placement.ts` — `resolveWhere`, `whereRefusalText`, `WherePlacement`
    - `.planning/phases/02.5-agent-spatial-verbs/02.5-04-SUMMARY.md` — whether 2.4 changed `createFrom`
  </read_first>
  <behavior>
    - without `where`: the existing x 390 / y 20 test passes unchanged; the new note has no `pinned` prop; the last commit block contains no `pinned` line and no `placed` text, holds exactly four `set n2 ` lines (position.x, position.y, body, title) plus the `create-node` and `create-edge` lines, and the returned value has exactly the keys tree, note, edge and seq
    - without `where`, a note already sitting at the parent's right is still overlapped exactly as today (no collision step)
    - `where: { near: 'n1' }` with `grewFrom: 'n1'` writes `pinned` bool false at 390,20 when clear, the commit message ends `, placed near n1`, and the returned value adds `follows: true`
    - with a note already at 390,20, the same call lands at 390,164 (height 120 plus gutter 24), and `displayPositions` over the world draws the new note exactly at its stored spot
    - `where: { near: <another note> }` and `where: { beyond, from }` write a fixed position with no `pinned` key and `follows: false`
    - refusals that write nothing (file size, node and edge counts, `@commit` count unchanged): `near n99`, `from n98`, `beyond n1 from n1`, near an unplaced note, a fully blocked column, two anchors with the same centre, an anchor id live only in a second open tree (D-12), and near the id the new note would receive (`near n2 is not a live note in notes`)
    - `CreateNoteArgs` accepts `where` absent, `{ near }` and `{ beyond, from }`, and refuses the space form, the orientation form, a lone `beyond`, `near` together with `beyond`, and an extra key inside `where`
  </behavior>
  <action>
    Write the `<behavior>` tests in `notes.test.ts` first; leave the existing tests as they are.

    In `app/src/main/mcp/schemas.ts`, add `where: WhereArgs.optional()` to `CreateNoteArgs`, update its doc comment (the relation is resolved by the host; without it the note goes to the right of `grewFrom` as before), and append to the `create_note` description: ` Pass where to choose the spot: near a note, or beyond one note as seen from another. A note placed near the note it grew from follows that note until a person moves it. Without where, the note goes to the right of grewFrom.` No new import.

    In `app/src/main/commands/agent-tools.ts`, add `where?: WherePlacement` to the `create_note` cast (the type import exists from Plan 04).

    In `app/src/main/commands/notes.ts`, import `resolveWhere`, `whereRefusalText` and the `WherePlacement` type from `'../../renderer/layout/placement'` alongside the existing constant import, and give `createFrom`'s argument type an optional `where?: WherePlacement`. Keep every existing check and its order. Add, right after the `grewFrom` id-shape check and before reconciling: when `where` is given, each anchor id must match the node-id pattern, else `<role> <id> is not a live note in <tree>`, and beyond and from must differ, else the `same-note` sentence from `whereRefusalText`. Inside the try, after the parent liveness check: leave the no-`where` computation of position, ops and message exactly as it is. When `where` is given, take `bridge.getNextIds()`, build the subject as a node with id `next.node`, type `NATIVE_NOTE_TYPE` and no props, and call `resolveWhere` with the current nodes and the current edges plus a `grew-from` edge with id `next.edge` from `next.node` to `grewFrom`; a refusal returns `whereRefusalText(refusal, next.node, tree.name)` with nothing written; otherwise use the resolved x and y for the two position props, add `pinned` with type `bool` and value `false` to the `createNode` props when `follows` is true, and append `, placed near <id>` or `, placed beyond <id> from <id>` to the commit message. The node and the edge stay in one commit, and the existing predicted-id check still runs. Return `follows` in the value only when `where` was given.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/notes.test.ts src/main/commands/notes-policy.test.ts</automated>
    <fails_when>non-zero exit, a `failed` count on the `Test Files` line, or `No test files found`</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line</fails_when>
    <automated>test "$(grep -E '^import' app/src/main/mcp/schemas.ts)" = "import * as z from 'zod'" && grep -q 'where: WhereArgs.optional()' app/src/main/mcp/schemas.ts && grep -q 'resolveWhere(' app/src/main/commands/notes.ts</automated>
    <fails_when>non-zero exit — schemas.ts gained an import, or the where field or resolver call is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `schemas.ts` contains `where: WhereArgs.optional()` inside `CreateNoteArgs` and the sentence `Without where, the note goes to the right of grewFrom.`
    - `agent-tools.ts` contains `where?: WherePlacement`
    - `notes.ts` contains `resolveWhere(`, `whereRefusalText(` and `placed near`
    - `notes.test.ts` still contains the unedited assertion `toBe(390)` and gains tests whose names mention `without where` and `follows`
    - `notes.test.ts`, `notes-policy.test.ts` and typecheck pass
  </acceptance_criteria>
  <done>An agent can create a note exactly where it wants it, following its parent if it asks to, and every call that does not ask gets today's behaviour byte for byte.</done>
</task>

<task type="auto">
  <name>Task 2: The agent does it over MCP, and the phase gate holds</name>
  <files>app/src/main/mcp/shim.test.ts</files>
  <read_first>
    - `app/src/main/mcp/shim.test.ts` — as left by Plan 04 (request ids 1-8 are taken)
    - `.planning/phases/02.5-agent-spatial-verbs/02.5-VALIDATION.md` — the grep gates and manual checks
  </read_first>
  <action>
    In `shim.test.ts`, add a test after the `place` tests, using request id 9: call `create_note` with `{ tree: 'agents', grewFrom: 'n1', title: 'Near', text: 'placed by where', where: { near: 'n1' } }`; expect no protocol error and no `isError`; expect the file to contain `create-node n3`, `set n3 pinned bool false`, `set n3 position.x real 360` and `set n3 position.y real 144` (n1 is at 0,0 with the default size and n2 already sits at 360,0, so the new note steps down once), and the text `placed near n1`. Leave the tool-count test alone: `where` adds no tool.

    Then run the phase gate below and record each result in the SUMMARY. If any gate fails because of this phase's code, fix it in the file that caused it and say so; if it fails because of the 2.4 merge, stop and report.
  </action>
  <verify>
    <automated>npm --prefix app run build:js && npm --prefix app run test -- src/main/mcp/shim.test.ts</automated>
    <fails_when>non-zero exit, or a `failed` count on the `Tests` line</fails_when>
    <automated>npm --prefix app run test && npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, a `failed` count on the `Test Files` line, or any `error TS` line</fails_when>
    <automated>! grep -nE 'facing|direction|(^|[{,])[[:space:]]*on[[:space:]]*:' app/src/main/mcp/schemas.ts app/src/main/commands/spatial.ts app/src/renderer/layout/placement.ts</automated>
    <fails_when>any line printed (exit status inverted to non-zero) — SC6 / D-11 vocabulary reached a gated file</fails_when>
    <automated>! grep -nE 'assertOwnNote|writeToOwnNote|isAgentActor' app/src/main/commands/spatial.ts</automated>
    <fails_when>any line printed (exit status inverted to non-zero) — the superseded authorship gate reached `place` (D-15)</fails_when>
    <automated>test "$(grep -rn 'DEFAULT_NOTE_WIDTH = ' app/src | wc -l)" -eq 1 && grep -q 'taller' app/src/main/mcp/schemas.ts</automated>
    <fails_when>non-zero exit — a size default is declared twice, or a tool description lost the D-07 overlap caveat</fails_when>
    <automated>S="$(git status --porcelain -- tapestry app/native)" && L="$(git log --format= --name-only --grep='(02\.5-0')" && test -z "$S" && ! printf '%s\n' "$L" | grep -E '^(tapestry|app/native)/'</automated>
    <fails_when>non-zero exit — this phase changed a kernel or addon file (SC5)</fails_when>
    <human-check>
      <test>Run `npm --prefix app run dev` with an agent connected. Ask the agent to create a note grown from one of your notes with `where: { near: <that note> }`, then to `place` another of its notes beyond it. Drag your note around; then drag the agent's following note; then quit and relaunch. Finally, type a long text into a note so its card grows taller, and ask the agent to `look` from a neighbour.</test>
      <expected>The created note appears beside your note and follows it; the placed note stays put; after your drag the follower stays where you dropped it and the `.tree` file shows `pinned bool true` under your human actor; relaunch reproduces every position; `look` may report the tall card clear of a neighbour it visibly overlaps, which is the documented D-07 caveat, not a bug. Asking the agent to `place` one of your own notes is refused with a message naming you.</expected>
      <why_human>The full agent-plus-person loop, relaunch and rendered card heights need a running window.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `shim.test.ts` contains `set n3 pinned bool false` and a `create_note` call carrying `where`
    - every automated gate in `<verify>` passes and the SUMMARY lists their results
  </acceptance_criteria>
  <done>An agent creates a following note through the real bridge, and the whole phase passes its gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent client -> shim -> socket -> main | `create_note` arguments, now with `where`, are untrusted and re-validated in main |
| main command layer -> kernel | The node, its edge and its resolved position are one commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-05-01 | Tampering | `createFrom` without `where` | high | mitigate | The no-`where` computation is left untouched; tests pin the spot, the absence of `pinned`, the exact `set` lines and the return keys |
| T-02.5-05-02 | Spoofing | `CreateNoteArgs.where` | high | mitigate | `where` is a union of two strict objects inside the strict `CreateNoteArgs`; an `actor` key anywhere is still refused, and the shim's actor-key regex still covers the tool |
| T-02.5-05-03 | Tampering | Positioning relative to another tree's note | medium | mitigate | `resolveWhere` looks anchors up only in the named tree's nodes; the two-tree test proves the refusal writes nothing (D-12) |
| T-02.5-05-04 | Information disclosure | `where` refusals and the return value | medium | mitigate | Sentences come from `whereRefusalText` (ids and tree name only); the return value adds only `follows` |
| T-02.5-05-05 | Tampering | A where refusal leaving a half-written note | medium | mitigate | Resolution happens before `submitAs`; a refusal returns before any op is built; tests check file size and the `@commit` count |
| T-02.5-05-06 | Elevation of privilege | Bypassing a 2.4 gate on creation | low | mitigate | The plan adds no gate of its own and routes the `where` branch through whatever gate 2.4 put on `createFrom`, as recorded in Plan 04's SUMMARY |

No package-manager install occurs in this plan, so no package-legitimacy gate applies.
</threat_model>

## Artifacts this phase produces

Created or changed by this plan:

| Kind | Name | Location |
|---|---|---|
| Schema field | `CreateNoteArgs.where` (`WhereArgs.optional()`) | `app/src/main/mcp/schemas.ts` |
| Description text | `create_note` gains the `where` sentences | `schemas.ts` |
| Cast | `create_note` args gain `where?: WherePlacement` | `app/src/main/commands/agent-tools.ts` |
| Method change | `NoteCommands.createFrom` accepts an optional `where`; its value adds `follows` only when `where` is given | `app/src/main/commands/notes.ts` |
| Commit shape | `grow note "<title>" from <id>, placed near <id>` / `…, placed beyond <id> from <id>` | written by `createFrom` with `where` |
| Written keys | `position.x`, `position.y`, and `pinned` (bool false) only for a note placed near its own parent | existing keys |
| Tests | no-`where` regression, `where` cases, schema cases, MCP end-to-end | `notes.test.ts`, `shim.test.ts` |

<verification>
- `npm --prefix app run test`, `npm --prefix app run typecheck` and `npm --prefix app run build:js` exit 0
- All Task 2 gates pass (SC5, SC6, D-07, D-15)
- End of phase: the `<human-check>` blocks from Plans 03 and 05 are harvested into UAT
</verification>

<success_criteria>
SC2 is complete: `create_note` accepts the same optional `where` as `place` and, without it, behaves exactly as today. Together with Plans 01-04, every roadmap criterion for Phase 2.5 holds and is gated by an automated check or a harvested human check.
</success_criteria>

<output>
Create `.planning/phases/02.5-agent-spatial-verbs/02.5-05-SUMMARY.md` when done, including the phase gate results.
</output>
