---
phase: "2.4"
slug: "lock-model"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-16"
---

# Phase 2.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `02.4-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`vitest run`), node environment, `pool: 'forks'` |
| **Config file** | `app/vitest.config.ts` |
| **Quick run command** | `npm --prefix app run test -- src/main/commands/` |
| **Full suite command** | `npm --prefix app run test && npm --prefix app run typecheck` |
| **Estimated runtime** | ~20 seconds |

**Baseline, measured by the orchestrator on 2026-09-16 after `npm --prefix app run build:native`:** `Test Files 1 failed | 11 passed (12)`, `Tests 136 passed | 5 skipped (141)`. The single failure, `src/main/mcp/shim.test.ts`, happens because `app/out/main/mcp.js` is missing: the app bundle isn't built in this worktree. It predates this phase. Plan 02 Task 2 runs `build:js` before re-running that file. `typecheck`: exit 0.

---

## Sampling Rate

- **After every task commit:** Run `npm --prefix app run test -- src/main/commands/`
- **After every plan wave:** Run `npm --prefix app run test && npm --prefix app run typecheck`
- **Before `/gsd-verify-work`:** The full suite must be green, except the known `shim.test.ts` build dependency until `build:js` has run, and the count must be at least the baseline plus the new tests.
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.4-01-01 | 01 | 1 | D-01, D-04, D-07, D-08, D-11, D-12 (tracer: text on update) | T-02.4-01..04 | An agent's update of a non-agent note is refused, nothing is written, and malformed locks fail closed | unit + integration | `npm --prefix app run test -- src/main/commands/locks.test.ts src/main/commands/notes-policy.test.ts` | ❌ W0 (`locks.test.ts`) | ⬜ pending |
| 02.4-01-02 | 01 | 1 | D-02, D-03, D-05, D-06, D-12, D-13 | T-02.4-04 | Rename is checked against text and delete against delete; the old gate is removed; agent-to-agent writes follow the constant | integration | `npm --prefix app run test -- src/main/commands/` and `npm --prefix app run typecheck` | ✅ | ⬜ pending |
| 02.4-02-01 | 02 | 2 | D-09, D-10, D-04 (vault/plugin creators), D-11 | T-02.4-10, -12, -13, -16 | Allow-list entries must match exactly; locks survive undo; non-agent creators are locked by default | unit + integration | `npm --prefix app run test -- src/main/commands/locks.test.ts src/main/commands/notes-policy.test.ts` | ✅ (created in 01) | ⬜ pending |
| 02.4-02-02 | 02 | 2 | D-01 (the rule as agents are told it), prohibitions | T-02.4-11, -15, -SC | No production code names `lock.*` keys, no dependency changes, the MCP tool surface is unchanged | static + full suite | `npm --prefix app run build:js && npm --prefix app run test && npm --prefix app run typecheck` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Native addon built: `npm --prefix app run build:native` (the orchestrator ran it; never `npm install`)
- [ ] `app/src/main/commands/locks.test.ts`: explicit locks, `open`, aspect independence, non-agent default, fail-closed. Must be red first (Plan 01 Task 1)
- [ ] Update `notes-policy.test.ts` expectations for the new messages and add branches on the two constants. Must be red first (Plan 01)

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
