---
phase: 02.4-lock-model
plan: 02
type: execute
wave: 2
depends_on:
  - 02.4-01
files_modified:
  - app/src/main/commands/locks.ts
  - app/src/main/commands/locks.test.ts
  - app/src/main/mcp/schemas.ts
  - app/src/main/commands/agent-tools.ts
  - app/src/main/commands/connections.ts
autonomous: true
requirements: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-3 (allow list): `lock.<aspect>.allow` is a text property holding whitespace-separated actor ids, and each listed agent may write despite the lock. This holds for an explicit lock and for a default lock. `lock.text` = `agent.claude` with `lock.text.allow` = `agent.chatgpt` admits agent.chatgpt and refuses agent.gemini with `<note> text is locked by agent.claude`. The value `  agent.gemini<TAB>agent.chatgpt <LF>` admits both (02.4 D-09, D-10, orchestrator resolution 2)"
    - "Allow-list matching is exact and fails closed. `agent.chatgpt` on the list does not admit agent.chat. A non-text `.allow` value admits nobody (D-09)"
    - "SC-1 (plugin and bridge notes): a note created by `plugin obsidian.bridge` refuses agent.claude's update with `<note> text is locked by obsidian.bridge`, and its delete with `<note> delete is locked by obsidian.bridge` while NON_AGENT_NOTES_DELETE_LOCKED is true. A note created by `plugin tapestry-notes` refuses rename with `<note> text is locked by tapestry-notes`. worldFingerprint is unchanged each time (D-04, D-05, D-11, D-12)"
    - "People and non-agent plugins are not checked: user.kaelen and `plugin tapestry-notes` may update a note carrying `lock.text` = `agent.claude`, and `plugin obsidian.bridge` may update n1 (D-10)"
    - "Growing from a locked note (createFrom) and connecting a locked note (connect) still succeed for an agent (D-01)"
    - "A note an agent deleted is then refused as `<note> is not a live note in locks`, not with a lock message (D-13)"
    - "If Kaelen has undone the commit that set `lock.text` = `agent.claude`, agent.chatgpt's update is still refused with `<note> text is locked by agent.claude`. The tree ends at its head, and worldFingerprint is unchanged (D-11)"
    - "The MCP tool titles and descriptions for update_note, rename_note and delete_note describe the lock rule instead of authorship. The tool names, count and annotations are unchanged, and shim.test.ts passes on a fresh build (D-01)"
    - "Every D-05 citation in the files this phase edits carries its phase prefix: `02.2 D-05` for the superseded authorship rule, `02.4 D-05` for the delete-default constant"
    - "Across the whole phase: no production file writes or names a `lock.text` or `lock.delete` key outside locks.ts, no dependency manifest changed, no 02.3 plan changed, and every test file that passed at the Plan 01 baseline still passes (D-08)"
  prohibitions:
    - requirement_id: D-08
      category: safety
      status: resolved
      verification: judgment
      resolution: "The phase gate in Task 2 checks two things: no file under app/src or plugins, other than locks.ts and test files, names a `lock.text` or `lock.delete` key, and locks.ts has no submit or setProperty call. TOOL_DEFINITIONS keeps exactly 8 tools, each with a .strict() schema and no new field"
      reason: "Decision Register #19 (the lock.* naming) is an unanswered one-way door"
      statement: "Production code must not write lock.* properties, and no tool, command or schema field may let an agent set one"
    - requirement_id: D-11
      category: safety
      status: resolved
      verification: judgment
      resolution: "Every refusal case in locks.test.ts asserts that worldFingerprint() is unchanged, including the case where a lock was undone"
      reason: "A refusal with a side effect would be the change the lock exists to prevent"
      statement: "A refused command must not append anything to the .tree file"
    - requirement_id: ENV
      category: safety
      status: resolved
      verification: judgment
      resolution: "`git diff --name-only 72ed27e..HEAD -- package.json package-lock.json app/package.json .planning/phases/02.3-time-threads .claude/CLAUDE.md .planning/config.json .gitignore` prints nothing"
      reason: "node_modules is a shared symlink, and 02.3 D-22 is recorded, not realigned"
      statement: "The executor must not run npm install, change dependency manifests, edit 02.3 plans, or edit CLAUDE.md, config.json or .gitignore"
  artifacts:
    - path: app/src/main/commands/locks.ts
      provides: "resolveLock fills LockState.allow from lock.<aspect>.allow for explicit and default locks"
      contains: "allowKey(aspect)"
    - path: app/src/main/commands/locks.test.ts
      provides: "Allow-list, creator-kind, non-agent-actor, deleted-note, regression and undone-lock coverage"
      min_lines: 250
    - path: app/src/main/mcp/schemas.ts
      provides: "Agent-facing statement of the lock rule in the update, rename and delete tool descriptions"
      contains: "locked"
  key_links:
    - from: app/src/main/commands/locks.ts
      to: app/src/main/commands/notes.ts
      via: "checkLock admits actor.id when it is in state.allow. notes.ts assertMayWrite passes node.props, so the .allow property is read from the head world"
      pattern: "allow\\.includes\\("
    - from: app/src/main/mcp/schemas.ts
      to: app/src/main/mcp/shim.test.ts
      via: "TOOL_DEFINITIONS is served by the built out/main/mcp.js. shim.test.ts asserts the 8 names and the annotations"
      pattern: "TOOL_DEFINITIONS"
---

<objective>
This plan finishes the phase:
- `lock.<aspect>.allow` (D-09) is read, test-first.
- The creator-kind default is proven end to end for Obsidian-bridge and plugin notes (D-04), which completes SC-1.
- The ungated paths (D-01), the not-live refusal (D-13) and the undone-lock case (D-11) get pinned.
- The rule is stated to agents in the MCP tool text, and every doc comment that still presents 02.2 D-05 as the rule is reworded.
- The phase gate runs: the full suite against the Plan 01 baseline, typecheck, the shim build and the prohibition checks.

Purpose: this completes ROADMAP Phase 2.4 SC-1 for bridge and plugin notes and SC-3 for the `.allow` list. It also leaves the agent-facing surface consistent with the rule the gate enforces.

Output:
- `locks.ts`, which now reads the allow list
- `locks.test.ts`, with expanded coverage
- `schemas.ts`, `agent-tools.ts` and `connections.ts`, with wording and comment changes only

Decision ids: a bare `D-NN` means `02.4-CONTEXT.md`, and `02.2 D-NN` means the Obsidian-bridge phase. Every D-05 you write into code must carry its phase prefix.

Recorded assumptions: A1 to A5 from Plan 01 apply. For A2, `.allow` is honoured whether the lock is explicit or derived.

Test status: some cases in Task 1 pin behaviour Plan 01 already implements. They are expected to pass on first run, and the SUMMARY lists them as characterization tests. If one fails, Plan 01 has regressed; fix `locks.ts` or report it, and do not weaken the test. The allow-list cases are the red-first part of Task 1.
</objective>

<environment_rules>
These rules bind every task in this plan:
- Work only in `/Users/kaelencook/Tapestry-trees/notifications-rank`, on branch `ws/notifications-rank`. Never touch `~/Tapestry` or any other worktree.
- NEVER run `npm install`, `npm ci` or anything that changes `package.json`, `package-lock.json` or `node_modules`, which is a shared symlink. The only build commands you may run are `npm --prefix app run build:native`, and only if the addon is missing, and `npm --prefix app run build:js`, which writes only the gitignored `app/out/`.
- Never edit `.claude/CLAUDE.md`, `.planning/config.json`, `.gitignore`, or anything under `.planning/phases/02.3-time-threads/`.
- Stage by explicit path only. Never use `git add -A`, `git add .` or `git commit -a`. Run `git show --stat HEAD` after each commit.
- Never use bare `git stash`.
- Add no command, tool, zod schema field or production code that writes `lock.*`. Test fixtures write `lock.*` only through a raw `tree.bridge.submitAs`.
- Outside `locks.ts` and `*.test.ts` files, do not quote the full text or delete lock key names in code or comments. A phase gate greps for them as potential writers. Say "the note's text lock" or "the delete lock", and point to `locks.ts`.
- In `schemas.ts`, change only the doc comments and the `title` and `description` strings of tool entries. Do not change any zod schema, tool `name`, `schema` reference or `annotations` value. `shim.test.ts` asserts the annotations.
- Do not edit `app/src/main/commands/notes.ts`, `notes-policy.test.ts`, `kernel-bridge.ts` or anything under `app/src/renderer/`. The authorship-display comments in `kernel-bridge.ts` and `NoteCard.tsx` stay as they are, because they describe authorship, which is still derived.
</environment_rules>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.4-lock-model/02.4-CONTEXT.md
@.planning/phases/02.4-lock-model/02.4-RESEARCH.md
@.planning/phases/02.4-lock-model/02.4-01-SUMMARY.md
@app/src/main/commands/locks.ts
@app/src/main/commands/locks.test.ts
@app/src/main/mcp/schemas.ts

<interfaces>
From Plan 01. Read `locks.ts` for the exact text.
- `resolveLock(props, createdBy, aspect, policy = DEFAULT_LOCK_POLICY): LockState` currently always returns `allow: []`.
- `checkLock(noteId, props, createdBy, actor, aspect, policy?)` returns null for non-agents. It also returns null when the aspect is open, the owner equals `actor.id`, or `state.allow.includes(actor.id)`. Otherwise it returns `<note> <aspect> is locked by <owner or (unknown)>`.
- `allowKey(aspect)` returns `lock.<aspect>.allow`.
- `locks.test.ts` already has the e2e harness: tree `locks`, n1 created by user.kaelen, Claude's note grown by agent.claude, `setLockProp(noteId, key, value, by = KAELEN, type = 'text')`, `worldFingerprint`, `lastCommitBlock`, `bodyTextOf`.
- `actor.ts` exports `OBSIDIAN_BRIDGE_ACTOR` (`plugin obsidian.bridge`), `pluginActor(id)`, `agentActor(name)` and `humanActor(name)`.
- `ConnectionCommands` (`./connections`) has `connect(actor, { from: { tree, note }, to: { tree, note }, label? })`.
- `NoteCommands.createFrom(actor, { tree, grewFrom, title, text })` returns `{ ok, value: { note, edge, seq } }`.
- `schemas.ts` `TOOL_DEFINITIONS`:
  - Entries for `update_note` (lines 162-168), `rename_note` (170-176) and `delete_note` (178-184) carry titles and descriptions that state the superseded authorship rule.
  - The doc comments at lines 56, 64, 73, 82, 91 and 113 cite D-05 without a phase prefix.
  - `shim.test.ts` asserts exactly 8 tool names: `readOnlyHint` on list_trees, search_notes and read_note, `destructiveHint` true on delete_note, `destructiveHint` false on update_note, and no tool property matching /actor/i.
- `agent-tools.ts` line 13 and `connections.ts` lines 4-10 and 78 are doc comments that cite D-05 as the ownership rule.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allow list (D-09), test-first, plus end-to-end coverage of creators, non-agent actors, deleted notes, ungated paths and undone locks</name>
  <precondition>`.planning/phases/02.4-lock-model/02.4-01-SUMMARY.md` exists and contains a "Baseline" section. `test -f app/native/build/Release/tapestry_addon.node` exits 0. `git status --porcelain -- app/src/main/commands/locks.ts app/src/main/commands/locks.test.ts` prints nothing.</precondition>
  <files>app/src/main/commands/locks.ts, app/src/main/commands/locks.test.ts</files>
  <read_first>
    - app/src/main/commands/locks.ts: the whole file (Plan 01)
    - app/src/main/commands/locks.test.ts: the whole file (Plan 01)
    - app/src/main/commands/actor.ts: `OBSIDIAN_BRIDGE_ACTOR`, `pluginActor`
    - app/src/main/commands/connections.ts: the `connect` signature (lines 70-90)
    - app/src/main/commands/notes-policy.test.ts: lines 427-489 (the rewound-tree harness: `tree.bridge.undo()`, `isRewound`)
    - .planning/phases/02.4-lock-model/02.4-CONTEXT.md: D-04, D-09, D-10, D-11, D-13
    - .planning/phases/02.4-lock-model/02.4-RESEARCH.md: "Resolution rules" rule 3, Pitfall 8, "Decision → Test Map" rows D-04, D-09, D-10 and Regression
  </read_first>
  <behavior>
    RED group: the allow list. These cases fail against Plan 01's resolver, which always returns an empty `allow`.

    Pure cases:
    - `lock.text` = `agent.claude`, `lock.text.allow` = `agent.chatgpt`: actor agent.chatgpt → null, and agent.gemini → `n1 text is locked by agent.claude`.
    - An allow value of `'  agent.gemini\tagent.chatgpt \n'` admits both agent.gemini and agent.chatgpt. `resolveLock(...).allow` deep-equals `['agent.gemini', 'agent.chatgpt']`.
    - Allow `agent.chatgpt` does not admit agent.chat.
    - An allow prop `{type:'ref', value:'agent.chatgpt'}` admits nobody, and neither does `{type:'int', value:1}`.
    - A default lock with an allow list. The creator is `{kind:'human', id:'user.kaelen'}`, there is no `lock.text`, and `lock.text.allow` = `agent.claude`: agent.claude → null, and agent.chatgpt → `n1 text is locked by user.kaelen` (orchestrator resolution 2).
    - `lock.delete.allow` affects only the delete aspect. With `lock.delete.allow` = `agent.claude` on a human-created note, the text aspect still refuses agent.claude, and delete admits it under a policy with `nonAgentNotesDeleteLocked: true`.
    - When the aspect resolves open, `allow` is irrelevant, and the resolver returns `{ locked: false }`.

    End-to-end case through NoteCommands:
    - On Claude's note, set `lock.text` = `agent.claude` and `lock.text.allow` = `agent.chatgpt`. agent.chatgpt update → ok, and the commit reads `actor plugin agent.chatgpt`. `agentActor('gemini')` update → refused with exactly `<claudeNote> text is locked by agent.claude`, and worldFingerprint is unchanged.
    - On n1, set only `lock.text.allow` = `agent.claude`. agent.claude update → ok. agent.chatgpt update → refused with exactly `n1 text is locked by user.kaelen`.

    Characterization group. These should pass on first run.
    - Obsidian bridge creator (D-04). Create a node with a raw `createNode` signed `OBSIDIAN_BRIDGE_ACTOR`: title `Rody`, body `plainTextToDocJson('From the vault')`, message `observed change to Rody.md`. Take its id from `nodeIds[0]`.
      - agent.claude update → refused with exactly `<id> text is locked by obsidian.bridge`, and worldFingerprint is unchanged.
      - agent.claude delete → if `NON_AGENT_NOTES_DELETE_LOCKED`, refused with exactly `<id> delete is locked by obsidian.bridge`, worldFingerprint unchanged; otherwise ok.
    - Plain plugin creator (D-04). A node created with a raw `createNode` signed `pluginActor('tapestry-notes')` refuses agent.claude's rename with exactly `<id> text is locked by tapestry-notes`. The title is unchanged, and so is worldFingerprint.
    - Non-agent actors (D-10). On Claude's note with `lock.text` = `agent.claude`:
      - `notes.updateNote(humanActor('kaelen'), ...)` → ok.
      - `notes.updateNote(pluginActor('tapestry-notes'), ...)` → ok, and the last commit contains `actor plugin tapestry-notes`.
      - `notes.updateNote(OBSIDIAN_BRIDGE_ACTOR, ...)` on n1 → ok.
    - Deleted note (D-13). agent.claude deletes Claude's note → ok. A later agent.claude update of the same id → refused with exactly `<claudeNote> is not a live note in locks`.
    - Ungated paths (D-01). With `lock.text` = `user.kaelen` and `lock.delete` = `user.kaelen` set on n1:
      - `notes.createFrom(agentActor('chatgpt'), { tree: 'locks', grewFrom: 'n1', title: 'Offshoot', text: 'grown from a locked note' })` → ok.
      - `new ConnectionCommands(registry).connect(agentActor('chatgpt'), { from: { tree: 'locks', note: 'n1' }, to: { tree: 'locks', note: claudeNote } })` → ok.
    - Lock hidden by undo (D-11). Set `lock.text` = `agent.claude` on Claude's note. Call `tree.bridge.undo()` and expect `true`, then check `tree.bridge.isRewound` is true. Take `before = worldFingerprint()`. agent.chatgpt update → refused with exactly `<claudeNote> text is locked by agent.claude`. Afterwards `tree.bridge.isRewound` is false, worldFingerprint equals `before`, and the body text is still `Grown by Claude`.
  </behavior>
  <action>
    RED:
    - Add the allow-list pure and e2e cases from `<behavior>` to `locks.test.ts`.
    - Run `npm --prefix app run test -- src/main/commands/locks.test.ts`. The cases expecting admission must fail, because the actor is refused with the lock message. The whitespace-parse case fails on the `allow` deep-equal. Cases that expect refusal (agent.chat, non-text allow) may already pass; note which in the SUMMARY.
    - Record the failing test names under "RED evidence".
    - Commit with `test(02.4-02): allow-list tests (red)`, staging `app/src/main/commands/locks.test.ts` explicitly.

    GREEN in `locks.ts`:
    - In `resolveLock`, whenever the result is locked, whether from an explicit value or a derived default, read `props[allowKey(aspect)]`.
    - Only when that prop exists and its type is `text`, set `allow` to its value with `.trim().split(/\s+/)`, then drop empty strings. Otherwise `allow` is an empty array. Never read `.allow` from any other type (fail closed, D-09).
    - When the aspect resolves open, return `{ locked: false }` unchanged.
    - `checkLock` already admits `state.allow.includes(actor.id)`. Keep that exact array membership test; do not use `startsWith` or substring matching.
    - Update the `resolveLock` doc comment to describe D-09, including that the list applies to default locks too. Remove any comment from Plan 01 saying `allow` is filled in later.

    Then add the characterization group from `<behavior>` to `locks.test.ts`:
    - Import `OBSIDIAN_BRIDGE_ACTOR` and `pluginActor` from `./actor`, `ConnectionCommands` from `./connections`, and `NON_AGENT_NOTES_DELETE_LOCKED` from `./locks`.
    - The delete case branches on the constant. The explicit-lock cases do not.
    - Take every `before` snapshot after the fixture commits. The undo case is the one exception: take its snapshot after `undo()`.

    Run the verify commands until green. List the characterization tests that passed on first run in the SUMMARY. Commit with `feat(02.4-02): honour lock.<aspect>.allow; cover creators, non-agents and undone locks`, staging the two paths explicitly, then run `git show --stat HEAD`.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/locks.test.ts src/main/commands/notes-policy.test.ts</automated>
    <fails_when>non-zero exit, or `failed` in the `Tests` summary line, or `No test files found` in the output</fails_when>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>grep -n 'allowKey(aspect)' app/src/main/commands/locks.ts</automated>
    <fails_when>non-zero exit because no line matched, meaning resolveLock never reads the allow property</fails_when>
    <automated>! grep -nE 'submitAs|setProperty|\.submit\(' app/src/main/commands/locks.ts</automated>
    <fails_when>grep prints a `line:` match, so the negated command exits 1 because locks.ts writes to the kernel</fails_when>
  </verify>
  <acceptance_criteria>
    - `locks.test.ts` contains each of these strings:
      - `lock.text.allow`
      - `lock.delete.allow`
      - `text is locked by obsidian.bridge`
      - `text is locked by tapestry-notes`
      - `is not a live note in locks`
      - `OBSIDIAN_BRIDGE_ACTOR`
      - `pluginActor('tapestry-notes')`
      - `ConnectionCommands`
      - `tree.bridge.undo()`
    - `locks.test.ts` has a case in which one agent is admitted by the allow list while another is refused on the same note, and a pure case whose `allow` deep-equals `['agent.gemini', 'agent.chatgpt']`.
    - `app/src/main/commands/locks.ts` reads `allowKey(aspect)` and checks the prop type equals `text` before splitting.
    - Both `<automated>` test commands and typecheck exit 0.
    - The SUMMARY records "RED evidence" for the allow-list cases, and names the characterization cases that passed on first run.
    - `git show --stat HEAD` lists only `app/src/main/commands/locks.ts` and `app/src/main/commands/locks.test.ts`.
  </acceptance_criteria>
  <done>The allow list admits exactly the listed agents on explicit and default locks and ignores malformed lists (D-09). Bridge and plugin notes start locked end to end (D-04, D-05). People and plugins pass (D-10). Deleted notes are refused as not live (D-13). Growing and connecting stay open (D-01). A lock hidden by undo still refuses the agent without writing anything (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: State the lock rule to agents, reword superseded comments, and run the phase gate</name>
  <files>app/src/main/mcp/schemas.ts, app/src/main/commands/agent-tools.ts, app/src/main/commands/connections.ts</files>
  <read_first>
    - app/src/main/mcp/schemas.ts: the whole file, especially the doc comments at lines 56, 64, 73, 82, 91 and 113, and the tool entries at lines 132-194
    - app/src/main/mcp/shim.test.ts: lines 1-35 (it spawns the built `out/main/mcp.js`) and 180-205 (the tool-name and annotation assertions)
    - app/src/main/commands/agent-tools.ts: lines 1-20
    - app/src/main/commands/connections.ts: lines 1-30 and 70-90
    - .planning/phases/02.4-lock-model/02.4-01-SUMMARY.md: "Baseline"
    - .planning/phases/02.4-lock-model/02.4-RESEARCH.md: the "Other Dependents of the Gate / D-05" table
  </read_first>
  <action>
    Change wording only; behaviour does not change. This is the agent-facing statement of D-01, which replaces the superseded 02.2 authorship rule.

    In `schemas.ts`, `TOOL_DEFINITIONS`. The strings below are the target wording; keep them this close:
    - `update_note`
      - title: `Replace the text of a note`
      - description: `Replaces a note's text. Refused, and nothing is written, when the note's text is locked against you; the error names the lock's owner. Notes created by the user, by the Obsidian bridge or by another plugin start with their text locked.`
    - `rename_note`
      - title: `Retitle a note`
      - description: `Changes a note's title. A title is part of the note's text, so renaming is refused, and nothing is written, when the note's text is locked against you; the error names the lock's owner.`
    - `delete_note`
      - title: `Delete a note`
      - description: `Deletes a note and its connections. Refused, and nothing is written, when the note's deletion is locked against you; the error names the lock's owner. The note stays in the tree's history either way.`
    - Leave the other five entries' text unchanged. Their "including notes you did not create" wording is still true under D-01.
    - Do not name the `lock.*` property keys in any string. Agents cannot set them, and this phase does not publish their names (D-08, Decision Register #19).
    - Do not change any `name`, `schema` or `annotations` value, and do not change any zod schema.

    Doc comments in `schemas.ts`:
    - Lines 56 (read_note), 64 (search_notes) and 113 (connect_notes): reading, searching and connecting stay unrestricted (02.4 D-01).
    - Lines 73 (update_note) and 82 (rename_note): refused when the note's `text` aspect is locked against the calling agent (02.4 D-01, D-03, D-08).
    - Line 91 (delete_note): refused when the `delete` aspect is locked (02.4 D-02).

    In `agent-tools.ts`, rewrite the sentence at line 13 so it says the lock rule (02.4 D-01) lives in the shared command layer, not in this dispatcher, so it holds for any transport.

    In `connections.ts`:
    - Rewrite the header paragraph (lines 4-10). Locks (02.4) restrict changing a note's text and deleting it, but connecting stays unrestricted, as 02.2 D-05 already had it and 02.4 D-01 keeps it. Keep the explanation of why a connection is a claim anyone reading the world may make.
    - Rewrite the `connect` doc comment at line 78 to say that any actor, including an agent, may connect notes whose text is locked against it (02.4 D-01).

    Every D-05 you leave or write in these three files must carry its phase prefix.

    Phase gate, after the edits:
    1. Run `npm --prefix app run build:js`. It writes only the gitignored `app/out/`. If it fails for a reason unrelated to these three files, STOP and report; do not change toolchain files.
    2. Run `npm --prefix app run test -- src/main/mcp/shim.test.ts`.
    3. Run `npm --prefix app run test 2>&1 | tail -60`. Compare it with the 02.4-01 SUMMARY "Baseline". Every test file that passed at baseline must still pass. `shim.test.ts` should now also pass, because `app/out/` exists. Record both summary lines, and any file still failing with its first error line, in this plan's SUMMARY under "Phase gate".
    4. Run the negative gates in `<verify>`.

    Commit with `docs(02.4-02): state the lock rule in agent tool text and comments`, staging the three paths explicitly, then run `git show --stat HEAD`.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or `error during build` in the output</fails_when>
    <automated>npm --prefix app run test -- src/main/mcp/shim.test.ts src/main/commands/</automated>
    <fails_when>non-zero exit, or `failed` in the `Tests` summary line, or `No test files found` in the output</fails_when>
    <automated>npm --prefix app run test</automated>
    <fails_when>a vitest `FAIL` line names a test file that passed at the 02.4-01 baseline, or the `Tests` summary line reports more failed tests than the baseline did. A non-zero exit caused only by files that also failed at baseline is recorded in the SUMMARY, not treated as a phase failure</fails_when>
    <automated>! grep -nE 'notes? you created' app/src/main/mcp/schemas.ts</automated>
    <fails_when>grep prints a `line:` match, so the negated command exits 1 because a tool still tells agents the superseded authorship rule</fails_when>
    <automated>! grep -rn 'may only change notes' app/src</automated>
    <fails_when>grep prints a `path:line:` match, so the negated command exits 1</fails_when>
    <automated>! (grep -n 'D-05' app/src/main/commands/notes.ts app/src/main/commands/notes-policy.test.ts app/src/main/commands/locks.ts app/src/main/commands/locks.test.ts app/src/main/commands/connections.ts app/src/main/commands/agent-tools.ts app/src/main/mcp/schemas.ts | grep -vE '02\.[24] D-05')</automated>
    <fails_when>the pipeline prints a `path:line:` citation of D-05 without its phase prefix, so the negated command exits 1</fails_when>
    <automated>! (grep -rnE "['\"\`]lock\.(text|delete)" app/src plugins | grep -v '\.test\.ts:' | grep -v 'app/src/main/commands/locks\.ts:')</automated>
    <fails_when>the pipeline prints a `path:line:` match, so the negated command exits 1 because a production file other than locks.ts names a lock key, which is a potential writer</fails_when>
    <automated>! grep -rn 'USER_NOTES_DELETE_LOCKED' app/src</automated>
    <fails_when>grep prints a `path:line:` match, so the negated command exits 1 because the vault plan's older constant name was used instead of the CONTEXT D-05 name</fails_when>
    <automated>test -z "$(git diff --name-only 72ed27e..HEAD -- package.json package-lock.json app/package.json .planning/phases/02.3-time-threads .claude/CLAUDE.md .planning/config.json .gitignore)"</automated>
    <fails_when>non-zero exit, meaning the diff since the phase base lists a dependency manifest, a 02.3 plan file or a protected config file</fails_when>
  </verify>
  <acceptance_criteria>
    - In `schemas.ts`, the titles read exactly `Replace the text of a note`, `Retitle a note` and `Delete a note`, and each of the three descriptions contains `locked against you` and `nothing is written`.
    - Read `git diff 72ed27e -- app/src/main/mcp/schemas.ts`. It changes only comment lines and `title:`/`description:` string lines. No added or removed line contains `annotations`, `Hint`, `name:`, `schema:` or `z.`.
    - `npm --prefix app run test -- src/main/mcp/shim.test.ts` exits 0 after `build:js`.
    - The SUMMARY "Phase gate" section records the full-suite `Test Files` and `Tests` lines. No file that passed at the 02.4-01 baseline fails.
    - Every negative gate in `<verify>` exits 0.
    - `git show --stat HEAD` lists only `app/src/main/mcp/schemas.ts`, `app/src/main/commands/agent-tools.ts` and `app/src/main/commands/connections.ts`.
  </acceptance_criteria>
  <done>Agents are told the lock rule instead of the superseded authorship rule, with tool names and annotations unchanged. Comments no longer present 02.2 D-05 as the rule. The full suite, typecheck, shim build and all prohibition checks pass against the Plan 01 baseline.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02 share)

| Artifact | Kind | Where | Notes |
|----------|------|-------|-------|
| `resolveLock` allow-list reading | behaviour change | `app/src/main/commands/locks.ts` | Reads `lock.<aspect>.allow`, type `text` only, whitespace-split, on explicit and default locks |
| Allow-list, creator, non-agent, deleted-note, ungated-path and undone-lock tests | new test cases | `app/src/main/commands/locks.test.ts` | Extends the Plan 01 file |
| Tool titles and descriptions for `update_note`, `rename_note`, `delete_note` | changed strings | `app/src/main/mcp/schemas.ts` | Served to agents by the built shim |
| Reworded doc comments | comments | `schemas.ts`, `agent-tools.ts`, `connections.ts` | D-05 citations carry their phase prefix |

No new files, symbols, commands, tools or schema fields are added in this plan.

## Edge coverage (no SPEC, so this is the fallback)

| Edge | Covered by | Expected |
|------|------------|----------|
| Allow list with tabs, newlines, leading and trailing spaces | Task 1, pure | Parsed into exact ids |
| Allow list names a prefix-similar id | Task 1, pure (`agent.chat`) | Refused |
| Non-text allow value | Task 1, pure (`ref`, `int`) | Admits nobody |
| Allow list on a default lock | Task 1, pure and e2e | Admits the listed agent; others are refused with the creator as owner |
| `lock.delete.allow` does not open text | Task 1, pure | Aspects independent |
| Bridge-created note, update and delete | Task 1, e2e | Refused, owner `obsidian.bridge`; delete follows the constant |
| Plugin-created note, rename | Task 1, e2e | Refused, owner `tapestry-notes` |
| Non-agent actors against an agent-owned lock | Task 1, e2e | Allowed |
| Note deleted, then written | Task 1, e2e | Not-live message |
| createFrom and connect with a locked note | Task 1, e2e | Allowed |
| Lock set in an undone commit | Task 1, e2e | Refused after reconciling to head; nothing written |
| Agent-facing text after the rule change | Task 2 | Lock rule stated; names and annotations unchanged |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (MCP shim) → main | Tool arguments are untrusted and re-validated by `.strict()` zod schemas. The actor is host-stamped |
| `.tree` props → `resolveLock` | `lock.<aspect>.allow` may be hand-edited or plugin-written |
| built shim (`app/out/main/mcp.js`) → agent | Tool text the model reads. Hints only; enforcement stays in main |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.4-10 | Elevation of privilege | allow-list parsing in `resolveLock` | high | mitigate | Only a `text`-typed `.allow` is read. It is split on whitespace, and membership is exact (`includes(actor.id)`), with no prefix or substring match. Pure tests cover `agent.chat`, the `ref` and `int` types, and whitespace variants |
| T-02.4-11 | Elevation of privilege | an agent getting a lock key or writer through tool changes | high | mitigate | The Task 2 environment rule forbids schema, name and annotation edits. The phase gate checks that no production file outside locks.ts names a lock key, and that shim.test.ts still asserts exactly 8 tools with no actor field |
| T-02.4-12 | Elevation of privilege | stale view after undo hiding a lock | medium | mitigate | An e2e test shows a lock set in an undone commit still refuses after `prepareWrite` reconciles to head, with `worldFingerprint` unchanged |
| T-02.4-13 | Spoofing | a note created by a non-agent plugin treated as agent-owned | medium | mitigate | E2e tests with `OBSIDIAN_BRIDGE_ACTOR` and `pluginActor('tapestry-notes')` creators prove the D-04 default. `pluginActor` refuses the `agent.` prefix |
| T-02.4-14 | Tampering | a non-agent plugin or the renderer writing `lock.text open` or an allow list on Kaelen's note | medium | accept | Plugins are trusted in-process ("stamping is policy, not a sandbox", actor.ts). The write is recorded with its actor. The `setLock` slice, deferred behind Decision Register #19, addresses who may change a lock |
| T-02.4-15 | Repudiation / Information disclosure | tool text misdescribing the rule, so agents skip permitted edits or retry refused ones | low | mitigate | The descriptions are reworded to the enforced rule, and a negative grep confirms the old wording is gone. The text does not publish `lock.*` key names |
| T-02.4-16 | Tampering | refusal side effects in new cases | medium | mitigate | Every new refusal case asserts `worldFingerprint()` is unchanged (D-11) |
| T-02.4-SC | Tampering | npm/pip/cargo installs | low | accept | Nothing is installed. `build:js` and `build:native` use existing dependencies and write only gitignored output. The manifest-diff gate proves no dependency change |
</threat_model>

<verification>
- `npm --prefix app run test` shows every file that passed at the 02.4-01 baseline still passing, and `locks.test.ts`, `notes-policy.test.ts` and `shim.test.ts` passing.
- `npm --prefix app run typecheck` exits 0.
- Every negative gate in Task 2 `<verify>` exits 0.
- `git log --oneline 72ed27e..HEAD -- app/` shows only `02.4-01` and `02.4-02` commits.
</verification>

<success_criteria>
- `lock.<aspect>.allow` admits exactly its listed agents on explicit and default locks (SC-3 complete).
- Obsidian-bridge and plugin-created notes start locked for agents, and each refusal names that creator (SC-1 complete).
- People and non-agent plugins are not checked. Growing and connecting remain open, deleted notes are refused as not live, and undone locks still hold.
- Agents read the lock rule in the tool descriptions. No production code writes `lock.*`, and no dependency or 02.3 file changed.
</success_criteria>

<output>
Create `.planning/phases/02.4-lock-model/02.4-02-SUMMARY.md` when done. Include "RED evidence" (the allow-list cases), the list of characterization cases that passed on first run, and "Phase gate" (full-suite summary lines compared with the 02.4-01 baseline, and the result of every negative gate).
</output>
