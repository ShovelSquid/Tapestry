---
phase: 02.8-agent-note-windows
plan: 05
type: execute
wave: 5
depends_on:
  - 02.8-04
files_modified:
  - app/src/renderer/state/chat-sessions.ts
  - app/src/renderer/state/chat-sessions.test.ts
  - app/src/renderer/components/ChatSessionHeader.tsx
  - app/src/renderer/components/ChatSessionCard.tsx
  - app/src/renderer/components/ChatPanel.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
autonomous: true
requirements: []
roadmap_criteria: [SC4]
decisions: [D-10, D-11, D-12, D-14]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11/SC4: the card and the panel show each session's state from 02.8-04's reducer, never from reply text. Idle: sharp and quiet. Working: the transcript blurred and desaturated under a sharp header, not the gray author-unknown token. Needs you: a \"!\" badge that persists until you reply. Done: a 2px border in the agent's author colour. Failed: the word and short phrase in destructive text, never the Done look"
    - "D-11: Needs you persists through done, hover, opening, scrolling, Stop and forking from the original, and clears only when a message is sent in that session. Done and Failed become Idle when you look (the pointer rests 1000 ms, focus inside the card, enlarging, or sending), keeping their status text"
    - "D-10/SC4: every visual here (status line, state, Working blur, the \"!\", the border) is renderer state in the chat-sessions store. Nothing in this plan writes to a tree or calls a commit"
    - "D-14: the card shows `cardText`, so a level-0 status updates only the enlarged view's line, not the card's"
    - "D-12/SC4: after a relaunch, every session card starts from `initialStatus({ busy: false, lastStatus })`: Idle with its last status text, never a stale Working"
    - "State changes at level 1 or above are announced through LiveAnnouncer, from the card only: polite '<title>: done. <text>', polite '<title> needs you: <text>', assertive '<title> failed: <phrase>'. Working is not announced"
    - "UI consideration (empty): a session with no turns shows 'New chat' in its status line"
    - "UI consideration (Working): while busy, the card shows the spark glyph and 'Working · <status>', a desaturated and blurred transcript under a sharp header, and Stop reply, with no spinner"
    - "UI consideration (turn started): the status text reads 'Reading your message' until the first tool call or reply text"
    - "UI consideration (failed turn): the × glyph, 'Failed · <short phrase>' in destructive text, and the error box with the full message and fix hint; Failed never jiggles or flashes"
    - "UI consideration (stopped): Stop reply ends the turn as Idle with the status text 'Stopped', and the reply so far stays in the transcript"
    - "UI consideration (long text): the status text and the session title are one line with an ellipsis, and the full text is in a title tooltip"
    - statement: "UI consideration (Needs you persistence): the \"!\" badge and 'Needs you' survive hover, opening and scrolling, and clear only on a reply sent in that session"
      verification: backstop
    - statement: "UI consideration (relaunch): after quitting mid-turn and relaunching, every session card shows Idle with its last status text, with no blur, no \"!\" and no arrow"
      verification: backstop
  artifacts:
    - path: app/src/renderer/components/ChatSessionHeader.tsx
      provides: "State glyph, title and status line, shared by card and panel"
    - path: app/src/renderer/state/chat-sessions.ts
      provides: "Per-session status in the store, acknowledgement, author colour"
      exports: ["acknowledgeSession", "setSessionAgentOrder", "sessionAuthorToken"]
  key_links:
    - from: app/src/renderer/state/chat-sessions.ts
      to: app/src/shared/chat/session-status.ts
      via: "every payload event -> reduceStatus(snapshot.status, { kind: 'event', event }, now)"
      pattern: "reduceStatus\\("
    - from: app/src/renderer/components/ChatPanel.tsx
      to: app/src/renderer/components/ChatSessionHeader.tsx
      via: "the panel and the card render the same header from the same snapshot"
      pattern: "ChatSessionHeader"
---

<objective>
Status, renderer half: every session card and the enlarged view show the state 02.8-04's reducer derives (glyph, word and status line), with the Working, Needs you, Done and Failed looks, acknowledgement and announcements.

Purpose: SC4 and D-10..D-12 on screen. It was split out of the status plan so that each plan stays inside its context budget (plan checker, iteration 1). 02.8-06 then adds motion, the threshold, the stack raise and edge arrows on top of these states.

Output:
- per-session `status` in the chat-sessions store, `acknowledgeSession`, and the author colour;
- `ChatSessionHeader`, shared by card and panel;
- the state looks in App.css, with the two destructive tokens the UI-SPEC adds.

Recorded choices:
- **The author colour** comes from `authorToken('agent.' + agent, orderAgentsByConnection(agents))`. App passes the ordered names to the store, and the colour is set inline as `--tap-session-author` and never written anywhere.
- **Announcements come from the card only**, so a session shown in both card and panel is announced once.
</objective>

## Working rules for this plan

- Work only in `/Users/kaelencook/Tapestrees/mergin` on `ws/mergin`. Stage files by name. Do not install packages.
- There is no renderer component harness: store logic is tested as pure functions, and the looks go to `<human-check>`. Do not launch the app.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.8-agent-note-windows/02.8-UI-SPEC.md
@.planning/phases/02.8-agent-note-windows/02.8-04-SUMMARY.md
@app/src/shared/chat/session-status.ts
@app/src/renderer/state/chat-sessions.ts

<interfaces>
- 02.8-04: `initialStatus({ busy, lastStatus })`, `reduceStatus(status, input, now)`, `statusLine(status, forCard?)`, `STATE_WORDS`, `failedPhrase`, `SessionStatus` (`state`, `text`, `cardText`, `level`, `needs`, `arrivedAt`); `chat.open` returns `lastStatus`; `status` events arrive on `chat-event`.
- 02.8-03: the store holds one snapshot per `${treeId}:${noteId}` and applies every payload with `applyChatPayload`; `useChatSession(treeId, noteId)`; `ChatSessionCard`, `ChatPanel` (enlarged view) and `ChatSessionComposer` exist.
- `threads/author-palette.ts`: `authorToken(actorId, orderedNames)` and `orderAgentsByConnection(agents)`. App holds `agents` (TapestryAgentSummary[]).
- `LiveAnnouncer.tsx`: `useAnnounce()` gives `{ polite(text), assertive(text) }`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "each card and the enlarged view show the session's state glyph, word and status line from observed events"</name>
  <files>
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/state/chat-sessions.test.ts,
    app/src/renderer/components/ChatSessionHeader.tsx,
    app/src/renderer/components/ChatSessionCard.tsx,
    app/src/renderer/components/ChatPanel.tsx,
    app/src/renderer/App.tsx
  </files>
  <read_first>
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/components/ChatSessionCard.tsx,
    app/src/renderer/components/ChatPanel.tsx,
    app/src/shared/chat/session-status.ts,
    app/src/renderer/threads/author-palette.ts,
    app/src/renderer/components/AgentsPanel.tsx (SparkGlyph),
    .planning/phases/02.8-agent-note-windows/02.8-UI-SPEC.md (States; Status line; Accessibility)
  </read_first>
  <behavior>
    - A status payload `{ needs: true }` makes the snapshot's status `needs`; a later `done` keeps it `needs`; a `user` payload clears it
    - A snapshot loaded with `{ busy: false, lastStatus: 'write_file a.ts' }` starts Idle with that text
    - The previous snapshot object is never mutated by a status payload
  </behavior>
  <action>
**Store (`state/chat-sessions.ts`).**
- Each snapshot gains `status: SessionStatus`. It starts from `initialStatus({ busy, lastStatus })` when `open` answers, and each payload's event runs `reduceStatus` inside `applyChatPayload`.
- Add `setSessionAgentOrder(orderedNames)` and `sessionAuthorToken(agent)`, which returns the CSS variable name from `authorToken('agent.' + agent, orderedNames)`.
- Write the `<behavior>` tests in chat-sessions.test.ts against the pure `applyChatPayload`.

**ChatSessionHeader.tsx (new, used by card and panel).**
- The state glyph: a 16px inline SVG with `fill="currentColor"`:
  - Spark in `--tap-muted` for Idle and Working;
  - "!" in a circle for Needs you, and a check in a circle for Done, both `--tap-session-author`;
  - × in a circle in `--tap-destructive-text` for Failed.
- The title slot: the card passes its title input, and the panel passes plain text.
- The status line: Label, `--tap-muted`, one line with an ellipsis and the full text in `title`.
  - It shows `statusLine(status)`, with the state word and text in `--tap-destructive-text` when Failed, and 'New chat' when the session has no turns and no text.
  - The card uses `cardText` (level 0 does not change it); the panel uses `text`.

**Card and panel.** Both render `ChatSessionHeader` from their snapshot. The card sets `--tap-session-author` inline from `sessionAuthorToken(agent)`. The panel never blurs and never animates.

**App.tsx.** Whenever `agents` changes, call `setSessionAgentOrder(orderAgentsByConnection(agents))`.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/state src/shared/chat && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "FAIL" or "error TS" (the store ignored a status event, mutated a snapshot, or cleared Needs you without a reply)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "reduceStatus(" app/src/renderer/state/chat-sessions.ts` prints a match
    - `grep -n "export default function ChatSessionHeader\|export function ChatSessionHeader" app/src/renderer/components/ChatSessionHeader.tsx` prints one match
    - `grep -n "ChatSessionHeader" app/src/renderer/components/ChatPanel.tsx app/src/renderer/components/ChatSessionCard.tsx` prints a match in each file
    - `grep -n "setSessionAgentOrder(" app/src/renderer/App.tsx` prints a match
    - chat-sessions.test.ts passes; `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>Every chat card and the enlarged view say, in a glyph, a word and a status line, what the chat is doing, straight from what the engine and Claude reported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The state looks: Working blur, the "!" badge, Done and Failed borders, acknowledgement and announcements</name>
  <files>
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/state/chat-sessions.test.ts,
    app/src/renderer/components/ChatSessionCard.tsx,
    app/src/renderer/components/ChatPanel.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    app/src/renderer/components/ChatSessionCard.tsx,
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/components/LiveAnnouncer.tsx,
    app/src/renderer/App.css (the `--tap-*` tokens, `.tapestry-chat-error`, `.tapestry-button--destructive`),
    .planning/phases/02.8-agent-note-windows/02.8-UI-SPEC.md (Color: tokens this phase adds; States; Leaving a state; Accessibility)
  </read_first>
  <behavior>
    - `acknowledgeSession` after a done payload gives Idle with the same text; after a needs payload it leaves Needs you
    - `acknowledgeSession` on a Working session changes nothing
  </behavior>
  <action>
**Store.** Add `acknowledgeSession(treeId, noteId)`, which runs `{ kind: 'ack' }`, with the `<behavior>` tests.

**Card.**
- Classes `tapestry-session-card--working`, `--needs`, `--done` and `--failed` from the snapshot's state.
- A `NeedsYouBadge` while needs: a 24px disc at (-12px, -12px) on the top-left corner, filled `--tap-session-author`, with a white "!" glyph and the control-bubble shadow, `aria-hidden` (the word carries it).
- Acknowledgement: `acknowledgeSession` after the pointer rests 1000 ms (a timer on pointerenter, cleared on leave), on focus within the card, on Enlarge and on send.
- Announcements: when the state changes into done, needs or failed at level 1 or above, announce as must_haves says.
- The card still shows its glyph, badge and border when closed.

**Panel.** Opening a session in the panel acknowledges Done and Failed. The panel shows the glyph and word only: no blur, no badge motion.

**App.css.**
- Add `--tap-destructive: #E5484D` and `--tap-destructive-wash: #FFF1F1` to the root tokens, and replace those raw literals in `.tapestry-button--destructive`, `.tapestry-chat-error` and the delete bubble with the tokens.
- Working: `.tapestry-session-card--working .tapestry-session-transcript { filter: saturate(0.2) blur(2px) }` and the composer `saturate(0.2)`, with a 200ms ease transition, and the blur dropped to 0 on `:hover` and `:focus-within`. The border stays `--tap-border`.
- Needs you and Done: a 2px `--tap-session-author` border.
- Failed: no author border.
- Selection keeps its 2px accent border in every state.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/state src/shared/chat && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "FAIL" or "error TS" (acknowledgement cleared Needs you or left Done in place)</fails_when>
    <human-check>
Queued for the human. In `npm --prefix app run dev` with a scratch workspace:
- Send a message: 'Working · Reading your message', then the tool or reply text, a blurred transcript under a sharp header, and Stop reply.
- Ask Claude a question it must answer back ("ask me which file to read"): the "!" badge and 'Needs you' appear and stay through hover, opening and scrolling, and go only when you reply.
- A finished turn shows Done with the author-colour border until you rest the pointer on the card.
- Stop a turn: Idle with 'Stopped'.
- Sign out of Claude Code (or rename the binary) and send: Failed with the short phrase, never the Done look.
- Quit mid-turn and relaunch: every card is Idle with its last status text, with no blur, no "!" and no arrow.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function acknowledgeSession" app/src/renderer/state/chat-sessions.ts` prints one match
    - `grep -n "\-\-tap-destructive:" app/src/renderer/App.css` prints one match
    - `grep -n "tapestry-session-card--working" app/src/renderer/App.css` prints a match
    - `grep -n "NeedsYouBadge" app/src/renderer/components/ChatSessionCard.tsx` prints a match
    - chat-sessions.test.ts passes; `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>At a glance, a person can tell whether each chat is idle, working, waiting for them, done or failed, and none of it is ever written into history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (`chat-event` status) | Status text from a model is shown on the card and in the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.8-21 | Information Disclosure | model text in the status line | low | accept | The text comes from the person's own chat and is rendered as plain text (React escaping), never as HTML |
| T-02.8-32 | Tampering | status shown as history | low | mitigate | The store and header only read snapshots; this plan adds no IPC call that writes, and main never commits status (02.8-04) |
</threat_model>

<verification>
- `npm --prefix app run build:js && npm --prefix app run typecheck && npm --prefix app run test`: the full suite passes
</verification>

<success_criteria>
- Idle, Working, Needs you, Done and Failed show on every card and in the enlarged view; Done becomes Idle when looked at and Needs you persists until a reply; after a relaunch every session is Idle with its last status (SC4, renderer half)
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| Snapshot `status`; `acknowledgeSession`, `setSessionAgentOrder`, `sessionAuthorToken` | store | app/src/renderer/state/chat-sessions.ts |
| `ChatSessionHeader` (state glyph, title slot, status line) | component | app/src/renderer/components/ChatSessionHeader.tsx |
| `NeedsYouBadge`; acknowledgement timer; announcements | component, behaviour | app/src/renderer/components/ChatSessionCard.tsx |
| CSS tokens `--tap-destructive`, `--tap-destructive-wash`, `--tap-session-author`; classes `tapestry-session-card--working/--needs/--done/--failed`, `.tapestry-needs-badge` | CSS | App.css |

<output>
Create `.planning/phases/02.8-agent-note-windows/02.8-05-SUMMARY.md` when done.
</output>
