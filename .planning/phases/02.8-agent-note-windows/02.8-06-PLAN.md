---
phase: 02.8-agent-note-windows
plan: 06
type: execute
wave: 6
depends_on:
  - 02.8-05
files_modified:
  - app/src/shared/chat/session.ts
  - app/src/shared/chat/transcript.ts
  - app/src/shared/chat/transcript.test.ts
  - app/src/main/chat/engine.ts
  - app/src/main/chat/claude-cli-engine.ts
  - app/src/main/chat/session-notes.ts
  - app/src/main/chat/session-notes.test.ts
  - app/src/main/chat/chat-service.ts
  - app/src/main/chat/chat-engine.test.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/state/chat-sessions.ts
  - app/src/renderer/state/chat-sessions.test.ts
  - app/src/renderer/state/chat.ts
  - app/src/renderer/components/ChatSessionTranscript.tsx
  - app/src/renderer/components/ChatSessionComposer.tsx
  - app/src/renderer/components/ChatSessionCard.tsx
  - app/src/renderer/components/ChatPanel.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
autonomous: false
requirements: []
roadmap_criteria: [SC6]
decisions: [D-03, D-09, D-17, D-18, D-19, D-20, D-21]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-17/SC6: replying at the last turn continues the note. Replying at an earlier completed turn k leaves the original untouched and creates a new session note beside it, holding turns 1..k, your reply and the conversation from there"
    - "D-17: the fork is placed with 2.5's resolver, `beyond` the original from its parent, where the parent is the original's own `forked-from` target. A session with no parent is placed `near` the original instead (orchestrator resolution 2). A refusal commits nothing"
    - "D-18/SC6: the fork is one commit: the new session node plus a `forked-from` edge from it to the original, carrying `turn int k`, which names turn k's passage anchor (the k-th `Turn` block). Forks of forks work the same way"
    - "D-19: the original never knows. Its engine session, its chats.json entry, its note's properties and its ability to keep messaging are unchanged by a fork. You can fork at any completed turn even while the original is answering, and the turn in progress is never a fork point (k above the committed turn count is refused)"
    - "D-20/SC6: the fork gets its own engine session: a new agent (D-03: a fork gets a new one), a new token and config file, and a fresh `--session-id`. It starts by replaying turns 1..k from the recorded transcript, with tool calls as their one-line summaries, as the preamble of its first message, through `ChatEngine.fork(atTurn, recorded)`. No CLI session-file internals and no `--fork-session` are used"
    - "D-09: a fork's context comes from the recorded passages only; nothing asks the model to regenerate a past turn. The replay preamble is never written into the fork's note: its first new passage holds only the person's own message and the reply"
    - "D-21: a fork is an event, and the world is not rewound. The fork commit touches only the new node and its edge, and the fork works against the tree and the workspace as they are when it is made"
    - "A fork is titled `<original title>, from turn k` (cut so the title stays within 200 characters), and a replay longer than `FORK_REPLAY_MAX_CHARS` keeps the latest turns and says which were left out (RESEARCH Pitfall 13)"
    - "Fork mode: every completed turn that is not the last turn offers **Reply from here** (aria-label 'Reply from turn k') on hover or focus. While a turn is running, the last completed turn offers it too. Choosing it highlights turn k (`.tapestry-passage-active`), dims later turns to 0.4, shows the chip 'Replying from turn k. Sending starts a new chat beside this one. This chat stays as it is.' with **Stop forking**, changes the placeholder to 'Reply from turn k' and the button to **Send as new chat** (enabled even while Working), and moves focus to the input. Escape or Stop forking leaves fork mode and keeps the draft"
    - "After a fork is sent, the original card is unchanged (state, border and scroll) and its composer leaves fork mode. The camera does not move. The fork card appears Working, on top of its frame's stack, with an edge arrow if it is off-screen (A-08). A fork made from the enlarged view switches the panel to the fork, and focus moves to the fork's composer. It is announced politely: 'New chat started from turn k of <original title>.'"
    - "UI consideration (fork refused): a refused fork shows the notice 'Couldn't start a new chat from turn k: <reason>. Nothing was changed.', keeps the draft in fork mode, and leaves both notes unchanged"
    - "UI consideration (turns in a session): with one turn there is no Reply from here, since it is the last turn; with many, every completed turn but the last offers it"
  artifacts:
    - path: app/src/main/chat/session-notes.ts
      provides: "SessionNotes.fork: one commit, node plus forked-from edge"
      exports: ["SessionNotes"]
    - path: app/src/main/chat/engine.ts
      provides: "ChatEngine.fork(atTurn, recorded)"
      contains: "fork(atTurn: number"
    - path: app/src/shared/chat/transcript.ts
      provides: "replayText and FORK_REPLAY_MAX_CHARS"
      exports: ["replayText", "FORK_REPLAY_MAX_CHARS"]
    - path: app/src/shared/chat/session.ts
      provides: "FORKED_FROM_LABEL, FORK_TURN_KEY, forkTitle"
      exports: ["FORKED_FROM_LABEL", "FORK_TURN_KEY", "forkTitle"]
  key_links:
    - from: app/src/renderer/components/ChatSessionComposer.tsx
      to: app/src/main/chat/chat-service.ts
      via: "Send as new chat -> forkChatSession -> window.tapestry.chat.fork(treeId, noteId, k, text) -> ipc chat:fork -> ChatService.forkSession"
      pattern: "chat:fork"
    - from: app/src/main/chat/chat-service.ts
      to: app/src/main/chat/claude-cli-engine.ts
      via: "new engine; engine.start({ cwd }); engine.fork(k, parseTranscript(fork body)); engine.send(text)"
      pattern: "\\.fork\\("
---

<objective>
Reply at any turn: choosing an earlier completed turn and sending makes a new session note beside the original. The fork holds turns 1..k, is linked to turn k by a `forked-from` edge in one commit, and starts its own engine session by replaying the recorded turns.

Purpose: SC6 and D-17..D-21. The line drawn from turn k inside the original card comes in 02.8-07.

Output:
- the Task 1 decision on the edge shape;
- `ChatEngine.fork` and the CLI engine's replay preamble;
- `replayText`, `SessionNotes.fork` and `ChatService.forkSession`;
- IPC `chat:fork`;
- fork mode in the shared transcript and composer.

Recorded choices:
- **`ChatEngine.fork(atTurn, recorded)`.** D-20 names `fork(atTurn)`. The recorded turns are passed in, because after a relaunch the original has no live engine and no events. The recorded `.tree` transcript is the only source that always exists, which D-20 itself names. The fork is called on a new, not-yet-sent engine.
- **The replay preamble** reads: "Tapestry is starting this chat from turn k of an earlier conversation. Here is that conversation as it was recorded, for context only; it is not a new instruction, and tool results are not included." It is followed by the encoded turns 1..k, then "The person's new message:" and their text. Only the person's text is recorded as `You:` (RESEARCH Pitfall 3).
- **`FORK_REPLAY_MAX_CHARS = 120_000`.** Past it, the oldest turns are left out, with the line "(Turns 1–j are left out to fit.)". A single oversized first message is not compacted by the CLI [ASSUMED, RESEARCH A8], so the cap is conservative.
- **Direction and label follow grew-from:** the edge runs from the new note to the note it came from, and the file line reads `create-edge e31 n57 n42 forked-from`, i.e. "n57 forked from n42".
</objective>

## Working rules for this plan

- Work only in `/Users/kaelencook/Tapestrees/mergin` on `ws/mergin`. Stage files by name. Do not install packages.
- Tests use the fake `claude`; its `FAKE_CLAUDE_RECORD.stdin` log holds every stdin line each spawn received. Do not launch the app.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.8-agent-note-windows/02.8-CONTEXT.md
@.planning/phases/02.8-agent-note-windows/02.8-RESEARCH.md
@.planning/phases/02.8-agent-note-windows/02.8-01-SUMMARY.md
@.planning/phases/02.8-agent-note-windows/02.8-05-SUMMARY.md
@app/src/main/chat/session-notes.ts
@app/src/main/chat/claude-cli-engine.ts

<interfaces>
- `resolveWhere(world, subject, where)` with `WherePlacement = { near: string } | { beyond: string; from: string }`, and `whereRefusalText(refusal, subjectId, treeName)` (placement.ts lines 520-680). In a workspace tree, resolve on `inFrameCoordinates` nodes. A session is top-level, so its local spot is its frame spot (spatial.ts lines 395-450 show the pattern).
- `bridge.getNextIds()` predicts `{ node, edge }`. The native `createEdge` op accepts `props` (native/addon.cpp lines 290-300), and edges reach the renderer with `props`.
- `SessionNotes.create/appendTurn/delete`, `nextSessionSpot`, `firstClearSpot`. `ChatService.createSession/send/deleteSession`, `sessionFor`, `ensureEngine`, per-session token and config, and `onAgentsChanged`.
- `parseTranscript`, `appendTurnText`, `encodeTurn`, `RecordedTurn`, `committedTurns`, `forkTitle` (added here).
- Renderer: `useChatSession`, `sendChatSession`, `requestComposerFocus`, `markNewSession`, `raiseSession`, `setChatAttachment`; ChatContext `enlarge` and `openChat`; App's TransientNotice.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decision: how a fork is linked to turn k of its original (one-way door)</name>
  <read_first>
    .planning/phases/02.8-agent-note-windows/02.8-RESEARCH.md (Pattern 9; Assumptions A4, A7),
    .planning/phases/02.8-agent-note-windows/02.8-CONTEXT.md (D-17, D-18),
    tapestry/docs/tree/FORMAT.md (create-edge, set on an edge)
  </read_first>
  <decision>The shape of the fork link written into the workspace `.tree`: its edge label, its direction, and where turn k is recorded</decision>
  <context>
    This is a one-way door: fork links go into append-only journals, and 02.8-07 draws the forked-from line from them. D-18 fixes the substance (one commit: the new note plus a `forked-from` edge to turn k's passage anchor). The 2.1 passage-anchor flow was never wired to edges (CR-03), and session bodies are plain text (02.8-01's decision), so turn k's anchor is its `Turn k` block. It needs a name on the link. Under the unattended driver this gate takes the Recommended option and queues it in `autonomy/REVIEW.md`.

    Option A as a `.tree` file would show it:

        create-node n57 tapestry.chat/session@1
        set n57 title text "Parser help, from turn 2"
        set n57 body text <<TEXT
        Turn 1
        …
        TEXT
        set n57 chat.turns int 2
        create-edge e31 n57 n42 forked-from
        set e31 turn int 2
  </context>
  <options>
    <option id="a" recommended="true">
      <name>(Recommended) Edge `forked-from` from the fork to the original, with an edge property `turn int k`</name>
      <pros>The anchor lives on the link itself, so forks of forks each carry their own turn. It reads as "n57 forked from n42 at turn 2", in the same direction as `grew-from`. Deleting either note cascades the edge, as every edge does. It is the first edge with a property, and the kernel and addon already support that.</pros>
      <cons>It is the first edge property in the codebase, so readers of edges must not assume edges are property-free.</cons>
    </option>
    <option id="b">
      <name>Edge `forked-from` with no property; the fork note carries `chat.forkedAt int k`</name>
      <pros>Keeps edges property-free.</pros>
      <cons>The anchor is split between the edge and a node key, so the edge alone does not say which turn. A fork could never have two parents or be re-linked without rewriting the node.</cons>
    </option>
    <option id="c">
      <name>An anchor node per turn (a knot-like node for turn k), with the edge pointing at it</name>
      <pros>Edges stay plain, and the anchor is addressable.</pros>
      <cons>Adds a node per fork point, and D-07 forbids notes-inside-notes for turns. The anchor node would also have to follow the transcript's scroll.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The chosen option id is recorded in this plan's SUMMARY under "Decisions" and, when chosen by the unattended driver, as a new open item in `autonomy/REVIEW.md` with the sample above and what undoing it would take
  </acceptance_criteria>
  <resume-signal>Reply `a` (recommended), `b`, `c`, or describe the link you want.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "fork at turn k: one commit makes the new note and its forked-from edge, and its own Claude session starts from the recorded turns"</name>
  <precondition>Task 1 is answered; otherwise stop and report.</precondition>
  <reversibility rating="one-way">The `forked-from` label, its direction and the `turn` edge property go into append-only workspace journals and are what 02.8-07 draws; changing them means reading old forks under the old shape forever.</reversibility>
  <files>
    app/src/shared/chat/session.ts,
    app/src/shared/chat/transcript.ts,
    app/src/shared/chat/transcript.test.ts,
    app/src/main/chat/engine.ts,
    app/src/main/chat/claude-cli-engine.ts,
    app/src/main/chat/session-notes.ts,
    app/src/main/chat/session-notes.test.ts,
    app/src/main/chat/chat-service.ts,
    app/src/main/chat/chat-engine.test.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts
  </files>
  <read_first>
    app/src/main/chat/session-notes.ts,
    app/src/main/chat/chat-service.ts,
    app/src/main/chat/engine.ts,
    app/src/main/chat/claude-cli-engine.ts (lines 140-180 start and send),
    app/src/main/commands/notes.ts (lines 316-400, createFrom with where),
    app/src/main/commands/spatial.ts (lines 395-450),
    app/src/renderer/layout/placement.ts (lines 520-680),
    app/src/shared/chat/transcript.ts,
    app/src/main/chat/chat-engine.test.ts (harness and the stdin-log tests)
  </read_first>
  <behavior>
    - replayText(turns 1..2 of a 3-turn transcript) contains turn 1's and turn 2's You and Claude text and Tool summary lines, and not turn 3's; with maxChars smaller than the whole, it keeps the latest turns and includes "(Turns 1–1 are left out to fit.)"
    - forkTitle('Parser help', 2) is 'Parser help, from turn 2'; a 199-character title is cut so the result is at most 200 characters
    - SessionNotes.fork on a 2-turn session at k = 1: exactly one commit, signed by the person; the new node is a session with `chat.turns` 1, a body equal to turn 1's passage only, the fork title, `width` 360 and `height` 440; one edge from the new node to the original with label `forked-from` and `props.turn` = { type: 'int', value: 1 }; the original's props are identical before and after; the `.tree` text contains `forked-from` and `turn int 1`
    - SessionNotes.fork refuses k = 0, k = 3 (only 2 committed) and a non-session note, committing nothing
    - Placement: a root session's fork lands where `resolveWhere({ near: original })` puts it; a fork of that fork lands where `resolveWhere({ beyond: fork1, from: original })` puts it
    - forkSession with the fake `text` scenario on a 2-turn session at k = 1 with text 'try again': the fork's spawn used `--session-id` (not `--resume`); the last stdin line (the fork's first message) contains turn 1's text and 'try again' and not turn 2's text; after `done` the fork's note has `chat.turns` 2 and its Turn 2 passage's You line is exactly 'try again'
    - The fork's agent name differs from the original's; after the fork, a send to the original still works and resumes its own session id
    - Forking while the original is busy in `slow` succeeds; the original's turn keeps running
  </behavior>
  <action>
**shared/chat/session.ts.** Export `FORKED_FROM_LABEL = 'forked-from'`, `FORK_TURN_KEY = 'turn'` and `forkTitle(originalTitle, k)`.

**shared/chat/transcript.ts.** Export `FORK_REPLAY_MAX_CHARS = 120_000` and `replayText(turns, maxChars = FORK_REPLAY_MAX_CHARS)`, with the preamble wording in the objective. Turns are encoded with `encodeTurn`, so tool calls appear only as their one-line summaries.

**engine.ts.** Add `fork(atTurn: number, recorded: readonly RecordedTurn[]): void` to `ChatEngine`, documented per D-20: it is called on a new engine before its first send, and the engine begins where turn `atTurn` of `recorded` ended.

**claude-cli-engine.ts.** `fork(atTurn, recorded)` throws if the engine has history or a process. Otherwise it stores `replayPreamble = replayText(recorded.slice(0, atTurn))`. On the next `send(text)`, the stdin line is `userMessageLine(preamble + text)`, and the preamble is then cleared. `start` without `resumeSessionId` keeps `--session-id <new uuid>`.

**SessionNotes.fork(tree, originalId, atTurn, actor): { noteId, edgeId, seq }.**
1. Check that the original is a session and that `1 ≤ atTurn ≤ committedTurns(original)`, else refuse with `Turn <k> is not a finished turn of this chat`.
2. `prepareWriteFor`.
3. The body is turns 1..k from `parseTranscript(original body)`, re-encoded by folding `appendTurnText`.
4. `where` is `{ beyond: originalId, from: target }` when the original has an outgoing `forked-from` edge to `target`, else `{ near: originalId }`. The subject carries `width` and `height`. It resolves on the nodes with their frame positions, which is what `spatial.ts`'s private `inFrameCoordinates` computes: call `absolutePositions` from `renderer/layout/subspaces.ts` the same way, without changing spatial.ts. A refusal returns `whereRefusalText`, and a non-finite spot is refused.
5. Take `getNextIds`, then one submit with `createNode` (position, title, body, `chat.turns` k, width, height) and `createEdge { from: next.node, to: originalId, label: FORKED_FROM_LABEL, props: { turn: { type: 'int', value: k } } }`. The message is `fork chat <originalId> at turn <k>`.
6. Check `nodeIds[0]`, then call `onCommitted`.

**ChatService.forkSession(treeId, noteId, atTurn, text).**
1. Validate the text as `send` does, and check the original with `sessionFor`. The original's engine and busy state are not touched.
2. `sessionNotes.fork(ws.tree, noteId, atTurn, humanActor())`.
3. Set up the fork's session: token, `onAgentsChanged`, and `ensureEngine` with no stored session id.
4. `engine.fork(atTurn, parseTranscript(forkBody))`, then record `user` with the person's text only, and send it.
5. Return `{ noteId, agent }`.

**IPC and types.** Add `chat:fork(treeId, noteId, atTurn, text)`, where `atTurn` must be a positive integer. preload gains `chat.fork`, and global.d.ts mirrors it.

**Tests.** Cover every line of `<behavior>` in transcript.test.ts, session-notes.test.ts and chat-engine.test.ts.
  </action>
  <verify>
    <automated>npm --prefix app run build:js && npm --prefix app run test -- src/shared/chat src/main/chat && npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "FAIL" or "error TS" (a fork took two commits, changed the original, replayed a later turn, resumed the original's CLI session, or wrote the preamble into its note)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "fork(atTurn: number" app/src/main/chat/engine.ts` prints one match
    - `grep -n "export const FORKED_FROM_LABEL = 'forked-from'" app/src/shared/chat/session.ts` prints one match
    - `grep -n "export function replayText" app/src/shared/chat/transcript.ts` prints one match
    - `grep -n "fork(" app/src/main/chat/session-notes.ts` prints a match, and `grep -n "forkSession" app/src/main/chat/chat-service.ts` prints a match
    - `grep -n "ipcMain.handle('chat:fork'" app/src/main/index.ts` prints one match
    - chat-engine.test.ts asserts that the fork's spawn argv (from `FAKE_CLAUDE_RECORD`) has a `--session-id` element and no `--resume` or `--fork-session` element, and that test passes
    - transcript, session-notes and chat-engine tests pass
  </acceptance_criteria>
  <done>From main to disk, one path works: a fork at turn k is one commit with its link, and its own Claude session starts from exactly the recorded turns 1..k.</done>
</task>

<task type="auto">
  <name>Task 3: Reply from here: fork mode in the card and the panel</name>
  <files>
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/state/chat-sessions.test.ts,
    app/src/renderer/state/chat.ts,
    app/src/renderer/components/ChatSessionTranscript.tsx,
    app/src/renderer/components/ChatSessionComposer.tsx,
    app/src/renderer/components/ChatSessionCard.tsx,
    app/src/renderer/components/ChatPanel.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    app/src/renderer/state/chat-sessions.ts,
    app/src/renderer/components/ChatSessionTranscript.tsx,
    app/src/renderer/components/ChatSessionComposer.tsx,
    app/src/renderer/components/ChatPanel.tsx,
    app/src/renderer/App.tsx (openChat, notices),
    app/src/renderer/App.css (`.tapestry-passage-active`),
    .planning/phases/02.8-agent-note-windows/02.8-UI-SPEC.md (Reply at any turn: forks; Copywriting Contract)
  </read_first>
  <action>
**Store.**
- The snapshot gains `forkAt: number | null`, with `enterForkMode(treeId, noteId, k)` and `leaveForkMode(treeId, noteId)` (the draft is kept).
- `forkChatSession(treeId, noteId)` sends `composeFirstMessage(attachment, draft)` with `chat.fork(treeId, noteId, forkAt, text)`.
  - On success it clears the original's draft, attachment and `forkAt`, calls `markNewSession` and `raiseSession` for the fork and `requestComposerFocus` for it, and returns `{ ok: true, noteId }`.
  - On failure it returns `{ ok: false, error }` and leaves fork mode and the draft as they were.
- Tests: entering and leaving fork mode keeps the draft; a failed fork keeps `forkAt`.

**ChatContext (`state/chat.ts`).** Add `showNotice(message)` and `announce` passthroughs so the composer can report.

**Transcript.**
- Each committed turn k, when k is below the total turn count (committed turns, plus one while a turn is in progress), shows **Reply from here**. It is a text button, Label, `--tap-muted`, underlined on hover, with `aria-label="Reply from turn k"`, visible on hover or `:focus-within` of the turn, and it calls `enterForkMode`.
- Show the turn label "Turn k" at the turn's top-left on hover or focus.
- In fork mode, turn k gets `.tapestry-passage-active` and later turns `opacity: 0.4`.

**Composer.** In fork mode:
- show the chip "Replying from turn k. Sending starts a new chat beside this one. This chat stays as it is." with a **Stop forking** button (`.tapestry-chat-chip-remove`);
- the placeholder is "Reply from turn k", and the primary button is **Send as new chat**, enabled even while busy;
- entering fork mode focuses the input with `preventScroll`, and Escape leaves fork mode (a second Escape blurs);
- sending calls `forkChatSession`:
  - on success it announces politely "New chat started from turn k of <original title>.", and when the composer is inside the panel it calls `enlarge(treeId, forkId)`;
  - on failure it shows the notice "Couldn't start a new chat from turn k: <reason>. Nothing was changed."

**App.css.** Styles for `.tapestry-reply-from-here`, `.tapestry-turn-label` and `.tapestry-turn--after-fork` (opacity 0.4).
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/state src/shared/chat && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or the output contains "FAIL" or "error TS" (fork mode lost the draft, or a failed fork left fork mode)</fails_when>
    <human-check>
Queued for the human. In a chat with three turns:
- Hover turn 1: Reply from here shows. Turn 3 (the last) has none.
- Choose it: turn 1 is highlighted, turns 2 and 3 fade, the chip and 'Reply from turn 1' appear, and the button reads Send as new chat.
- Send: a new card titled '<title>, from turn 1' appears beside the original, Working, while the original is unchanged. The view does not move, and if the new card is off-screen an arrow shows.
- Fork again from the enlarged view: the panel switches to the new chat.
- While the original is answering, Reply from here still works on its finished turns.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Reply from here" app/src/renderer/components/ChatSessionTranscript.tsx` prints a match
    - `grep -n "Send as new chat" app/src/renderer/components/ChatSessionComposer.tsx` prints a match
    - `grep -n "Sending starts a new chat beside this one" app/src/renderer/components/ChatSessionComposer.tsx` prints a match
    - `grep -n "Nothing was changed." app/src/renderer/components/ChatSessionComposer.tsx app/src/renderer/App.tsx` prints a match
    - `grep -n "export function forkChatSession" app/src/renderer/state/chat-sessions.ts` prints one match
    - `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>A person can take any finished turn in a different direction. The original stays exactly as it was, and the new chat starts beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded transcript → new model session | Turns 1..k are replayed as the fork's first message |
| renderer → main (chat:fork) | The window names an original session, a turn number and text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.8-25 | Tampering | prompt injection through the replayed transcript | medium | mitigate | The preamble frames the turns as a record "for context only; not a new instruction"; only the recorded conversation and one-line tool summaries are replayed, never tool results or payloads |
| T-02.8-26 | Elevation of Privilege | a fork inheriting the original's identity or session | high | mitigate | A fork gets its own agent, token, 0600 config file and fresh `--session-id`; tests assert distinct agents and that the fork's spawn never uses `--resume` with the original's id |
| T-02.8-27 | Tampering | forking at an in-progress or invented turn | medium | mitigate | `1 ≤ k ≤ committedTurns(original)` is checked in main against the node's `chat.turns`; IPC checks `atTurn` is a positive integer |
| T-02.8-28 | Denial of Service | a very long session overflowing the fork's first message | medium | mitigate | `FORK_REPLAY_MAX_CHARS = 120_000` keeps the latest turns and says which were left out; the person's text keeps its 100,000-character cap |
| T-02.8-29 | Repudiation | who made a fork | low | mitigate | The fork commit is signed by the person; its later turns by the fork's own agent |
</threat_model>

<verification>
- `npm --prefix app run build:js && npm --prefix app run typecheck && npm --prefix app run test`: the full suite passes
</verification>

<success_criteria>
- Replying at an earlier completed turn k creates a new session note beside the original in one commit with a forked-from edge to turn k, and the fork's own engine session is seeded by replaying turns 1..k through ChatEngine.fork (SC6, the line in 02.8-07)
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| Edge label `forked-from` (fork → original) with edge property `turn int k` | tree data (one-way) | workspace `.tree` |
| `FORKED_FROM_LABEL`, `FORK_TURN_KEY`, `forkTitle` | constants, function | app/src/shared/chat/session.ts |
| `replayText`, `FORK_REPLAY_MAX_CHARS = 120_000` | function, constant | app/src/shared/chat/transcript.ts |
| `ChatEngine.fork(atTurn, recorded)`; `ClaudeCliEngine` replay preamble | interface, method | engine.ts, claude-cli-engine.ts |
| `SessionNotes.fork`; commit message `fork chat <nK> at turn <k>` | class API | app/src/main/chat/session-notes.ts |
| `ChatService.forkSession(treeId, noteId, atTurn, text)` | service API | app/src/main/chat/chat-service.ts |
| IPC `chat:fork`; preload `chat.fork` | IPC | index.ts, preload/index.ts, global.d.ts |
| `enterForkMode`, `leaveForkMode`, `forkChatSession`, snapshot `forkAt`; ChatContext `showNotice`, `announce` | store, context | chat-sessions.ts, chat.ts |
| **Reply from here**, "Turn k", fork chip, **Stop forking**, "Reply from turn k", **Send as new chat**, fork announced and refused copy | UI copy | ChatSessionTranscript.tsx, ChatSessionComposer.tsx |
| CSS `.tapestry-reply-from-here`, `.tapestry-turn-label`, `.tapestry-turn--after-fork` | CSS | App.css |

<output>
Create `.planning/phases/02.8-agent-note-windows/02.8-06-SUMMARY.md` when done. Record the Task 1 answer, the preamble wording, the replay cap, and the near/beyond placement rule.
</output>
