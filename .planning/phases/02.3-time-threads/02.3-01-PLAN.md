---
phase: 02.3-time-threads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/components/KnotNode.tsx
  - app/src/renderer/components/ThreadCenterNode.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/App.css
  - app/src/renderer/components/FloatingToolbar.tsx
  - app/src/renderer/editor/use-prosemirror.ts
  - app/src/renderer/editor/schema.ts
autonomous: true
requirements: [TREE-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-26: the word `thread` in tracked renderer source no longer names Phase 2.1's centre note or its two edges; those are the knot and its ties"
    - "D-26: Phase 2.1's feature still works — a centre note renders, both of its ties draw, and the editor opens on it"
    - "D-26: the node type is matched by an exact `===` comparison against a single exported constant, never by substring"
    - "UI-SPEC § Rename: every 02.1 visual value is carried unchanged — 8px radius on the empty node, 16px radius on the text node, 44px minimum, #C8C5BE 1.5px idle border, #4A7CFF hovered/editing border, 13px/1.4 editor text, 150ms transitions"
  artifacts:
    - path: "app/src/renderer/components/KnotNode.tsx"
      provides: "The renamed centre-note component and its exported size constants"
      exports: ["default", "KNOT_WIDTH", "KNOT_MIN_HEIGHT"]
    - path: "app/src/renderer/components/Canvas.tsx"
      provides: "Exact-match knot typing and knot-tie edge labelling"
      contains: "KNOT_TYPE"
  key_links:
    - from: "app/src/renderer/components/Canvas.tsx"
      to: "app/src/renderer/components/KnotNode.tsx"
      via: "default import plus the KNOT_WIDTH / KNOT_MIN_HEIGHT constants"
      pattern: "from './KnotNode'"
    - from: "app/src/renderer/components/Canvas.tsx"
      to: "app/src/renderer/App.css"
      via: "className strings the stylesheet selects on"
      pattern: "knot-node|knot-editor"
---

<objective>
Phase 2.1 built a connection with a centre note and called it a thread. From Phase 2.3 on, **thread** means a time thread and nothing else, so 2.1's feature is renamed: the centre note is the **knot**, and its two lines are **knot-ties** (D-26).

Purpose: free the name before any thread code is written, so no file ever holds both meanings. This runs first and alone because it rewrites `Canvas.tsx`, which the tracer plan then extends.

Output: `KnotNode.tsx`, exact-match type and label constants, renamed CSS classes, and a grep gate proving the old vocabulary is gone from tracked source.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-time-threads/02.3-CONTEXT.md
@.planning/phases/02.3-time-threads/02.3-PATTERNS.md
@.planning/phases/02.3-time-threads/02.3-UI-SPEC.md

<interfaces>
<!-- The substring match to replace. Canvas.tsx:123-128, verified on disk this session. -->
```typescript
/** Fallback thread-center size until the node registers its real dims. */
const THREAD_CENTER_FALLBACK_WIDTH = 200
const THREAD_CENTER_FALLBACK_HEIGHT = 44

function isThreadCenter(n: NodeInfo): boolean {
  return n.type.includes('thread-center')
}
```
Replace the predicate body with an exact comparison against `KNOT_TYPE`.
Edge labels are compared as `e.label === 'thread-arm'` at Canvas.tsx:485-486, 597, 600.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the centre-node component, its constants and its stylesheet</name>
  <reversibility rating="reversible">No saved world contains these node or edge types — `~/Documents/we.tree` has none and nothing in the app creates them — so the rename is a code edit with no migration (CONTEXT D-26).</reversibility>
  <files>app/src/renderer/components/KnotNode.tsx, app/src/renderer/components/ThreadCenterNode.tsx, app/src/renderer/App.css, app/src/renderer/components/FloatingToolbar.tsx, app/src/renderer/editor/use-prosemirror.ts, app/src/renderer/editor/schema.ts</files>
  <read_first>
    - `app/src/renderer/components/ThreadCenterNode.tsx` — the whole file; it is being renamed to `KnotNode.tsx`
    - `app/src/renderer/App.css` — the centre-node and editor rule blocks around lines 434-502
    - `.planning/phases/02.3-time-threads/02.3-PATTERNS.md` lines 520-538 — the verified inventory of every rename site, per file and line
    - `.planning/phases/02.3-time-threads/02.3-UI-SPEC.md` lines 87-101 — the rename table and the rule that every 02.1 visual value is carried unchanged
  </read_first>
  <action>
    Re-run the inventory grep first; 02.2 is executing in parallel in this checkout and may have added call sites since PATTERNS.md was written. Use the inventory it prints, not the one in PATTERNS.md, wherever the two disagree.

    Rename the file `app/src/renderer/components/ThreadCenterNode.tsx` to `app/src/renderer/components/KnotNode.tsx` with `git mv`, so history follows the file.

    Inside it, rename the React props interface to `KnotProps`, the exported width constant to `KNOT_WIDTH`, the exported minimum-height constant to `KNOT_MIN_HEIGHT`, and the default export to `KnotNode`. Rename the two className strings to `knot-node` and `knot-editor`. Add and export `KNOT_TYPE = 'tapestry.notes/knot@1'` and `KNOT_TIE_LABEL = 'knot-tie'` from this file — one module owns both constants so no second spelling can drift in.

    In `app/src/renderer/App.css`, rename every selector in the two affected rule blocks to `.knot-node` and `.knot-editor` and their descendant/state variants. Change selector names only: every declared value — the 8px and 16px radii, the 44px minimum, the `#C8C5BE` 1.5px idle border, the `#4A7CFF` hovered and editing border, 13px/1.4 editor text and the 150ms transitions — is carried through byte for byte, per UI-SPEC § Rename.

    Update the three stale head comments that name the old feature, in `app/src/renderer/components/FloatingToolbar.tsx` line 6, `app/src/renderer/editor/use-prosemirror.ts` line 4 and `app/src/renderer/editor/schema.ts` line 4, so each reads as the knot. These are comment-only edits; no code in those three files changes.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>test -f app/src/renderer/components/KnotNode.tsx && ! test -f app/src/renderer/components/ThreadCenterNode.tsx</automated>
    <fails_when>non-zero exit — either the new file is absent or the old one still exists</fails_when>
    <automated>grep -n 'knot-node\|knot-editor' app/src/renderer/App.css</automated>
    <fails_when>non-zero exit because no line matched</fails_when>
  </verify>
  <acceptance_criteria>
    - `app/src/renderer/components/KnotNode.tsx` exists and `ThreadCenterNode.tsx` does not
    - `KnotNode.tsx` exports the literal strings `tapestry.notes/knot@1` and `knot-tie` as `KNOT_TYPE` and `KNOT_TIE_LABEL`
    - `KnotNode.tsx` exports `KNOT_WIDTH` and `KNOT_MIN_HEIGHT`
    - `app/src/renderer/App.css` contains `.knot-node` and `.knot-editor` selectors
    - `git diff -- app/src/renderer/App.css` in the uncommitted working tree shows changed selector names and **no changed declaration values**
    - `npm --prefix app run typecheck` exits 0
  </acceptance_criteria>
  <done>The centre-note component, its constants and its stylesheet read as the knot, with every 02.1 visual value unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Match the knot exactly in Canvas, and prove the old vocabulary is gone</name>
  <files>app/src/renderer/components/Canvas.tsx</files>
  <read_first>
    - `app/src/renderer/components/Canvas.tsx` — lines 20-30 (imports), 120-132 (the fallback constants and the substring predicate), 455-505 and 580-650 (the layout, edge-label and node-render call sites)
    - `app/src/renderer/components/KnotNode.tsx` — as written by Task 1, for `KNOT_TYPE`, `KNOT_TIE_LABEL`, `KNOT_WIDTH`, `KNOT_MIN_HEIGHT`
    - `.planning/phases/02.3-time-threads/02.3-PATTERNS.md` lines 520-543 — the per-line Canvas inventory and the warning that a substring match on `knot` would be fragile
  </read_first>
  <action>
    In `app/src/renderer/components/Canvas.tsx`, import `KnotNode` together with `KNOT_TYPE` and `KNOT_TIE_LABEL` from `./KnotNode`.

    Replace the predicate that tests the node type with an exact comparison: the function becomes `isKnot(n: NodeInfo): boolean` returning `n.type === KNOT_TYPE`. Do not test with `String.prototype.includes` — a substring test on the four-letter word would also match unrelated future types, which is the fragility PATTERNS.md calls out.

    Rename the two module-level fallback size constants to `KNOT_FALLBACK_WIDTH` and `KNOT_FALLBACK_HEIGHT`, keeping their values 200 and 44.

    Replace every edge-label string comparison for the two arm edges with `e.label === KNOT_TIE_LABEL`, at all three comparison sites.

    Rename the local auto-layout variable that tracks the centre node's measured size to `knotAuto`, and rename every remaining identifier in this file that carries the old feature name so it reads as the knot. Update the JSX element name at the node-render site to `KnotNode`.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or any line matching `error TS` in the output</fails_when>
    <automated>npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or `Build failed` in the output</fails_when>
    <!-- planner-discipline-allow: thread-center -->
    <!-- planner-discipline-allow: thread-arm -->
    <!-- planner-discipline-allow: threadcenter -->
    <!-- planner-discipline-allow: THREAD_CENTER -->
    <automated>! grep -rn -iE 'thread-center|thread-arm|threadcenter|THREAD_CENTER' app/src plugins sdk</automated>
    <fails_when>grep prints one or more `path:line:` matches, which makes the negated command exit 1</fails_when>
    <automated>grep -n 'n.type === KNOT_TYPE' app/src/renderer/components/Canvas.tsx</automated>
    <fails_when>non-zero exit because no line matched — the exact-match predicate is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `Canvas.tsx` contains the exact string `n.type === KNOT_TYPE` and no call to `.includes(` on a node type
    - `Canvas.tsx` compares edge labels against `KNOT_TIE_LABEL` at all three sites
    - The recursive grep over `app/src`, `plugins` and `sdk` for the four old spellings returns no matches
    - `npm --prefix app run typecheck` and `npm --prefix app run build:js` both exit 0
  </acceptance_criteria>
  <verify>
    <human-check>Open the app, create a centre note with two connections, and confirm it renders with its two ties, opens its editor on click, and looks exactly as it did before this rename — same border colours, same corner radii, same text size.</human-check>
  </verify>
  <done>The knot is matched by exact type, its ties by exact label, and no tracked source under `app/src`, `plugins/` or `sdk/` still uses the 2.1 vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.tree` node type string → renderer component selection | A node type read from a world file decides which React component renders it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.3-01-01 | Spoofing | `Canvas.tsx` node-type predicate | medium | mitigate | Exact `n.type === KNOT_TYPE` comparison replaces the substring test, so a world file cannot supply an unrelated type such as `evil/knotwork@1` that a substring match would have accepted into the knot render path |
| T-02.3-01-02 | Tampering | `App.css` selector rename | low | accept | Selector renames carry no declaration-value change; the working-tree `git diff` acceptance criterion on `App.css` is the check. No untrusted input reaches CSS |

No package-manager install occurs in this plan, so no package-legitimacy gate applies.
</threat_model>

## Artifacts this phase produces

Created by this plan:

| Kind | Name | Location |
|---|---|---|
| File (renamed) | `KnotNode.tsx` | `app/src/renderer/components/KnotNode.tsx` |
| React component | `KnotNode` (default export) | `KnotNode.tsx` |
| TS interface | `KnotProps` | `KnotNode.tsx` |
| Constant | `KNOT_TYPE` = `tapestry.notes/knot@1` | `KnotNode.tsx` |
| Constant | `KNOT_TIE_LABEL` = `knot-tie` | `KnotNode.tsx` |
| Constant | `KNOT_WIDTH`, `KNOT_MIN_HEIGHT` | `KnotNode.tsx` |
| Constant | `KNOT_FALLBACK_WIDTH`, `KNOT_FALLBACK_HEIGHT` | `Canvas.tsx` |
| Function | `isKnot(n: NodeInfo): boolean` | `Canvas.tsx` |
| CSS class | `.knot-node`, `.knot-editor` (and their state variants) | `App.css` |

Removed by this plan: the 2.1 centre-node component file, its four size constants, its substring predicate, its two CSS class families, and the arm edge-label literal.

<verification>
- `npm --prefix app run typecheck` exits 0
- `npm --prefix app run build:js` exits 0
- The recursive grep for the four old spellings over `app/src`, `plugins` and `sdk` returns no matches
- `git diff -- app/src/renderer/App.css` in the uncommitted working tree shows no changed declaration values
</verification>

<success_criteria>
D-26 is satisfied: no occurrence of the 2.1 centre-node or arm-edge vocabulary remains in tracked source under `app/src`, `plugins/` or `sdk/`; the feature works as knot and knot-tie; the node type is matched exactly; and every 02.1 visual value is unchanged.
</success_criteria>

<output>
Create `.planning/phases/02.3-time-threads/02.3-01-SUMMARY.md` when done.
</output>
