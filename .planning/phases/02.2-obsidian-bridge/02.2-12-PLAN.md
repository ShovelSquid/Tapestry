---
phase: 02.2-obsidian-bridge
plan: 12
type: execute
wave: 12
depends_on:
  - 02.2-11
files_modified:
  - app/src/main/commands/vault-notes.ts
  - app/src/main/commands/vault-structure.test.ts
  - app/src/main/obsidian/writer.ts
  - app/src/main/obsidian/vault-service.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/components/FolderGroup.tsx
  - app/src/renderer/components/PlaceholderNote.tsx
  - app/src/renderer/components/LinkLineInput.tsx
autonomous: true
requirements:
  - D-26
  - D-27
  - D-32
  - D-34

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Double-clicking empty space inside a vault folder group creates `Untitled.md` in that folder (a second one is `Untitled 1.md`), commits it as the human, and focuses its title so typing a title renames the file (D-27, D-28)"
    - "Moving a note to another group moves its file to that folder, a write to the source, keeping the note's id, connections and history; dropping outside every group moves it to the vault root (D-26)"
    - "Drawing a connection between two vault notes appends a `[[link]]` line to the source note's file, `[[Max]]` or the typed line such as `best friends with [[Max]]`, so Obsidian sees the link (D-32); cross-tree connections are never written into .md (UA-07)"
    - "An appended line keeps the file's trailing-newline convention: a file ending in a newline gains `line\\n`; a file without a trailing newline gains `\\nline`"
    - "Typing the first character into a placeholder creates `[link text].md` in the placeholder's group folder at the placeholder's position, replaces the placeholder with the new note and turns the dashed link solid (D-34, UA-08)"
    - "For agents, a link line may be added only to a vault note the agent created; the tool explains how to connect from its own note instead (unattended default: D-05 wins over D-32 for agents)"
    - "Frame renders at minimum 480x320 with the vault empty-state copy centered; double-click inside creates Untitled.md in the vault root"
    - "Dashed, unfilled card showing the link text as title and the placeholder hint; first typed character creates the .md file"
    - "Target group shows refused style during hover; drop snaps the note back and shows the collision copy under the group label for 6s"
  artifacts:
    - path: app/src/main/commands/vault-notes.ts
      provides: "createNote, moveToFolder, appendLinkLine, createFromPlaceholder"
      contains: "appendLinkLine"
    - path: app/src/renderer/components/LinkLineInput.tsx
      provides: "Inline line input prefilled [[Target]]"
    - path: app/src/main/commands/vault-structure.test.ts
      provides: "Integration tests for structure writes on temp vaults"
  key_links:
    - from: app/src/renderer/components/LinkLineInput.tsx
      to: app/src/main/commands/vault-notes.ts
      via: "Enter -> window.tapestry.vault.appendLinkLine(treeId, from, to, line) -> VaultNoteCommands.appendLinkLine(getHumanActor())"
      pattern: "appendLinkLine"
    - from: app/src/renderer/components/PlaceholderNote.tsx
      to: app/src/main/obsidian/writer.ts
      via: "first keystroke -> vault.createFromPlaceholder -> commit -> writer.createNow"
      pattern: "createNow\\("
---

<objective>
Vault structure can be changed from Tapestry, and every change lands in the source:
- a new note becomes `Title.md` in the folder of the group it was placed in (D-27);
- moving a note between groups moves its file (D-26);
- connecting two vault notes appends a link line Obsidian can see (D-32);
- typing into a placeholder creates the missing file (D-34).

Purpose: this completes the "both are places to edit" half of D-10 for structure, not just text. Every change goes through the shared VaultNoteCommands and the writer from Plan 11.
Output: createNote, moveToFolder, appendLinkLine, createFromPlaceholder, their renderer interactions (double-click in groups, group drop targets, the link line input, placeholder typing) and integration tests.

Concurrency: other sessions hold uncommitted work in `.planning/spikes/**` and `.planning/phases/02.3-*`. This plan's executor must never stage those paths, and asserts a clean working tree for its own `files_modified` before editing (shared preflight, Plan 01).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md
@.planning/phases/02.2-obsidian-bridge/02.2-11-SUMMARY.md
@app/src/main/commands/vault-notes.ts
@app/src/main/obsidian/writer.ts
@app/src/renderer/components/FolderGroup.tsx
@app/src/renderer/components/PlaceholderNote.tsx

<interfaces>
From Plans 07-11:
- VaultNoteCommands(registry, vaults, hooks): updateText, retitle, deleteNote; isValidVaultTitle; the retitle collision copy "[Folder] already has a note named [name]."
- VaultService: planTextChange(treeId, rel, text), planStructuralChange(treeId, { renames, overrides }), writerFor(treeId), undo stacks (push a unit for each command).
- VaultWriter: scheduleWrite, flush, renameNow(from, to, base), removeNow(rel, base), createNow(rel, text) (COPYFILE_EXCL), isOwnEcho.
- paths.ts: isSafeVaultRel, resolveWritableVaultPath; scan.ts scanMarkdown; resolve.ts buildVaultIndex, resolveLink.
- History index createdBy per node (Plan 02) for the agent own-note rule.
- Renderer: groups.ts computeGroupBounds; FolderGroup; PlaceholderNote (dashed, hint, not editable yet); Canvas connecting mode (drag connect handle, handleEdgeCreate for native trees); useAnnounce; TransientNotice.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "double-click inside the Characters group and get Characters/Untitled.md"</name>
  <precondition>Working tree is clean for this plan's files: `git status --porcelain -- <every path in this plan's files_modified>` prints nothing before any edit. Other active sessions hold uncommitted work in `.planning/spikes/**` and `.planning/phases/02.3-*`; those paths are never staged by this phase. If the check prints anything, stop and report rather than committing another session's work.</precondition>
  <files>
    app/src/main/commands/vault-notes.ts,
    app/src/main/commands/vault-structure.test.ts,
    app/src/main/obsidian/writer.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/components/TreeFrame.tsx,
    app/src/renderer/components/FolderGroup.tsx,
    app/src/renderer/App.tsx
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Folder groups (D-26, D-27)" (double-click rows), "Copywriting Contract" (empty vault frame), "UI Considerations" empty rows,
    .planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md "Assumptions Log" A6 (Untitled 1.md),
    app/src/main/commands/vault-notes.ts,
    app/src/main/obsidian/writer.ts,
    app/src/renderer/components/Canvas.tsx (double-click handling from Plan 05)
  </read_first>
  <action>
    **app/src/main/commands/vault-notes.ts**: add createNote(actor, { tree, folder: string, x: number, y: number, title?: string }): CommandResult<{ note: string; rel: string }>. The steps:
    1. Validate the tree: kind vault and journal Ok.
    2. Validate the folder: '' for root, or a folder node md.path in this tree that exists on disk as a directory.
    3. Choose the name. When title is omitted, the base is 'Untitled' and it takes the first of 'Untitled', 'Untitled 1', 'Untitled 2', … with no case-insensitive collision in that folder (disk or tree). A title passed explicitly must pass isValidVaultTitle and must not collide, else return the collision copy.
    4. Build rel = folder ? `${folder}/${name}.md` : `${name}.md`.
    5. Build ops with planStructuralChange(treeId, { overrides: new Map([[rel, '']]) }). Include the createNode for that path at local (x, y) with width 280, and any placeholder conversions (a new file may satisfy existing links).
    6. Commit with submitAs(actor, `create ${rel}`, ops).
    7. Call writer.createNow(rel, ''), push an undo unit and return.

    **app/src/main/commands/vault-structure.test.ts** (integration: makeTempVault, VaultService, VaultNoteCommands, real watcher and timers):
    - Two createNote calls in 'Characters' produce files Characters/Untitled.md and 'Characters/Untitled 1.md', each with a commit signed `actor human user.kaelen`.
    - No observed commit follows within 3 s.
    - retitle of the first to 'Luna' produces Characters/Luna.md.
    - createNote with folder '' in an empty vault (makeTempDir with a .obsidian folder only) creates Untitled.md at the root.

    **index.ts**: register vault:createNote(treeId, folder, x, y), stamping getHumanActor(). Expose it in preload and global.d.ts.

    **Canvas and TreeFrame**: for a vault frame, a double-click on empty space works out the folder from the pointer's frame-local point:
    - inside the deepest FolderGroup bounds (computeGroupBounds) → that folder;
    - inside the frame but outside groups → the root.

    It calls vault.createNote, waits for the tree refresh, then puts the new card into title-editing mode with the title text selected; typing a title uses retitle from Plan 11.

    **Empty vault frame** (no note nodes): TreeFrame shows centered Body text "This vault has no notes yet. Double-click inside the frame to create Untitled.md." at the 480x320 minimum.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/vault-structure.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: double-click inside Characters in a temp House Party frame; Characters/Untitled.md appears in Finder, the title input is focused, typing "Luna" and Enter renames it to Luna.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "createNote(" app/src/main/commands/vault-notes.ts` prints the method definition
    - `grep -n "This vault has no notes yet. Double-click inside the frame to create Untitled.md." app/src/renderer/components/TreeFrame.tsx` prints one match
    - `grep -n "vault:createNote" app/src/main/index.ts app/src/preload/index.ts` prints a match in each file
    - vault-structure.test.ts passes the Untitled, Untitled 1, retitle and empty-vault cases
  </acceptance_criteria>
  <done>New vault notes are created as real files in the folder where Kaelen places them, and titling them names the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Moving a note between groups moves its file</name>
  <files>
    app/src/main/commands/vault-notes.ts,
    app/src/main/commands/vault-structure.test.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/components/FolderGroup.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Folder group visual states (D-26)" (move drop target, move refused), "Folder groups (D-26, D-27)" (note dragged over another group, dropped rows), "Copywriting Contract" (Move refused, collision), "Screen-reader announcements" (folder move target / refusal),
    app/src/main/commands/vault-notes.ts,
    app/src/renderer/components/FolderGroup.tsx
  </read_first>
  <behavior>
    - moveToFolder of Characters/Rody.md to '' (root) renames the file to Rody.md, keeps the node id and its wikilink edges, and updates path-form links `[[Characters/Rody]]` in other notes to `[[Rody]]`
    - moveToFolder to a folder that already has Rody.md (case-insensitive) returns "Concepts already has a note named Rody. Rename one first." with no commit and no file change
    - checkMove returns { ok: false, error } for that collision and { ok: true } otherwise, without committing
  </behavior>
  <action>
    Write the vault-structure.test.ts cases for the behaviors listed first.

    In vault-notes.ts add two methods:
    - checkMove({ tree, note, folder }): CommandResult<null>, read-only.
    - moveToFolder(actor, { tree, note, folder, x, y }).
      1. Compute newRel as the folder joined with the basename.
      2. Refuse a collision with "[Folder name, or This vault] already has a note named [name]. Rename one first."
      3. Rewrite path-form links in other notes that resolved to this note, from the old folder path to the new one or to the bare name.
      4. Build ops with planStructuralChange(treeId, { renames: [[oldRel, newRel]], overrides }) plus position.x and position.y.
      5. Commit with submitAs(actor, `move ${oldRel} -> ${newRel}`).
      6. Call writer.renameNow and the rewrite writes, then push an undo unit.

    In index.ts register vault:checkMove and vault:moveNote, stamping the human actor for moveNote. Expose both in preload and global.d.ts.

    Canvas: while a vault note is dragged, find the group under the pointer (deepest bounds). When it differs from the note's own folder:
    - Call vault.checkMove, throttled to one call per group change.
    - Style the target: move drop target is a 2px solid #4A7CFF border; move refused is a 2px dashed #E5484D border with the refusal text under the group label.
    - Announce once per target change through useAnnounce polite: "Move to [folder]" or the refusal copy.

    On drop:
    - Over another group: call vault.moveNote with the drop's local position.
    - Over the frame outside every group: move to the root.
    - Refused: snap the note back to its original position and keep the collision copy under the group label for 6 s.
    - Within its own group: the position commit behaves as in Plan 07.

    Add the CSS classes tapestry-folder-group--drop-target, tapestry-folder-group--refused and tapestry-folder-refusal.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/vault-structure.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: drag Rody from Characters onto the frame background in a temp House Party copy; Rody.md moves to the vault root in Finder and its lines stay; create Concepts/Rody.md first and repeat the drag onto Concepts: the group shows the red dashed refusal and the note snaps back.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Rename one first." app/src/main/commands/vault-notes.ts` prints one match
    - `grep -n "vault:moveNote" app/src/main/index.ts app/src/preload/index.ts` prints a match in each file
    - `grep -n "tapestry-folder-group--refused" app/src/renderer/App.css` prints a match
    - vault-structure.test.ts passes the move, collision and checkMove behaviors
  </acceptance_criteria>
  <done>Moving a note between groups moves its file safely, with visible refusal on collisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connections add link lines, and typing into a placeholder creates its file</name>
  <files>
    app/src/main/commands/vault-notes.ts,
    app/src/main/commands/vault-structure.test.ts,
    app/src/main/obsidian/vault-service.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/LinkLineInput.tsx,
    app/src/renderer/components/PlaceholderNote.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Connections" (drag connect handle vault-to-vault and cross-tree rows; new vault-to-vault connection line input copy), "Placeholder notes (D-34)", "Assumptions for Review" UA-07, UA-08, UA-12,
    .planning/phases/02.2-obsidian-bridge/02.2-CONTEXT.md D-05, D-32, D-34,
    app/src/main/commands/vault-notes.ts,
    app/src/renderer/components/PlaceholderNote.tsx,
    app/src/renderer/components/Canvas.tsx (connecting mode)
  </read_first>
  <behavior>
    - appendLinkLine as user.kaelen from Characters/Rody.md (ending in a newline) to Characters/Max.md with line 'best friends with [[Max]]' makes the file end with 'best friends with [[Max]]\n' and creates a wikilink edge whose md.line is that line
    - The same on a file without a trailing newline appends '\nbest friends with [[Max]]' and still ends without a newline
    - A line that does not contain a link resolving to the target note is refused with "The line must contain a link to Max"
    - When the target's basename is ambiguous, the prefilled line uses the folder path form `[[Characters/Max]]`
    - appendLinkLine as agent.claude on a note created by user.kaelen is refused with "agent.claude may add link lines only to notes it created; draw the connection from its own note instead"; on a note agent.claude created it succeeds
    - createFromPlaceholder on Welcome.md's "create a link" placeholder with text 'Hello' creates 'create a link.md' at the vault root containing 'Hello', removes the placeholder node, keeps the new note at the placeholder's position, and turns Welcome.md's edge into a wikilink edge to the new note
  </behavior>
  <action>
    Write the vault-structure.test.ts cases for the behaviors listed first.

    In vault-notes.ts add three methods.

    prefillLinkLine({ tree, to }): string. It returns `[[${basename}]]`, or `[[${pathWithoutExt}]]` when resolveLink(basename) is ambiguous.

    appendLinkLine(actor, { tree, from, to, line }). The steps:
    1. Both notes must be vault notes in the same tree; otherwise return 'Connections between trees are recorded by Tapestry, not written into notes', and callers use the cross-tree path in Plan 15.
    2. The line must be a single line (no '\n' or '\r') of at most 1,000 characters.
    3. scanMarkdown(line) must contain a link that resolveLink resolves to the `to` note's md.path. Otherwise return `The line must contain a link to ${basename}`.
    4. When actor.id starts with 'agent.', history createdBy of `from` must equal the actor; otherwise return the refusal in the behavior list.
    5. Compute newText. When the text is empty or ends with '\n', it is text + line + '\n'; otherwise it is text + '\n' + line.
    6. Reuse updateText's planning with message `link ${fromRel} -> ${toRel}`, then flush the writer immediately (no 1 s wait for a deliberate structural act).

    createFromPlaceholder(actor, { tree, placeholder, text }). The steps:
    1. Read md.link.
    2. The folder is the directory of the first note linking to the placeholder (UA-08). When the link text contains '/', use that path instead.
    3. The name must pass isValidVaultTitle for its last segment; otherwise return the title validation copy.
    4. rel = `${folder}/${link}.md`.
    5. Build ops with planStructuralChange(treeId, { overrides: new Map([[rel, text]]) }). This creates the note at the placeholder's position, deletes the placeholder and creates resolved edges; the placeholder position is passed as a layout override.
    6. Commit with `create ${rel} from placeholder`, then call writer.createNow(rel, text) and push an undo unit.

    In index.ts register vault:prefillLinkLine, vault:appendLinkLine and vault:createFromPlaceholder, stamping the human actor. Expose them in preload and global.d.ts.

    Canvas connecting mode:
    - When a connect drag starts and ends on two different vault notes of the same tree, do not create an edge directly. Show LinkLineInput at the line midpoint.
    - When the ends are in different trees, leave the existing behavior; Plan 15 creates cross-tree connections, and nothing is written to .md (UA-07).

    Create LinkLineInput.tsx:
    - Label "Line to add to [source file]".
    - A text input prefilled from vault.prefillLinkLine with the caret at the start.
    - Hint "Enter to add · Escape to cancel".
    - Enter calls vault.appendLinkLine and shows an error under the input.
    - Escape cancels, with nothing written or drawn.

    PlaceholderNote:
    - Clicking the body enters edit mode with a textarea; the title stays the link text and is not editable.
    - The first character typed calls vault.createFromPlaceholder with the typed text. The renderer keeps the textarea focused and swaps in the new VaultNoteCard at the same position with no jump, and further typing flows through updateText.
    - Escape before typing leaves edit mode and creates nothing.

    Add the CSS classes tapestry-link-line-input and tapestry-placeholder-note--editing.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/vault-structure.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: connect Rody to Max in a temp House Party frame, edit the prefilled line to "best friends with [[Max]]" and press Enter; Rody.md gains that line and Obsidian's graph shows the link. Click the "create a link" placeholder and type "Hello": "create a link.md" appears at the vault root and the dashed line turns solid.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Line to add to" app/src/renderer/components/LinkLineInput.tsx` prints one match
    - `grep -n "Enter to add · Escape to cancel" app/src/renderer/components/LinkLineInput.tsx` prints one match
    - `grep -n "may add link lines only to notes it created" app/src/main/commands/vault-notes.ts` prints one match
    - `grep -n "createFromPlaceholder" app/src/main/commands/vault-notes.ts app/src/renderer/components/PlaceholderNote.tsx` prints a match in each file
    - vault-structure.test.ts passes the append (both newline conventions), refusal, ambiguous prefill, agent rule and placeholder behaviors
  </acceptance_criteria>
  <done>Connections between vault notes become real link lines in the files, and missing notes can be brought into existence by typing into their placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer / agents -> structural vault commands | Titles, folders, link lines and placeholder text become file names and file content |
| main -> vault filesystem | Creates, renames and appends to user files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-56 | Elevation of Privilege | createNote / moveToFolder / createFromPlaceholder paths | high | mitigate | Folder must be an existing folder node in the tree; names pass isValidVaultTitle; every target goes through resolveWritableVaultPath; creates use COPYFILE_EXCL so nothing is overwritten |
| T-02.2-57 | Tampering | agent link lines in others' files | high | mitigate | appendLinkLine refuses agent actors on notes they did not create (history createdBy), per the recorded D-05-over-D-32 default for agents |
| T-02.2-58 | Tampering | link line injection (extra lines) | medium | mitigate | Line must be single-line, at most 1,000 characters, and contain a link resolving to the target; newline convention preserved |
| T-02.2-59 | Tampering | collisions on move/create | medium | mitigate | Case-insensitive collision checks against disk and tree before commit; refusal copy shown; no automatic suffixing for explicit titles |
| T-02.2-60 | Repudiation | structural changes attribution | low | mitigate | Every structural command commits with the stamped actor before touching the file, and pushes an undo unit |
</threat_model>

<verification>
- vault-structure tests pass; typecheck and build:js pass
</verification>

<success_criteria>
- New notes, moves, link lines and placeholder creations made in Tapestry all land in the vault's files with honest history
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `VaultNoteCommands.createNote`, `checkMove`, `moveToFolder`, `prefillLinkLine`, `appendLinkLine`, `createFromPlaceholder` | methods | app/src/main/commands/vault-notes.ts |
| `vault:createNote`, `vault:checkMove`, `vault:moveNote`, `vault:prefillLinkLine`, `vault:appendLinkLine`, `vault:createFromPlaceholder` | IPC | index.ts, preload |
| `LinkLineInput` | React component | app/src/renderer/components/LinkLineInput.tsx |
| `tapestry-folder-group--drop-target`, `tapestry-folder-group--refused`, `tapestry-folder-refusal`, `tapestry-link-line-input`, `tapestry-placeholder-note--editing` | CSS classes | App.css |
| commit messages `create <rel>`, `move <a> -> <b>`, `link <a> -> <b>`, `create <rel> from placeholder` | history conventions | vault journals |

<output>
Create `.planning/phases/02.2-obsidian-bridge/02.2-12-SUMMARY.md` when done
</output>
