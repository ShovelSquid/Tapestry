---
phase: 02.2-obsidian-bridge
plan: 14
type: execute
wave: 14
depends_on:
  - 02.2-13
files_modified:
  - package-lock.json
  - app/package.json
  - app/src/main/commands/forest.ts
  - app/src/main/commands/forest-copy.test.ts
  - app/src/main/commands/vault-notes.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/editor/to-markdown.ts
  - app/src/renderer/editor/to-markdown.test.ts
  - app/src/renderer/components/LossyCopyDialog.tsx
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
autonomous: true
requirements:
  - D-12
  - D-17

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dragging a note into another tree's frame creates a copy at the drop point, connected back to the original with a grew-from connection recorded in both trees; the original snaps back and nothing disappears from its source tree (D-17)"
    - "While a note is dragged over another tree's frame, that frame shows the copy drop target style, the cursor is copy, a chip reads `Copy to [tree name]`, and screen readers hear `Copy to [tree name]` once per target change and `Not copying` on leaving"
    - "Tapestry-native notes keep editor JSON; conversion to Markdown happens only when a native note is copied into a vault (D-12)"
    - "The Markdown serializer emits exact Markdown for every supported node and mark and reports every text color, font, alignment and passage link it cannot store; an unknown node type is an error, never a silent drop"
    - "Copying a native note that uses text color, font, alignment or passage links into a vault first shows the lossy copy dialog with Copy as Markdown, Keep look with HTML and Don't copy; the note is never silently degraded; a copy with no losses asks nothing"
    - "Keep look with HTML writes only colors and fonts that pass the existing allow-lists, with text escaped"
    - "A copy into a vault becomes `Title.md` in the folder of the group it was dropped in, with invalid title characters removed and a numeric suffix on collision; no cross-tree link is written into the file"
    - statement: "Loss detection is synchronous over the ProseMirror doc; drop shows no spinner. Needs a visual check with a long note"
      verification: backstop
    - statement: "At most four lines by construction; long note title in the dialog title wraps to 2 lines then ellipsis"
      verification: backstop
    - statement: "LossyCopyDialog probe coverage (loading, error, overflow, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What is shown when the load or submit fails?' (a failed copy shows the reason under the options and the original stays unchanged)"
      verification: backstop
  artifacts:
    - path: app/src/renderer/editor/to-markdown.ts
      provides: "ProseMirror JSON -> Markdown with loss report and HTML option"
      exports: ["serializeNoteToMarkdown", "analyzeMarkdownLoss", "MarkdownLossReport"]
    - path: app/src/renderer/components/LossyCopyDialog.tsx
      provides: "Never-silent copy dialog listing formatting losses"
    - path: app/src/main/commands/forest-copy.test.ts
      provides: "Copy integration tests across native and vault trees"
  key_links:
    - from: app/src/renderer/components/Canvas.tsx
      to: app/src/main/commands/forest.ts
      via: "note drop on another frame -> (native->vault: analyzeMarkdownLoss -> LossyCopyDialog -> serializeNoteToMarkdown) -> window.tapestry.forest.copyNote -> ForestCommands.copyNote(getHumanActor())"
      pattern: "copyNote\\("
    - from: app/src/main/commands/forest.ts
      to: app/src/main/commands/vault-notes.ts
      via: "copy into a vault -> VaultNoteCommands.createNote(actor, { folder, x, y, title, text }) -> writer.createNow"
      pattern: "createNote\\("
---

<objective>
Notes can be copied between trees by dragging them into another frame. The copy is connected back to the original in both trees, and the original is never moved or removed (D-17). When a Tapestry-native note is copied into a vault, and only then, its editor JSON is converted to Markdown (D-12). If the conversion would lose formatting, Kaelen sees exactly what would be lost and chooses; nothing is degraded silently.

Purpose: this completes the forest interactions. The research open question is resolved as the explicit loss dialog, never silent.
Output: ForestCommands.copyNote for all four tree-kind combinations, the drag-copy UI, to-markdown.ts with tests, and LossyCopyDialog.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-obsidian-bridge/02.2-CONTEXT.md
@.planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md
@.planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md
@.planning/phases/02.2-obsidian-bridge/02.2-13-SUMMARY.md
@app/src/renderer/editor/schema.ts
@app/src/main/commands/forest.ts
@app/src/main/commands/vault-notes.ts

<interfaces>
From the code and Plans 01-13:
- app/src/renderer/editor/schema.ts: tapestrySchema = basic schema nodes (doc, paragraph with align attr, blockquote, horizontal_rule, heading with align attr, code_block, text, image, hard_break) plus addListNodes (ordered_list, bullet_list, list_item); marks link, em, strong, code, passage (anchorId), textColor (color), fontFamily (family); exports isValidTextColor, isValidFontFamily, DEFAULT_TEXT_COLOR, TEXT_COLORS, FONT_FAMILIES. Read the file for exact node and attribute names before writing serializers.
- Native note body: JSON.stringify(doc.toJSON()); title prop text.
- ForestCommands.connectAcrossTrees(actor, { from, to, label?, kind: 'cross-tree' | 'grew-from' }) writes the target end first; grew-from direction: new copy is the `from` side.
- VaultNoteCommands.createNote(actor, { tree, folder, x, y, title? }) creates Title.md (Untitled suffixing), commits, writer.createNow; isValidVaultTitle; the collision copy.
- NoteCommands: plainTextToDocJson, docJsonToPlainText, NATIVE_NOTE_TYPE.
- Renderer: Canvas drag of notes within a frame, computeFrameBounds, computeGroupBounds, useAnnounce, Dialog shell, TreeFrame frame states (Plan 05 added idle/hover/selected/dragging).
- prosemirror-markdown 1.13.5: MarkdownSerializer(nodes, marks) with serialize(doc); dependencies markdown-it ^14 and prosemirror-model ^1.25.0 (installed prosemirror-model is 1.25.11). API shape is from training data (RESEARCH A7): confirm against the installed package's type declarations before use.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "drag a note into another world's frame and get a connected copy"</name>
  <files>
    app/src/main/commands/forest.ts,
    app/src/main/commands/forest-copy.test.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/components/TreeFrame.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Tree frame visual states" (Copy drop target row), "Tree frames (D-15, D-16, D-17)" (note dragged over / dropped on another tree's frame rows), "Connections" (Copy drop-target chip), "Screen-reader announcements" (copy drop-target chip),
    app/src/main/commands/forest.ts,
    app/src/renderer/components/Canvas.tsx (note drag handling),
    app/src/renderer/components/TreeFrame.tsx
  </read_first>
  <action>
    In forest.ts add copyNote(actor, { from: { tree, note }, to: { tree, x, y, folder?: string }, markdown?: string }): CommandResult<{ note: string; pair: string }>. Both trees must be open and Ok, their ids must differ, and the source note must be live. This task implements the two combinations that target a Tapestry world:
    - **world to world:** read the source's title, body and width, and create a NATIVE_NOTE_TYPE node in the target at (x, y) with the same title, body JSON and width.
    - **vault to world:** title = basename of md.path; body = plainTextToDocJson(md.text).

    Combine the copy and its target-side stub into one target commit: createNode for the copy, createNode for the stub, and createEdge from the copy to the stub with label grew-from. Use message `copy of ${sourceName} · ${sourceTitle}`. Then commit the source side with the grew-from edge from the source stub to the original. This reuses connectAcrossTrees internals: refactor its target and source halves into private helpers that accept extra leading ops.

    In index.ts register forest:copyNote(from, to, markdown?), stamping getHumanActor(), and emit 'tree-changed' for both trees. Expose forest.copyNote in preload and global.d.ts.

    Canvas: while dragging a note, hit-test the pointer against the other trees' frame rects.
    - Over another tree's frame:
      - TreeFrame applies the copy drop target style: 2px solid #4A7CFF border plus box-shadow 0 0 0 4px rgba(74,124,255,0.20).
      - The canvas cursor is 'copy'.
      - A pointer-following chip (aria-hidden, class tapestry-copy-chip) reads "Copy to [tree name]".
      - useAnnounce polite says "Copy to [tree name]" when the target changes, and "Not copying" when the pointer leaves every other frame.
    - On drop over another frame:
      1. The original snaps back to its start position, and no position commit is made in the source tree.
      2. Compute the drop's frame-local point in the target, and the target folder from computeGroupBounds when the target is a vault.
      3. Call forest.copyNote. A vault target goes through Task 3's flow first.
    - A drop outside every frame keeps the carried behavior: a move within its own frame.

    forest-copy.test.ts (tracer case):
    1. Create two temp worlds; world A has a note n1 with title 'Idea' and a ProseMirror body with a bold mark.
    2. copyNote as humanActor('kaelen') to world B at (100, 50).
    3. B has a new note with identical title and body JSON at position (100, 50), a tapestry.forest/remote@1 stub naming A and n1, and an edge `create-edge eK <copy> <stub> grew-from`.
    4. A has a stub naming B and the copy, with a grew-from edge to n1.
    5. A's n1 is unchanged: same props, same position.
    6. Both commits are signed user.kaelen.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/forest-copy.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: drag a note from the we frame over a second world's frame; the frame shows the accent ring and "Copy to …" chip; on release a copy appears there joined to the original by an arrowed line, and the original returns to its place.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "copyNote(" app/src/main/commands/forest.ts` prints the method definition
    - `grep -n "Copy to" app/src/renderer/components/Canvas.tsx` prints a match
    - `grep -n "Not copying" app/src/renderer/components/Canvas.tsx` prints one match
    - `grep -n "rgba(74, 124, 255, 0.2\|rgba(74,124,255,0.20)" app/src/renderer/App.css` prints a match
    - forest-copy.test.ts tracer case passes
  </acceptance_criteria>
  <done>Dragging a note into another world's frame copies it with a two-sided grew-from connection while the original stays put.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Markdown serializer that reports every formatting loss</name>
  <files>
    package-lock.json,
    app/package.json,
    app/src/renderer/editor/to-markdown.ts,
    app/src/renderer/editor/to-markdown.test.ts
  </files>
  <read_first>
    app/src/renderer/editor/schema.ts (whole file),
    .planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md section "ProseMirror -> Markdown (D-12, D-17)" and "Security Domain" (style injection row),
    node_modules/prosemirror-markdown type declarations after install (MarkdownSerializer constructor and serialize signature)
  </read_first>
  <behavior>
    - A doc with heading level 2 "Title" and a paragraph "Hello" serializes to "## Title\n\nHello"
    - strong, em, code and link marks serialize to **b**, *i*, `c` and [text](https://x)
    - bullet_list ["a", "b"] serializes to "- a\n- b" (or the serializer's canonical bullet marker, asserted exactly); ordered_list to "1. a\n2. b"; blockquote to "> q"; code_block to a fenced block; horizontal_rule to "---"; hard_break inside a paragraph keeps the line break
    - Markdown special characters in text are escaped (for example a literal * stays literal after a round trip through markdown-it)
    - analyzeMarkdownLoss counts textColor marks (places), fontFamily marks (places), non-null align attrs (paragraphs/headings) and passage marks
    - With keepLookWithHtml false, color/font/align produce no HTML and are counted as losses; passage marks are always dropped and counted
    - With keepLookWithHtml true, a textColor #E5484D span serializes as <span style="color:#E5484D">text</span>, fontFamily as <span style="font-family: …">, align center as <p align="center">…</p>; a color or family failing isValidTextColor/isValidFontFamily is not emitted
    - A doc containing a node type the serializer does not know throws "Unsupported node type <name>"
  </behavior>
  <action>
    **Dependency.** From the repo root run `npm install -w app --save-exact prosemirror-markdown@1.13.5`. Legitimacy record: prosemirror-markdown 1.13.5 was published 2026-07-06, 71 days before planning, from github.com/ProseMirror, with 6.5M weekly downloads and no postinstall. The SUS flag applied to the latest 1.13.7, and 1.13.6 sits exactly at the 30-day boundary, so the older 1.13.5 is pinned. It requires prosemirror-model ^1.25.0; 1.25.11 is installed.

    Write to-markdown.test.ts first, covering the behaviors listed. Build docs with tapestrySchema.nodeFromJSON.

    Create app/src/renderer/editor/to-markdown.ts exporting:
    - type MarkdownLossReport = { textColor: number; fontFamily: number; align: number; passage: number };
    - analyzeMarkdownLoss(docJson: unknown): MarkdownLossReport;
    - serializeNoteToMarkdown(docJson: unknown, opts: { keepLookWithHtml: boolean }): { markdown: string; losses: MarkdownLossReport }.

    Build one MarkdownSerializer with explicit node serializers for every node in tapestrySchema and mark serializers for every mark. passage always serializes to its text only. textColor, fontFamily and align emit HTML only when keepLookWithHtml is set and the value passes isValidTextColor or isValidFontFamily, and HTML-escape their text content. Before serializing, walk the doc and throw Error(`Unsupported node type ${name}`) for any node or mark not covered. A non-JSON or invalid body is treated as plain text: one paragraph per line, zero losses.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/editor/to-markdown.test.ts && npm --prefix app run typecheck</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - app/package.json dependencies contain "prosemirror-markdown": "1.13.5"
    - `grep -n "Unsupported node type" app/src/renderer/editor/to-markdown.ts` prints one match
    - `grep -n "isValidTextColor" app/src/renderer/editor/to-markdown.ts` prints a match
    - to-markdown.test.ts passes all eight behaviors
  </acceptance_criteria>
  <done>Native note JSON converts to exact Markdown with a complete, testable account of what cannot be stored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Copy into a vault with the lossy copy dialog, and vault-to-vault copies</name>
  <files>
    app/src/renderer/components/LossyCopyDialog.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css,
    app/src/main/commands/forest.ts,
    app/src/main/commands/vault-notes.ts,
    app/src/main/commands/forest-copy.test.ts
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Lossy copy dialog (D-12, D-17; research open question: never silent)", "Dialogs (shared shell)" (lossy option layout), "Visual Hierarchy" (lossy copy row), "Assumptions for Review" UA-10,
    app/src/renderer/editor/to-markdown.ts,
    app/src/renderer/components/Dialog.tsx,
    app/src/main/commands/vault-notes.ts (createNote),
    app/src/main/commands/forest.ts (copyNote)
  </read_first>
  <behavior>
    - copyNote from a native note to a vault with markdown "# Idea\n\nHello" and folder 'Characters' creates Characters/Idea.md containing exactly that markdown plus a trailing newline, commits as user.kaelen, and records grew-from stubs in both trees; the .md contains no link to the other tree
    - A native title "A/B: C#1" becomes the file name "AB C1.md"; an empty result becomes "Untitled.md"; a collision adds " 1", " 2"
    - copyNote from a vault note to another vault copies md.text byte for byte into the drop folder
    - copyNote into a vault without markdown for a native source is refused with "Markdown is required when copying a Tapestry note into a vault"
  </behavior>
  <action>
    Write the forest-copy.test.ts cases for the behaviors listed first.

    **app/src/main/commands/vault-notes.ts:** extend createNote to accept text?: string (default ''). The file is created with that text and the commit sets md.text and md.sha256 accordingly.

    **forest.ts copyNote, vault targets:**
    - A world source requires `markdown` (else the refusal copy).
      1. Make the title safe: remove / \ : # | ^ [ ], trim, strip leading dots, cap at 200 characters, fall back to 'Untitled', and add ' 1', ' 2' … on collision in the folder.
      2. Call VaultNoteCommands.createNote(actor, { tree: target, folder, x, y, title, text: markdown ending with '\n' }).
      3. Then add the grew-from stubs with the two-sided helper, target first; the target commit is the stub plus edge, since the note commit already exists.
    - A vault source copies md.text verbatim with title = basename.
    - Cross-tree links are never written into the new file (UA-07).

    **LossyCopyDialog.tsx** (Dialog shell):
    - Title: "Copy \"[note title]\" into [vault name]?", wrapping to 2 lines then an ellipsis.
    - Body: "Obsidian notes are Markdown files. Markdown can't store some of this note's formatting:".
    - A loss list showing only the lines that apply: "Text color ([n] places)", "Font ([n] places)", "Alignment ([n] paragraphs)", "Passage links ([n]) -- the copy won't have them".
    - Reassurance line: "The original note in [tree name] stays unchanged."
    - Options, as three stacked full-width buttons, each with its helper line below in Muted Label:
      - **Copy as Markdown** (primary, initial focus), helper "Formatting above is left out.";
      - **Keep look with HTML**, helper "Colors, fonts and alignment are written as HTML tags. Obsidian shows them, but the file is harder to read as plain text.";
      - then **Don't copy**, right-aligned, also bound to Escape.
    - A failed copy shows its reason under the options, and the original is unchanged.

    **Canvas and App**, on a drop of a Tapestry note onto a vault frame:
    1. Parse the source body and run analyzeMarkdownLoss.
    2. When every count is 0, call serializeNoteToMarkdown(…, { keepLookWithHtml: false }) and forest.copyNote directly, with no dialog (copying without formatting loss needs no confirmation).
    3. Otherwise open LossyCopyDialog. Copy as Markdown serializes with keepLookWithHtml false, Keep look with HTML with true, and Don't copy removes the pending copy with nothing written.

    A vault note dropped on another vault or on a world copies directly.

    Add the CSS classes tapestry-lossy-options, tapestry-lossy-option and tapestry-lossy-helper.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/commands/forest-copy.test.ts src/renderer/editor/to-markdown.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js && grep -n "Keep look with HTML" app/src/renderer/components/LossyCopyDialog.tsx && grep -n "stays unchanged." app/src/renderer/components/LossyCopyDialog.tsx</automated>
    <fails_when>non-zero exit (including any grep finding no match), or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: color part of a we note red and center a paragraph, drag it into the Characters group of a temp House Party frame; the dialog lists "Text color (1 places)" and "Alignment (1 paragraphs)"; Keep look with HTML writes Characters/<title>.md with a span and p align that Obsidian renders; Don't copy writes nothing; the original note is unchanged in we.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Obsidian notes are Markdown files." app/src/renderer/components/LossyCopyDialog.tsx` prints one match
    - `grep -n "Don't copy" app/src/renderer/components/LossyCopyDialog.tsx` prints one match
    - `grep -n "Markdown is required when copying a Tapestry note into a vault" app/src/main/commands/forest.ts` prints one match
    - forest-copy.test.ts passes the native-to-vault, title sanitizing, vault-to-vault and refusal behaviors
  </acceptance_criteria>
  <done>Copies into vaults become real Markdown files in the drop folder, with every formatting loss stated and chosen by Kaelen before anything is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer serializer -> main (markdown string) | Renderer produces file content for a vault |
| native note formatting -> HTML in a vault file | Attribute values become inline HTML read by Obsidian |
| npm registry -> app | prosemirror-markdown install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-74 | Tampering | style injection via Keep look with HTML | high | mitigate | Colors and fonts emitted only when isValidTextColor/isValidFontFamily pass; text HTML-escaped (to-markdown.test.ts) |
| T-02.2-75 | Tampering | silent formatting degradation | high | mitigate | analyzeMarkdownLoss runs before every native-to-vault copy; any loss opens LossyCopyDialog; unknown nodes throw instead of vanishing |
| T-02.2-76 | Elevation of Privilege | copied titles as file names | high | mitigate | Forbidden characters removed, leading dots stripped, collisions suffixed, and creation through VaultNoteCommands.createNote (resolveWritableVaultPath, COPYFILE_EXCL) |
| T-02.2-77 | Tampering | markdown content from renderer | medium | mitigate | Main validates the string through the kernel text rules (UTF-8, no NUL, 1 MiB lines) before commit and write |
| T-02.2-SC | Tampering | npm install prosemirror-markdown@1.13.5 | high | mitigate | Exact pin 71 days old, official ProseMirror repo, no postinstall |
</threat_model>

<verification>
- forest-copy and to-markdown tests pass; typecheck and build:js pass
</verification>

<success_criteria>
- Notes copy between any two trees with a two-sided grew-from connection; native-to-vault copies are Markdown with every loss stated and chosen
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `ForestCommands.copyNote` | method | app/src/main/commands/forest.ts |
| `VaultNoteCommands.createNote` `text` option | method option | app/src/main/commands/vault-notes.ts |
| `forest:copyNote` | IPC channel | index.ts, preload |
| `serializeNoteToMarkdown`, `analyzeMarkdownLoss`, `MarkdownLossReport` | functions, type | app/src/renderer/editor/to-markdown.ts |
| `LossyCopyDialog` | React component | app/src/renderer/components/LossyCopyDialog.tsx |
| copy commit message `copy of <tree> · <title>` | history convention | journals |
| `tapestry-copy-chip`, `tapestry-lossy-options`, `tapestry-lossy-option`, `tapestry-lossy-helper` | CSS classes | App.css |

<output>
Create `.planning/phases/02.2-obsidian-bridge/02.2-14-SUMMARY.md` when done
</output>
