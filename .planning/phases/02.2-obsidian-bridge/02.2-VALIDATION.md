---
phase: "02.2"
slug: "obsidian-bridge"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-15"
---

# Phase 02.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `02.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | C++: doctest v2.5.3 (`tapestry_kernel_tests`); TypeScript: none today → vitest (Wave 0 install) |
| **Config file** | `tapestry/CMakeLists.txt`; TS: none — Wave 0 creates `app/vitest.config.ts` |
| **Quick run command** | `npx -w app vitest run src/main/obsidian src/main/commands` |
| **Full suite command** | `cmake -S tapestry -B tapestry/build-kernel -DTAPESTRY_BUILD_APP=OFF -DTAPESTRY_BUILD_RENDER=OFF && cmake --build tapestry/build-kernel && ctest --test-dir tapestry/build-kernel && npm run -w app typecheck && npx -w app vitest run && npm run -w app build` |
| **Estimated runtime** | ~180 seconds (kernel build dominates on a cold build) |

---

## Sampling Rate

- **After every task commit:** Run `npx -w app vitest run <touched dir>` + `npm run -w app typecheck`
- **After every plan wave:** Run the full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

Task IDs are filled in by the planner; the decision → test mapping below is the contract each plan's tasks must cover.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-06/D-07 | actor spoofing | Main process stamps every actor; a plugin claiming `human`/`system` throws; commits read `actor plugin agent.claude` / `actor human user.kaelen` | unit + integration | `npx -w app vitest run src/main/commands/actor.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-04/D-05 | unauthorized edit | `create_note` without a live grew-from note refused; node + edge in one commit; editing/deleting another actor's note refused | unit + integration | `npx -w app vitest run src/main/commands/notes.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-03 | local exposure | MCP shim lists tools with no actor argument; stdio/0600 socket only; stdout carries only protocol frames | integration | `npx -w app vitest run src/main/mcp/shim.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | addon origins | N/A | `getNodeOrigins` and read-only open behave in kernel | C++ doctest | `ctest --test-dir tapestry/build-kernel -R origin` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-31/D-33/D-34 | N/A | Scanner: every `[[…]]` → literal line; tags/frontmatter → properties; `[[create a link]]` unresolved | unit (golden) | `npx -w app vitest run src/main/obsidian/scan.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-12/D-13 | N/A | Mirror tree `md.text` byte-equal to file after reopen (no trailing LF, CR, a line `TEXT`) | integration | `npx -w app vitest run src/main/obsidian/mirror.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-20/D-24/D-25 | N/A | Catch-up diff: create/modify/delete/rename-by-hash; delete cuts edges | unit + integration | `npx -w app vitest run src/main/obsidian/reconciler.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-21/D-09 | attribution forgery | Unknown change → `plugin obsidian.bridge`; logged hash → `agent.<name>`; stale log line → bridge | unit | `npx -w app vitest run src/main/obsidian/reconciler.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-22/D-11 | N/A | Own write not re-attributed; outside change during debounce wins (file text committed) | integration (fake timers) | `npx -w app vitest run src/main/obsidian/writer.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-23 | N/A | Events within the quiet window → one commit listing files; split per actor | unit | `npx -w app vitest run src/main/obsidian/reconciler.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-15..D-18 | N/A | Two trees open; settings round-trip; cross-tree stub in both journals; crash-between-commits repair | integration | `npx -w app vitest run src/main/trees/registry.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-08 / path safety | path traversal | Agent/vault writes resolving outside the vault root are refused | unit | `npx -w app vitest run src/main/obsidian/writer.test.ts` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-17 lossy | N/A | Serializer emits exact Markdown for supported nodes and reports color/font/align/passage losses | unit | `npx -w app vitest run src/renderer/editor/to-markdown.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/vitest.config.ts` — node environment for main; native addon externalized
- [ ] vitest dev-dependency install in `app` (version pinned by planner after legitimacy check)
- [ ] `app/test/helpers/temp-vault.ts` — copies the fixture into a temp dir, under a root named with a space so world-token sanitisation is exercised; throws if any path resolves under `~/House Party` or `~/Documents`
- [ ] `app/test/helpers/temp-tree.ts` — create/open a `.tree` via the addon in a temp dir
- [ ] `app/test/fixtures/vault-sample/` — synthetic fixture authored in-repo (Plan 07), reproducing the asserted file shapes; no real-vault content is committed, and the real vault is opened only in end-of-phase manual UAT with Kaelen present
- [ ] Documented addon rebuild for tests (`npm run -w app build:native`; `ELECTRON_RUN_AS_NODE=1` fallback if the addon will not load under system Node)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tree frames, labeled folder groups, placeholder and file notes render and move correctly | D-15, D-26, D-29, D-34 | Visual/spatial UI | `npm run -w app dev`; open a temp House Party copy beside `we.tree`; drag frames; confirm no overlap and groups/placeholders show |
| Claude Code connected via `claude mcp add` creates a note grown from a House Party note | D-03, D-04, D-06 | Needs a real agent client | Connect Claude Code to the shim; create a note from `Characters/Rody.md` in a temp copy; confirm the `.md` file, the connection, and `actor plugin agent.claude` in the tree |
| Editing the same note in Obsidian and Tapestry | D-11, D-20, D-22 | Needs Obsidian running | Edit in Obsidian while Tapestry is open and while closed; confirm the file wins and history keeps the Tapestry edit; real vault only with Kaelen present |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
