---
phase: 02.2-obsidian-bridge
plan: 08
type: execute
wave: 8
depends_on:
  - 02.2-07
files_modified:
  - plugins/obsidian-vault/index.js
  - app/src/main/obsidian/paths.ts
  - app/src/main/obsidian/paths.test.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
  - app/src/renderer/layout/groups.ts
  - app/src/renderer/layout/groups.test.ts
  - app/src/renderer/layout/vault-text.ts
  - app/src/renderer/layout/vault-text.test.ts
  - app/src/renderer/components/Canvas.tsx
  - app/src/renderer/components/VaultNoteCard.tsx
  - app/src/renderer/components/FolderGroup.tsx
  - app/src/renderer/components/PlaceholderNote.tsx
  - app/src/renderer/components/FileNote.tsx
  - app/src/renderer/components/ConnectionLine.tsx
  - app/src/renderer/components/ConnectionLabel.tsx
  - app/src/renderer/components/ConnectionPopover.tsx
  - app/src/renderer/components/FrameHeader.tsx
autonomous: true
requirements:
  - D-14
  - D-26
  - D-29
  - D-31
  - D-33
  - D-34

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Vault folders (such as Characters/) appear as labeled groups inside the vault's frame; dragging a group label moves the group and its notes as Tapestry positions only, without touching any file (D-26 display, D-14)"
    - "A connection's short label typed in Tapestry is stored as the edge property tapestry.label in the vault tree and never written to a .md file (D-14, D-31)"
    - "Wikilink connections show the whole literal source line on hover and in the selected-connection popover, never an extracted phrase (D-31)"
    - "Tags render as chips with the literal token, and clicking a chip filters the frame: notes without the tag drop to 35% opacity and become non-interactive, Clear filter or Escape restores them (D-33 filterable)"
    - "Placeholder notes for missing links render as faint dashed cards titled with the link text (D-34); file notes show name, type, size and Open file, which opens only files inside the vault (D-29)"
    - "Every connection kind differs by dash pattern, arrowhead or label text, not by color alone (DRAW-04): wikilink solid, embed 2/4 dash, grew-from arrowhead at the new note, placeholder link 6/4 dash, cross-tree 2px 8/4 dash"
    - "Group renders at minimum 240x120 with its label and \"Empty folder\""
    - "Body shows the carried \"Start typing...\" placeholder; no tag chip row; footer still shows the path"
    - "Rendered as a file note with dashed border, the unreadable reason and Open file; text is never transcoded"
    - "Label line under the file note name in destructive color with Show vault in Finder; stays until Open file succeeds or the note is deselected"
    - "Root-level notes first, then one labeled group per folder in path order; nested folders render as nested groups"
    - "Title = file name, verbatim Markdown body with link/tag/frontmatter styling, tag chip row, two-line provenance footer"
    - "Solid lines; idle shows only Tapestry short labels; hover/selection shows the literal source line chip"
    - "240px file cards with glyph, name, type line, Open file; embed edges dotted"
    - "Placeholder note placed 48px to the right of the linking note inside the same group, joined by a dashed placeholder link"
    - "Ambiguous placeholder with its hint; never connected to either candidate"
    - "Frontmatter shown verbatim in muted color at the top of the body; derived keys also appear as tag-style chips only for `tags`; other keys are listed in the footer tooltip"
    - "Group grows with its notes; initial layout is a 3-column grid with 24px gaps sorted by name"
    - "Chip max width 280px, wraps to 3 lines then ellipsis; popover on selection shows the whole line unwrapped up to 480px and scrolls beyond"
    - statement: "Row wraps up to 2 lines, then shows \"+[n] more\" chip that expands on click. Needs visual check on Cast.md"
      verification: backstop
    - statement: "Title input wraps to 2 lines on the card; footer path truncates from the middle with ellipsis. Needs visual check"
      verification: backstop
    - statement: "Carried note behavior: card grows vertically and can be resized; verbatim text wraps with pre-wrap. Verify on a long lore note"
      verification: backstop
    - statement: "FolderGroup probe coverage (empty, loading, error, populated, partial, overflow, zero-one-many, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What happens with unusually long text?' (a long folder name truncates with ellipsis, full path in tooltip)"
      verification: backstop
    - statement: "PlaceholderNote probe coverage (empty, loading, error, partial, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What happens with unusually long text?' (link text wraps to 2 lines then ellipsis)"
      verification: backstop
    - statement: "FileNote probe coverage (empty, loading, error, populated, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What is shown when there is no data?' (a 0-byte file shows '0 B')"
      verification: backstop
    - statement: "ConnectionLabel probe coverage (loading, error, overflow, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What is shown when the load or submit fails?' (a failed short-label save shows the error under the field and keeps the typed text)"
      verification: backstop
    - "Planner assumption (UI-SPEC unresolved, backlinks in a vault note): no backlinks UI is built in 2.2; incoming wikilinks remain visible as connection lines"
  artifacts:
    - path: app/src/renderer/layout/groups.ts
      provides: "Folder group bounds from member notes"
      exports: ["computeGroupBounds", "GROUP_MIN_WIDTH", "GROUP_MIN_HEIGHT"]
    - path: app/src/renderer/layout/vault-text.ts
      provides: "Verbatim text segmentation for link/tag/frontmatter styling"
      exports: ["segmentVaultText", "VaultTextSegment"]
    - path: app/src/main/obsidian/paths.ts
      provides: "Vault path containment for opening files"
      exports: ["resolveExistingInVault", "isSafeVaultRel"]
    - path: app/src/renderer/components/ConnectionPopover.tsx
      provides: "Selected connection: literal line, short label field, provenance"
  key_links:
    - from: app/src/renderer/components/FileNote.tsx
      to: app/src/main/obsidian/paths.ts
      via: "Open file -> window.tapestry.vault.openFile(treeId, nodeId) -> main reads md.path from the tree -> resolveExistingInVault -> shell.openPath"
      pattern: "resolveExistingInVault\\("
    - from: app/src/renderer/components/ConnectionPopover.tsx
      to: app/src/renderer/state/use-forest.ts
      via: "short label -> submitChange(treeId, 'label connection', [setProperty edge tapestry.label])"
      pattern: "tapestry\\.label"
---

<objective>
The vault frame reads the way Obsidian does, while showing exactly what the files say:
- folders as labeled groups (D-26);
- notes as their verbatim text, with light link, tag and frontmatter styling, tag chips and a tag filter (D-33);
- placeholders for missing links (D-34);
- file notes with Open file (D-29);
- wikilink connections that reveal their literal line, plus a Tapestry-only short label (D-31, D-14).

Purpose: Plan 07 proved the record shapes. This plan makes them legible and usable, following the approved UI-SPEC, so Kaelen can inspect and trust the mirror before any file writes exist.
Output: FolderGroup, full VaultNoteCard, PlaceholderNote, FileNote, connection variants, ConnectionLabel, ConnectionPopover, tag filter, and the safe-open IPC.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md
@.planning/phases/02.2-obsidian-bridge/02.2-07-SUMMARY.md
@app/src/main/obsidian/shapes.ts
@app/src/renderer/components/VaultNoteCard.tsx
@app/src/renderer/components/Canvas.tsx
@app/src/renderer/components/ConnectionLine.tsx
@plugins/obsidian-vault/index.js

<interfaces>
From Plans 02-07:
- Vault node types and keys (shapes.ts): note md.path, md.text, md.sha256, md.tags (space-separated literal tokens), md.frontmatter (raw), md.fm.<key>; folder md.path; file md.path, md.ext, md.bytes, md.sha256, md.unreadable; placeholder md.link, md.ambiguous; edges label wikilink | embed with md.line, md.occurrence, optional tapestry.label; native grew-from and link edges.
- plugins/obsidian-vault registers component names VaultNoteCard, FolderGroup, FileNote, PlaceholderNote; Canvas NODE_VIEW_COMPONENTS maps names to components (FolderGroup/FileNote/PlaceholderNote currently fall back).
- useForest: trees (with nodes, edges, history, kind, vaultRoot), submitChange(treeId, message, ops), nodeKey; LiveAnnouncer useAnnounce(); ProvenanceBadge; TransientNotice; Dialog.
- ConnectionLine.tsx today: props x1, y1, x2, y2, isTemporary; stroke #B0ADA6 1.5px; temporary 6/4 dash accent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Folder groups and the full vault note view with tags and frontmatter</name>
  <files>
    app/src/renderer/layout/groups.ts,
    app/src/renderer/layout/groups.test.ts,
    app/src/renderer/layout/vault-text.ts,
    app/src/renderer/layout/vault-text.test.ts,
    app/src/renderer/components/FolderGroup.tsx,
    app/src/renderer/components/VaultNoteCard.tsx,
    app/src/renderer/components/Canvas.tsx,
    plugins/obsidian-vault/index.js,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Folder group visual states (D-26)", "Note variants", "Tag chips (D-33)", "Vault body text styling", "Folder groups (D-26, D-27)" (drag group label row), "Vault notes, placeholders and file notes" (copy), "Provenance footer" (vault path line), "Typography" role assignments,
    app/src/renderer/components/VaultNoteCard.tsx,
    app/src/renderer/components/NoteCard.tsx (card surface, drag, resize),
    app/src/renderer/components/Canvas.tsx
  </read_first>
  <behavior>
    - segmentVaultText('---\ntags: [x]\n---\nBest friends with [[Max]] #Guy `#code`\n') yields a frontmatter segment for the first three lines, then text 'Best friends with ', link '[[Max]]', text ' ', tag '#Guy', text ' ', code '`#code`', text '\n'; concatenating every segment's text reproduces the input exactly
    - Links and tags inside fenced code are plain text segments
    - computeGroupBounds for a folder with two notes returns their union expanded by 16px padding plus a 32px label band above
    - An empty folder uses the folder node's position with minimum 240x120
    - A nested folder's bounds sit inside its parent's bounds (the parent grows to include child groups)
  </behavior>
  <action>
    Write groups.test.ts and vault-text.test.ts for the behaviors listed first.

    app/src/renderer/layout/vault-text.ts exports type VaultTextSegment = { kind: 'text' | 'link' | 'tag' | 'frontmatter' | 'code'; text: string } and segmentVaultText(text: string): VaultTextSegment[]. It mirrors the scanner rules from app/src/main/obsidian/scan.ts: frontmatter only on line 0 with a closing ---, fences, inline code, the link regex and the tag regex. The renderer cannot import main modules, so keep a comment that it mirrors scan.ts. Segments must reproduce the input exactly when joined.

    app/src/renderer/layout/groups.ts exports GROUP_MIN_WIDTH = 240, GROUP_MIN_HEIGHT = 120, GROUP_PADDING = 16, GROUP_LABEL_BAND = 32 and computeGroupBounds(folders: Array<{ path: string; x: number; y: number }>, notes: Array<{ path: string; x: number; y: number; width: number; height: number }>): Map<folderPath, { x, y, width, height }>. A note belongs to a folder when its path starts with `${folder}/`, and the note's direct folder decides nesting. Parents include the bounds of child groups.

    Create app/src/renderer/components/FolderGroup.tsx:
    - A rectangle at the computed bounds, rendered behind notes in the frame content layer. Fill #F7F5F0, 1px solid #E0DDD7, radius 8px.
    - The label is the folder name (last path segment) in Label role, #6B6B6B, with the full path in its title. It is the drag handle.
    - Dragging the label translates the group and every member note (and nested group) live. On drop it submits one commit to the vault tree through submitChange(treeId, `arrange group ${path}`, [...]) setting position.x and position.y on each member note and on the folder node. These are Tapestry keys only; no file is written.
    - Empty groups show "Empty folder" in Label role inside the minimum 240x120.

    Canvas renders FolderGroup for obsidian.vault/folder@1 from the component map. Add FolderGroup to NODE_VIEW_COMPONENTS. plugins/obsidian-vault/index.js already registers it; verify the component name matches.

    Extend VaultNoteCard.tsx:
    - The title is the file name without .md, in Note Title style, wrapping to 2 lines and then an ellipsis. It stays read-only here; retitling arrives in Plan 10.
    - Render the body from segmentVaultText inside a pre-wrap block:
      - link segments: #2C2C2C text with a 1px #B0ADA6 underline;
      - tag segments: #6B6B6B;
      - frontmatter segments: #6B6B6B;
      - code: monospace.
      The literal characters, including brackets, # and ---, stay visible.
    - An empty md.text shows the placeholder "Start typing..." with no tag chip row.
    - Tag chip row:
      - One chip per md.tags token plus one per item of md.fm.tags (split on ', '), deduplicated case-sensitively.
      - Chip style: #F7F5F0 fill, 1px #E0DDD7, radius 4px, height 24px, padding 0 8px, Label #6B6B6B, literal token.
      - The row wraps up to 2 lines, then shows a "+[n] more" chip that expands on click.
    - Footer: the Plan 02 provenance footer plus a second line with the vault-relative path. The path truncates from the middle (keep the first 12 and last 20 characters with an ellipsis between, full path in title) and is always visible for vault notes.
    - Frontmatter keys other than tags are listed in the footer tooltip as `key: value` lines.
    - The card grows vertically with its text and keeps NoteCard's resize handles.

    Add the CSS classes tapestry-folder-group, tapestry-folder-label, tapestry-vault-note-body, tapestry-md-link, tapestry-md-tag, tapestry-md-frontmatter, tapestry-tag-chip, tapestry-tag-chip--more and tapestry-vault-path, using UI-SPEC tokens only.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/renderer/layout/groups.test.ts src/renderer/layout/vault-text.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: in a temp House Party frame, Characters appears as a labeled group containing its 13 notes; dragging the label moves them together and no .md file's modified time changes; Cast.md shows its tag chips wrapping with "+n more".</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function segmentVaultText" app/src/renderer/layout/vault-text.ts` prints one match
    - `grep -n "export function computeGroupBounds" app/src/renderer/layout/groups.ts` prints one match
    - `grep -n "Empty folder" app/src/renderer/components/FolderGroup.tsx` prints one match
    - `grep -n "more" app/src/renderer/components/VaultNoteCard.tsx` prints the "+[n] more" chip match
    - groups and vault-text tests pass; typecheck and build:js pass
  </acceptance_criteria>
  <done>Folder groups and faithful, styled vault notes with tag chips and path footers render in the vault frame.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Placeholder notes, file notes and unreadable files with safe Open file</name>
  <files>
    app/src/main/obsidian/paths.ts,
    app/src/main/obsidian/paths.test.ts,
    app/src/main/index.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/PlaceholderNote.tsx,
    app/src/renderer/components/FileNote.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Note variants" (placeholder, ambiguous, file note, unreadable rows), "Vault notes, placeholders and file notes" (copy), "Connections" interaction rows for File note Open file / Show vault in Finder,
    .planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md "Security Domain" (path traversal pattern),
    app/src/main/obsidian/vault-fs.ts (isIgnoredVaultPath),
    app/src/main/index.ts
  </read_first>
  <behavior>
    - isSafeVaultRel accepts 'Characters/Rody.md' and 'map.png'; rejects '', '/etc/passwd', '../x.md', 'a/../../b.md', 'a\\b.md', 'x\u0000.md', '.obsidian/app.json', 'Characters/.hidden.md'
    - resolveExistingInVault(root, 'Characters/Rody.md') returns the real absolute path inside the vault
    - resolveExistingInVault rejects a rel whose target is a symlink pointing outside the vault, and a rel whose realpath escapes the vault root
    - resolveExistingInVault rejects a missing file with an error containing "does not exist"
  </behavior>
  <action>
    Write paths.test.ts first. Use makeTempVault, and create a symlink with fs.symlinkSync to a file in a separate temp dir.

    Create app/src/main/obsidian/paths.ts exporting two functions:
    - isSafeVaultRel(rel: unknown): rel is string. It requires a non-empty string with no NUL or backslash, one that is not absolute, has no segment equal to '..' or '' and no segment starting with '.', and for which isIgnoredVaultPath(rel) is false.
    - resolveExistingInVault(root: string, rel: string): Promise<string>. It rejects unsafe rels, then computes realRoot = fs.realpath(root) and abs = path.join(realRoot, ...rel.split('/')). It lstats abs, rejecting a symlink and rejecting a missing file with `${rel} does not exist`. It then requires fs.realpath(abs) to start with realRoot + path.sep, and returns abs.

    In index.ts register two IPC handlers:
    - vault:openFile(treeId, nodeId). It resolves the vault tree from the registry and reads node md.path from its bridge (the renderer never supplies a path), calls resolveExistingInVault(tree.vaultRoot, mdPath), and awaits shell.openPath(abs). It returns { ok: true }, or { ok: false, error } when shell.openPath returns an error string or resolution fails.
    - vault:revealFile(treeId, nodeId). It calls shell.showItemInFolder on the file when it resolves, otherwise on the vault root.

    Expose both in preload and global.d.ts.

    Create PlaceholderNote.tsx:
    - Transparent card, 1px dashed #B0ADA6, radius 8px, min width 200px.
    - Title: md.link in Note Title style, #6B6B6B, wrapping to 2 lines then an ellipsis. Not editable.
    - Hint in Label role: "No file yet -- type to create [link text].md". When md.ambiguous is true, the hint is "Several notes match [link text] -- link not resolved. Rename one of the notes, or write the folder path in the link (for example [[Characters/[link text]]])."
    - Positioned by its stored frame-local position; Plan 07 layout places it 48px right of the linking note. Typing behavior arrives in Plan 11.

    Create FileNote.tsx:
    - Fixed 240px width, #FFFFFF, 1px solid #E0DDD7, radius 8px.
    - A 16px file-type glyph (aria-hidden): image for png/jpg/jpeg/gif/svg/webp, document for pdf, board for canvas, generic otherwise.
    - The name (basename of md.path) in Body role.
    - Type line in Label role: "[EXT] file · [size]", with size formatted as B, KB or MB using 1024 steps and rounded to whole KB/MB (for example "PNG file · 240 KB").
    - An **Open file** action.
    - When Open file fails: a destructive Label line under the name, "Couldn't open [file]. It may have been moved or deleted. Choose Show vault in Finder to look for it.", plus **Show vault in Finder** (vault.revealFile). The line stays until Open file succeeds or the note is deselected.
    - When md.unreadable is set: a 1px dashed #B0ADA6 border, a warning glyph, the line "Can't show this note as text -- [reason].", and Open file. The text is never transcoded.

    Register PlaceholderNote and FileNote in Canvas NODE_VIEW_COMPONENTS.

    Add the CSS classes tapestry-placeholder-note, tapestry-file-note, tapestry-file-note--unreadable and tapestry-file-note-error.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/obsidian/paths.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
    <human-check>End of phase: in a temp House Party frame, Welcome.md's "[[create a link]]" shows a dashed placeholder titled "create a link" with the hint; adding a PNG to the temp vault and reopening shows a 240px file card whose Open file launches Preview.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function resolveExistingInVault\|export function resolveExistingInVault" app/src/main/obsidian/paths.ts` prints one match
    - `grep -n "No file yet -- type to create" app/src/renderer/components/PlaceholderNote.tsx` prints one match
    - `grep -n "Several notes match" app/src/renderer/components/PlaceholderNote.tsx` prints one match
    - `grep -n "Show vault in Finder" app/src/renderer/components/FileNote.tsx` prints one match
    - `grep -n "vault:openFile" app/src/main/index.ts app/src/preload/index.ts` prints a match in each file
    - paths.test.ts passes all four behaviors
  </acceptance_criteria>
  <done>Missing links, ambiguous links, attachments and unreadable Markdown each render as their UI-SPEC variant, and opening a file can never reach outside the vault.</done>
</task>

<task type="auto">
  <name>Task 3: Connection variants, literal-line labels, the connection popover and tag filtering</name>
  <files>
    app/src/renderer/components/ConnectionLine.tsx,
    app/src/renderer/components/ConnectionLabel.tsx,
    app/src/renderer/components/ConnectionPopover.tsx,
    app/src/renderer/components/Canvas.tsx,
    app/src/renderer/components/FrameHeader.tsx,
    app/src/renderer/components/VaultNoteCard.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Connection line variants", "Connection label chip", "Connections" (copy and interaction rows), "Vault note editing" (tag chip filter row), "Spacing Scale" exceptions (280px chip, 480px popover), "Assumptions for Review" UA-09 and UA-13,
    app/src/renderer/components/ConnectionLine.tsx,
    app/src/renderer/components/Canvas.tsx (edge layer),
    app/src/renderer/components/ProvenanceBadge.tsx
  </read_first>
  <action>
    Extend ConnectionLine.tsx with prop variant: 'link' | 'wikilink' | 'embed' | 'grew-from' | 'placeholder' | 'cross-tree' | 'temporary' (default 'link') and prop hovered?: boolean. Strokes by variant:
    - link: #B0ADA6, 1.5px, solid.
    - wikilink: #B0ADA6, 1.5px, solid.
    - embed: stroke-dasharray '2 4'.
    - grew-from: solid, with an 8px arrowhead marker (defined once in the SVG defs) at the from end, where the new note is.
    - placeholder: dasharray '6 4'.
    - cross-tree: 2px, dasharray '8 4'.
    - temporary: #4A7CFF, 2px, '6 4'.
    - hovered or selected, any variant: #4A7CFF, 2px, keeping the variant's dash pattern.

    Canvas picks the variant from each edge: label grew-from gives grew-from; embed gives embed; wikilink to a placeholder node gives placeholder; wikilink gives wikilink; anything else gives link.

    Hit-test connections with an invisible 12px-wide stroke per edge (pointer-events stroke). Hovering a line, or either endpoint note, highlights it and shows its label chip at the midpoint.

    Create ConnectionLabel.tsx, a chip at the midpoint:
    - #FFFFFF, 1px solid #E0DDD7, radius 4px, padding 4px 8px, Label #2C2C2C, max-width 280px.
    - Clamped to 3 lines with an ellipsis; the full text is in its title.
    - When idle, it shows only tapestry.label when set (UA-13).
    - On hover or selection it shows the literal md.line for wikilink and placeholder edges, "embed" for embed, "grew from" for grew-from, and tapestry.label for native link edges.
    - The chip is aria-hidden; the popover carries the accessible text.

    Create ConnectionPopover.tsx, opened by clicking a line (up to 480px wide, near the midpoint, PassageChooser surface):
    - For wikilink edges: heading "Line in [source file basename]", then the literal line unwrapped with horizontal scrolling beyond 480px.
    - A field labeled "Your short label (Tapestry only)" with placeholder "Add a label". Enter or blur commits through submitChange(treeId, 'label connection', …): a setProperty of tapestry.label as text when non-empty, an unsetProperty when cleared. Values are limited to 80 characters. A failure shows the error under the field and keeps the typed text.
    - The edge creator's ProvenanceBadge, from history edges createdBy.
    - Escape or a click outside closes it.
    - Embed and grew-from edges show their label text and provenance only.

    Tag filter (UA-09):
    - Clicking a tag chip in VaultNoteCard sets filter { treeId, tag }.
    - Every note in that frame whose md.tags tokens and md.fm.tags items lack the tag renders at opacity 0.35 with pointer-events none. Groups, file notes and placeholders are dimmed too.
    - FrameHeader row 2 shows an active filter chip (#FFFFFF fill, 1px solid #2C2C2C, #2C2C2C text, no accent) with the tag, plus a button labeled "Clear filter".
    - Escape clears the filter when no editor or popover is open.

    Add the CSS classes tapestry-connection-label, tapestry-connection-popover, tapestry-short-label-field, tapestry-tag-filter-chip and tapestry-dimmed.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && npm --prefix app run build:js && grep -n "Your short label (Tapestry only)" app/src/renderer/components/ConnectionPopover.tsx && grep -n "8 4" app/src/renderer/components/ConnectionLine.tsx && grep -n "Clear filter" app/src/renderer/components/FrameHeader.tsx</automated>
    <fails_when>non-zero exit (including any grep finding no match), or the output contains "error TS"</fails_when>
    <human-check>End of phase: hover the line from Rody to Max in a temp House Party frame: the chip shows "Best friends with [[Max]]" (or the actual line); click it, type "best friends" as the short label: the idle chip shows it, the .tree gains `tapestry.label`, and Rody.md is byte-identical; click #Guy: non-Guy notes dim until Clear filter.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "variant" app/src/renderer/components/ConnectionLine.tsx` prints matches for wikilink, embed, grew-from, placeholder and cross-tree
    - `grep -n "Line in" app/src/renderer/components/ConnectionPopover.tsx` prints one match
    - `grep -n "tapestry.label" app/src/renderer/components/ConnectionPopover.tsx` prints a match
    - `grep -n "0.35" app/src/renderer/App.css app/src/renderer/components/Canvas.tsx` prints a match
    - typecheck and build:js pass
  </acceptance_criteria>
  <done>Connections are distinguishable without color, show their literal source line on demand, accept a Tapestry-only short label, and tags filter a frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> vault:openFile / vault:revealFile | Renderer asks main to open a file for a node |
| vault file content -> renderer | Untrusted Markdown text is displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-40 | Elevation of Privilege | vault:openFile | high | mitigate | Main reads md.path from the tree (never from the renderer); resolveExistingInVault rejects unsafe rels, symlinks and realpaths outside the vault (paths.test.ts) before shell.openPath |
| T-02.2-41 | Tampering | rendering Markdown text | medium | mitigate | Text rendered as React text nodes from segmentVaultText; no dangerouslySetInnerHTML or HTML parsing of vault content |
| T-02.2-42 | Tampering | short label writes | low | mitigate | tapestry.label written only to the vault tree through submitChange (human actor stamped in main); never to .md; 80-character cap |
| T-02.2-43 | Denial of Service | many connection hit areas | low | accept | House Party has tens of edges; per-edge SVG hit strokes are cheap; larger vaults are a later concern (RESEARCH Pitfall 7 note) |
</threat_model>

<verification>
- groups, vault-text and paths tests pass; typecheck and build:js pass; approved copy grep checks pass
</verification>

<success_criteria>
- A mirrored vault is legible in Tapestry with folder groups, faithful note text, tags and filters, placeholders, file notes and inspectable literal-line connections
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `computeGroupBounds`, `GROUP_MIN_WIDTH`, `GROUP_MIN_HEIGHT`, `GROUP_PADDING`, `GROUP_LABEL_BAND` | pure layout | app/src/renderer/layout/groups.ts |
| `segmentVaultText`, `VaultTextSegment` | pure function, type | app/src/renderer/layout/vault-text.ts |
| `isSafeVaultRel`, `resolveExistingInVault` | functions | app/src/main/obsidian/paths.ts |
| `vault:openFile`, `vault:revealFile` | IPC channels | index.ts, preload |
| `FolderGroup`, `PlaceholderNote`, `FileNote`, `ConnectionLabel`, `ConnectionPopover` | React components | app/src/renderer/components/ |
| `ConnectionLine` props `variant`, `hovered`; grew-from arrowhead marker | component API | ConnectionLine.tsx |
| tag filter state `{ treeId, tag }` | renderer state | App.tsx / Canvas.tsx |
| `tapestry-folder-group`, `tapestry-folder-label`, `tapestry-vault-note-body`, `tapestry-md-link`, `tapestry-md-tag`, `tapestry-md-frontmatter`, `tapestry-tag-chip(--more)`, `tapestry-vault-path`, `tapestry-placeholder-note`, `tapestry-file-note(--unreadable)`, `tapestry-file-note-error`, `tapestry-connection-label`, `tapestry-connection-popover`, `tapestry-short-label-field`, `tapestry-tag-filter-chip`, `tapestry-dimmed` | CSS classes | App.css |

<output>
Create `.planning/phases/02.2-obsidian-bridge/02.2-08-SUMMARY.md` when done
</output>
