---
phase: 02.2-obsidian-bridge
plan: 08
type: execute
wave: 8
depends_on:
  - 02.2-07
files_modified:
  - package-lock.json
  - app/package.json
  - app/src/main/obsidian/scan.ts
  - app/src/main/obsidian/scan.test.ts
  - app/src/main/obsidian/resolve.ts
  - app/src/main/obsidian/resolve.test.ts
  - app/src/main/obsidian/reconcile.ts
  - app/src/main/obsidian/mirror.test.ts
  - app/src/main/obsidian/vault-service.ts
  - app/test/helpers/edge-vault.ts
  - app/src/main/index.ts
  - app/src/preload/index.ts
  - app/src/renderer/global.d.ts
  - app/src/renderer/components/AddVaultDialog.tsx
  - app/src/renderer/components/ForestBar.tsx
  - app/src/renderer/components/FrameHeader.tsx
  - app/src/renderer/components/TreeFrame.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/App.css
  - tapestry/docs/tree/obsidian-vault.md
autonomous: true
requirements:
  - D-13
  - D-14
  - D-18
  - D-29
  - D-31
  - D-33
  - D-34

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each `[[link]]` becomes a `wikilink` edge whose md.line is the whole literal line it sits in (for example `Best friends with [[Max]]`), never an extracted label; `![[file]]` becomes an `embed` edge (D-31, D-29)"
    - "Links inside fenced code blocks and inline code are ignored, and backtick-wrapped `#tags` are not tags"
    - "`#tags` become md.tags (literal tokens in file order) and frontmatter becomes md.frontmatter (raw) plus md.fm.<key> properties, without changing the file (D-33)"
    - "A link to a missing note becomes one faint placeholder node (md.link) connected to each linking note; a link matching several notes becomes an ambiguous placeholder connected to neither candidate (D-34; `[[create a link]]` in Welcome.md)"
    - "Non-Markdown files (images, PDFs, .canvas) become file notes with md.ext, md.bytes and md.sha256; their bytes are not copied into the tree (D-29)"
    - "A .md file that is not valid UTF-8, contains NUL or has a line over 1 MiB becomes a file note with md.unreadable and is never transcoded"
    - "Tapestry refuses to open a vault tree whose journal is not Ok, rather than repairing it, and then never writes that vault (RESEARCH Pitfall 8)"
    - "Frame placeholder at 480x320 with frame name and status \"Reading vault (n of total files)\"; notes appear when the first grouped commit lands"
    - "Frame shows unavailable style at its remembered position with missing-folder copy and Locate folder... / Close tree"
    - statement: "AddVaultDialog probe coverage (empty, loading, error, partial, long-text): states not named by an explicit row are checked with their taxonomy question, for example 'What is shown when the load or submit fails?' (a failed add shows the reason in destructive Label text under the body)"
      verification: backstop
    - "Add vault warns (does not block) when .obsidian/core-plugins.json enables Sync or when the folder has no .obsidian folder"
    - "Tests build and exercise only temp copies: makeTempVault and makeEdgeVault build under os.tmpdir() and the real vault is never opened by tests"
  artifacts:
    - path: app/src/main/obsidian/scan.ts
      provides: "Pure line-based Markdown scanner (links, embeds, tags, frontmatter)"
      exports: ["scanMarkdown", "ScannedLink", "ScanResult"]
    - path: app/src/main/obsidian/resolve.ts
      provides: "Link target resolution: resolved | unresolved | ambiguous"
      exports: ["buildVaultIndex", "resolveLink"]
    - path: app/src/renderer/components/AddVaultDialog.tsx
      provides: "Add vault confirmation with non-blocking Sync and not-a-vault warnings"
    - path: tapestry/docs/tree/obsidian-vault.md
      provides: "Reader's guide to vault tree record shapes"
  key_links:
    - from: app/src/main/obsidian/reconcile.ts
      to: app/src/main/obsidian/scan.ts
      via: "buildVaultModel scans each readable note and resolves links through resolve.ts"
      pattern: "scanMarkdown\\("
    - from: app/src/renderer/components/ForestBar.tsx
      to: app/src/main/index.ts
      via: "Add Obsidian Vault... -> vault.inspectFolder -> AddVaultDialog -> vault.add"
      pattern: "inspectFolder"
---

<objective>
Plan 07 proved that a vault becomes a tree holding each file's exact text. This plan derives everything else the files already say, without altering them:
- `[[links]]` as connections labeled with their literal line (D-31) and `![[embeds]]` (D-29);
- `#tags` and frontmatter as readable properties (D-33);
- file notes for non-Markdown files and unreadable Markdown (D-29);
- placeholders for links to notes that do not exist yet (D-34).

It also completes adding a vault as a visible, honest act: a confirmation dialog with non-blocking warnings, reading and catch-up progress, recovery when a vault folder moves (D-18), and the reader's guide that documents the record shapes (D-13, D-14).

Purpose: this is the rest of the Obsidian mirror. Splitting it from Plan 07 keeps the tracer's byte-equality proof separate from the derivation and presentation work built on top of it.

Output: scan.ts, resolve.ts, the reconcile extension, edge-vault fixtures, AddVaultDialog, vault status and locate, and docs/tree/obsidian-vault.md.

File count: 20 files, the union of two tightly coupled tasks. The derivation task owns the main-process modules; the add-vault task necessarily touches one main file, the two IPC surface files (preload, global.d.ts) and four renderer components, because a single user-visible flow (choose folder, confirm, watch progress, recover a moved folder) crosses all of them. Splitting further would separate an IPC handler from its only caller.

Concurrency: other sessions hold uncommitted work in `.planning/spikes/**` and `.planning/phases/02.3-*`. This plan's executor must never stage those paths, and asserts a clean working tree for its own `files_modified` before editing (shared preflight, Plan 01).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-obsidian-bridge/02.2-CONTEXT.md
@.planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md
@.planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md
@.planning/phases/02.2-obsidian-bridge/02.2-VALIDATION.md
@.planning/phases/02.2-obsidian-bridge/02.2-07-SUMMARY.md
@tapestry/docs/tree/FORMAT.md
@app/src/main/obsidian/reconcile.ts
@app/src/main/obsidian/vault-service.ts
@app/src/renderer/components/Dialog.tsx

<interfaces>
From Plan 07:
- shapes.ts: VAULT_NOTE_TYPE, VAULT_FOLDER_TYPE, VAULT_FILE_TYPE, VAULT_PLACEHOLDER_TYPE; MD_PATH, MD_TEXT, MD_SHA256, MD_TAGS, MD_FRONTMATTER, MD_FM_PREFIX, MD_EXT, MD_BYTES, MD_UNREADABLE, MD_LINK, MD_AMBIGUOUS, MD_LINE, MD_OCCURRENCE, TAPESTRY_LABEL; WIKILINK_LABEL, EMBED_LABEL.
- vault-fs.ts: vaultTreeFiles, isIgnoredVaultPath, walkVault, readVaultBytes, decodeMarkdown, VaultEntry { rel, kind, size, ino }.
- reconcile.ts: buildVaultModel(root, entries) -> VaultModel { notes, folders, files }, layoutVault(model), planReconcile(tree, model, nextIds) -> { ops, summary }.
- vault-service.ts: VaultService(registry, hooks?) with addVault, openVault, catchUp; onStatus kinds reading | catching-up | up-to-date.
- index.ts: dialog:showOpenVaultFolder with approvedVaultRoots, vault:add, webContents 'vault-status' and 'tree-changed'.
- Canvas NODE_VIEW_COMPONENTS map; VaultNoteCard (verbatim, read-only).
- app/test/helpers/temp-vault.ts makeTempVault; real-data-guard assertNotRealData.
From earlier plans: TreeRegistry.tryOpen and UnavailableTree with status/reason; TreeFrame unavailable rendering; FrameHeader status slot; Dialog shell; TransientNotice; ForestBar Add tree menu; panToFrame.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Links, embeds, tags, frontmatter, file notes and placeholders from the files</name>
  <precondition>Working tree is clean for this plan's files: `git status --porcelain -- <every path in this plan's files_modified>` prints nothing before any edit. Other active sessions hold uncommitted work in `.planning/spikes/**` and `.planning/phases/02.3-*`; those paths are never staged by this phase. If the check prints anything, stop and report rather than committing another session's work.</precondition>
  <files>
    package-lock.json,
    app/package.json,
    app/src/main/obsidian/scan.ts,
    app/src/main/obsidian/scan.test.ts,
    app/src/main/obsidian/resolve.ts,
    app/src/main/obsidian/resolve.test.ts,
    app/src/main/obsidian/reconcile.ts,
    app/src/main/obsidian/mirror.test.ts,
    app/test/helpers/edge-vault.ts
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md sections "Markdown scanning (D-31, D-33, D-34)", "Vault tree record shapes" (edges), "Anti-Patterns to Avoid", "Assumptions Log" A4,
    app/src/main/obsidian/reconcile.ts,
    app/src/main/obsidian/shapes.ts,
    app/test/fixtures/house-party/Welcome.md,
    app/test/fixtures/house-party/Characters/Cast.md
  </read_first>
  <behavior>
    - scanMarkdown('Best friends with [[Max]]\n') yields one link { target: 'Max', embed: false, line: 'Best friends with [[Max]]', lineIndex: 0, occurrence: 0 }
    - `Close friends with [[Max]], [[Rody]], and [[Eve]]` yields three links sharing that literal line
    - `[[Max|Maxie]]`, `[[Max#Past]]` and `[[Characters/Max]]` target Max, Max and Characters/Max
    - `![[map.png]]` yields embed true
    - Links and tags inside ``` or ~~~ fences and inside `inline code` are ignored; `` `#Guy` `` is not a tag; `#Guy` and `#Galleaning` are tags; `#2024` is not a tag
    - Frontmatter is recognized only when line 0 is exactly --- and a closing --- exists; its raw text is returned and parsed with yaml (maxAliasCount 50); a YAML error keeps raw text and sets data null
    - With CRLF lines, md.line excludes the trailing CR while md.text keeps it
    - resolveLink: 'max' resolves to Characters/Max.md case-insensitively; 'Characters/Max' resolves by path; 'create a link' is unresolved; a basename present in two folders is ambiguous with both candidates; 'map.png' resolves to an attachment only with its extension
    - edge-vault import: invalid UTF-8 .md -> file@1 with md.unreadable 'is not valid UTF-8'; map.png -> file@1 md.ext 'png', md.bytes, md.sha256 and an embed edge from the note; a note ending without LF and a note containing a line TEXT round-trip byte-equal; duplicate basenames produce one placeholder with md.ambiguous true and no edge to either candidate
  </behavior>
  <action>
    **Dependency.** From the repo root run `npm install -w app --save-exact yaml@2.9.0`. Legitimacy record: yaml 2.9.0 was published 2026-05-11, 127 days before planning, from github.com/eemeli/yaml, with no postinstall script. The SUS flag applied to the latest 2.9.1, so no checkpoint is needed. Add yaml to the externalizeDepsPlugin exclude list in app/electron.vite.config.ts only if build:js leaves `require("yaml")` in out/main/index.js; in that case add that file to the task commit.

    **Tests first.** Create app/test/helpers/edge-vault.ts. Its makeEdgeVault(): { root; cleanup() } builds a temp vault with:
    - crlf.md ("line one\r\n[[b]]\r\n")
    - no-lf.md ("ends here [[b]]", no trailing newline)
    - text-line.md ("before\nTEXT\nafter [[b]]\n")
    - b.md
    - bad.md (bytes 0xff 0xfe)
    - map.png (8 bytes)
    - embeds.md ("![[map.png]]\n")
    - A/dup.md and B/dup.md
    - uses-dup.md ("[[dup]]\n")
    - fence.md ("```\n[[ghost]]\n```\n`#nottag` #real\n")
    - front.md ("---\ntitle: Front\nwritten by: me\ntags: [x, y]\n---\nbody [[b]]\n")

    Write scan.test.ts and resolve.test.ts for the behaviors listed, then extend mirror.test.ts with edge-vault assertions and these House Party fixture assertions:
    - Welcome.md is connected to a placeholder whose md.link is 'create a link'.
    - Every edge from a note whose md.line contains `[[` has md.line equal to a literal line of that note's file.
    - At least one Characters note has md.tags starting with '#'.

    **app/src/main/obsidian/scan.ts** exports interface ScannedLink { target: string; embed: boolean; line: string; lineIndex: number; occurrence: number }, interface ScanResult { links: ScannedLink[]; tags: string[]; frontmatter: { raw: string; data: Record<string, unknown> | null } | null }, and scanMarkdown(text: string): ScanResult.
    - Split the text on '\n'.
    - Frontmatter: when line 0 is exactly '---' (after stripping a trailing '\r') and a later line is exactly '---', the lines between are raw, YAML-parsed with YAML.parse(raw, { maxAliasCount: 50 }) inside try/catch. Skip those lines for links and tags.
    - Code: track fences opened by a line starting with ``` or ~~~ and closed by the same marker. Blank out inline code spans (backtick runs) before matching.
    - Links: match /(!?)\[\[([^\]|#^]+)(#[^\]|]*)?(\|[^\]]*)?\]\]/g; the target is group 2 trimmed.
    - Tags: match /(^|\s)#([\p{L}\p{N}_\/-]*[\p{L}_\/-][\p{L}\p{N}_\/-]*)/gu; keep the literal token with its '#', in file order, without duplicates.
    - md.line is the whole line minus a trailing '\r'.
    - occurrence counts earlier links in the same note with the same lowercased target and line.

    **app/src/main/obsidian/resolve.ts** exports buildVaultIndex(entries: VaultEntry[]) and resolveLink(target, index): { kind: 'resolved'; rel } | { kind: 'unresolved' } | { kind: 'ambiguous'; candidates: string[] }.
    - A target containing '/' matches a vault-relative path case-insensitively, with or without '.md'.
    - Otherwise match basenames case-insensitively: first `${target}.md` among notes, then an exact file name with extension among attachments.
    - One match resolves; zero is unresolved; several are ambiguous. Never choose between candidates.

    **Extend app/src/main/obsidian/reconcile.ts.** The VaultModel now also carries, for each note, scan results with resolved links, and for each note md.tags (tokens joined by a space), md.frontmatter (raw) and md.fm.<key>.
    - md.fm.<key> is emitted only for keys matching /^[A-Za-z_][A-Za-z0-9_.:-]*$/. Scalars become strings; arrays join their items with ', '; objects become JSON.stringify. Keys with spaces stay only in md.frontmatter.
    - Files: file@1 nodes with md.ext (lowercase, no dot), md.bytes int and md.sha256. Hash files of 16 MiB or less; larger files get no hash, which is compared by size.
    - Unreadable .md files and symlinks become file@1 with md.unreadable (the decode reason, or 'symbolic link not followed').
    - Placeholders: one obsidian.vault/placeholder@1 per lowercased unresolved or ambiguous target. It carries md.link (the first occurrence's text) and md.ambiguous (bool true when ambiguous), and is positioned 48px right of the first linking note's right edge (note x + 280 + 48) at the same y.

    Desired edges:
    - Resolved links become label wikilink (or embed) from the note to the target node.
    - Unresolved links become wikilink or embed edges to the placeholder.
    - Ambiguous links get a placeholder node but no edge to any candidate, and no edge to the placeholder either (UI-SPEC: never connected to either candidate). Only the placeholder note is shown.
    - Each edge carries md.line (text) and md.occurrence (int).

    planReconcile diffs these desired edges against existing edges keyed by (from md.path, label, target key, md.line, md.occurrence), where the target key is the target's md.path or `placeholder:${lowercased link}`.
    - An edge whose key still matches is kept, so a tapestry.label set on it survives.
    - An edge with no desired counterpart is deleted.
    - A desired edge with no existing counterpart is created; create target nodes first, predicting their ids with nextIds.
    - Delete placeholders nobody links to.
    - When a real note now exists for a placeholder's target, create the note at the placeholder's position and then delete the placeholder, so it replaces the placeholder in place.
    - Changed tags and frontmatter set or unset the md.* properties.
  </action>
  <verify>
    <automated>npm --prefix app run test -- src/main/obsidian/scan.test.ts src/main/obsidian/resolve.test.ts src/main/obsidian/mirror.test.ts && npm --prefix app run typecheck && npm --prefix app run build:js</automated>
    <fails_when>non-zero exit, or vitest prints "No test files found", or the output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - app/package.json dependencies contain "yaml": "2.9.0"
    - `grep -n "maxAliasCount" app/src/main/obsidian/scan.ts` prints one match
    - `grep -n "kind: 'ambiguous'" app/src/main/obsidian/resolve.ts` prints a match
    - scan, resolve and mirror tests pass, including byte equality for crlf.md, no-lf.md and text-line.md and the create a link placeholder in the House Party copy
  </acceptance_criteria>
  <done>The vault tree records links with literal lines, embeds, tags, frontmatter, file notes, unreadable files and placeholders, all derived from the files without altering them.</done>
</task>

<task type="auto">
  <name>Task 2: Add vault confirmation, reading and catch-up status, missing vault folders, and the reader's guide</name>
  <files>
    app/src/main/index.ts,
    app/src/main/obsidian/vault-service.ts,
    app/src/preload/index.ts,
    app/src/renderer/global.d.ts,
    app/src/renderer/components/AddVaultDialog.tsx,
    app/src/renderer/components/ForestBar.tsx,
    app/src/renderer/components/FrameHeader.tsx,
    app/src/renderer/components/TreeFrame.tsx,
    app/src/renderer/App.tsx,
    app/src/renderer/App.css,
    tapestry/docs/tree/obsidian-vault.md
  </files>
  <read_first>
    .planning/phases/02.2-obsidian-bridge/02.2-UI-SPEC.md sections "Add Obsidian vault dialog (D-13, D-18)", "Frame status slot" (vault rows: first read, catch-up, up to date), "Copywriting Contract" (vault folder missing on launch), "Visual Hierarchy" (Add vault dialog row), "Forest bar and entry points" (placement rule),
    .planning/phases/02.2-obsidian-bridge/02.2-RESEARCH.md "Pitfall 8" and "Vault tree record shapes",
    tapestry/docs/tree/FORMAT.md (tone and structure of the reader's guide),
    app/src/main/obsidian/vault-service.ts,
    app/src/renderer/components/Dialog.tsx
  </read_first>
  <action>
    **Main process.** Register vault:inspectFolder(root). It requires an approved root and returns { name, hasObsidianFolder: existsSync(join(root, '.obsidian')), syncEnabled, alreadyOpenTreeId: string | null, treeExists: boolean }. syncEnabled is true when .obsidian/core-plugins.json parses to an object with sync === true; this is read-only.

    Launch restore, for each settings tree of kind vault:
    - When vaultRoot is not an existing directory, record an UnavailableTree with status missing and reason `${name} can't be found at ${vaultRoot}. It may have been moved or renamed.`
    - Otherwise run vaultService.openVault(vaultRoot) inside try/catch. A non-Ok journal becomes damaged, and the vault is never written.

    Register vault:locate(treeId). It shows the folder dialog; when the chosen folder contains `${name}.tree`, it updates the settings entry's vaultRoot and path, calls reopen and emits 'trees-changed'. Otherwise it returns { ok: false, error: 'That folder has no <name>.tree' }.

    Expose vault.inspectFolder and vault.locate in preload and global.d.ts.

    **AddVaultDialog.tsx** (Dialog shell).
    - Title "Add [vault name] as a tree?".
    - Body "Tapestry will create [vault name].tree and [vault name].signin.log inside the vault folder to keep its history. Your notes stay as they are. Tapestry writes to them only when you or a connected agent edit them."
    - Directly under the title, when applicable:
      - Sync warning: "Obsidian Sync is turned on for this vault. Exclude *.tree and *.signin.log in Sync settings so another device doesn't write to the same history."
      - Not-a-vault warning: "This folder has no .obsidian folder, so it may not be an Obsidian vault."
      - The warnings do not block.
    - Primary **Add vault** (initial focus). Secondary **Don't add vault** (Escape).
    - A failure from vault.add shows its reason under the body in destructive Label text.

    ForestBar flow:
    1. Show the folder dialog, then call inspectFolder.
    2. If alreadyOpenTreeId is set, panToFrame that tree, flash its header status, and show the TransientNotice "[vault name] is already in this space."
    3. Otherwise show AddVaultDialog. On Add vault, call vault.add, then place the new frame and pan to center it (Plan 06 rule).

    **Status.** FrameHeader shows vault statuses from onVaultStatus: "Reading vault ([n] of [total] files)", "Catching up ([n] of [total] files)" and "Up to date", all Muted.
    - While the first read runs, TreeFrame shows the frame at its 480x320 minimum with name and status. Notes appear when the first commit lands (tree-changed).
    - During catch-up, notes render immediately from the tree.
    - An unavailable vault frame with status missing shows **Locate folder...** (vault.locate) and **Close tree** using the missing-folder copy.

    **tapestry/docs/tree/obsidian-vault.md**, a reader's guide, no format change. It covers:
    - Where the tree lives: `<vault>/<vault name>.tree`, world token sanitized.
    - The rule: every key starting md. is what the source file says or is derived from it; every other key is Tapestry's (positions, sizes, tapestry.label).
    - The four node types and their properties, with a short example of a note node, a wikilink edge with md.line, a placeholder and a file note in .tree syntax.
    - Edge labels wikilink, embed and grew-from.
    - The actors: plugin obsidian.bridge for observed changes with unknown authors, human user.<name> for Tapestry edits, plugin agent.<name> for agent edits.
    - Unreadable files.
    - Why Sync should exclude *.tree and *.signin.log.
    - A note that the sign-in log format is described in the section added by Plan 10.
    - A link to FORMAT.md.
    State explicitly that FORMAT.md and example.tree are unchanged because the vault uses only existing v1 verbs.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && npm --prefix app run build:js && npm --prefix app run test -- src/main/obsidian/mirror.test.ts && grep -n "Obsidian Sync is turned on for this vault." app/src/renderer/components/AddVaultDialog.tsx && grep -n "every key starting" tapestry/docs/tree/obsidian-vault.md && git diff --quiet HEAD -- tapestry/docs/tree/FORMAT.md tapestry/docs/tree/example.tree</automated>
    <fails_when>non-zero exit (including any grep with no match or a diff in FORMAT.md/example.tree), or the output contains "error TS"</fails_when>
    <human-check>End of phase: Add Obsidian Vault on a temp House Party copy shows the Sync warning and "Add House Party as a tree?"; the frame reads "Reading vault (n of 19 files)" then "Up to date"; quit, move the temp folder, relaunch: the frame shows the missing-folder copy with Locate folder...</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "as a tree?" app/src/renderer/components/AddVaultDialog.tsx` prints a match
    - `grep -n "Catching up (" app/src/renderer/components/FrameHeader.tsx` prints a match
    - `grep -n "vault:locate" app/src/main/index.ts app/src/preload/index.ts` prints a match in each file
    - tapestry/docs/tree/obsidian-vault.md exists and names obsidian.vault/note@1, obsidian.vault/placeholder@1, md.line and plugin obsidian.bridge
    - FORMAT.md and example.tree are unchanged
  </acceptance_criteria>
  <done>Adding a vault is confirmed with honest warnings, reading and catch-up progress is visible, a moved vault can be located again, and the vault record shapes are documented for readers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault file content -> scanner | Arbitrary Markdown, YAML frontmatter and link text are parsed |
| renderer -> vault:inspectFolder / vault:locate | Renderer supplies folder paths |
| npm registry -> app | yaml runtime install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-35 | Denial of Service | YAML frontmatter alias bombs | medium | mitigate | YAML.parse with maxAliasCount 50 inside try/catch; file text never re-serialized |
| T-02.2-36 | Tampering | vault tree synced to other devices | high | mitigate | Add vault warns when Sync is enabled; a non-Ok vault journal is opened as unavailable and never written or repaired |
| T-02.2-38 | Information Disclosure | link resolution by text | low | mitigate | resolveLink matches only against the walked index; link text never becomes a filesystem path |
| T-02.2-40 | Tampering | vault:locate paths | medium | mitigate | Only dialog-approved absolute roots accepted; the chosen folder must already contain the tree file for that vault |
| T-02.2-SC | Tampering | npm install yaml@2.9.0 | high | mitigate | Exact pin 127 days old, official repo, no postinstall |
</threat_model>

<verification>
- scan, resolve and mirror tests pass; typecheck and build:js pass; FORMAT.md and example.tree unchanged
</verification>

<success_criteria>
- House Party (as a temp copy) shows links with literal lines, embeds, tags, frontmatter, file notes and placeholders; adding a vault is confirmed with honest warnings and visible progress; the record shapes are documented
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `scanMarkdown`, `ScannedLink`, `ScanResult` | function, types | app/src/main/obsidian/scan.ts |
| `buildVaultIndex`, `resolveLink` | functions | app/src/main/obsidian/resolve.ts |
| Node types `obsidian.vault/file@1`, `placeholder@1`; edge labels `wikilink`, `embed` with `md.line`, `md.occurrence` | record conventions | vault folder |
| `vault:inspectFolder`, `vault:locate` | IPC | index.ts, preload |
| `AddVaultDialog` | React component | app/src/renderer/components/ |
| `makeEdgeVault` | test helper | app/test/helpers/ |
| `tapestry/docs/tree/obsidian-vault.md` | reader's guide | docs |

<output>
Create `.planning/phases/02.2-obsidian-bridge/02.2-08-SUMMARY.md` when done
</output>
