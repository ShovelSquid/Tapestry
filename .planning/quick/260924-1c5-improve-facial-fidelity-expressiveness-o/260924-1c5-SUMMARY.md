---
phase: quick-260924-1c5
plan: 01
subsystem: ui
tags: [mediapipe, face-landmarker, blendshapes, threejs, tasks-vision]

# Dependency graph
requires: []
provides:
  - "FaceLandmarker configured with outputFaceBlendshapes: true"
  - "onLandmarks payload carries faceBlendshapes through to scene3d.js"
  - "Metrics readout shows top 6 non-neutral blendshape scores"
  - "3D face point cloud built from full FACE_LANDMARKS_TESSELATION mesh instead of FACE_LANDMARKS_CONTOURS subset"
affects: [hands-face-voice-playground]

actuals:
  tokens: 601
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Semantic blendshape summary derived per-frame: filter _neutral, sort by score desc, take top 6, format as 'name:score.toFixed(2)'"
    - "3D point cloud density independent from connector-line source: cloud uses dense TESSELATION indices while lines() keeps sparse CONTOURS for a readable outline, mirroring the 2D overlay's existing dual-layer draw"

key-files:
  created: []
  modified:
    - hands-face-voice/src/hands-face.js
    - hands-face-voice/src/scene3d.js

key-decisions:
  - "Kept FACE_LANDMARKS_CONTOURS for the 3D connector lines() call — only the point cloud switched to the full tessellation mesh, matching the plan's explicit scope boundary"
  - "Used the first detected face's blendshapes only (faceResult.faceBlendshapes?.[0]?.categories), consistent with the playground's numFaces: 1 assumption"

patterns-established:
  - "Pattern: derive a display summary from a MediaPipe result array with filter -> sort -> slice -> join, keeping the raw array in the onLandmarks payload for downstream consumers"

requirements-completed: []

coverage:
  - id: D1
    description: "Metrics readout shows named, changing blendshape scores as the user makes facial expressions"
    verification:
      - kind: manual_procedural
        ref: "npx serve . in hands-face-voice/, click 'Start camera', observe #metrics second line while smiling/raising eyebrows/opening jaw"
        status: unknown
    human_judgment: true
    rationale: "Requires a live webcam feed and a real human face performing expressions; cannot be automated in a headless environment"
  - id: D2
    description: "3D face point cloud visibly covers the whole face surface (cheeks, nose, forehead, inner mouth), not just an eye/brow/lip/jaw outline"
    verification:
      - kind: manual_procedural
        ref: "Same live session, observe 'Landmark Points' 3D panel"
        status: unknown
    human_judgment: true
    rationale: "Visual density/coverage judgment on a live camera feed requires human eyes; headless browser has no camera to feed the model"
  - id: D3
    description: "Both modified files are syntactically valid and the page loads with no console errors or failed requests"
    verification:
      - kind: other
        ref: "node --check hands-face-voice/src/hands-face.js && node --check hands-face-voice/src/scene3d.js"
        status: pass
      - kind: automated_ui
        ref: "browser-automation skill load of http://localhost:4173/ — 0 console errors, 0 failed requests, page mounted (bodyChars 766)"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-09-24
status: complete
---

# Quick Task 260924-1c5: Improve Facial Fidelity/Expressiveness Summary

**Enabled MediaPipe FaceLandmarker blendshapes end-to-end (metrics readout) and switched the 3D face point cloud from the ~132-point contour subset to the full ~478-point tessellation mesh**

## Performance

- **Duration:** 12 min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `FaceLandmarker.createFromOptions` now requests `outputFaceBlendshapes: true`, and each frame derives the top 6 non-neutral blendshape categories (sorted by score) into a second `#metrics` line (`blendshapes: name:score …`, or `none` when empty)
- The `onLandmarks` payload now carries a `faceBlendshapes` field (passed through `main.js` untouched, as designed) so downstream consumers can use it
- `scene3d.js`'s 3D face point cloud is now built from `FACE_LANDMARKS_TESSELATION` indices instead of `FACE_LANDMARKS_CONTOURS`, giving full surface coverage (cheeks, nose bridge, forehead, inner mouth) while the contour connector lines stay unchanged as the structural outline layer

## Task Commits

Each task was committed atomically:

1. **Task 1: Turn on face blendshapes and surface them in the metrics readout** - `802af71` (feat)
2. **Task 2: Render the full face tessellation mesh as the 3D point cloud** - `46d8cc8` (feat)

## Files Created/Modified
- `hands-face-voice/src/hands-face.js` - Added `outputFaceBlendshapes: true` to FaceLandmarker options; derives and displays top-6 non-neutral blendshapes; forwards `faceBlendshapes` in the `onLandmarks` payload
- `hands-face-voice/src/scene3d.js` - Face point cloud in `update()` now sourced from `FACE_LANDMARKS_TESSELATION` instead of `FACE_LANDMARKS_CONTOURS`; contour `lines()` call unchanged

## Decisions Made
- Kept the contour connector lines untouched — only the point-cloud source changed, matching the plan's explicit "keep contours for lines" instruction and avoiding visual regression of the outline layer
- Used `.textContent` (not `.innerHTML`) for the blendshape summary, consistent with the plan's threat register disposition (category names are from MediaPipe's fixed enum, not user input, but `.textContent` is the existing safe pattern in this file anyway)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Both files passed `node --check` on first edit; no auto-fixes were needed.

## Verification Performed

- **Automated:** `node --check` on both files (exit 0); `grep -c "outputFaceBlendshapes: true"` = 1 and `grep -c "faceBlendshapes"` = 2 in `hands-face.js`; `grep -c "FACE_LANDMARKS_TESSELATION"` = 1 and `grep -c "FACE_LANDMARKS_CONTOURS"` = 1 in `scene3d.js` (confirming the tessellation swap happened and the contour lines call is the sole remaining reference)
- **Headless browser check:** served the playground locally (`npx serve .` on port 4173) and loaded it via the browser-automation skill — page mounted cleanly, 0 console errors, 0 failed network requests, confirming the ES module imports across `main.js` -> `hands-face.js` / `scene3d.js` still resolve correctly after the edits
- **Deferred to human (documented in `coverage` D1/D2):** the actual live-camera visual verification (blendshape scores changing with real facial expressions; point cloud visibly denser/covering the full face) requires a real webcam and a human face, which this environment cannot provide. Steps to verify: from `hands-face-voice/`, run `npx serve .`, open the printed localhost URL, click "Start camera (hands + face)", then (1) smile/raise eyebrows/open jaw and watch the `#metrics` second line change, and (2) look at the "Landmark Points" 3D panel and confirm the purple point cloud covers cheeks/nose/forehead/inner mouth, not just an outline.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

This is a leaf quick task within the `hands-face-voice` spike playground; no downstream phase depends on it. The live-camera human-check items (D1, D2 in `coverage`) remain open until a human runs the manual verification steps above.

## Self-Check: PASSED

- FOUND: hands-face-voice/src/hands-face.js
- FOUND: hands-face-voice/src/scene3d.js
- FOUND: commit 802af71
- FOUND: commit 46d8cc8

---
*Quick task: 260924-1c5*
*Completed: 2026-09-24*
