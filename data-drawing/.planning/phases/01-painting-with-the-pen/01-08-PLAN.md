---
phase: 01-painting-with-the-pen
plan: 08
type: execute
wave: 5
depends_on: ["01-07", "01-04"]
files_modified:
  - plugins/data-drawing/tsconfig.json
  - plugins/data-drawing/surface/src/replay.ts
  - plugins/data-drawing/surface/src/latency.ts
  - plugins/data-drawing/surface/src/sim-host.ts
  - plugins/data-drawing/surface/src/sim.worker.ts
  - plugins/data-drawing/surface/src/main.ts
  - plugins/data-drawing/surface/src/dev-host.ts
  - plugins/data-drawing/surface/index.html
  - plugins/data-drawing/surface/test/replay.test.ts
autonomous: false
requirements: [SIM-03, CANV-04, CANV-03, STRK-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Inside Tapestry (not the dev page), the Data Drawing surface opened from the Surfaces launcher paints with the pressure pen: pressure drives node size, the lead preset visibly lags and carries momentum through a curve, the ink preset follows closely, and the overlay shows pen, body and spring (phase success criterion 1, STRK-02, CANV-03)"
    - "Verify replay: replaying the session's recorded action list from tick zero in a fresh Worker-side sim reproduces the live hash and node count at the current tick, shown as MATCH in the panel; the Vitest replay test proves the same equality in Node (phase success criterion 5, SIM-03)"
    - "Pen-to-ink latency (pointer event to the first painted frame containing that sample's node) is measured with the Worker transport and with the main-thread transport over the same interface and both p50/p95 are recorded (roadmap wave 4)"
    - "Opening and closing the surface 20 times in Tapestry leaves it still rendering, with mounts equal to disposes, no 'Too many active WebGL contexts' warning, and the worker terminated each time (pitfall 7)"
    - "The renderer backend actually in use (webgpu or webgl2) and the GPU adapter description are shown in the panel and recorded; if WebGPU is absent the default flips to forceWebGL and the SUMMARY says so (RESEARCH A13)"
    - "The plugin's surface code imports only from three, its own files and @tapestry/sdk types; it contains no Electron API and no reference to SharedArrayBuffer (PLUG-03, SIM-03)"
  artifacts:
    - path: "plugins/data-drawing/surface/src/replay.ts"
      provides: "replayFromZero orchestration and the MATCH/DIFF panel line"
      contains: "replayFromZero"
    - path: "plugins/data-drawing/surface/src/latency.ts"
      provides: "LatencyMeter with p50/p95 per stroke"
      contains: "p95"
    - path: "plugins/data-drawing/surface/src/sim-host.ts"
      provides: "MainThreadTransport beside WorkerTransport"
      contains: "MainThreadTransport"
  key_links:
    - from: "plugins/data-drawing/surface/src/sim.worker.ts"
      to: "plugins/data-drawing/surface/wasm/ddsim.mjs"
      via: "replayFromZero creates a second dd_create(seed), applies the log at each tick, steps to the live tick, hashes, destroys"
      pattern: "_dd_create"
    - from: "plugins/data-drawing/surface/src/main.ts"
      to: "plugins/data-drawing/surface/src/latency.ts"
      via: "LatencyMeter.markSent on pushSamples, markPainted in onFrame when the snapshot tick passes the sample's tick"
      pattern: "LatencyMeter"
  prohibitions:
    - statement: "MUST NOT use the replay-from-zero result to correct the live state; a DIFF is reported and recorded as a bug, never reconciled silently"
      status: resolved
      verification: judgment
---

## Phase Goal

**As a** painter holding a pressure pen (Kaelen), **I want to** paint inside Tapestry and watch nodes land on a plane facing a fixed camera, placed by a deterministic fixed-point sim running live in a Worker, **so that** the brush body's mass can be felt and seen.

<objective>
Close the phase where the roadmap says it closes: the same surface mounted inside Tapestry through the public extension point, replay-from-zero equality between the live Worker sim and a fresh replay of the session log, pen-to-ink latency measured with and without the Worker split, the renderer backend confirmed, twenty open/close cycles leak-free, and the feel of the presets verified by the person the project is for.

Purpose: phase success criteria 1, 3, 4 and 5 are only observable here; everything before was headless or in the dev page.

Output: `replay.ts`, `latency.ts`, `MainThreadTransport`, the panel lines, the cycles harness in the dev page, `replay.test.ts`, and a recorded verification sheet.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/kaelencook/Tapestry/data-drawing/.planning/PROJECT.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/ROADMAP.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/STATE.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-04-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-07-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md
@/Users/kaelencook/Tapestry/.claude/skills/spike-findings-tapestry/references/editor-and-app-integration.md
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim-host.ts
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim.worker.ts
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/main.ts

<interfaces>
SimHost additions:
  replayFromZero(): Promise<{ liveHash: Uint8Array; replayHash: Uint8Array; liveTick: number; nodeCount: number; replayNodeCount: number; firstDiffTick: number | null }>
    Worker: sim2 = _dd_create(seed); for t in 0..liveTick-1: apply every log entry with tick t; if t is a multiple of 60 compare _dd_hash of sim2 with a hash of the live sim recorded at that tick (keep a ring of the last N live hashes taken at multiples of 60 — cheap, on-demand) to find firstDiffTick; step; at liveTick compare final hashes; _dd_destroy(sim2)
  transport selection: new WorkerTransport(seed) | new MainThreadTransport(seed) — MainThreadTransport loads the same ddsim.mjs on the main thread, runs the same accumulator on requestAnimationFrame, and implements the identical SimHost interface (for latency comparison and tests only; the Worker is the default)

latency.ts:
  export class LatencyMeter { markSent(sampleKey: number, tick: number, t0: number): void; onSnapshot(tick: number): void; onPainted(now: number): void; strokeStats(): { count; p50; p95; max } ; reset(): void }
  Rule: a sample sent at wall time t0 stamped for sim tick T is "painted" at the first rAF callback that runs after a snapshot with tick > T has been applied to the node field; latency = paintedTime - t0

Panel lines (main.ts): backend=<webgpu|webgl2> adapter=<string|n/a> | transport=<worker|main> | latency p50=<ms> p95=<ms> (last stroke) | replay: <MATCH|DIFF at tick N> | tick=<n> nodes=<n> hash=<hex>
Dev page query params: ?webgl=1 (forceWebGL), ?transport=main, ?cycles=20 (mount/dispose loop with a 250 ms period, then a final mount; logs contexts created/lost counts and mounts/disposes)
</interfaces>
</context>

## Artifacts this phase produces

See `01-01-PLAN.md` for the consolidated phase table. This plan creates `replay.ts` (`verifyReplay`), `latency.ts` (`LatencyMeter`), `MainThreadTransport` and `replayFromZero` on `SimHost`/the worker, the panel lines, the `?cycles` harness in `dev-host.ts`, and `test/replay.test.ts`.

## Edge Coverage

Closes flagged assumption 3 (SIM-03: "reached only through a flat C ABI; the renderer sees copies") with the source gates below and the live/replay equality; the CANV-03 adjacency backstop from 01-07 is answered by the human in Task 2 (overlapping strokes composite later-on-top).

<tasks>

<task type="auto">
  <name>Task 1: Replay-from-zero verification, main-thread transport, latency meter, cycles harness, backend report</name>
  <read_first>
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim-host.ts and sim.worker.ts (01-07 state)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/main.ts and dev-host.ts
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 738-740 (Pattern 4: two transports), 1008 (SIM-03 test row), 803-806 (pitfall 7: 20-cycle test), 143-146 (alternatives: main-thread transport for latency only)
    - /Users/kaelencook/Tapestry/.claude/skills/spike-findings-tapestry/references/editor-and-app-integration.md lines 20-40 (contexts created/disposed counting; measuring input latency as event → painted frame)
    - /Users/kaelencook/Tapestry/.planning/spikes/CONVENTIONS.md lines 30-33 (measure frame intervals from requestAnimationFrame; gl.finish does not measure GPU time)
  </read_first>
  <files>plugins/data-drawing/tsconfig.json, plugins/data-drawing/surface/src/replay.ts, plugins/data-drawing/surface/src/latency.ts, plugins/data-drawing/surface/src/sim-host.ts, plugins/data-drawing/surface/src/sim.worker.ts, plugins/data-drawing/surface/src/main.ts, plugins/data-drawing/surface/src/dev-host.ts, plugins/data-drawing/surface/index.html, plugins/data-drawing/surface/test/replay.test.ts</files>
  <action>
    Worker + host: implement `replayFromZero` per the interfaces block (the live sim keeps a ring of the last 64 hashes taken at ticks that are multiples of 60 — compute them inside the accumulator loop only when `tick % 60 === 0`, that is one SHA-256 per second of sim time); add `MainThreadTransport` implementing the identical `SimHost` interface with the same accumulator driven by `requestAnimationFrame` (whole ticks only; wall clock only decides how many) — factor the accumulator + recorder into `sim-core.ts`? No: keep it in one module `sim-driver.ts` imported by both the worker and the main-thread transport so there is exactly one stamping/recording implementation (add `plugins/data-drawing/surface/src/sim-driver.ts` to files if you create it and say so in the SUMMARY). `replay.ts`: `verifyReplay(simHost) -> Promise<ReplayReport>` and `formatReplayLine(report)` producing `replay: MATCH (tick N, nodes M)` or `replay: DIFF at tick K`. `latency.ts`: `LatencyMeter` per the interfaces block using `performance.now()` on the main thread (allowed: left of the fence, never recorded); `main.ts` calls `markSent` in `onSamples` with the stamped tick the host will use (the host echoes the applied tick in `applied` messages — use that to bind the sample key to its tick), `onSnapshot(tick)` in the snapshot handler, `onPainted(now)` at the end of `onFrame`. Panel: the lines from the interfaces block; a "Verify replay" button; a transport switch (dev page: `?transport=main`; in Tapestry: a checkbox that disposes and re-creates the host — the log is re-sent to the new transport by replaying it, so the switch itself is a replay); backend line from `stage.backendInfo()` plus `navigator.gpu?.requestAdapter()` → `adapter.info?.description ?? adapter.info?.vendor ?? 'n/a'` (add `"@webgpu/types"` to `tsconfig.json` `types` for this). `dev-host.ts`: `?cycles=N` — mount, wait 250 ms, dispose, repeat N times, then a final mount; count `webglcontextlost` events and mounts/disposes and print `cycles: mounted=<n> disposed=<n> lost=<n>`; also `?webgl=1` passthrough.

    `test/replay.test.ts` (node, real Wasm, no Worker): drive the shared driver with a scripted log (define ink, stroke ordinal 1 with 40 synthetic samples over 40 ticks, end) applying live-style tick by tick to 300, then `replayFromZero`-style into a second instance from the recorded log — hashes equal at tick 300 and at every multiple of 60; then mutate one recorded byte and assert the replay reports a DIFF with a `firstDiffTick` <= the mutated action's tick + 60.

    Source gates for SIM-03/PLUG-03: no `SharedArrayBuffer`, no `from 'electron'`, no `window.tapestry` in `plugins/data-drawing/surface/src` (comment lines excluded); the worker and the main-thread transport both talk to the sim only through `_dd_*` calls (grep for `HEAPU8` shows only slice/subarray reads and the action-byte copy-in).
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry && npm --prefix plugins/data-drawing run typecheck && npm --prefix plugins/data-drawing test && npm --prefix plugins/data-drawing run build && ! (grep -rnE "SharedArrayBuffer|from 'electron'|window\.tapestry" plugins/data-drawing/surface/src | grep -vE '^[^:]+:[0-9]+:\s*(//|\*)' | grep -q .) && grep -n "MainThreadTransport" plugins/data-drawing/surface/src/sim-host.ts && grep -n "replayFromZero" plugins/data-drawing/surface/src/sim.worker.ts</automated>
    <fails_when>non-zero exit; vitest "failed" or replay.test.ts missing from the list; build fails; the negated grep group finds a non-comment match; either positive grep prints nothing</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: SharedArrayBuffer -->
    <!-- planner-discipline-allow: from 'electron' -->
    <!-- planner-discipline-allow: window\.tapestry -->
    <!-- planner-discipline-allow: window.tapestry -->
    <!-- planner-discipline-allow: performance.now -->
    <!-- planner-discipline-allow: Date.now -->
    - `replay.test.ts` passes both the MATCH and the DIFF cases and prints the firstDiffTick
    - `grep -c "performance.now" plugins/data-drawing/surface/src/latency.ts` is at least 1 and `grep -rn "performance.now\|Date.now" plugins/data-drawing/surface/src/ddsim-abi.ts` prints nothing (the clock never touches encoding)
    - `grep -n "cycles" plugins/data-drawing/surface/src/dev-host.ts` prints at least one line
    - Dev-page observation (executor, in the SUMMARY): `?cycles=20` prints `cycles: mounted=21 disposed=20 lost=<n>` with the page still drawing after the final mount; `?transport=main` paints with the mouse and the panel shows `transport=main`; "Verify replay" after a mouse stroke shows `replay: MATCH`
  </acceptance_criteria>
  <done>Live equals replay by construction and by test, both transports exist behind one interface, latency and backend are measured and displayed, and the cycles harness proves lifetime hygiene.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Feel, replay, latency, backend and lifetime verification inside Tapestry with the pen</name>
  <what-built>The finished Phase 1 surface inside Tapestry. Before this checkpoint the executor: rebuilt the Wasm (`npm --prefix plugins/data-drawing run sim:wasm`), built the surface (`npm --prefix plugins/data-drawing run build`), ran every test suite green (`ctest` on all native presets, `npm --prefix plugins/data-drawing test`, `npm --prefix app test`), and started `npm --prefix app run dev` in the background — Tapestry is running with "Open Data Drawing" in the launcher.</what-built>
  <how-to-verify>
    With the pressure pen, in the running Tapestry window:
    1. Click "Open Data Drawing". Read and copy the panel's first line: `backend=... adapter=...`. (If backend=webgl2 and adapter=n/a, WebGPU is absent under Electron 32 on this Mac — that is a finding, not a failure.)
    2. Preset `ink` (v1): draw a fast S-curve. Expected: nodes land close under the pen; the body disc stays near the pen ring; the spring line is short.
    3. Preset `lead` (v4): draw the same S-curve at the same speed. Expected: the body disc visibly trails the pen ring, the spring stretches, and the node trail cuts the corners of the S (momentum through curves); after you stop, the body drifts on for a moment and settles.
    4. Preset `rust` (v2) and `clay` (v3): one stroke each. Expected: lag between ink and lead; each preset's nodes have their own colour.
    5. Pressure: with `ink`, draw one stroke from feather-light to full pressure. Expected: node discs grow from small to full radius along the stroke.
    6. Overlapping: draw a `lead` stroke across an earlier `ink` stroke. Expected: the later stroke's nodes appear on top where they overlap (compositing in id order).
    7. Edit a brush: select `ink`, change mass to 8, click "Save as new version". Expected: the select shows `v5 ink m=8`; draw with v5 — it lags more than v1; earlier v1 strokes are unchanged on screen.
    8. Click "Verify replay". Expected: `replay: MATCH (tick N, nodes M)`.
    9. Read the latency line after your last stroke with the Worker transport: `latency p50=<a> p95=<b>`. Tick the "main-thread transport" checkbox, draw one more stroke, read the line again: `p50=<c> p95=<d>`. Copy all four numbers.
    10. Close the surface (Escape) and reopen it; repeat until you have opened it 20 times (a quick Escape / click rhythm is fine). Expected: it still paints on the 20th open. In DevTools Console, confirm there is no warning containing "Too many active WebGL contexts".
    11. Optional: the measurement overlay is still available with M; nothing else is expected there.
  </how-to-verify>
  <resume-signal>Paste: the backend/adapter line; one sentence each for steps 2, 3, 5, 6, 7 ("as expected" or what differed); the replay line; the four latency numbers; "20 opens OK" or the failure. Type "approved" if steps 2, 3, 7 and 8 matched expectations — those are the phase's success criteria 1, 4 and 5.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session log → replay sim | The recorded action list is re-applied to a second sim instance inside the Worker |
| Main-thread transport | The same Wasm module now also runs on the main thread (tests and latency comparison only) |
| Tapestry renderer ↔ plugin surface | The final mounted surface runs in the host realm through the CANV-04 layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `replayFromZero` (sim.worker.ts) | medium | mitigate | Replays only the worker's own recorded log; a DIFF is reported, never reconciled; the second instance is destroyed after use (no leak) |
| T-08-02 | Denial of service | Second sim instance memory during replay; 20-cycle Worker/renderer churn | medium | mitigate | `_dd_destroy` after replay; `dispose()` terminates the worker and forces context loss; cycles harness counts mounts/disposes/lost contexts |
| T-08-03 | Information disclosure | Latency numbers and adapter string in the panel | low | accept | Local display only; never recorded into actions |
| T-08-04 | Elevation of privilege | Plugin surface in the host realm | medium | accept | Per 01-02 policy; source gates prove no Electron API, no host globals, no SharedArrayBuffer |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- All suites green (native presets, plugin Vitest, app Vitest); build green; source gates green.
- Human verification sheet recorded: backend/adapter, feel of ink vs lead, pressure, overlap order, version edit, replay MATCH, four latency numbers, 20 opens.
</verification>

<success_criteria>
- Phase success criterion 1: plugin-owned canvas through the public extension point; pressure drives weight; heavy lags with momentum; light follows.
- Criterion 3: Wasm in a Worker through the flat C ABI; renderer reads copies; colour by description hash; pen/body/spring overlay.
- Criterion 4: presets pickable; edit creates a version; old strokes keep theirs.
- Criterion 5 (live half): replay from zero reproduces the live field and hash.
- Latency measured with and without the Worker split; backend confirmed; lifetime hygiene proven.
</success_criteria>

<output>
Create `.planning/phases/01-painting-with-the-pen/01-08-SUMMARY.md` when done, including the human's verification sheet verbatim, the cycles line, and the transport latency comparison.
</output>
