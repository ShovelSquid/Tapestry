---
phase: 01-painting-with-the-pen
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - data-drawing/sim/CMakeLists.txt
  - data-drawing/sim/CMakePresets.json
  - data-drawing/sim/cmake/two_process.cmake
  - data-drawing/sim/include/ddsim/fx64.hpp
  - data-drawing/sim/include/ddsim/rng.hpp
  - data-drawing/sim/src/sim.cpp
  - data-drawing/sim/tests/fx64_test.cpp
  - data-drawing/sim/tests/rng_test.cpp
  - data-drawing/sim/tests/restore_test.cpp
  - data-drawing/sim/tests/golden/many-brushes.actions
  - data-drawing/sim/tests/golden/many-brushes.sha256
  - data-drawing/sim/tools/gen_fixtures/main.cpp
  - data-drawing/sim/tools/ddsim_replay/main.cpp
autonomous: true
requirements: [SIM-01, SIM-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "fx64 multiply uses four 32x32->64 partial products with no 128-bit integer on the hot path, and matches a native __int128 oracle on one million seeded pairs covering all four sign combinations plus INT64_MIN, -1, 0, ONE and ONE-1 (SIM-01)"
    - "fx64 division and square root round toward negative infinity (floor) and are exact against integer oracles; sqrt of a negative input yields zero in Debug and Release alike (never a differing result) (SIM-01)"
    - "xoshiro256** seeded through splitmix64 produces identical sequences for identical seeds; per-entity streams derived from (seed, purpose, entity) differ; uniform_fx is in [0,1) and range_u32 uses Lemire multiply-shift with no standard-library distribution (SIM-01)"
    - "The native-ubsan preset (-fsanitize=undefined -fno-sanitize-recover=all) runs the full test suite and every golden fixture clean (SIM-01)"
    - "dd_restore either rejects a mutated serialized state with DD_ERR_RESTORE or accepts it and re-serializes to exactly the same bytes — never a silently corrected value (SIM-02)"
    - "ddsim_replay run twice as separate processes (CTest golden_two_process_<fixture>) prints identical checkpoint hashes that equal the committed .sha256, for every fixture, in Debug and Release (SIM-02)"
    - "The Wasm module rebuilt from the same source still matches every committed golden in Vitest, including many-brushes with multibyte UTF-8 descriptions (SIM-02)"
  artifacts:
    - path: "data-drawing/sim/include/ddsim/fx64.hpp"
      provides: "mul_q32, div_q32, isqrt64, isqrt128, sqrt, abs, min, max, clamp, lerp with a documented floor rounding rule"
      contains: "mul_q32"
    - path: "data-drawing/sim/tools/ddsim_replay/main.cpp"
      provides: "headless replay CLI printing '<tick> <sha256>' per checkpoint; --write-golden and --compare"
      contains: "write-golden"
    - path: "data-drawing/sim/cmake/two_process.cmake"
      provides: "CTest script running the CLI twice and diffing against the golden"
      contains: "STREQUAL"
    - path: "data-drawing/sim/tests/fx64_test.cpp"
      provides: "__int128 oracle tests"
      contains: "__SIZEOF_INT128__"
  key_links:
    - from: "data-drawing/sim/CMakeLists.txt"
      to: "data-drawing/sim/cmake/two_process.cmake"
      via: "add_test golden_two_process_<name> per fixture, passing $<TARGET_FILE:ddsim_replay>"
      pattern: "golden_two_process"
    - from: "data-drawing/sim/tools/ddsim_replay/main.cpp"
      to: "data-drawing/sim/tests/golden_support.hpp"
      via: "shares parseActions/replayFixture"
      pattern: "golden_support"
  prohibitions:
    - statement: "MUST NOT use a floating-point oracle, <random>, or std::sort-with-ties anywhere in the sim tests either; oracles are __int128 integer arithmetic, seeds come from splitmix64, so the harness can never certify a result the sim itself could not reproduce"
      status: resolved
      verification: test
---

## Phase Goal

**As a** painter holding a pressure pen (Kaelen), **I want to** paint inside Tapestry and watch nodes land on a plane facing a fixed camera, placed by a deterministic fixed-point sim running live in a Worker, **so that** the brush body's mass can be felt and seen.

<objective>
Complete the determinism harness on top of the skeleton: the full `fx64` operation set proven against an integer oracle, the PRNG with per-entity streams, the UBSan preset, strict-restore proofs, and the `ddsim_replay` CLI with two-process and Debug-vs-Release golden checks — everything the roadmap's wave 1 lists, still on a no-op `step()`.

Purpose: SIM-01 and SIM-02 in full. The brush body (01-05) needs `mul`, `div`, `sqrt` and the stability clamps; those must be exact and identical native/Wasm before any behaviour exists to blame.

Output: `fx64.hpp` complete, `rng.hpp` complete, `native-ubsan` preset, `ddsim_replay`, `two_process.cmake`, three new test files, one new fixture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/kaelencook/Tapestry/data-drawing/.planning/PROJECT.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/ROADMAP.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/STATE.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-01-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/research/PITFALLS.md
@/Users/kaelencook/Tapestry/data-drawing/sim/include/ddsim/fx64.hpp
@/Users/kaelencook/Tapestry/data-drawing/sim/include/ddsim/rng.hpp
@/Users/kaelencook/Tapestry/data-drawing/sim/tests/golden_support.hpp
@/Users/kaelencook/Tapestry/data-drawing/sim/CMakeLists.txt

<interfaces>
<!-- Contracts from 01-01 this plan extends; new signatures this plan adds. -->

From 01-01 fx64.hpp: struct fx64 { std::int64_t raw; static constexpr std::int64_t ONE = 1LL << 32; from_int, from_raw, from_q16, to_q16, + - unary-, comparisons, floor_to_int }

Added here (fx64.hpp, all constexpr-friendly, integer only):
  std::int64_t mul_q32(std::int64_t a, std::int64_t b);          // (a*b) >> 32 exact, four partial products (RESEARCH lines 420-434)
  std::int64_t div_q32(std::int64_t a, std::int64_t b);          // floor((a << 32) / b); b == 0 -> returns 0 in every build (never UB, never differs)
  std::uint64_t isqrt64(std::uint64_t x);                        // floor(sqrt(x)), fixed 32-iteration digit-by-digit loop
  std::uint64_t isqrt128(std::uint64_t hi, std::uint64_t lo);    // floor(sqrt(hi:lo)), fixed 64-iteration loop over two words
  fx64 operator*(fx64, fx64); fx64 operator/(fx64, fx64); fx64 sqrt(fx64);   // sqrt(x) = from_raw(isqrt128(x.raw >> 32, x.raw << 32)) for x >= 0; x < 0 -> from_raw(0) (+ assert in Debug)
  fx64 abs(fx64); fx64 min(fx64, fx64); fx64 max(fx64, fx64); fx64 clamp(fx64 v, fx64 lo, fx64 hi); fx64 lerp(fx64 a, fx64 b, fx64 t);
  Rounding rule (documented at the top of fx64.hpp): every operation truncates toward negative infinity; there is no round-half anything anywhere in the sim.

Added here (rng.hpp):
  std::uint64_t rotl(std::uint64_t x, int k);
  std::uint64_t Xoshiro256ss::next();                                   // reference algorithm, RESEARCH lines 455-461
  Xoshiro256ss seed_stream(std::uint64_t world_seed, std::uint64_t purpose_tag, std::uint64_t entity_id);  // splitmix64 state = world_seed ^ purpose_tag ^ entity_id, four outputs fill s[0..3]
  fx64 uniform_fx(Xoshiro256ss&);                                       // from_raw(next() >> 32) in [0, 1)
  std::uint32_t range_u32(Xoshiro256ss&, std::uint32_t n);              // Lemire multiply-shift: (uint64_t(uint32_t(next())) * n) >> 32; n == 0 -> 0
  constexpr std::uint64_t DD_PURPOSE_SETTLE = 0x5345544c45000000ull;    // reserved for Phase 2; unused now

ddsim_replay CLI:
  ddsim_replay <fixture.actions>                       -> prints "<tick> <hex>" per checkpoint, exit 0
  ddsim_replay <fixture.actions> --compare <golden>    -> exit 0 if equal, else prints "MISMATCH tick <t>: got <hex> expected <hex>" and exit 1
  ddsim_replay <fixture.actions> --write-golden <out>  -> writes the file, exit 0
  ddsim_replay <fixture.actions> --roundtrip           -> at every checkpoint also serialize/restore into a fresh sim and assert equal hash; exit 1 on divergence

CMake additions: option DDSIM_SANITIZE (OFF) -> adds -fsanitize=undefined -fno-sanitize-recover=all to ddsim_settings; preset native-ubsan (Debug, DDSIM_SANITIZE=ON);
  target ddsim_replay (tools/ddsim_replay/main.cpp, links ddsim, includes tests/ for golden_support.hpp);
  foreach fixture in tests/golden/*.actions: add_test(NAME golden_two_process_<name> COMMAND ${CMAKE_COMMAND} -DREPLAY=$<TARGET_FILE:ddsim_replay> -DACTIONS=<path> -DEXPECTED=<path>.sha256 -DWORK=${CMAKE_CURRENT_BINARY_DIR}/two_process -P cmake/two_process.cmake)
</interfaces>
</context>

## Artifacts this phase produces

See `01-01-PLAN.md` for the consolidated phase table. This plan completes `fx64.hpp` (`mul_q32`, `div_q32`, `isqrt64`, `isqrt128`, `sqrt`, `abs/min/max/clamp/lerp`), `rng.hpp` (`next`, `seed_stream`, `uniform_fx`, `range_u32`), adds the `native-ubsan` preset, `DDSIM_SANITIZE`, target `ddsim_replay`, CTest `golden_two_process_*`, `cmake/two_process.cmake`, tests `fx64_test.cpp`, `rng_test.cpp`, `restore_test.cpp`, and fixture `many-brushes`.

## Edge Coverage

Carries flagged assumptions 1 (SIM-01 gate scope) and 2 (SIM-02 walk) from the consolidated table in `01-01-PLAN.md`; this plan turns them into tests (UBSan run of every fixture; reject-or-exact restore sweep) but they remain documented assumptions about what the requirement text means.

<tasks>

<task type="auto">
  <name>Task 1: Full fx64 op set with integer oracles</name>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/sim/include/ddsim/fx64.hpp (the 01-01 skeleton being extended)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 412-442 (fx64: portable multiply listing, rounding policy, isqrt, oracle test list), 783-790 (pitfalls 2 and 3)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/research/PITFALLS.md lines 42-64 (Pitfall 2: fixed-point that is deterministic but wrong or undefined)
    - /Users/kaelencook/Tapestry/data-drawing/sim/tests/skeleton_test.cpp (suite style established in 01-01)
  </read_first>
  <files>data-drawing/sim/include/ddsim/fx64.hpp, data-drawing/sim/tests/fx64_test.cpp</files>
  <action>
    Extend `fx64.hpp` with the signatures in the interfaces block. `mul_q32` is the four-partial-product decomposition transcribed in RESEARCH lines 420-434 (low halves as unsigned 32-bit, high halves as signed via arithmetic shift; the low 64 bits from a wrapping unsigned multiply; result = high 32 of the low word | low 32 of the high word); no `__int128` may appear in include/ or src/ (the forbidden-token gate does not cover it, so add a source assertion in the test: grep-free — a `static_assert(sizeof(std::int64_t) == 8)` plus a comment is not enough; instead `fx64_test.cpp` includes a case that reads fx64.hpp through `readFile` and CHECKs the substring `__int128` is absent from the header). `div_q32(a, b)`: compute on magnitudes with a 96-by-64-bit shift-subtract long division built from two `uint64_t` words (numerator = |a| << 32), then apply the sign and floor: when the signs differ and the remainder is non-zero, subtract one — so the result is `floor(a * 2^32 / b)` exactly; `b == 0` returns 0 in every build (documented, asserted in Debug, never UB). `isqrt64` and `isqrt128` are digit-by-digit binary square roots with a FIXED iteration count (32 and 64) — never "until converged". `sqrt(fx64 x)` = `from_raw(isqrt128(x.raw >> 32, x.raw << 32))` for `x.raw >= 0` (that is floor(sqrt(raw * 2^32))), and `from_raw(0)` for negative input in both Debug and Release with an additional `assert` in Debug — the two builds must never differ. `abs`, `min`, `max`, `clamp`, `lerp(a, b, t) = a + (b - a) * t`. Put the one-paragraph rounding rule ("floor everywhere") at the top of the header. Keep the deleted floating-point constructors.

    `tests/fx64_test.cpp` (doctest, anonymous namespace, no floating point, no `<random>` — seeds from `splitmix64` in rng.hpp): under `#if defined(__SIZEOF_INT128__)` (native only): "fx64: mul_q32 matches __int128 oracle on 1e6 seeded pairs" — pairs drawn from splitmix64 and shaped into all four sign combinations plus a 10% share of small magnitudes; "fx64: mul edge cases" — INT64_MIN x 1, INT64_MIN x -1 (wraps, must equal the oracle's truncation to 64 bits), -1 x -1, ONE x ONE == ONE, x x ONE == x for 1000 x, commutativity; "fx64: div_q32 equals floor((a<<32)/b) oracle" on 1e5 seeded pairs with b != 0 including negative divisors and |b| < |a|; "fx64: div by zero returns 0"; "fx64: isqrt64/isqrt128 satisfy r*r <= x < (r+1)*(r+1)" using __int128 for the squares on 1e5 seeded inputs plus 0, 1, 2, 3, 4, UINT64_MAX, and powers of two; "fx64: sqrt(from_int(k*k)) == from_int(k)" for k in 0..1000 and "sqrt(negative) == 0"; "fx64: lerp endpoints and midpoint" (t = 0, ONE, ONE/2 with even inputs). Every case must also compile and pass with the sanitizer preset (Task 2).
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry/data-drawing/sim && cmake --preset native-debug && cmake --build --preset native-debug && ctest --preset native-debug --output-on-failure -R "fx64|ddsim_forbidden_tokens"</automated>
    <fails_when>non-zero exit; ctest prints "No tests were found" or any "Failed"; fewer than 7 fx64 cases listed by `ctest --preset native-debug -N -R fx64`</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: __int128 -->
    - `grep -v '^\s*//' include/ddsim/fx64.hpp | grep -c "__int128"` prints 0 (the header holds no 128-bit type outside comments; the test asserts the same on the raw file)
    - `grep -n "isqrt128\|div_q32\|mul_q32" include/ddsim/fx64.hpp` shows all three definitions
    - `ctest --preset native-debug -R fx64` reports at least 7 passing cases and the oracle case reports 1000000 iterations in its message
    - `ctest --preset native-release -R fx64` also passes (release build)
  </acceptance_criteria>
  <done>fx64 has its full op set, floor semantics are documented and oracle-proven, and Debug and Release agree by construction.</done>
</task>

<task type="auto">
  <name>Task 2: PRNG streams, strict-restore sweep, UBSan preset</name>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/sim/include/ddsim/rng.hpp (01-01 seeding skeleton)
    - /Users/kaelencook/Tapestry/data-drawing/sim/src/sim.cpp (restore path from 01-01)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 444-464 (PRNG reference C and stream derivation), 510-523 (canonical walk and strict restore)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/research/PITFALLS.md lines 87-107 (Pitfall 4: seeded randomness that is not reproducible)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-PATTERNS.md lines 632-634 (strict parse: reject, never clamp)
    - /Users/kaelencook/Tapestry/data-drawing/sim/CMakePresets.json and CMakeLists.txt (01-01)
  </read_first>
  <files>data-drawing/sim/include/ddsim/rng.hpp, data-drawing/sim/src/sim.cpp, data-drawing/sim/tests/rng_test.cpp, data-drawing/sim/tests/restore_test.cpp, data-drawing/sim/CMakeLists.txt, data-drawing/sim/CMakePresets.json</files>
  <action>
    `rng.hpp`: add `rotl`, `Xoshiro256ss::next()` exactly as the reference (result = rotl(s[1] * 5, 7) * 9; t = s[1] << 17; the four xors; s[2] ^= t; s[3] = rotl(s[3], 45)), `seed_stream(world_seed, purpose_tag, entity_id)`, `uniform_fx`, `range_u32` (Lemire multiply-shift), and the reserved purpose constant. All arithmetic on unsigned 64-bit (wrapping is defined). `rng_test.cpp`: "rng: same seed gives the same first 10000 outputs across two instances"; "rng: streams for different entity ids differ within the first 4 outputs"; "rng: an all-zero seed does not produce an all-zero state" (splitmix64 guarantees it); "rng: uniform_fx is in [0, ONE) for 1e5 draws"; "rng: range_u32(n) < n for n in {1, 2, 3, 7, 1000, 65536, UINT32_MAX} over 1e4 draws each and range_u32(0) == 0"; "rng: golden first output" — record `seed_stream(1234, 0, 0).next()` once as a hex constant in the test with a comment that regenerating it is a deliberate act (write the value on first run, then freeze it in the source).

    Strict restore: in `sim.cpp` make sure every field read by `restore` is range-checked against the DD_MAX_* limits and the version constants, that brush ids are strictly sequential from 1, that node ids are strictly ascending, and that a trailing byte is an error. `restore_test.cpp`: "restore: reject-or-exact byte sweep" — serialize the one-brush state at tick 60, then for every byte offset flip the byte (xor 0xFF) and call restore on a fresh sim: the result must be DD_ERR_RESTORE, OR DD_OK with `serialize()` equal to the mutated bytes (accepted exactly, never silently corrected); "restore: truncation at every length from 0 to len-1 is rejected"; "restore: hash after restore equals the hash of the source for noop, one-brush and many-brushes at every checkpoint" (uses the fixture parser; many-brushes arrives in Task 3 — write the loop over whatever fixtures exist).

    UBSan: `CMakeLists.txt` option `DDSIM_SANITIZE` (OFF) that, when ON and the compiler is Clang or GCC, adds `-fsanitize=undefined -fno-sanitize-recover=all` to `ddsim_settings` compile and link options (all targets, not just tests — pitfall 2 says the full fixture set must run sanitized). `CMakePresets.json`: configure preset `native-ubsan` (Debug, `DDSIM_SANITIZE=ON`, same binaryDir rule) with build and test presets.
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry/data-drawing/sim && cmake --preset native-ubsan && cmake --build --preset native-ubsan && ctest --preset native-ubsan --output-on-failure && ctest --preset native-debug --output-on-failure -R "rng|restore"</automated>
    <fails_when>non-zero exit; any "runtime error:" line from UBSan in the output; ctest prints "No tests were found" or any "Failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "fsanitize=undefined" CMakeLists.txt` prints one line inside an `if(DDSIM_SANITIZE)` block, and `grep -n '"native-ubsan"' CMakePresets.json` prints at least 3 lines (configure, build, test presets)
    - `ctest --preset native-ubsan` passes every test including `ddsim_forbidden_tokens` and every golden case with zero `runtime error:` lines
    - `ctest --preset native-debug -R rng` reports 6 passing cases; `-R restore` reports 3 passing cases
    - `grep -n "range_u32\|seed_stream\|uniform_fx" include/ddsim/rng.hpp` shows all three
  </acceptance_criteria>
  <done>PRNG streams exist and are tested, restore is proven reject-or-exact, and the whole suite runs clean under UBSan.</done>
</task>

<task type="auto">
  <name>Task 3: ddsim_replay CLI, two-process and Debug/Release golden checks, many-brushes fixture</name>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/sim/tests/golden_support.hpp (parser and replay rule from 01-01 — reuse, do not duplicate)
    - /Users/kaelencook/Tapestry/data-drawing/sim/tools/gen_fixtures/main.cpp (01-01 encoder; extend)
    - /Users/kaelencook/Tapestry/data-drawing/sim/tools/wasm-hash-check.mjs (the Node twin of the CLI)
    - /Users/kaelencook/Tapestry/tapestry/CMakeLists.txt lines 196-208 (plain-executable test registration pattern)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 712-716 (Pattern 1: golden hash files are the harness), 1002-1008 (SIM-02 test map row)
  </read_first>
  <files>data-drawing/sim/tools/ddsim_replay/main.cpp, data-drawing/sim/cmake/two_process.cmake, data-drawing/sim/CMakeLists.txt, data-drawing/sim/tools/gen_fixtures/main.cpp, data-drawing/sim/tests/golden/many-brushes.actions, data-drawing/sim/tests/golden/many-brushes.sha256</files>
  <action>
    `tools/ddsim_replay/main.cpp`: the CLI from the interfaces block. It includes `golden_support.hpp` (add `${PROJECT_SOURCE_DIR}/tests` to the target's PRIVATE include dirs) and drives the sim ONLY through `ddsim_c.h` (`dd_create/dd_apply/dd_step/dd_hash/dd_serialize/dd_restore`) so the CLI exercises the same ABI the Wasm build exports. `--roundtrip` serializes at each checkpoint, restores into a second `dd_create(seed)` instance, and compares `dd_hash` of both; `--compare` reads the golden and prints the first differing tick; `--write-golden` writes the `<tick> <hex>` lines. Exit codes 0/1; usage on bad arguments with exit 2.

    `cmake/two_process.cmake`: `execute_process` the CLI twice into `${WORK}/<name>.a` and `${WORK}/<name>.b` (fresh processes, so ASLR and allocator state differ), `file(READ)` both and `${EXPECTED}`, and `message(FATAL_ERROR)` unless `a STREQUAL b` and `a STREQUAL expected`; also run once with `--roundtrip` and require exit code 0. `CMakeLists.txt`: `add_executable(ddsim_replay ...)`, then `file(GLOB DDSIM_FIXTURES CONFIGURE_DEPENDS tests/golden/*.actions)` and a `foreach` registering `golden_two_process_<name>` per fixture with the command from the interfaces block. Because both native presets run these tests against the same committed `.sha256`, Debug-vs-Release equality is checked on every run without a special test.

    `tools/gen_fixtures/main.cpp`: add `many-brushes.actions` — seed 7, 50 `DefineBrush` actions at ticks 0, 1, 2, ... 49 (ids 1..50), descriptions cycling through `ink`, `green rust`, `loneliness`, `青苔`, `rust ✓` (multibyte UTF-8 on purpose: desc_len counts bytes), masses 1, 4, 16, 64 cycling, radius 0.75, spacing 0.5, identity curve; checkpoints 0, 25, 50, 600. Run the generator, then produce the golden with `ddsim_replay tests/golden/many-brushes.actions --write-golden tests/golden/many-brushes.sha256` from the native-release build, commit both. Rebuild the Wasm module (`npm --prefix plugins/data-drawing run sim:wasm`) and run the plugin's Vitest golden test, which iterates every fixture automatically — it must pass on the new fixture without any TS change.
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry/data-drawing/sim && cmake --preset native-release && cmake --build --preset native-release && ctest --preset native-release --output-on-failure && ctest --preset native-debug --output-on-failure -R golden_two_process && ./build/native-release/ddsim_replay tests/golden/many-brushes.actions --compare tests/golden/many-brushes.sha256 && ./build/native-release/ddsim_replay tests/golden/many-brushes.actions --roundtrip && cd /Users/kaelencook/Tapestry && npm --prefix plugins/data-drawing run sim:wasm && npm --prefix plugins/data-drawing test</automated>
    <fails_when>non-zero exit; ctest "No tests were found" for golden_two_process; ddsim_replay prints a "MISMATCH" line; vitest reports "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `ctest --preset native-release -N | grep -c golden_two_process` prints 3 (noop, one-brush, many-brushes)
    - `./build/native-release/ddsim_replay tests/golden/noop.actions` prints exactly 4 lines matching `^[0-9]+ [0-9a-f]{64}$`
    - `tests/golden/many-brushes.actions` contains 50 `action` lines and its `.sha256` has 4 lines; the line for tick 600 differs from one-brush's tick-600 line
    - `npm --prefix plugins/data-drawing test` output lists the many-brushes fixture as passing (the golden test names each fixture)
    - Running `ddsim_replay` from the `native-debug` build dir on `many-brushes.actions --compare ...` also exits 0 (Debug equals Release)
  </acceptance_criteria>
  <done>A person can verify "same file, same seed, same hash" from the command line, two processes and two build types agree with the committed goldens, and the Wasm build still matches every fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → `ddsim_replay` / tests | `.actions` text and hex are parsed by test-side code; a malformed fixture must fail loudly, not silently pass |
| Serialized bytes → `dd_restore` | Any byte sequence can be offered to restore (Phase 3 will feed it snapshots) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `dd_restore` (sim.cpp) — crafted state bytes | high | mitigate | Reject-or-exact byte sweep test; limits on every count; strictly ascending ids; trailing bytes rejected; state swapped in only on success |
| T-03-02 | Tampering | `parseActions` (golden_support.hpp) — malformed fixture text | medium | mitigate | Parser rejects unknown directives, non-hex, odd-length hex, non-ascending checkpoints with an exception the test reports; CLI exits 2 |
| T-03-03 | Repudiation | Golden regeneration | medium | mitigate | `.sha256` files are committed artefacts; only `--write-golden` (an explicit flag) or `DDSIM_WRITE_GOLDEN=1` writes them; CTest only compares |
| T-03-04 | Denial of service | Sanitizer/oracle loops (1e6 iterations) | low | accept | Test runtime is seconds; bounded fixed iteration counts |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `ctest` passes on `native-debug`, `native-release` and `native-ubsan` with the two-process golden tests present for every fixture.
- `fx64` oracle, `rng`, and `restore` cases are listed and green.
- Wasm rebuilt and `npm --prefix plugins/data-drawing test` green on all three fixtures.
</verification>

<success_criteria>
- SIM-01: fixed-point op set proven against integer oracles, PRNG project-owned with per-entity streams, UBSan clean, forbidden-token gate still active.
- SIM-02: replay twice across Debug/Release/two processes/serialize-restore yields identical SHA-256 at every checkpoint; the CLI a person can run exists.
</success_criteria>

<output>
Create `.planning/phases/01-painting-with-the-pen/01-03-SUMMARY.md` when done, including the frozen rng golden constant, the many-brushes golden lines, and the UBSan run summary.
</output>
