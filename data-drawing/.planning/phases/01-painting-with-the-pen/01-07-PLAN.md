---
phase: 01-painting-with-the-pen
plan: 07
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - plugins/data-drawing/package.json
  - plugins/data-drawing/surface/src/stage/scene.ts
  - plugins/data-drawing/surface/src/stage/nodes.ts
  - plugins/data-drawing/surface/src/stage/overlay.ts
  - plugins/data-drawing/surface/src/stage/colour.ts
  - plugins/data-drawing/surface/src/brushes.ts
  - plugins/data-drawing/surface/src/ddsim-abi.ts
  - plugins/data-drawing/surface/src/sim-host.ts
  - plugins/data-drawing/surface/src/sim.worker.ts
  - plugins/data-drawing/surface/src/main.ts
  - plugins/data-drawing/surface/index.html
  - plugins/data-drawing/surface/test/colour.test.ts
  - plugins/data-drawing/surface/test/camera-parity.test.ts
  - plugins/data-drawing/surface/test/nodes.test.ts
  - plugins/data-drawing/surface/test/encoder.test.ts
autonomous: false
requirements: [CANV-03, CANV-01, CANV-02, STRK-04, STRK-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A human confirmed three@0.186.0, @types/three@0.186.0 and @webgpu/types@0.1.74 on npmjs.com before they were installed with exact pins into plugins/data-drawing (package legitimacy: all three flagged [SUS] too-new)"
    - "The stage is one three.js renderer per surface created from three/webgpu WebGPURenderer with a forceWebGL switch (query ?webgl=1 in the dev page, a setting in Tapestry), a PerspectiveCamera built from FIXED_CAMERA, a plane mesh at z = 0, an InstancedMesh node field and a cursor overlay; dispose calls setAnimationLoop(null), traverses geometry/material disposal, renderer.dispose(), forceContextLoss when available, removes the canvas, and is a no-op on the second call (CANV-03, spike rules)"
    - "Each node is coloured from fnv1a32 over the UTF-8 bytes of its brush description (Math.imul, hue = h mod 360 / 360, s 0.6, l 0.5) — the same bytes the sim hashes, so 'ink' and 'ink ' differ; node size = brush radius x (0.2 + 0.8 x weight) so pressure drives visible weight (CANV-03, STRK-04 encoding)"
    - "The cursor overlay draws the pen position (latest quantized sample), the brush body position (from the transferred body snapshot) and the spring between them; it is hidden when no stroke is open (CANV-03)"
    - "With zero nodes the stage renders the plane and overlay with instance count 0 and no error; with N nodes above capacity the InstancedMesh is re-allocated to the next power of two >= N and all N are drawn; instances are uploaded in the snapshot's ascending-NodeId order and never re-sorted on the render side; Q32.32 to float conversion happens only on the main thread via Number(BigInt) / 2^32 (CANV-03 boundary, empty, ordering, precision)"
    - statement: "Two nodes at identical positions are both drawn and overlapping instances composite in ascending NodeId order (later stroke on top) because the node material writes no depth"
      verification: backstop
    - "The brush picker offers the four presets (ink 1, rust 4, clay 16, lead 64) mirrored byte-for-byte from DD_PRESETS (encoder test against presets.actions); editing a brush (description, mass, radius, spacing) creates a NEW version id through DefineBrush and selects it; earlier versions remain in the table and are never mutated (STRK-04)"
    - "Painting in the dev page: pen-down captures the plane frame once into StrokeBegin, coalesced samples flow through PenFence -> SimHost.pushSamples -> the Worker, which stamps (tick, index) at record time, encodes, applies, and logs; nodes appear on the plane in the next snapshot; the predicted tail is drawn only in the overlay (STRK-01, CANV-01, CANV-02)"
    - "The TS encoder is byte-identical to the C++ one: encoding the presets equals presets.actions, and encoding the synthetic one-stroke log and replaying it through the Wasm module in Node reproduces one-stroke.sha256 (STRK-01)"
  artifacts:
    - path: "plugins/data-drawing/surface/src/stage/scene.ts"
      provides: "createStage with WebGPURenderer/forceWebGL and idempotent dispose"
      contains: "forceContextLoss"
    - path: "plugins/data-drawing/surface/src/stage/colour.ts"
      provides: "fnv1a32, colourFor"
      contains: "Math.imul"
    - path: "plugins/data-drawing/surface/src/brushes.ts"
      provides: "PRESETS, BrushTable"
      contains: "export const PRESETS"
    - path: "plugins/data-drawing/surface/test/encoder.test.ts"
      provides: "TS encoder vs C++ goldens"
      contains: "one-stroke"
  key_links:
    - from: "plugins/data-drawing/surface/src/main.ts"
      to: "plugins/data-drawing/surface/src/input.ts"
      via: "PenFence callbacks onBegin/onSamples/onEnd/onPreview wired to SimHost and the overlay"
      pattern: "onSamples"
    - from: "plugins/data-drawing/surface/src/sim.worker.ts"
      to: "plugins/data-drawing/surface/src/ddsim-abi.ts"
      via: "encodeStrokeBegin/encodeStrokeSamples/encodeStrokeEnd on worker-stamped samples"
      pattern: "encodeStrokeSamples"
    - from: "plugins/data-drawing/surface/src/stage/nodes.ts"
      to: "plugins/data-drawing/surface/src/ddsim-abi.ts"
      via: "decodeNodes(snapshot.nodes, count) with NODE_STRIDE"
      pattern: "decodeNodes"
  prohibitions:
    - statement: "MUST NOT add input smoothing, stabilization, or prediction anywhere on the render side; the predicted tail is drawn and discarded and the brush mass in the sim is the only stabilizer"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the renderer hold or mutate sim state: the stage reads transferred snapshot copies only and every change to the world goes through a recorded action"
      status: resolved
      verification: test
---

## Phase Goal

**As a** painter holding a pressure pen (Kaelen), **I want to** paint inside Tapestry and watch nodes land on a plane facing a fixed camera, placed by a deterministic fixed-point sim running live in a Worker, **so that** the brush body's mass can be felt and seen.

<objective>
Make painting visible in the plugin's own dev page: the three.js stage (fixed camera, plane, instanced node field coloured by description hash, cursor overlay of pen, body and spring), the brush picker with four versioned presets and edit-creates-a-version, the TS action encoder proven byte-identical to the C++ one, and the Worker's stroke API that stamps samples at record time — all wired from the fence to the sim and back.

Purpose: CANV-03 and the surface halves of STRK-04, STRK-01, CANV-01, CANV-02. After this plan a person paints and sees nodes; 01-08 moves the same surface into Tapestry and verifies feel.

Output: `stage/*`, `brushes.ts`, extended `ddsim-abi.ts`, `sim-host.ts`, `sim.worker.ts`, `main.ts`, four tests, three.js installed after the legitimacy checkpoint.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/kaelencook/Tapestry/data-drawing/.planning/PROJECT.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/ROADMAP.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/STATE.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-05-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-06-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md
@/Users/kaelencook/Tapestry/data-drawing/.claude/CLAUDE.md
@/Users/kaelencook/Tapestry/.claude/skills/spike-findings-tapestry/SKILL.md
@/Users/kaelencook/Tapestry/.planning/spikes/CONVENTIONS.md
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim-host.ts
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim.worker.ts
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/ddsim-abi.ts
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/input.ts
@/Users/kaelencook/Tapestry/data-drawing/sim/include/ddsim/presets.hpp
@/Users/kaelencook/Tapestry/data-drawing/sim/tests/action_writer.hpp

<interfaces>
<!-- From 01-01/01-05/01-06; new members this plan adds. -->

Wire layouts (01-05 interfaces block) — the TS encoder must reproduce them exactly: header u8 kind|u8 1|u16 0|u32 len; StrokeBegin 56 bytes; Sample 24 bytes; StrokeEnd 12 bytes. DD_PRESETS raw values (01-05). Synthetic curve: u = t*16384, v = ((t*7)%40-20)*8192, pressure = min(65535, t*546).

SimHost additions (sim-host.ts):
  beginStroke(spec: { ordinal: number; brushVersionId: number; frameQ16: Int32Array; pressureSource: 0 | 1 }): void
  pushSamples(ordinal: number, samples: RawSample[]): void        // RawSample from input.ts (no tick/index)
  endStroke(ordinal: number): void
  Worker side stamps: tick = _dd_tick(sim) at message arrival; index = per-stroke counter that resets when the tick changes; encodes and applies immediately; appends { tick, bytes } to the session log; posts rejected { code, name, kind, ordinal } on DD_ERR_*. beginStroke/endStroke use the same current tick for start_tick/end_tick.
  Painting while paused: the surface refuses to begin a stroke while paused (samples would pile into one tick and hit DD_MAX_SAMPLES_PER_TICK).

ddsim-abi.ts additions: encodeStrokeBegin(spec + startTick), encodeStrokeSamples(strokeId: bigint, stamped: StampedSample[]), encodeStrokeEnd(strokeId, endTick), strokeIdOf(branch, ordinal): bigint, ordinalOf(id: bigint), q32ToFloat(dv, offset), decodeNodes -> { count, id(i): bigint, x(i), y(i), z(i), weight(i), brush(i) } lazy accessors over the DataView, decodeBodies -> { count, strokeId(i), x(i), y(i), vx(i), vy(i), targetU(i), targetV(i) }

stage/scene.ts: export async function createStage(container: HTMLElement, opts: { forceWebGL: boolean; onFrame(): void }): Promise<Stage>
  Stage { renderer; camera: PerspectiveCamera; scene; plane: Mesh; nodes: NodeField; overlay: CursorOverlay; resize(w, h, dpr): void; backendInfo(): 'webgpu' | 'webgl2'; dispose(): void }
stage/nodes.ts: class NodeField { update(snapshot: Snapshot, brushes: BrushTable): void; count: number; capacity: number }   (mesh: InstancedMesh(CircleGeometry(1, 24), MeshBasicMaterial({ vertexColors: false, depthWrite: false, transparent: true, opacity: 0.9 })), instanceColor per node)
stage/overlay.ts: class CursorOverlay { update(pen: { u; v } | null, body: { x; y } | null, preview: Array<{ u; v }>, frame: PlaneFrame): void }   (pen ring, body disc, spring Line, preview polyline)
stage/colour.ts: export function fnv1a32(s: string): number; export function colourFor(description: string): Color
brushes.ts: export interface BrushVersionSpec { description: string; mass: number; radius: number; spacing: number; curve: Uint16Array(17) }
  export const PRESETS: BrushVersionSpec[]   // ink/rust/clay/lead with mass 1/4/16/64 and the raw radius/spacing of DD_PRESETS expressed as floats that round back to the same raw (0.75/0.9/1.1/1.3 and 0.5/0.45/0.4/0.35) — the encoder test proves equality
  export class BrushTable { versions: Array<BrushVersionSpec & { id: number }>; current: number; define(spec): Promise<number>; edit(baseId, patch): Promise<number>; get(id) }
</interfaces>
</context>

## Artifacts this phase produces

See `01-01-PLAN.md` for the consolidated phase table. This plan creates `stage/scene.ts` (`createStage`, `Stage`), `stage/nodes.ts` (`NodeField`), `stage/overlay.ts` (`CursorOverlay`), `stage/colour.ts` (`fnv1a32`, `colourFor`), `brushes.ts` (`PRESETS`, `BrushTable`, `BrushVersionSpec`), the stroke encoders/decoders in `ddsim-abi.ts`, `beginStroke/pushSamples/endStroke` on `SimHost` and the worker's stamping recorder, the painting wiring in `main.ts`, the dev page's brush panel, and tests `colour`, `camera-parity`, `nodes`, `encoder`.

## Edge Coverage

Explicit truths authored above for probe items 13 (CANV-03 boundary), 15 (empty), 16 (ordering), 17 (precision) and 8 (STRK-04 encoding, colour side); item 14 (CANV-03 adjacency) is authored as a `verification: backstop` marker because compositing order is a visual property the verifier cannot confirm from source alone (it abstains to human_needed, which 01-08's checkpoint answers).

<assumption_delta_decision>
Renderer backend (`pluralization: fallback` signal): the backend is a construction parameter `createStage({ forceWebGL })` of one Stage; the sim, the actions and the log never see it. Decision: `no-change`; nothing to promote. WebGPU-vs-WebGL2 is reported by `backendInfo()` and recorded by the 01-08 checkpoint; if WebGPU is unavailable on this Mac the default flips to `forceWebGL: true` in one place.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy — confirm three, @types/three and @webgpu/types on npmjs.com before install</name>
  <what-built>Nothing installed yet. RESEARCH's Package Legitimacy Audit flagged three npm packages as [SUS] "too-new" by age only (published 3-14 days before research): three@0.186.0 (12.18M downloads/week, github.com/mrdoob/three.js), @types/three@0.186.0 (8.03M/week, DefinitelyTyped), @webgpu/types@0.1.74 (5.91M/week, github.com/gpuweb/types). The outer 02.3-04 plan gated the same three.js version the same way. Nothing in this plan proceeds until you confirm them.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/three — confirm the latest version is 0.186.0 (or that 0.186.0 exists in the Versions tab), the repository link is github.com/mrdoob/three.js, and weekly downloads are in the millions.
    2. Open https://www.npmjs.com/package/@types/three — confirm 0.186.0 exists and the publisher is the DefinitelyTyped bot ("types").
    3. Open https://www.npmjs.com/package/@webgpu/types — confirm 0.1.74 exists and the repository is github.com/gpuweb/types.
    4. Confirm none of the three pages shows a deprecation or security banner.
  </how-to-verify>
  <resume-signal>Type "approved: three, @types/three, @webgpu/types" to allow the exact-pinned install, or name any package to reject (the plan then stops and reports).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install three.js and build the stage — renderer lifetime, node field, colour hash, cursor overlay — with node-side tests</name>
  <precondition>The human approved all three packages in Task 1 (the approval text is in the conversation or 01-07-SUMMARY.md)</precondition>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 629-639 (renderer choice), 351-359 (2.3 stage precedent and three 0.186 gotchas), 803-806 (pitfall 7), 859-881 (main-thread float conversion, colour hash examples)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-PATTERNS.md lines 404-439 (renderer creation and dispose analogs), 648-651 (context lifetime shared pattern)
    - /Users/kaelencook/Tapestry/.claude/skills/spike-findings-tapestry/references/editor-and-app-integration.md lines 20-40 (dispose + forceContextLoss; StrictMode; contexts created/disposed counting)
    - /Users/kaelencook/Tapestry/.planning/spikes/CONVENTIONS.md lines 43-47 (three 0.186.0 gotchas)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/camera.ts and plane.ts (01-06)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/ddsim-abi.ts (01-01: NODE_STRIDE, BODY_STRIDE, decode helpers)
  </read_first>
  <files>plugins/data-drawing/package.json, plugins/data-drawing/surface/src/stage/scene.ts, plugins/data-drawing/surface/src/stage/nodes.ts, plugins/data-drawing/surface/src/stage/overlay.ts, plugins/data-drawing/surface/src/stage/colour.ts, plugins/data-drawing/surface/test/colour.test.ts, plugins/data-drawing/surface/test/camera-parity.test.ts, plugins/data-drawing/surface/test/nodes.test.ts</files>
  <action>
    Install from the workspace root with exact pins: `npm install -w plugins/data-drawing --save-exact three@0.186.0` and `npm install -w plugins/data-drawing --save-exact -D @types/three@0.186.0 @webgpu/types@0.1.74`; confirm `npm ls three` shows `three@0.186.0` (grep without a `^` anchor — the tree prefix characters). Add `"@webgpu/types"` to `tsconfig.json` `types` only if a file references `navigator.gpu` typed members (01-08 does; add it there if not needed now).

    `stage/colour.ts` per the interfaces block and RESEARCH lines 872-881 (FNV-1a 32 with `Math.imul`, `>>> 0`, over `new TextEncoder().encode(s)`). `stage/scene.ts`: `createStage` — `import { WebGPURenderer } from 'three/webgpu'` and the rest from `three`; `new WebGPURenderer({ canvas, antialias: true, forceWebGL: opts.forceWebGL })`, `await renderer.init()`, `setPixelRatio(dpr)`, clear colour 0x0d0f14; `PerspectiveCamera(FIXED_CAMERA.fovDeg, aspect, near, far)` positioned from `FIXED_CAMERA` with `lookAt(target)`; plane: `PlaneGeometry(200, 125)` at z = 0 with a faint `MeshBasicMaterial` (colour 0x1a1d24) plus a `GridHelper`-like line grid (use `LineSegments` with a 10-unit grid, colour 0x2a2e38) so the plane reads as a surface; `NodeField` and `CursorOverlay` added to the scene; `renderer.setAnimationLoop(() => { opts.onFrame(); renderer.render(scene, camera) })`; `resize(w, h, dpr)` updates renderer size, pixel ratio and camera aspect; `backendInfo()` returns `'webgpu'` when `renderer.backend?.isWebGPUBackend` is truthy else `'webgl2'`; `dispose()` guarded by a `disposed` flag: `setAnimationLoop(null)` → `scene.traverse` disposing geometries, materials and texture uniforms → `renderer.dispose()` → `if (typeof renderer.forceContextLoss === 'function') renderer.forceContextLoss()` → `canvas.remove()`; register `webglcontextlost`/`webglcontextrestored` listeners on the canvas that log and, on restore, rebuild the node field from the last snapshot. Keep every material a built-in (no ShaderMaterial) so WebGPU and WebGL2 render identically.

    `stage/nodes.ts`: `NodeField` with an `InstancedMesh` of `CircleGeometry(1, 24)` facing the camera (plane at z=0 faces +z; the camera looks down -z, so circles in the XY plane face it) and `MeshBasicMaterial({ depthWrite: false, transparent: true, opacity: 0.9 })`; `instanceColor` buffer; capacity starts at 4096 and, when `snapshot.nodeCount > capacity`, is re-allocated to the next power of two >= count (new mesh replaces the old in the scene; old geometry/material disposed); `update(snapshot, brushes)` walks `decodeNodes(snapshot.nodes, snapshot.nodeCount)` in order i = 0..n-1 (the snapshot is ascending NodeId; never sort here): position from `q32ToFloat` for x, y, z; scale = `brushes.get(brush).radius * (0.2 + 0.8 * weight)`; `setMatrixAt(i, ...)`, `setColorAt(i, colourFor(brushes.get(brush).description))`; `mesh.count = n`; `instanceMatrix.needsUpdate = true`, `instanceColor.needsUpdate = true`. `stage/overlay.ts`: `CursorOverlay` with a pen ring (`RingGeometry`, white), a body disc (`CircleGeometry`, the current brush colour), a `Line` (BufferGeometry with 2 points, `LineBasicMaterial`) from pen to body, and a preview polyline (`Line`, dim colour) for the predicted tail; `update(pen, body, preview, frame)` maps plane-local (u, v) to world via `origin + u*right + v*up` (floats) and sets visibility false for pen/body/spring when `pen` is null (no open stroke).

    Tests (node env; three's math and object model work without a renderer): `test/colour.test.ts` — `fnv1a32('')` equals 0x811c9dc5, `fnv1a32('ink')` equals a value computed once and frozen in the test, `colourFor('ink')` differs from `colourFor('ink ')`, same input twice gives equal `Color.getHex()`; `test/camera-parity.test.ts` — build a `PerspectiveCamera` from `FIXED_CAMERA` and a `Raycaster` from five NDC points, intersect `new Plane(new Vector3(0,0,1), 0)`, compare with `rayPlane` from plane.ts: |Δu|, |Δv| < 1e-6 for each; `test/nodes.test.ts` — construct `NodeField` (no renderer), feed a synthetic snapshot buffer of 5000 nodes with ascending ids → `capacity === 8192`, `count === 5000`, instance 0's matrix translation equals node 0's decoded position; feed a 0-node snapshot → `count === 0` with no throw; feed nodes in ascending order and assert instance i's colour equals `colourFor(description of node i)` (ordering preserved).
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry && npm ls three 2>/dev/null | grep -F "three@0.186.0" && npm --prefix plugins/data-drawing run typecheck && npm --prefix plugins/data-drawing test && npm --prefix plugins/data-drawing run build</automated>
    <fails_when>non-zero exit; "three@0.186.0" absent from npm ls; vitest "failed" or fewer than 3 new test files listed (colour, camera-parity, nodes); build fails</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: ShaderMaterial -->
    - `plugins/data-drawing/package.json` pins `"three": "0.186.0"` (no caret) and dev pins `"@types/three": "0.186.0"`, `"@webgpu/types": "0.1.74"`
    - `grep -n "from 'three/webgpu'" plugins/data-drawing/surface/src/stage/scene.ts` prints one line; `grep -n "setAnimationLoop(null)" scene.ts` and `grep -n "forceContextLoss" scene.ts` and `grep -n "renderer.dispose()" scene.ts` each print at least one line, and `setAnimationLoop(null)` appears on a smaller line number than `renderer.dispose()`
    - `grep -n "ShaderMaterial" plugins/data-drawing/surface/src/stage/*.ts` prints nothing
    - `grep -n "sort(" plugins/data-drawing/surface/src/stage/nodes.ts` prints nothing (never re-sorted)
    - `grep -n "getBigInt64" plugins/data-drawing/surface/src/ddsim-abi.ts` prints at least one line (float conversion via BigInt on the main thread)
    - `nodes.test.ts` asserts `capacity === 8192` after 5000 nodes and passes
  </acceptance_criteria>
  <done>three.js is installed with human-confirmed pins, and the stage renders a node field and overlay from snapshots with correct lifetime rules, verified where node can verify it.</done>
</task>

<task type="auto">
  <name>Task 3: Brush presets and versioning, the TS stroke encoder proven against C++ goldens, the Worker's stamping recorder, and live painting in the dev page</name>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-05-PLAN.md (the interfaces block: wire layouts, validation table, presets raw values, synthetic curve formula) and 01-05-SUMMARY.md
    - /Users/kaelencook/Tapestry/data-drawing/sim/tests/action_writer.hpp (the C++ encoder the TS must match byte for byte)
    - /Users/kaelencook/Tapestry/data-drawing/sim/tests/golden/presets.actions and one-stroke.actions (the expected bytes)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/sim-host.ts, sim.worker.ts, ddsim-abi.ts, main.ts, input.ts (extend)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 718-740 (Patterns 2-4), 746-755 (anti-patterns: BigInt ids, no React in the input path, predicted never applied)
  </read_first>
  <files>plugins/data-drawing/surface/src/brushes.ts, plugins/data-drawing/surface/src/ddsim-abi.ts, plugins/data-drawing/surface/src/sim-host.ts, plugins/data-drawing/surface/src/sim.worker.ts, plugins/data-drawing/surface/src/main.ts, plugins/data-drawing/surface/index.html, plugins/data-drawing/surface/test/encoder.test.ts</files>
  <action>
    `ddsim-abi.ts`: add the three stroke encoders per the 01-05 wire layouts (a `ByteWriter` over `DataView` LE; stroke ids as `bigint` via `strokeIdOf(branch, ordinal) = (BigInt(branch) << 32n) | BigInt(ordinal)`), `StampedSample = RawSample & { tick: number; index: number }`, the lazy `decodeNodes`/`decodeBodies` accessors, and `q32ToFloat`. `brushes.ts`: `PRESETS` and `BrushTable` per the interfaces block; `define` calls `simHost.defineBrush(spec)` and records the returned id; `edit(baseId, patch)` builds a new spec from the base plus the patch and calls `define` (append-only; never touches `versions[baseId]`); `current` is the selected id.

    `sim.worker.ts`: handle `beginStroke { ordinal, brushVersionId, frameQ16, pressureSource }`, `samples { ordinal, samples: RawSample[] }`, `endStroke { ordinal }` — the worker is the recorder: read `tick = _dd_tick(sim)`; for samples, keep `stamp[ordinal] = { tick, nextIndex }` and reset `nextIndex` to 0 when the tick changed; build `StampedSample`s, encode, `_dd_apply`, on DD_OK push `{ tick, bytes }` onto the session log and post `applied`, otherwise post `rejected { code, name, kind, ordinal }` and record nothing; beginStroke/endStroke likewise with `start_tick/end_tick = tick`. `sim-host.ts`: add `beginStroke`, `pushSamples`, `endStroke` to `SimHost` and `WorkerTransport` (fire-and-forget posts; the `samples` array is copied into a compact `Int32Array`/typed layout before posting to avoid structured-clone overhead — or post the `RawSample[]` directly if under 1 ms in the dev page; measure once and note).

    `main.ts` wiring: on mount, `await createStage(container, { forceWebGL: new URLSearchParams(location.search).get('webgl') === '1' || host-provided setting, onFrame })`; create `BrushTable` and define the four presets on `ready` (ids 1..4; `current = 1`); a small DOM brush panel (a `<select>` of versions labelled `v<id> <description> m=<mass>`, an Edit form with description/mass/radius/spacing fields and an "Save as new version" button that calls `edit(current, patch)` and selects the new id, a "Mouse painting" checkbox bound to `Settings.allowMouse`, a pause toggle); `PenFence.attach(stage.renderer.domElement, { viewport: () => container size, frame: () => DEFAULT_PLANE, settings, callbacks })` with `onBegin` → if paused return without beginning; `ordinal = ++session.nextOrdinal` (starts at 1, branch 0); `simHost.beginStroke({ ordinal, brushVersionId: brushes.current, frameQ16: frameToQ16(frame), pressureSource })`; `onSamples` → `simHost.pushSamples(ordinal, samples)` and remember the last sample as the overlay's pen position; `onEnd` → `simHost.endStroke(ordinal)`, pen null; `onPreview` → overlay preview only. `onSnapshot` → keep `lastSnapshot`; `onFrame` → `nodes.update(lastSnapshot, brushes)`, `overlay.update(pen, bodyFor(ordinal) from decodeBodies, preview, frame)`. `onRejected` → show the error name in the panel (never silent). `dispose` order: detach fence → simHost.dispose → stage.dispose (idempotent). `index.html`: no change beyond a container for the panel if needed.

    `test/encoder.test.ts` (node, real Wasm): (1) `encodeDefineBrush(PRESETS[i])` for i = 0..3 equals the `action` bytes at ticks 0..3 in `presets.actions` (parse the hex lines with the shared fixture parser); (2) build the one-stroke log in TS: DefineBrush ink at tick 0, StrokeBegin ordinal 1 at tick 10 with `frameToQ16(DEFAULT_PLANE)`, one StrokeSamples per tick 10..129 with the synthetic curve sample (tick t+10, index 0), StrokeEnd at 130 — assert the encoded bytes equal `one-stroke.actions` action bytes line by line; (3) replay that TS-built log through the Wasm module (fresh sim, seed 42) and assert the hashes at ticks 10, 70, 130, 600 equal `one-stroke.sha256`; (4) a worker-stamping unit: simulate three `samples` messages within one tick then one after a step and assert indices 0,1,2 then 0 (factor the stamping into a pure function `stampSamples(state, tick, samples)` in ddsim-abi.ts so it is testable without a Worker).
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry && npm --prefix plugins/data-drawing run typecheck && npm --prefix plugins/data-drawing test && npm --prefix plugins/data-drawing run build && grep -n "stampSamples" plugins/data-drawing/surface/src/ddsim-abi.ts plugins/data-drawing/surface/src/sim.worker.ts</automated>
    <fails_when>non-zero exit; vitest "failed"; encoder.test.ts not listed; build fails; the stampSamples grep prints fewer than 2 lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `encoder.test.ts` passes cases (1)-(4); case (3) reports the four matching hashes
    - `grep -n "export const PRESETS" plugins/data-drawing/surface/src/brushes.ts` prints one line and the array has four entries with masses 1, 4, 16, 64 and descriptions ink, rust, clay, lead
    - `grep -n "versions\[" plugins/data-drawing/surface/src/brushes.ts | grep -v "push\|length"` shows no assignment into an existing version (append-only)
    - `grep -rn "getPredictedEvents" plugins/data-drawing/surface/src/main.ts plugins/data-drawing/surface/src/sim-host.ts plugins/data-drawing/surface/src/sim.worker.ts` prints nothing (predicted points exist only inside input.ts and the overlay)
    - Dev-page observation (executor, recorded in the SUMMARY): `npm --prefix plugins/data-drawing run sim:wasm && npm --prefix plugins/data-drawing run dev`; with the mouse checkbox ON and a mouse (no pen at the executor's disposal), a drag draws a row of coloured discs on the plane, the overlay shows the pen ring, the body disc and the spring, selecting `lead` and dragging shows the body visibly trailing the pointer, "Save as new version" adds `v5` to the select, and `window.__dd.sim.log()` returns Begin/Samples/End entries whose ticks are non-decreasing
  </acceptance_criteria>
  <done>A person can paint in the dev page with any of four versioned presets, see nodes and the spring cursor, create a new brush version, and every recorded byte is proven identical to the native encoder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → plugin package | Three new packages enter the workspace; a compromised package would run inside the Tapestry renderer |
| Fence → Worker → sim | Sample objects cross a postMessage boundary and become recorded bytes |
| Snapshot → GPU | Node bytes are turned into instance buffers on the main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | npm installs: three, @types/three, @webgpu/types | high | mitigate | `checkpoint:human-verify gate="blocking-human"` before install (all three are [SUS] too-new); exact pins (`--save-exact`); repositories and download counts confirmed by a human on npmjs.com; no postinstall scripts (RESEARCH verified) |
| T-07-01 | Tampering | Worker stamping recorder (sim.worker.ts) | high | mitigate | `stampSamples` is a pure, unit-tested function; every action is `_dd_apply`'d and only DD_OK actions enter the log; rejections are surfaced, never swallowed |
| T-07-02 | Denial of service | Instance buffer growth from a hostile node count | medium | accept | Capacity doubles to the sim's DD_MAX_NODES ceiling (2^20 instances ~ 64 MB of matrices) on a local single-user app; the sim's cap bounds it |
| T-07-03 | Information disclosure | Brush descriptions in the render side | low | accept | User's own text, local |
| T-07-04 | Tampering | Brush edits mutating versions | medium | mitigate | `BrushTable.edit` only appends; the grep gate finds no assignment into existing versions; the sim rejects non-sequential ids |
</threat_model>

<verification>
- Human legitimacy approval recorded; exact pins in package.json.
- All Vitest files green (wasm-golden, plane, fence, colour, camera-parity, nodes, encoder); typecheck and build green.
- Dev-page painting observation recorded by the executor.
</verification>

<success_criteria>
- CANV-03: deterministic placeholder renderer coloured from a hash of the brush description with a pen/body/spring overlay; renderer lifetime rules obeyed.
- STRK-04 (surface): four presets, edit creates a new version, old versions untouched.
- STRK-01/CANV-01/CANV-02 (surface): samples stamped at record time by the Worker, plane frame captured once at pen-down, predicted events preview-only; the TS encoder equals the C++ encoder byte for byte.
</success_criteria>

<output>
Create `.planning/phases/01-painting-with-the-pen/01-07-SUMMARY.md` when done, including the legitimacy approval text, `npm ls three` output, the frozen `fnv1a32('ink')` value, and the dev-page painting observation.
</output>
