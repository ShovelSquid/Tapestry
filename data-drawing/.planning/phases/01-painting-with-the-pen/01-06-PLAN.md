---
phase: 01-painting-with-the-pen
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - plugins/data-drawing/tapestry.plugin.json
  - plugins/data-drawing/index.js
  - plugins/data-drawing/tsconfig.json
  - plugins/data-drawing/surface/src/main.ts
  - plugins/data-drawing/surface/src/dev-host.ts
  - plugins/data-drawing/surface/src/camera.ts
  - plugins/data-drawing/surface/src/plane.ts
  - plugins/data-drawing/surface/src/measure.ts
  - plugins/data-drawing/surface/src/input.ts
  - plugins/data-drawing/surface/test/plane.test.ts
  - plugins/data-drawing/surface/test/fence.test.ts
autonomous: false
requirements: [CANV-02, STRK-01, STRK-06]
coupling_justified: ["01-05: 01-05 rebuilds plugins/data-drawing/surface/wasm artifacts; 01-06 only reads whatever wasm is present for its measurement build and its tests are pure TS, so either order yields the same result"]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Data Drawing surface is registered through registerSurface (id datadrawing.canvas, entry surface/dist/surface.js) and opens inside Tapestry from the Surfaces launcher; main.ts imports SurfaceHost/SurfaceModule types from @tapestry/sdk and nothing from Electron (CANV-04 consumer, PLUG-03)"
    - "The pen was measured on this Mac inside the Electron dev build before the fence was written: pointerType on down, pressure min/max/distinct count, tilt presence and sign, twist presence, buttons on the eraser end, coalesced samples per second, predicted events per move, isSecureContext, pointerrawupdate rate, crossOriginIsolated, navigator.gpu — the numbers are recorded in PEN_FACTS in input.ts and in the SUMMARY (CANV-02)"
    - "paintable(ev, settings) is true only for pointerType 'pen' with (buttons & 1) === 1, or pointerType 'mouse' with (buttons & 1) === 1 when settings.allowMouse is true (default false); hover (buttons 0), the eraser end (buttons 32), touch, and any other button are never recorded (CANV-02 boundary)"
    - "The fence is a native addEventListener on the surface canvas (no React, no synthetic events) with touch-action none and setPointerCapture on pointerdown; every pointermove iterates getCoalescedEvents() for recording; getPredictedEvents() only ever reaches the preview callback; touch pointers are ignored while a pen stroke is open (CANV-02)"
    - "Quantization happens exactly once, at the fence: u = Math.round(hit.u * 65536) and v likewise clamped to int32; pressure = Math.round(pressure * 65535) (u16; 0 -> 0, 1.0 -> 65535); tiltX/tiltY = Math.round(tilt) clamped to -90..90 (i8); twist = Math.round(twist) mod 360 (u16); flags bit0 = tilt reported, bit1 = twist reported, bit2 = pressureSource (1 for mouse); no float ever leaves input.ts (CANV-02 precision, STRK-01, STRK-06)"
    - "rayPlane is pure math from FIXED_CAMERA (position (0,0,120), target origin, up +y, fov 40 degrees) and returns plane-local (u, v) for a client point, null when the ray misses; the viewport centre maps to (0, 0) (CANV-01 surface side)"
    - "npm --prefix plugins/data-drawing test passes plane.test.ts and fence.test.ts alongside the earlier wasm goldens"
  artifacts:
    - path: "plugins/data-drawing/surface/src/input.ts"
      provides: "paintable, quantizeSample, PenFence, PEN_FACTS, Settings"
      contains: "getCoalescedEvents"
    - path: "plugins/data-drawing/surface/src/measure.ts"
      provides: "PenMeasure overlay and JSON summary"
      contains: "pointerrawupdate"
    - path: "plugins/data-drawing/surface/src/plane.ts"
      provides: "PlaneFrame, DEFAULT_PLANE, frameToQ16, rayPlane"
      contains: "export function rayPlane"
    - path: "plugins/data-drawing/surface/src/camera.ts"
      provides: "FIXED_CAMERA constants"
      contains: "FIXED_CAMERA"
  key_links:
    - from: "plugins/data-drawing/index.js"
      to: "sdk/src/contributions.ts"
      via: "context.registerSurface({ id: 'datadrawing.canvas', entry: 'surface/dist/surface.js', placement: 'stage' })"
      pattern: "registerSurface"
    - from: "plugins/data-drawing/surface/src/input.ts"
      to: "plugins/data-drawing/surface/src/plane.ts"
      via: "rayPlane(FIXED_CAMERA, viewport, frame, clientX, clientY) before quantizeSample"
      pattern: "rayPlane"
  prohibitions:
    - statement: "MUST NOT record a mouse or no-sensor stroke as though it carried pen pressure: mouse painting is off by default and, when enabled, every such stroke carries pressureSource = 1 in its StrokeBegin and flags bit2 in every sample"
      status: resolved
      verification: test
    - statement: "MUST NOT persist or transmit pen measurement telemetry; the measurement overlay writes to the on-screen panel and the console only, and never to disk or the network"
      status: resolved
      verification: test
    - statement: "MUST NOT let a predicted pointer sample or a wall-clock timestamp reach the recorded stroke; predicted events feed only the preview callback and event.timeStamp is never copied into a sample"
      status: resolved
      verification: test
---

## Phase Goal

**As a** painter holding a pressure pen (Kaelen), **I want to** paint inside Tapestry and watch nodes land on a plane facing a fixed camera, placed by a deterministic fixed-point sim running live in a Worker, **so that** the brush body's mass can be felt and seen.

<objective>
Start the surface the way the roadmap orders it: mount the Data Drawing surface in Tapestry through the extension point, measure the pen on this Mac in the Electron dev build, and only then write the deterministic fence whose constants are the measured numbers — a native pointer listener with capture and `touch-action: none`, pen-only with the mouse behind a setting, coalesced events recorded, predicted events preview-only, ray-plane unprojection and quantization once at the boundary.

Purpose: CANV-02 and the surface half of STRK-01/STRK-06. "The numbers become the fence's constants, not a side spike's notes" (roadmap wave 4). Pitfall 9 lists everything that goes wrong when the fence is written from assumptions.

Output: `camera.ts`, `plane.ts`, `measure.ts`, `input.ts`, tests, the surface registered in Tapestry, the measured facts recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/kaelencook/Tapestry/data-drawing/.planning/PROJECT.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/ROADMAP.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/STATE.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-04-SUMMARY.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md
@/Users/kaelencook/Tapestry/data-drawing/.planning/research/PITFALLS.md
@/Users/kaelencook/Tapestry/plugins/data-drawing/index.js
@/Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/main.ts
@/Users/kaelencook/Tapestry/sdk/src/contributions.ts
@/Users/kaelencook/Tapestry/app/src/renderer/components/Canvas.tsx

<interfaces>
<!-- Contracts this plan creates; 01-07 consumes them. -->

camera.ts:
  export const FIXED_CAMERA = { position: [0, 0, 120], target: [0, 0, 0], up: [0, 1, 0], fovDeg: 40, near: 0.1, far: 1000 } as const
  (never recorded in any action — CANV-01 says replay never depends on the camera)

plane.ts:
  export interface PlaneFrame { origin: [number, number, number]; right: [...]; up: [...] }      // world units, floats (left of the fence)
  export const DEFAULT_PLANE: PlaneFrame = { origin: [0,0,0], right: [1,0,0], up: [0,1,0] }
  export function frameToQ16(f: PlaneFrame): Int32Array   // 9 x i32 Q16.16 = Math.round(x * 65536), the order StrokeBegin expects (origin, right, up)
  export function rayPlane(cam: typeof FIXED_CAMERA, viewport: { width: number; height: number }, frame: PlaneFrame, clientX: number, clientY: number): { u: number; v: number } | null
    // NDC from client coords; ray = camera position + t * dir (dir from fov/aspect and the camera basis built from target/up); intersect the plane with normal = right x up through origin; return projections onto right and up; null if |dir . normal| < 1e-9 or t <= 0

input.ts:
  export interface Settings { allowMouse: boolean }      // default { allowMouse: false }
  export interface RawSample { u: number; v: number; pressure: number; tiltX: number; tiltY: number; twist: number; flags: number }   // all integers, already quantized; tick/index are stamped by the Worker (01-07)
  export const PEN_FACTS = { ... }                        // filled from Task 2's measurement; every field a number or boolean with a one-line comment naming the measurement
  export function paintable(ev: PointerEventLike, settings: Settings): boolean
  export function pressureSourceOf(ev: PointerEventLike): 0 | 1
  export function quantizeSample(hit: { u: number; v: number }, ev: PointerEventLike, pressureSource: 0 | 1): RawSample
  export interface FenceCallbacks { onBegin(pressureSource: 0 | 1, frame: PlaneFrame): void; onSamples(samples: RawSample[]): void; onEnd(): void; onPreview(points: Array<{ u: number; v: number }>): void }
  export class PenFence { attach(canvas: HTMLCanvasElement, deps: { viewport(): { width; height }; frame(): PlaneFrame; settings(): Settings; callbacks: FenceCallbacks }): () => void }
  PointerEventLike = Pick<PointerEvent, 'pointerType' | 'buttons' | 'button' | 'pressure' | 'tiltX' | 'tiltY' | 'twist' | 'clientX' | 'clientY' | 'pointerId' | 'isPrimary'> & { getCoalescedEvents?(): PointerEventLike[]; getPredictedEvents?(): PointerEventLike[] }

measure.ts:
  export class PenMeasure { attach(el: HTMLElement): () => void; summary(): MeasureSummary; render(into: HTMLElement): void }
  MeasureSummary = { pointerTypesOnDown: string[]; isPrimary: boolean[]; pressure: { min; max; distinct }; tilt: { seen: boolean; xSign: -1|0|1; ySign: -1|0|1; maxAbs }; twist: { seen; max }; eraserButtons: number | null; coalescedPerSecond: number; predictedPerMove: { min; max }; rawUpdatePerSecond: number; isSecureContext: boolean; crossOriginIsolated: boolean; gpu: boolean; strokes: number }
  Output channels: the overlay <pre> and console.log('[dd-measure]', JSON) only.

Plugin wiring: tapestry.plugin.json contributions.surfaces = ["datadrawing.canvas"]; index.js activate -> context.registerSurface({ id: 'datadrawing.canvas', displayName: 'Data Drawing', entry: 'surface/dist/surface.js', placement: 'stage' })
tsconfig.json paths: { "@tapestry/sdk": ["../../sdk/src/index.ts"] } (type-only import, erased by Vite)
</interfaces>
</context>

## Artifacts this phase produces

See `01-01-PLAN.md` for the consolidated phase table. This plan creates `camera.ts` (`FIXED_CAMERA`), `plane.ts` (`PlaneFrame`, `DEFAULT_PLANE`, `frameToQ16`, `rayPlane`), `measure.ts` (`PenMeasure`), `input.ts` (`Settings`, `RawSample`, `PEN_FACTS`, `paintable`, `pressureSourceOf`, `quantizeSample`, `PenFence`), the surface registration in `index.js` and the manifest, the SDK type import in `main.ts` (removing `host-types.ts`), and tests `plane.test.ts`, `fence.test.ts`.

## Edge Coverage

Explicit truths authored above for probe items 11 (CANV-02 boundary) and 12 (CANV-02 precision). Flagged assumptions 4 (STRK-01 record time — the Worker stamps), 9 (STRK-06 widths) and 10 (CANV-01 frame) are implemented as stated in `01-01-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Camera and plane math, the pen measurement overlay, and the surface mounted in Tapestry</name>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 559-627 (pen input facts, capture semantics, quantization, the measurement list), 742-745 (Pattern 5)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/research/PITFALLS.md lines 192-214 (Pitfall 9, including the "verify the actual device" list)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/main.ts and dev-host.ts (01-01; extend)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/index.js and tapestry.plugin.json (01-01)
    - /Users/kaelencook/Tapestry/sdk/src/contributions.ts (SurfaceHost / SurfaceModule as landed in 01-02)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-04-SUMMARY.md (how the launcher opens a surface; the fact sheet)
  </read_first>
  <files>plugins/data-drawing/surface/src/camera.ts, plugins/data-drawing/surface/src/plane.ts, plugins/data-drawing/surface/src/measure.ts, plugins/data-drawing/surface/src/main.ts, plugins/data-drawing/surface/src/dev-host.ts, plugins/data-drawing/tsconfig.json, plugins/data-drawing/index.js, plugins/data-drawing/tapestry.plugin.json, plugins/data-drawing/surface/test/plane.test.ts</files>
  <action>
    `camera.ts` and `plane.ts` per the interfaces block — pure math, no three.js (not installed until 01-07), floats allowed (left of the fence). Build the camera basis as forward = normalize(target - position), rightAxis = normalize(forward x up), upAxis = rightAxis x forward; for a client point compute NDC x = (clientX / width) * 2 - 1, y = 1 - (clientY / height) * 2, then dir = normalize(forward + rightAxis * (x * tan(fov/2) * aspect) + upAxis * (y * tan(fov/2))); intersect the plane; return (u, v) = ((hit - origin) . right, (hit - origin) . up).

    `measure.ts`: `PenMeasure` attaches native listeners (`pointerdown`, `pointermove`, `pointerup`, `pointercancel`, `pointerrawupdate`) on an element with `touch-action: none` and pointer capture, accumulating the `MeasureSummary` fields: pointer types on down, `isPrimary`, pressure min/max and distinct-value count over all coalesced samples, tilt seen + sign (record the sign of tiltX when |tiltX| is largest and likewise tiltY) + max abs, twist seen + max, `buttons` on a down whose `button === 5` or `(buttons & 32)` (eraser), coalesced events per second (count `getCoalescedEvents().length` over a moving one-second window during motion), predicted events per move min/max, `pointerrawupdate` events per second, `isSecureContext`, `crossOriginIsolated`, `'gpu' in navigator`, stroke count. `render(into)` writes the JSON (2-space) into a `<pre>` and `console.log('[dd-measure]', json)`; no fetch, no localStorage, no IPC.

    `main.ts`: replace the `host-types.ts` import with `import type { SurfaceHost, SurfaceModule, SurfaceHandle } from '@tapestry/sdk'` (delete `host-types.ts`; add the `paths` mapping to `tsconfig.json`); keep the tick/hash panel from 01-01; add a full-size measurement element inside `host.container` (a `<div>` that fills the container below the panel) with `PenMeasure` attached and its overlay toggled by the `M` key (default ON in this plan so the measurement is the first thing a person sees); `dispose` detaches everything (idempotent). `dev-host.ts`: unchanged behaviour; pass through. `index.js`: `context.registerSurface({...})` as in the interfaces block with a `/** @type {import('@tapestry/sdk').SurfaceContribution} */` typedef; `tapestry.plugin.json`: add `"surfaces": ["datadrawing.canvas"]`. Build the surface (`npm --prefix plugins/data-drawing run build`) so `surface/dist/surface.js` exists for the host.

    `test/plane.test.ts`: viewport 1600x1000 — centre maps to (0, 0) within 1e-9; right edge middle maps to u = 120 * tan(20 deg) * 1.6 = 69.89 (tolerance 1e-3) and v = 0; top edge middle maps to v = 43.68 (tolerance 1e-3); a frame with origin z = 5 (parallel plane) gives the same (u, v) scaled by (120-5)/120 for the same pixel; a frame whose normal is perpendicular to the view direction returns null; `frameToQ16(DEFAULT_PLANE)` equals `[0,0,0, 65536,0,0, 0,65536,0]`.
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry && npm --prefix plugins/data-drawing run typecheck && npm --prefix plugins/data-drawing test && npm --prefix plugins/data-drawing run build && test -f plugins/data-drawing/surface/dist/surface.js && grep -n "registerSurface" plugins/data-drawing/index.js && grep -n '"surfaces"' plugins/data-drawing/tapestry.plugin.json && ! test -f plugins/data-drawing/surface/src/host-types.ts && npm --prefix app test -- src/main/plugin-host.test.ts</automated>
    <fails_when>non-zero exit; vitest "failed"; dist/surface.js missing; either grep prints nothing; host-types.ts still exists; the app's plugin-host test fails to load the data-drawing plugin</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "from '@tapestry/sdk'" plugins/data-drawing/surface/src/main.ts` prints one `import type` line; `grep -rn "from 'three'" plugins/data-drawing/surface/src` prints nothing (three is not installed yet)
    - `plane.test.ts` reports at least 6 passing cases
    - `measure.ts` contains listeners for `pointerrawupdate` and `getCoalescedEvents` and contains no `fetch(`, `localStorage`, or `XMLHttpRequest` (grep prints nothing)
    - `npm --prefix app run dev` started by the executor shows "Open Data Drawing" in the launcher (executor confirms by the plugin list: `plugin:list` via the app's DevTools is not needed — the host test proves registration; the human confirms visually in Task 2)
  </acceptance_criteria>
  <done>The Data Drawing surface opens inside Tapestry through the public extension point and shows the pen measurement overlay; the plane math is tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Measure the pen on this Mac inside Tapestry and report the JSON</name>
  <what-built>The Data Drawing surface mounted in Tapestry with the pen measurement overlay. The executor has: built `surface/dist`, confirmed the app's plugin-host test loads the plugin, and started `npm --prefix app run dev` in the background — Tapestry is running now with an "Open Data Drawing" button top-right.</what-built>
  <how-to-verify>
    With the pressure pen (Wacom driver 6.4.13 or newer per CLAUDE.md), in the running Tapestry window:
    1. Click "Open Data Drawing". A dark layer with a tick/hash panel and a measurement `<pre>` appears (press M if the JSON is hidden).
    2. Draw three strokes with the pen tip: one slow, one as fast as you can across the whole width (this fills coalescedPerSecond), one with varying pressure from feather-light to full.
    3. Tilt the pen clearly toward the RIGHT of the screen while drawing a short stroke, then clearly toward the TOP of the screen for another (this sets tilt.xSign / tilt.ySign). If your pen has a barrel rotation, rotate it during a stroke (twist).
    4. Flip the pen and touch the surface with the ERASER end once (eraserButtons; expected 32).
    5. Hover the pen above the surface without touching (should add nothing to strokes).
    6. Copy the full JSON from the overlay (or from the DevTools console line starting with `[dd-measure]`).
    Expected (from RESEARCH): pointerTypesOnDown contains "pen"; pressure.max close to 1 and pressure.distinct in the hundreds or thousands; tilt.seen true with xSign +1 for a rightward tilt; eraserButtons 32; coalescedPerSecond well above 60 (typically 120-240); isSecureContext true; crossOriginIsolated false; rawUpdatePerSecond a number or 0.
  </how-to-verify>
  <resume-signal>Paste the JSON. Add one line: "pen model: <name>, driver: <version>". Type "approved" when pasted; the executor writes these numbers into PEN_FACTS.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The deterministic fence — input.ts with the measured constants, and its tests</name>
  <precondition>The measurement JSON from Task 2 is available in the conversation (or in 01-06-SUMMARY.md if resumed), including pressure.distinct, tilt.xSign/ySign, eraserButtons and coalescedPerSecond</precondition>
  <read_first>
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-RESEARCH.md lines 608-623 (capture semantics, quantization recommendation), 791-797 (pitfalls 4 and 5), 883-911 (the fence code example)
    - /Users/kaelencook/Tapestry/data-drawing/.planning/phases/01-painting-with-the-pen/01-PATTERNS.md lines 443-455 (Canvas.tsx pointer capture analog — React synthetic, so pattern only)
    - /Users/kaelencook/Tapestry/app/src/renderer/components/Canvas.tsx lines 455-480 (setPointerCapture / preventDefault usage in the app)
    - /Users/kaelencook/Tapestry/plugins/data-drawing/surface/src/plane.ts (Task 1)
    - the <interfaces> block above (Settings, RawSample, PenFence, quantization contract)
  </read_first>
  <files>plugins/data-drawing/surface/src/input.ts, plugins/data-drawing/surface/test/fence.test.ts</files>
  <action>
    `input.ts`: `PEN_FACTS` — an object literal whose fields are the measured numbers from Task 2 (pressureDistinct, pressureMax, tiltSeen, tiltXSign, tiltYSign, twistSeen, eraserButtons, coalescedPerSecond, predictedPerMoveMax, rawUpdatePerSecond, isSecureContext, crossOriginIsolated, gpu, penModel, driverVersion) each with a trailing comment `// measured 2026-09-22 in Electron 32 dev build`; plus the quantization constants `PRESSURE_MAX = 65535` (provisional — mirror of DD_PRESSURE_MAX; Phase 2 freezes 12 vs 16 bit from pressureDistinct), `Q16_ONE = 65536`. If `tiltXSign` measured as -1 for a rightward tilt, NEGATE tiltX at the fence so the recorded convention is "+x = toward screen right" and say so in a comment (same for y with "+y = toward screen top"); if tilt was never seen, record it and leave the mapping identity. `paintable`, `pressureSourceOf` (1 when pointerType is mouse), `quantizeSample` exactly per the must_haves precision truth (clamp u/v to the int32 range; pressure `Math.round(Math.min(1, Math.max(0, p)) * PRESSURE_MAX)`; tilt `Math.round` then clamp -90..90; twist `((Math.round(t) % 360) + 360) % 360`; flags per the contract). `PenFence.attach`: sets `canvas.style.touchAction = 'none'`; native `addEventListener` for `pointerdown`, `pointermove`, `pointerup`, `pointercancel`, `lostpointercapture`; state = one open stroke `{ pointerId, pressureSource, frame }` at most; on down: `if (!paintable(ev, settings())) return`; ignore `touch` pointers entirely while a stroke is open; `canvas.setPointerCapture(ev.pointerId)`; `frame = frame()` captured once; `callbacks.onBegin(pressureSource, frame)`; then push the down event's own coalesced list; on move for the captured pointer: `const events = ev.getCoalescedEvents?.() ?? [ev]`, map each through `rayPlane` (skip nulls) and `quantizeSample`, `callbacks.onSamples(samples)` if non-empty, then `callbacks.onPreview((ev.getPredictedEvents?.() ?? []).map(rayPlane).filter(nonNull))` — the predicted points are floats and never touch `RawSample`; on up/cancel/lostpointercapture for the captured pointer: `callbacks.onEnd()`, `releasePointerCapture` (guarded), clear state. `event.timeStamp` is never read. Return a detach function removing all listeners.

    `test/fence.test.ts` (no jsdom; a `FakeCanvas` with `addEventListener/removeEventListener/setPointerCapture/releasePointerCapture/style` and a `fire(type, ev)` helper; events are plain objects satisfying `PointerEventLike`): paintable: pen buttons 1 → true; pen buttons 0 (hover) → false; pen buttons 32 (eraser) → false; pen buttons 33 → false (the eraser end never has bit 0 per Blink, but the fence checks `=== 1` on the masked bit only — document); mouse buttons 1 with allowMouse false → false, true → true; touch → false. quantizeSample: pressure 0 → 0, 1 → 65535, 0.5 → 32768; tilt 45.4 → 45, -90.6 → -90; twist 359.6 → 0 (rounds to 360 → 0), -1 → 359; u 1.5 → 98304; flags: tilt reported bit0, twist bit1, mouse bit2; absent tilt (0,0) and twist 0 → bits clear. PenFence: a down with 1 coalesced + a move with 4 coalesced events delivers onBegin once, onSamples with 1 then 4 samples in order, onPreview with the predicted points and NONE of the predicted points appear in any sample (assert by using distinct client coordinates for predicted events); a down while another stroke is open is ignored; a touch move during a pen stroke adds nothing; up delivers onEnd once and releases capture; a mouse down with allowMouse false delivers nothing; with allowMouse true delivers onBegin(1, frame) and samples with flags bit2 set.
  </action>
  <verify>
    <automated>cd /Users/kaelencook/Tapestry && npm --prefix plugins/data-drawing run typecheck && npm --prefix plugins/data-drawing test && grep -n "PEN_FACTS" plugins/data-drawing/surface/src/input.ts && ! (grep -n "timeStamp" plugins/data-drawing/surface/src/input.ts | grep -vE '^[0-9]+:\s*(//|\*)' | grep -q .) && npm --prefix plugins/data-drawing run build</automated>
    <fails_when>non-zero exit; vitest "failed"; PEN_FACTS grep prints nothing; a non-comment line of input.ts reads timeStamp (the negated group exits non-zero); build fails</fails_when>
  </verify>
  <acceptance_criteria>
    <!-- planner-discipline-allow: timeStamp -->
    <!-- planner-discipline-allow: localStorage -->
    - `fence.test.ts` reports at least 20 passing assertions across at least 8 cases
    - `grep -c "getCoalescedEvents" plugins/data-drawing/surface/src/input.ts` is at least 1 and `grep -c "getPredictedEvents" ...` is at least 1, and the predicted call's result flows only into `onPreview`
    - `grep -n "touchAction = 'none'" plugins/data-drawing/surface/src/input.ts` prints one line; `grep -n "setPointerCapture" ...` prints at least one line
    - `PEN_FACTS` contains the measured `coalescedPerSecond`, `eraserButtons`, `pressureDistinct`, `tiltXSign`, `tiltYSign` with the values from the checkpoint JSON (the SUMMARY quotes both)
    - `grep -rn "React\|useState" plugins/data-drawing/surface/src/input.ts` prints nothing
  </acceptance_criteria>
  <done>The fence records only pen-tip-down samples at full coalesced rate, quantized once with the measured constants, and never lets a predicted point or a timestamp into a sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer events → fence | Browser-delivered floats and pointer metadata become integers that will be recorded forever (Phase 2) |
| Measurement overlay → outside world | Telemetry about the person's hardware must stay local |
| Plugin surface → Tapestry host | The data-drawing module now runs inside the host renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `quantizeSample` (input.ts) — NaN/Infinity/out-of-range pointer fields | medium | mitigate | Clamp every field to its wire range before `Math.round`; NaN pressure becomes 0; u/v clamped to int32; unit-tested edges (0, 1, -90.6, 359.6, -1) |
| T-06-02 | Information disclosure | `PenMeasure` telemetry (pen model, driver, sample rates) | medium | mitigate | Output only to the overlay and console; grep gate for `fetch(`/`localStorage`/`XMLHttpRequest` prints nothing; the JSON is copied by the human by hand |
| T-06-03 | Spoofing | Synthetic pointer events dispatched into the fence (would return empty coalesced lists) | low | accept | Only real user input reaches the surface canvas; `getCoalescedEvents?.() ?? [ev]` fallback keeps behaviour defined |
| T-06-04 | Elevation of privilege | Surface module inside the host realm | medium | accept | Per 01-02 policy; the module imports only SDK types and its own code (grep gate for Electron imports from 01-01 still applies) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- Surface registered and mountable in Tapestry (host test + human visual in the checkpoint).
- Measurement JSON recorded verbatim in the SUMMARY and reflected in `PEN_FACTS`.
- `plane.test.ts` and `fence.test.ts` green; build green; no timestamp, React, or Electron references in the fence.
</verification>

<success_criteria>
- CANV-02: pen-only (mouse behind a setting) full-rate capture through pointer capture and coalesced events, predicted events preview-only, measured on this Mac.
- STRK-01/STRK-06 (surface side): samples quantized once to integers including tilt and twist with presence flags.
- CANV-01 (surface side): plane-local coordinates from a pure ray-plane hit against the fixed camera; the camera is never recorded.
</success_criteria>

<output>
Create `.planning/phases/01-painting-with-the-pen/01-06-SUMMARY.md` when done, including the measurement JSON verbatim, the pen model/driver line, and the final `PEN_FACTS` object.
</output>
