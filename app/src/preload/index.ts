/**
 * Electron preload script.
 *
 * Plan 02 will expose the kernel IPC methods to the renderer via
 * contextBridge.exposeInMainWorld(). This file exists now so the
 * electron-vite config has a valid entry.
 */

export {}
